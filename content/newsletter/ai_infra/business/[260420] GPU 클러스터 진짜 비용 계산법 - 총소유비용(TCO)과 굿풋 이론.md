---
categories: [ai-infra/business, ai-infra/compute]
---

# How Much Do GPU Clusters Really Cost?

> **출처**: [SemiAnalysis Newsletter](https://newsletter.semianalysis.com/p/how-much-do-gpu-clusters-really-cost)
> **저자**: Jordan Nanos, Bryan Shan, Cheang Kang Wen
> **발행일**: 2026-04-20

---

## 📑 목차

### 전체 섹션
 1. [서론 - GPU 클러스터 비용을 다시 생각한다](#1-서론---gpu-클러스터-비용을-다시-생각한다)
 2. [TCO(총소유비용)를 구성하는 8가지 항목](#2-tco총소유비용를-구성하는-8가지-항목)
 3. [굿풋(Goodput) 이론과 장애 대응 프레임워크 3종](#3-굿풋goodput-이론과-장애-대응-프레임워크-3종)
 4. [3개 등급 클라우드 사업자 비교](#4-3개-등급-클라우드-사업자-비교)
 5. [시나리오 1 - 대형 LLM 사전학습](#5-시나리오-1---대형-llm-사전학습)
 6. [시나리오 2 - 멀티모달 강화학습(RL) 연구](#6-시나리오-2---멀티모달-강화학습rl-연구)
 7. [시나리오 3 - 추론 엔드포인트](#7-시나리오-3---추론-엔드포인트)
 8. [결론과 한계, 향후 연구 방향](#8-결론과-한계-향후-연구-방향)
 9. [ClusterMAX 2.1 업데이트 - 신규 사업자 평가](#9-clustermax-21-업데이트---신규-사업자-평가)
10. [굿풋 비용이 마진에 미치는 영향 - 추론 사업자 민감도 분석](#10-굿풋-비용이-마진에-미치는-영향---추론-사업자-민감도-분석)

---

## 🔑 용어 정리

본문을 순서대로 읽기 전에 알아두면 좋은 용어들입니다. 자세한 수치와 설명은 본문에서 처음 등장하는 위치에 나옵니다.

- **TCO (Total Cost of Ownership, 총소유비용)**: GPU 시간당 임대료만이 아니라 저장장치·네트워크·지원·장애 손실·구축 인건비까지 다 더한 "실제로 드는 전체 비용" — 이 글 전체의 핵심 틀
- **굿풋 (Goodput)**: 처리량(throughput) 중에서 실제로 쓸모 있는 작업만 걸러낸 값 — GPU가 돌아가고 있어도 장애·재시작으로 낭비되는 시간은 굿풋에 포함되지 않음
- **네오클라우드 (Neocloud)**: 아마존웹서비스·구글 같은 종합 클라우드가 아니라 GPU 임대만 전문으로 하는 신생 클라우드 사업자 — 이 글에서는 골드 등급·실버 등급으로 나눠 비교
- **클라우드 등급 (Gold-tier / Hyperscaler / Silver-tier)**: SemiAnalysis가 80곳 넘는 GPU 클라우드를 실사해 매긴 품질 등급 — 골드는 최상위 네오클라우드, 하이퍼스케일러는 아마존웹서비스·마이크로소프트 등 종합 클라우드, 실버는 중하위 네오클라우드
- **MTBF·MTTR**: MTBF(Mean Time Between Failures)는 장애와 장애 사이 평균 시간(클수록 안정적), MTTR(Mean Time To Repair, 이 글에서는 t_repair)은 고장난 노드를 고치거나 교체하는 데 걸리는 평균 시간
- **블라스트 반경 (Blast Radius)**: 노드 하나가 고장 났을 때 그 여파로 함께 멈추는 GPU 범위 — 8개짜리 서버 한 대일 수도, 72개짜리 랙 전체일 수도 있음
- **체크포인트 재시작 vs 장애 허용 학습**: 체크포인트 재시작은 장애 발생 시 마지막 저장 시점부터 처음부터 다시 시작하는 전통적 방식, 장애 허용 학습(Fault-Tolerant Training)은 일부 노드가 죽어도 나머지가 멈추지 않고 계속 도는 최신 방식
- **ClusterMAX**: SemiAnalysis가 GPU 클라우드 사업자를 실사해 등급을 매기는 자체 평가 시스템 — 이 글의 3개 등급 분류와 9번 섹션의 신규 사업자 평가가 이 시스템의 결과물

---

## 1. 서론 - GPU 클러스터 비용을 다시 생각한다

**📌 핵심:**
- 블랙웰 GPU 1개 가격은 평균 승용차 1대보다 비싸고 전력 소비는 가정집 1채보다 많음 — 유니콘 스타트업이 이런 GPU 수천 개를 24시간 돌리는 게 흔한 일이 됨
- 상당수 파운데이션 모델(대형 AI 모델) 회사는 직원 인건비보다 GPU에 자릿수 하나 더 많은 돈을 씀 — 초기 투자금의 80% 이상을 GPU에 쓰는 회사도 다수 확인됨
- 기존 관행은 GPU 시간당 임대료만 비교하는 것인데, 같은 시간당 가격이라도 다운타임(장애로 멈춘 시간)·구축 인건비·디버깅 시간에 따라 실제 총비용(TCO)이 크게 달라짐 — 저렴해 보이는 클러스터가 실제론 더 비쌀 수 있음
- 결론: SemiAnalysis는 GPU 외 저장장치·네트워크·지원 같은 직접 비용과 신뢰성·디버깅·구축 같은 간접 비용까지 합친 TCO 계산법을 제시하고, 실측 결과 학습용 클러스터에서는 골드 등급이 실버 등급보다 5\~15% 더 저렴함을 확인

---

```mermaid
flowchart TD
    Founder["AI 스타트업의 4대 지출 항목"] --> C1["① GPU 클러스터"]
    Founder --> C2["② 토큰(모델 API 사용료)"]
    Founder --> C3["③ 직원 인건비"]
    Founder --> C4["④ 그 외 전부"]

    C1 --> Fact["다수 회사: 초기 투자금의<br/>80% 이상을 GPU에 지출"]

    style C1 fill:#fff7ed,stroke:#ea580c,stroke-width:2px
    style Fact fill:#fef2f2,stroke:#dc2626
```

블랙웰(Blackwell) GPU 1개는 평균 승용차 1대보다 비싸고, 사용 전력은 가정집 1채보다 많습니다. 이런 GPU를 수천 개씩 밤낮으로 돌리는 유니콘 스타트업이 이제 흔합니다.

### GPU 시간당 가격만 보면 안 되는 이유

```mermaid
flowchart TD
    Naive["기존 관행: GPU 시간당($/GPU-hr)<br/>가격만 비교"] --> Problem["문제: 같은 시간당 가격이어도<br/>다운타임·구축·디버깅 시간에 따라<br/>실제 TCO는 크게 다름"]
    Problem --> Hidden["숨은 비용:<br/>CPU 컴퓨트·네트워킹·저장장치·<br/>오케스트레이션 소프트웨어·지원"]
    Hidden --> Result["결과: 겉보기엔 저렴한 클러스터가<br/>실제론 더 비쌀 수 있음"]

    style Problem fill:#fff7ed,stroke:#ea580c,stroke-width:2px
    style Result fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

SemiAnalysis의 ClusterMAX 연구가 출발하는 전제는, GPU 클라우드 사업자마다 클러스터 품질 차이가 크고 이 차이가 사용자 경험·생산성·TCO에 실질적 영향을 준다는 것입니다. 이런 차이는 하드웨어 스펙표나 1회성 벤치마크에는 드러나지 않습니다.

신뢰성·네트워크 동작·저장장치 성능·지원 품질이 결국 "연구 목표까지 걸리는 시간"이라는 유일하게 중요한 지표를 좌우합니다.

### 이 글의 방법론

```mermaid
flowchart TD
    Framework["TCO 계산 프레임워크"] --> Direct["직접 비용<br/>(컴퓨트·저장장치·네트워크·지원)"]
    Framework --> Indirect["간접 비용<br/>(신뢰성·디버깅·구축)"]

    Direct --> Compare["3개 등급 사업자 비교<br/>(골드 네오클라우드·하이퍼스케일러·실버 네오클라우드)"]
    Indirect --> Compare

    Compare --> Scenarios["3개 대표 시나리오에 적용<br/>대형 LLM 사전학습 · 멀티모달 RL 연구 · 추론 엔드포인트"]

    style Compare fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

이 계산은 SemiAnalysis가 무료로 공개한 [GPU 클러스터 TCO 계산기](https://www.clustermax.ai/tco)와 굿풋(Goodput) 계산기를 통해 누구나 직접 값을 넣어 재현할 수 있습니다.

근거 데이터는 GPU 임대 가격 데이터 시리즈, 80개 넘는 네오클라우드 실사 경험, ClusterMAX 1.0\~3.0 연구 과정에서 진행한 고객 150명 이상 인터뷰입니다.

```mermaid
flowchart TD
    Finding["핵심 실측 결과<br/>(GPU 시간당 가격을 동일하게 고정했을 때)"] --> Training["학습용 워크로드:<br/>골드 등급이 실버 등급보다<br/>TCO 5~15% 저렴"]
    Finding --> Inference["장애 허용도 높은 워크로드<br/>(단일 노드 추론 등):<br/>등급 간 차이 거의 0"]

    style Training fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style Inference fill:#eff6ff,stroke:#3b82f6
```

즉 사용자들이 막연히 느껴온 "장애 허용성이 좋으면 이득"이라는 직관에 실제 달러 값을 붙인 것이 이 글의 목표입니다.

---

## 2. TCO(총소유비용)를 구성하는 8가지 항목

**📌 핵심:**
- SemiAnalysis는 GPU 클러스터의 월간 총비용(TCO)을 GPU·저장장치·네트워크·컨트롤 플레인·지원·굿풋(장애 손실)·구축·디버깅 8개 항목의 합으로 정의
- GPU 항목 하나만 봐도 계약 기간·물량 할인, 예비(스팟) 인스턴스 사용, 오케스트레이션 프리미엄(예: 아마존웹서비스에서 세이지메이커 하이퍼팟 슬럼을 쓰면 일반 EC2보다 비싼 것)까지 감안해야 진짜 시간당 단가가 나옴
- 구축·디버깅 비용은 계약 기간에 나눠(상각) 계산 — 3년 쓸 클러스터에 몇 주 들여 구축하는 건 괜찮지만, 3개월 쓸 클러스터에 같은 시간을 들이면 큰 부담
- 결론: 8개 항목 중 어느 하나라도 빠뜨리면 두 클라우드의 GPU 시간당 가격이 같아도 실제 TCO는 크게 벌어질 수 있음

---

| 항목 | 단위 | 무엇을 재는가 |
|---|---|---|
| ① GPU | \$/GPU-hr | 목록가에 계약기간·물량 할인, 스팟 사용, 오케스트레이션 프리미엄을 반영한 실제 시간당 단가 |
| ② 저장장치 | \$/GB-월 | 고성능("핫") NVMe 병렬 파일시스템부터 "웜"·"콜드" 저장까지, API 호출·데이터 반출(egress) 비용 포함 |
| ③ 네트워킹 | \$/hr 또는 \$/GB-월 | 외부 접속용(프론트엔드) 네트워크 — 공인 IP·방화벽·로드밸런서·데이터 반출·전송료 |
| ④ 컨트롤 플레인 | \$/hr | 클러스터 관리용 노드(로그인·개발·작업 제출) 및 데이터 처리·RL 롤아웃용 CPU 노드 |
| ⑤ 지원 | % 할증 | 전체 청구액에 붙는 지원 등급별 할증(예: 아마존웹서비스는 월 지출이 늘수록 10%→3%로 체감) |
| ⑥ 굿풋 손실 | % 할증 | 다운타임·재시작으로 인한 암묵적 손실(3번 섹션에서 상세) |
| ⑦ 구축 | \$/hr | 엔지니어가 클러스터를 세팅하고 성능을 튜닝하는 비용, 계약 기간에 상각 |
| ⑧ 디버깅 | \$/hr | 엔지니어가 지속적으로 문제를 진단·해결하는 비용 + 실패한 작업에 낭비된 클러스터 시간 |

📌 용어 풀이: 오케스트레이션 프리미엄이란
> - 같은 GPU 서버라도 쿠버네티스(Kubernetes)나 슬럼(Slurm) 같은 오케스트레이션(작업 관리) 소프트웨어를 통해 빌리면 기본 인스턴스보다 비싸지는 경우가 있음
> - 예: 아마존웹서비스에서 세이지메이커 하이퍼팟 슬럼(SageMaker HyperPod Slurm)으로 빌리면 같은 GPU 머신인데도 일반 EC2 인스턴스보다 비싼 요금이 매겨짐

저장장치 비율 가정은 고객 설문을 바탕으로 GPU당 2TB(적게 쓰는 경우)부터 25TB(많이 쓰는 경우)까지 폭넓게 잡습니다. 예를 들어 아마존웹서비스의 FSx for Lustre는 처리량 등급이 4단계(초당 GB당 125MB/s부터 1,000MB/s까지)로 나뉘고, 목록가 기준 처리량이 4배 좋아지면 가격은 약 3배 비싸집니다.

```mermaid
flowchart TD
    Formula["TCO(월/클러스터) ="] --> Sum["①GPU + ②저장장치 + ③네트워크<br/>+ ④컨트롤플레인 + ⑤지원<br/>+ ⑥굿풋손실 + ⑦구축 + ⑧디버깅"]
    Sum --> Amortize["⑦구축·⑧디버깅은<br/>계약 기간(3개월~3년)에 걸쳐 상각"]

    style Sum fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
    style Amortize fill:#fff7ed,stroke:#ea580c
```

이 상각 방식 때문에 3년 쓸 클러스터에 몇 주를 들여 구축하는 건 큰 문제가 아니지만, 3개월만 쓸 클러스터에 똑같은 구축 시간을 들이면 비용 부담이 훨씬 커집니다.

---

## 3. 굿풋(Goodput) 이론과 장애 대응 프레임워크 3종

**📌 핵심:**
- 굿풋은 처리량(throughput) 중 실제로 쓸모 있는 작업만 걸러낸 값 — GPU가 버스에서 이탈하거나 NCCL(GPU 간 통신 라이브러리)이 멈추거나 체크포인트 저장 직전 메모리 부족(OOM)이 나면 "돌아가고 있어도 나쁜 처리량"
- 클러스터가 클수록, 작업이 클수록 개별 장애의 타격이 커짐 — 클러스터의 80%를 쓰는 작업이 재시작(10\~15분 소요)하면 그 시간 전부 + 마지막 체크포인트 이후 계산이 통째로 날아감
- 장애 대응 방식은 두 갈래: ① 체크포인트 재시작(중소 규모 클러스터에서 여전히 가장 흔함) ② 장애 허용 학습 프레임워크(TorchFT·AWS 체크포인트리스·TorchPass) — 프레임워크마다 블라스트 반경(장애 여파 범위)과 성능 손실의 트레이드오프가 다름
- 결론: 프레임워크가 늘었어도 진짜 어려움은 그대로 남아 — 1,000개 이상 GPU 규모에서는 사업자에게만 기대지 않고 학습 코드 자체를 장애 현실에 맞게 설계해야 함

---

```mermaid
flowchart TD
    Size["클러스터·작업 규모 커짐"] --> Freq["개별 노드 장애 빈도 증가"]
    Freq --> MTBF["장애와 장애 사이<br/>평균 시간(MTBF) 감소"]
    MTBF --> Loss["대형 작업일수록<br/>굿풋(유효 작업량) 손실 커짐"]

    style Loss fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

좋은 사업자의 조건은 명확합니다 — ① 깨끗한 데이터센터를 노련한 운영팀이 관리하고 ② 장애를 빠르게 식별(가능하면 사전 예측)하며 ③ 예비 노드 풀과 물량 보장으로 빠르게 복구하는 곳입니다.

### 굿풋 손실을 계산하는 3가지 시나리오

```mermaid
flowchart TD
    Fail["노드 장애 발생"] --> Cold["콜드 스페어 방식<br/>(사업자가 관리하는 예비 노드,<br/>수리·교체까지 대기)<br/>→ 몇 시간~며칠, 최악 시나리오"]
    Fail --> Hot["핫 스페어 방식<br/>(고객 또는 상위 사업자가 관리,<br/>유휴 노드로 즉시 재시작)<br/>→ 상위 사업자는 전체의 2~6%를<br/>예비 풀로 상시 확보"]
    Fail --> Tolerant["장애 허용 방식<br/>(작업이 멈추지 않고 계속 실행)<br/>→ 단일 노드 추론에서는 표준,<br/>학습에서는 아직 미성숙"]

    style Hot fill:#f0fdf4,stroke:#16a34a
    style Tolerant fill:#eff6ff,stroke:#3b82f6
```

세 시나리오 모두 장애 식별 시간(t_id), 체크포인트 주기(t_chkpt), 학습 재시작 시간(t_init), 노드 수리·교체 시간(t_repair, MTTR), 블라스트 반경(b_radius, 예: 8기 서버 단위 또는 NVL72의 64기 단위), 장애 빈도(MTBF)를 입력값으로 씁니다.

### 장애 허용(Fault-Tolerant) 학습 프레임워크 3종 비교

```mermaid
flowchart TD
    Root["장애 허용 학습 프레임워크 3종"] --> A["TorchFT<br/>(메타-파이토치, 오픈소스)"]
    Root --> B["AWS 체크포인트리스 학습<br/>(세이지메이커 하이퍼팟 전용)"]
    Root --> C["Clockwork TorchPass<br/>(유료 라이선스)"]

    A --> A2["블라스트 반경: 복제 그룹 전체<br/>(FSDP 샤드=16이면 GPU 1개 장애가<br/>16개를 통째로 멈춤)<br/>성능: GLOO 통신 사용으로<br/>비교 작업 대비 10%+ 저하"]
    B --> B2["블라스트 반경: 복제 그룹 크기에 비례<br/>(각 GPU가 동료 GPU에<br/>모델 조각 예비본 보관)<br/>성능: 메모리 압박으로<br/>배치 크기 축소 필요"]
    C --> C2["블라스트 반경: 유휴 노드로 즉시 대체<br/>(3개 중 유일하게 성능 저하 없음)<br/>비용: 유휴 노드 확보<br/>또는 저순위 작업 선점 비용"]

    style A2 fill:#fff7ed,stroke:#ea580c
    style B2 fill:#fff7ed,stroke:#ea580c
    style C2 fill:#f0fdf4,stroke:#16a34a
```

📌 용어 풀이: TorchFT는 실제로 어떻게 복구하나
> - 복제 그룹(레플리카 그룹) 하나가 죽으면 노드가 교체될 때까지 그 그룹의 GPU 전체가 놀게 됨
> - 살아남은 그룹이 모델·옵티마이저 상태 전체를 `state_dict()`로 직렬화해 HTTP로 전달하면, 복구된 그룹이 `load_state_dict()`로 받아 스텝 카운터를 맞추고 대열(쿼럼)에 재합류 — 이 과정을 TorchFT의 "라이트하우스(lighthouse)" 서버가 지휘
> - 모든 장애가 GPU가 완전히 죽는 형태는 아니고, 통신이 멈춘 채 걸려있는 "행(hang)" 상태도 흔함 — 이런 경우 굿풋 손실에는 수리 시간뿐 아니라 원인을 찾아 풀어내는 시간까지 포함됨

📌 용어 풀이: AWS 체크포인트리스 학습의 트레이드오프
> - 2025년 12월 도입된 AWS 세이지메이커 하이퍼팟 전용 기능으로, 노바(Nova) 모델 학습에 쓰던 내부 기술을 GPU 1,000개 이상 규모에서 검증
> - 각 GPU가 동료 GPU에 자기 모델 조각의 예비본을 들고 있다가 장애 시 EFA(아마존의 고속 네트워크)를 통해 즉시 복구 — AWS는 복구 시간 1분 45초(체크포인트 재시작은 15분)를 주장, SemiAnalysis 실측도 이를 확인
> - 대가는 메모리 압박 — 예비본 하나를 추가할 때마다 GPU 메모리 사용량이 체크포인트 1개 분량만큼 늘어 배치 크기를 줄이거나 병렬화 전략을 바꿔야 함

TorchPass는 스케줄러 단(플러그인 형태)에서 동작해 훈련 스크립트 코드를 몇 줄만 바꾸면 됩니다. SemiAnalysis가 GKE(구글 쿠버네티스 엔진) 8노드 클러스터에서 계획된 유지보수(펌웨어 업그레이드 등)와 예상치 못한 하드웨어 장애 모두에 대해 테스트한 결과, 재시작 시간과 작업 성능이 장애 허용 기능이 없는 기준선과 비슷했습니다.

여전히 어려움은 남아 있습니다. TorchFT는 유일한 오픈소스 옵션이지만 학습용으로 널리 쓰이진 않고, 세 프레임워크 모두 통신 오버헤드·메모리 오버헤드·유휴 노드 비용이라는 트레이드오프를 안고 있습니다.

결국 대형 연구소이거나 라이선스 비용을 감당할 수 있는 회사의 전유물에 가깝습니다. 반면 단일 8기 시스템 단위 추론에서는 장애 허용이 이미 표준이고, NVIDIA Dynamo 같은 프레임워크가 KV 캐시 오프로딩 단계까지 이 개념을 확장하고 있습니다.

---

## 4. 3개 등급 클라우드 사업자 비교

**📌 핵심:**
- SemiAnalysis는 GPU 클라우드 사업자를 골드(최상위 네오클라우드)·하이퍼스케일러(종합 클라우드)·실버(중하위 네오클라우드) 3개 등급으로 나눠 비교 — 각각 실제 회사 여러 곳의 평균적 경험을 합성한 것으로, 특정 1개 회사를 지칭하지 않음
- 골드 등급은 가격도 낮고(대형·장기계약 시 하위 25백분위권) 즉시 최상 성능이 나오는 반면, 하이퍼스케일러는 가격이 비싸고(50\~75백분위권) 네트워킹·저장장치 세팅에 시간이 오래 걸림
- 실버 등급은 가격은 가장 저렴할 수 있지만(25\~50백분위권) 사업자별 역량 편차가 크고, 지나치게 공격적인 할인은 오히려 품질 경고 신호로 봐야 함
- 결론: "GPU 시간당 가격"이라는 한 줄만 보면 실버가 가장 싸 보이지만, 아래 8개 항목을 다 더한 TCO는 등급 순서가 뒤집히거나 좁혀지는 경우가 많음 (5\~7번 시나리오에서 실측)

---

```mermaid
flowchart TD
    Root["ClusterMAX 3개 등급<br/>(2026년 4월 기준)"] --> Gold["골드 등급 네오클라우드<br/>(넵비우스·플루이드스택·크루소 등)"]
    Root --> Hyper["하이퍼스케일러<br/>(오라클·애저·AWS·GCP)"]
    Root --> Silver["실버 등급 네오클라우드<br/>(투게더·람다·볼티지파크 등 9곳)"]

    Gold --> GoldDetail["가격: 대형·장기계약 시 하위 25백분위권<br/>저장장치·네트워킹: 즉시 최상 성능, 세팅 거의 불필요<br/>지원: 24x7·무료 POC·엔지니어 직접 대응<br/>모니터링: 기본 제공, 핫스페어 풀 상시 확보"]
    Hyper --> HyperDetail["가격: 50~75백분위권<br/>(대기업 MSA는 75백분위 이상)<br/>저장장치·네트워킹: 기본 성능 낮음,<br/>세팅·디버깅에 시간 많이 소요<br/>지원: 유료 등급제(월 지출 커질수록 10%→3%)<br/>모니터링: 기본 미제공, 단 데이터센터 운영은 안정적"]
    Silver --> SilverDetail["가격: 25~50백분위권<br/>(과도한 할인은 품질 경고 신호)<br/>저장장치·네트워킹: 사업자 역량에 따라 편차 큼<br/>지원: 포함되나 24x7 직통 엔지니어는 드묾<br/>모니터링: 기본 미제공,<br/>콜드 스페어 위주(서버 제조사 의존 수리)"]

    style GoldDetail fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style HyperDetail fill:#fff7ed,stroke:#ea580c
    style SilverDetail fill:#fef2f2,stroke:#dc2626
```

📌 용어 풀이: 백분위(percentile)로 가격을 말하는 이유
> - SemiAnalysis는 매일 전 세계 GPU 임대 가격을 수집하는 자체 데이터 시리즈를 운영 — 같은 GPU라도 사업자·계약조건마다 가격이 천차만별이라 "몇 번째로 싼 편인가"를 백분위로 표현
> - 하위 25백분위권이라는 건 시장에 나온 가격 중 가장 싼 축(하위 25%) 안에 든다는 뜻

이 3개 등급 분류는 SemiAnalysis가 GPU 클라우드 80곳 이상을 직접 사용해보고, 렌탈 가격 데이터와 함께 매기는 ClusterMAX 등급 시스템의 결과물입니다(2026년 4월 ClusterMAX 2.1 기준). 이어지는 3개 시나리오는 이 등급 분류를 실제 클러스터 구성에 적용해 TCO를 계산합니다.

---

## 5. 시나리오 1 - 대형 LLM 사전학습

**📌 핵심:**
- 5,184개 GB300 NVL72 GPU 중 4,096개(80%)를 단일 대형 사전학습 작업에 쓰는 구성 — GPU 시간당 가격을 3개 등급 모두 4달러로 고정해 순수하게 다른 7개 항목만으로 TCO 차이를 봄
- 3년 계약 기준 TCO는 골드 1배, 하이퍼스케일러 1.10배, 실버 1.15배 — 하이퍼스케일러의 10% 프리미엄은 주로 지원비용과 구축(EFA 네트워크 튜닝) 때문, 실버의 15% 프리미엄은 주로 굿풋 손실(다운타임)·구축·저장장치 비용 때문
- 굿풋 손실률은 골드 6.14%, 하이퍼스케일러 10.53%, 실버 20.91%로 격차가 큼 — 실버는 장애 빈도(MTBF)가 60% 더 나쁘고, 장애 식별·노드 교체에 각 1시간(골드·하이퍼스케일러는 15분)이 걸린다고 가정
- 결론: 대규모 단일 작업이 클러스터 대부분을 차지하는 사전학습에서는 등급 간 TCO 격차가 뚜렷하게 나타남 — 신뢰성 차이가 곧바로 돈으로 환산되는 워크로드

---

**입력 가정(SemiAnalysis가 시나리오용으로 설정한 값, 실측 가격 데이터와 혼합):**

| 항목 | 값 | 성격 |
|---|---|---|
| GPU | GB300 NVL72 5,184개, 4,096개(80%)를 단일 작업에 사용 | 시나리오 가정 |
| GPU 단가 | 3개 등급 모두 $4/GPU-hr 동일 적용(하이퍼스케일러는 오케스트레이션 프리미엄 면제) | 시나리오 가정(등급 간 순수 비GPU 비용만 비교하기 위한 통제) |
| 저장장치 | 핫티어 500TiB(NVMe급) + 콜드티어 10PiB(오브젝트 스토리지급) | 시나리오 가정 |
| 저장장치 할인 | 골드는 공격적 할인, 하이퍼스케일러는 할인 없음 → 약 2배 가격차 (단 GPU 대비 비중 작아 TCO 영향은 작음) | 실측 관행 기반 가정 |
| 컨트롤 플레인 | CPU 데이터처리 머신 0대, 컨트롤 플레인(로그인·slurmctld)용 3대만 사용 | 시나리오 가정 |
| 지원 | 하이퍼스케일러=AWS Unified Operations급 최고 등급, 골드·실버는 지원비용이 클러스터 가격에 포함 | 시나리오 가정 |
| 구축·디버깅 | 엔지니어 연봉 $20만 기준, 하이퍼스케일러에서 1개월 유료 POC + 매월 엔지니어 2명이 1주일씩 네트워크 디버깅 | 시나리오 가정(EFA 튜닝이 InfiniBand·Spectrum-X보다 오래 걸린다는 실측 경험 반영) |

```mermaid
flowchart TD
    Result["3년 계약 기준 TCO 배율<br/>(GPU 단가 동일 고정)"] --> G["골드: 1.00배 (기준)"]
    Result --> H["하이퍼스케일러: 1.10배<br/>→ 주 원인: 지원비용 + EFA 튜닝 구축비"]
    Result --> S["실버: 1.15배<br/>→ 주 원인: 굿풋 손실 + 구축(헬스체크·튜닝) + 저장장치"]

    style G fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style H fill:#fff7ed,stroke:#ea580c
    style S fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

### 굿풋 손실 계산 - 등급별 장애 대응 방식

이 시나리오는 등급마다 다른 장애 허용 방식을 쓴다고 가정합니다: 골드는 TorchPass(또는 동급 자체 코드), 하이퍼스케일러는 세이지메이커 하이퍼팟 체크포인트리스 학습, 실버는 예비 유휴 노드를 통한 체크포인트 재시작입니다.

| 입력값 | 골드 | 하이퍼스케일러 | 실버 |
|---|---|---|---|
| 장애 빈도(MTBF) | 기준 | 기준 | 60% 더 나쁨 |
| 장애 식별 시간 | 15분 | 15분 | 1시간 |
| 노드 교체 시간 | 15분 | 15분 | 1시간 |
| 작업 초기화 시간 | 10분 | 10분 | 15분(저장장치 성능 저하 반영) |
| 방식별 추가 비용 | TorchPass: 유휴 GPU 32개(전체의 0.62%) | 체크포인트리스: 메모리 오버헤드로 성능 5% 저하 | 콜드/핫 예비 노드 대기 |

이 표의 실버 등급 수치(60% 더 나쁜 MTBF, 1시간 vs 15분)는 SemiAnalysis의 실사·고객 인터뷰 경험에 기반한 시나리오 가정으로, 특정 회사의 실측 통계가 아닙니다.

```mermaid
flowchart TD
    Goodput["굿풋 손실률(TCO 대비)"] --> G2["골드: 6.14%"]
    Goodput --> H2["하이퍼스케일러: 10.53%"]
    Goodput --> S2["실버: 20.91%"]

    style G2 fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style S2 fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

---

## 6. 시나리오 2 - 멀티모달 강화학습(RL) 연구

**📌 핵심:**
- 2,048개 B200 GPU 클러스터, 실제 2025년 8월 임대 가격 데이터를 그대로 사용(네오클라우드 25백분위 $2.40/GPU-hr, 하이퍼스케일러 50백분위 $3.10/GPU-hr) — 시나리오 1과 달리 GPU 단가를 등급 간에 동일하게 두지 않음
- 이 워크로드는 통신량이 아니라 연산·메모리 대역폭에 좌우돼 네트워크 세팅 부담이 작지만, 저장장치 비율이 GPU당 12TB로 매우 높음(멀티모달 학습 데이터·합성 데이터·체크포인트용)
- 3년 계약 기준 TCO는 골드 1배, 하이퍼스케일러 1.61배, 실버 1.15배 — 하이퍼스케일러의 61% 프리미엄은 대부분 GPU 자체 단가·오케스트레이션 비용·저장장치·구축 시간 때문(장애 손실 비중은 작음)
- 결론: 연구팀이 장애 허용 코드를 따로 구현하지 않은 소규모 작업 다수 환경에서는, 등급 간 굿풋 손실률 차이가 0.23\~0.96%포인트에 불과 — 이런 워크로드에서는 신뢰성보다 GPU 단가·저장장치 비용이 TCO를 더 크게 좌우

---

**입력 가정:**

| 항목 | 값 | 성격 |
|---|---|---|
| GPU | B200 2,048개, 평균 작업 크기 64개(전체의 약 3%) | 시나리오 가정 |
| GPU 단가 | 네오클라우드 25백분위 $2.40/GPU-hr(2025년 8월 실측), 하이퍼스케일러 50백분위 $3.10/GPU-hr(온디맨드 목록가 대비 75% 할인) | 실측 가격 데이터 |
| 오케스트레이션 프리미엄 | 하이퍼스케일러 인스턴스 가격에 10% 추가 | 시나리오 가정 |
| 저장장치 | 핫티어 25PB(GPU당 약 12TB) + 콜드티어는 별도 원격 호스팅 | 시나리오 가정 |
| 저장장치 할인 | 골드만 공격적 할인, 하이퍼스케일러·실버는 할인 없음 | 실측 관행 기반 가정 |
| 체크포인트 주기 | 1시간 간격 비동기 체크포인트 | 시나리오 가정 |
| 컨트롤 플레인 | 3대(로그인·slurmctld) | 시나리오 가정 |
| 지원 | 하이퍼스케일러=AWS Enterprise Support급(중간 등급), 골드·실버는 클러스터 가격에 포함 | 시나리오 가정 |
| 구축·디버깅 | 하이퍼스케일러·실버 모두 2주 유료 POC + 매월 엔지니어 1명이 1주일 디버깅 | 시나리오 가정 |

```mermaid
flowchart TD
    Result2["3년 계약 기준 TCO 배율"] --> G3["골드: 1.00배 (기준)"]
    Result2 --> H3["하이퍼스케일러: 1.61배<br/>→ 주 원인: GPU 단가·오케스트레이션비<br/>+ 저장장치 + 구축시간"]
    Result2 --> S3["실버: 1.15배<br/>→ 주 원인: 저장장치<br/>(굿풋·디버깅은 비중 작음)"]

    style G3 fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style H3 fill:#fef2f2,stroke:#dc2626,stroke-width:2px
    style S3 fill:#fff7ed,stroke:#ea580c
```

### 굿풋 손실이 작은 이유

이 연구팀은 장애 허용 코드를 따로 구현하지 않았다고 가정합니다 — 장애가 나면 사업자가 관리하는 핫 또는 콜드 예비 노드를 이용해 큐에서 대기하다 마지막 체크포인트부터 재시작합니다.

| 입력값 | 골드·하이퍼스케일러 | 실버 |
|---|---|---|
| GPU 단위 MTBF | 약 25,000 GPU-hr | 약 15,000 GPU-hr |
| 장애 식별 시간 | 15분 | 1시간 |
| 노드 교체 시간 | 15분 | 1시간 |

```mermaid
flowchart TD
    Small["작은 작업(전체의 3%) +<br/>장애 허용 코드 미적용"] --> Impact["장애 시 영향 범위가<br/>클러스터 전체가 아니라 해당 작업(64개)에 한정"]
    Impact --> Result3["굿풋 손실률 격차:<br/>0.23~0.96%포인트<br/>(시나리오 1의 두 자릿수 격차 대비 미미)"]

    style Result3 fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

이는 작업 하나하나가 클러스터 전체에서 차지하는 비중이 작을수록, 사업자 등급에 따른 신뢰성 차이가 TCO에 미치는 영향도 함께 작아진다는 것을 보여줍니다 — 시나리오 1(대형 단일 작업)과 정반대의 패턴입니다.

---

## 7. 시나리오 3 - 추론 엔드포인트

**📌 핵심:**
- 512개 H200 GPU 중 단일 노드(8개, 전체의 1.5%)가 하나의 추론 작업 단위 — 로드밸런서가 실패한 요청을 자동으로 다른 노드에 재시도하는 최신 프레임워크를 쓴다고 가정해 장애의 영향이 매우 제한적
- 3년 계약 기준 TCO는 GPU 단가만 동일하면 골드·실버 격차가 1% 미만 — 하이퍼스케일러만 59% 비쌈(주로 GPU 단가·오케스트레이션비·저장장치·지원비용 때문)
- 실버 등급은 노드 교체에 8시간이나 걸린다고 가정했는데도(골드·하이퍼스케일러는 15분) 굿풋 손실은 전체 TCO의 약 0.5%에 불과 — 이게 바로 추론 사업자들이 저가 사업자의 유휴 용량을 끌어모아 싸게 서비스할 수 있는 이유
- 결론: 대형 학습(시나리오 1)과 정반대로, 단일 노드 추론에서는 사업자 등급보다 GPU 단가 자체가 TCO를 거의 전부 결정 — 신뢰성에 웃돈을 낼 이유가 크지 않은 워크로드

---

**입력 가정:**

| 항목 | 값 | 성격 |
|---|---|---|
| GPU | H200 512개, 평균 작업 크기 8개(전체의 1.5%) | 시나리오 가정 |
| GPU 단가 | 골드·실버는 H200 가격대 25백분위권, 하이퍼스케일러는 50백분위권(온디맨드 목록가 대비 75% 할인) | 실측 가격 데이터 |
| 저장장치 | 핫티어 500TB(모델·로그용, GPU당 약 1TB) + 콜드티어는 원격 호스팅 | 시나리오 가정 |
| 저장장치 성능 영향 | 3개 등급 가격이 비슷해 TCO에 미치는 영향 없다고 가정 | 시나리오 가정 |
| 컨트롤 플레인 | 3대(쿠버네티스 컨트롤 플레인) | 시나리오 가정 |
| 지원 | 하이퍼스케일러=AWS Business Support+급(최하 등급), 골드·실버는 클러스터 가격에 포함 | 시나리오 가정 |
| 구축·디버깅 | 하이퍼스케일러·실버 모두 엔지니어 1명이 2주만 투입 | 시나리오 가정(최소 수준) |

```mermaid
flowchart TD
    Result4["3년 계약 기준 TCO 배율"] --> G4["골드·실버: 거의 동일<br/>(GPU 단가가 같으면 1% 미만 격차)"]
    Result4 --> H4["하이퍼스케일러: 1.59배<br/>→ 주 원인: GPU 단가·오케스트레이션비<br/>+ 저장장치 + 지원비용"]

    style G4 fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style H4 fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

### 왜 신뢰성 차이가 거의 영향을 안 주나

이 시나리오는 llm-d나 SGLang OME 같은 최신 추론 프레임워크를 쓴다고 가정합니다 — 노드가 죽거나 오토스케일링이 일어나도 진행 중이던 요청은 로드밸런서가 다른 노드로 재시도할 뿐, 체크포인트나 초기화 시간 자체가 필요 없습니다.

| 입력값 | 골드·하이퍼스케일러 | 실버 |
|---|---|---|
| GPU 단위 MTBF | 약 25,000 GPU-hr | 약 15,000 GPU-hr |
| 장애 식별 시간 | 15분 | 1시간 |
| 노드 교체 시간 | 15분 | **8시간**(시나리오 1·2보다 훨씬 긺 — 콜드 스왑, 물리적 수리·교체 절차 전체 포함) |

```mermaid
flowchart TD
    Tolerant2["장애 허용 추론 프레임워크:<br/>실패 요청만 다른 노드로 재시도,<br/>작업 자체는 계속 실행"] --> Small2["단일 노드(8개)가<br/>전체(512개)의 1.5%에 불과"]
    Small2 --> Impact2["실버 등급: 노드 교체 8시간이어도<br/>굿풋 손실은 TCO의 약 0.5%뿐"]

    style Impact2 fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

이 결과는 추론 사업자들이 전 세계 저가 사업자의 유휴 GPU 용량을 끌어모아 단일 노드 추론에 효과적으로 활용할 수 있는 이유를 잘 보여줍니다. 다만 WideEP(광역 전문가 병렬 처리)·분리형 프리필/디코드처럼 요청 하나가 여러 노드에 걸치는 최신 아키텍처에서는 이 작업 크기(1.5%)와 블라스트 반경이 훨씬 커진다는 점은 유의해야 합니다.

---

## 8. 결론과 한계, 향후 연구 방향

**📌 핵심:**
- 3개 시나리오 모두 GPU 시간당 가격을 동일하게 고정해도 저장장치·네트워크·컨트롤플레인·지원·굿풋·구축·디버깅이라는 숨은 비용 때문에 TCO가 벌어짐을 확인 — 하이퍼스케일러는 골드 등급 대비 최대 10%+, 실버 등급 네오클라우드는 최대 15%+ 더 비쌀 수 있음
- 모든 입력값·기본값은 2025년 8월 시점 가격 데이터 기반의 스냅샷이며, 이후 GPU 임대 가격은 계속 오르는 추세 — 계산기 자체는 무료 공개돼 있어 독자가 자기 상황에 맞는 값을 넣어 재현 가능
- 장애 허용 학습 프레임워크는 아직 미성숙 — TorchFT는 유일한 오픈소스지만 학습용으로 널리 쓰이지 않고, 3개 프레임워크 모두 통신·메모리 오버헤드나 유휴 노드 비용 같은 트레이드오프를 안고 있어 최전선 연구소나 라이선스 비용을 감당하는 회사의 전유물에 가까움
- 결론: 반면 장애 허용 추론은 이미 8기 시스템 단위의 표준이고, NVIDIA Dynamo 같은 프레임워크가 KV 캐시 오프로딩 단계까지 이를 확장 중 — SemiAnalysis는 올여름 ClusterMAX 3.0에서 B300·GB300 800Gb 네트워킹까지 포함해 전 사업자에 이 방법론을 적용할 계획

---

```mermaid
flowchart TD
    Conclusion["핵심 결론"] --> C1["GPU 시간당 가격이 같아도<br/>숨은 비용 때문에 TCO는 벌어짐"]
    Conclusion --> C2["하이퍼스케일러: 골드 대비<br/>최대 10%+ 비쌈"]
    Conclusion --> C3["실버 네오클라우드: 골드 대비<br/>최대 15%+ 비쌈"]

    style C2 fill:#fff7ed,stroke:#ea580c
    style C3 fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

### 한계와 향후 방향

```mermaid
flowchart TD
    Future["향후 연구 방향"] --> F1["ClusterMAX 3.0(올여름)에서<br/>모든 등급 사업자에 이 방법론 전면 적용<br/>(B300·GB300, 800Gb 네트워킹 포함)"]
    Future --> F2["실제 MTBF 데이터 수집 확대<br/>(고객의 장애 로그·경험담 제보 요청)"]
    Future --> F3["NVIDIA DGXC 벤치마킹·인증 저장소로<br/>같은 레퍼런스 아키텍처를 따르는<br/>사업자 간 성능차까지 추적"]

    style F1 fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

NVIDIA의 DGXC 벤치마킹 저장소와 NCP/DGXC 인증 절차를 보면, 같은 레퍼런스 아키텍처를 따르는 사업자끼리도 워크로드에 따라 성능 차이가 나는 경우가 확인됩니다. 특히 WideEP·분리형 프리필/디코드(PDD) 같은 기법이 확산되며 스케일업·스케일아웃 양쪽에서 대역폭을 크게 쓰는 통신량-bound 연산일수록 이 차이가 두드러집니다.

가격 데이터는 2025년 8월 시점의 스냅샷이며, SemiAnalysis가 최근 별도 기사에서 다뤘듯 이후 GPU 임대 가격은 계속 오르는 추세입니다. 이 데이터 시리즈는 스팟 인스턴스는 매일, 클러스터 단위 계약가는 매월 갱신됩니다.

📌 용어 풀이: 왜 장애 허용 학습은 아직 "비밀 병기"인가
> - 학습에서는 TorchFT(유일한 오픈소스)·AWS 체크포인트리스(AWS 전용)·TorchPass(유료 라이선스) 모두 통신 오버헤드·메모리 오버헤드·유휴 노드 비용 중 하나 이상을 감수해야 함
> - 결국 이 기술을 쓸 수 있는 곳은 최전선 AI 연구소이거나 라이선스 비용을 낼 수 있는 회사로 제한됨 — 반면 단일 8기 시스템 단위 추론에서는 장애 허용이 이미 업계 표준이고, LMCache·Mooncake 같은 프레임워크가 KV 캐시(대화 맥락 저장 공간) 오프로딩 단계까지 이 개념을 확장 중

---

## 9. ClusterMAX 2.1 업데이트 - 신규 사업자 평가

**📌 핵심:**
- 이번 업데이트는 전체 재평가가 아니라 신규 사업자 소규모 추가 — 본 평가팀은 올여름 B300·GB300과 800Gb 네트워킹에 집중한 ClusterMAX 3.0 테스트를 진행 중
- 코어42(Core42)는 아랍에미리트 최대 사업자 G42 계열로 미국 진출을 확대 중이며, 처음 테스트한 브로드컴 Thor-II 네트워크 카드에서 기존 AMD 표준 이미지가 전혀 호환되지 않는 문제를 겪음 — 다만 코어42 엔지니어링팀의 적극적 지원으로 극복
- FPT 스마트클라우드(베트남)와 라디언트/오리(Radiant/Ori, 브룩필드가 인수한 사우디 아람코 배경 사업자)는 둘 다 같은 유형의 보안 결함(PKey·SAKey 설정 오류로 다른 고객의 네트워크 엔드포인트가 노출됨)으로 실버 등급 진입에 실패
- 결론: 코어42는 미국에 최신 GPU를 내놓거나 UAE 규제를 완화하면 빠르게 실버 등급 이상으로 오를 가능성, 나머지 3곳은 보안·모니터링 미비로 아직 등급 미달 — 신규 진입자가 계속 늘고 있다는 신호 자체가 시장 경쟁 심화를 보여줌

---

```mermaid
flowchart TD
    Core42["코어42(Core42)<br/>G42 계열, UAE 최대 규모 + 미국 확장 중"] --> C42Good["장점: 슬럼·쿠버네티스 모두 제공,<br/>적극적·전문적인 엔지니어링 지원"]
    Core42 --> C42Bad["문제: 브로드컴 Thor-II NIC 최초 테스트<br/>→ 기존 AMD 표준 이미지 전부 비호환,<br/>드라이버 직접 재빌드 필요했음"]
    Core42 --> C42Future["전망: 미국 내 최신 GPU 출시나<br/>UAE 규제 완화 시 실버 등급 이상 진입 가능"]

    style C42Bad fill:#fff7ed,stroke:#ea580c
    style C42Future fill:#f0fdf4,stroke:#16a34a
```

📌 용어 풀이: Thor-II NIC 호환성 문제란
> - AMD 진영 GPU 클러스터용 컨테이너 이미지는 대부분 AMD 자체 폴라라(Pollara) 네트워크 카드 기준으로 빌드돼 있음
> - 코어42가 처음으로 브로드컴 Thor-II 카드를 갖춘 클러스터를 제공하면서, 기존에 검증했던 vLLM·SGLang·torchtitan·MoRI 이미지가 전부 작동하지 않아 브로드컴 드라이버를 직접 내려받아 컨테이너를 처음부터 다시 빌드해야 했음

```mermaid
flowchart TD
    Others["기타 신규 테스트 사업자"] --> BitDeer["BitDeer(말레이시아)<br/>GB200 NVL72 2노드 테스트,<br/>NVLink IMEX 도메인 설정 미완료로 검증 보류"]
    Others --> FPT["FPT 스마트클라우드(베트남)<br/>모니터링은 최상급이나<br/>PKey·SAKey 오류로 다른 고객 네트워크 노출"]
    Others --> Radiant["라디언트/오리(Radiant/Ori)<br/>브룩필드 인수, 사우디 아람코 배경<br/>FPT와 동일한 보안 결함 +<br/>18시간 장애 시뮬레이션에도 자동 알림 없음"]

    style FPT fill:#fef2f2,stroke:#dc2626
    style Radiant fill:#fef2f2,stroke:#dc2626
```

FPT와 라디언트/오리 모두 PKey·SAKey(네트워크 접근 권한을 구분하는 보안 키) 설정 오류로 실버 등급에서 탈락했습니다.

이 외에도 타트라 슈퍼컴퓨트(슬로바키아)·쿠물러스AI(텍사스·오클라호마)·부스트런(시애틀·텍사스·노스캐롤라이나)·문라이트(재판매·운영 대행)·베슬(한국계 마켓플레이스)·SK텔레콤·바이트플러스(바이트댄스 계열) 등이 올해 용량을 늘리며 주목할 만한 신규 진입자로 추가됐습니다.

---

## 10. 굿풋 비용이 마진에 미치는 영향 - 추론 사업자 민감도 분석

**📌 핵심:**
- 굿풋 비용은 추론 엔드포인트 사업자의 마진을 좌우하는 핵심 변수 — H200 기준 굿풋 비용이 시간당 GPU당 1.90달러에서 2.10달러로 약 20% 오르면, 토큰당 3달러 판매가 기준 매출총이익률이 28%에서 20%로 8%포인트 떨어짐
- 하이퍼스케일러 환경의 굿풋 비용은 약 1.59배 프리미엄이 붙는데, 이 프리미엄이 마진 압박으로 거의 그대로 전이됨 — 같은 토큰당 3달러 판매가에서 시간당 1.90달러(정상)일 때 매출총이익률 28%, 시간당 3.10달러(하이퍼스케일러 프리미엄)일 때는 마이너스 18%까지 추락
- 토큰 처리량은 SemiAnalysis InferenceX 벤치마크(딥시크 R1, 1k1k·8k1k 조합, 인터랙티비티 100 기준)로 산출 — H200 1대당 초당 326토큰, 유효 가동률 75% 가정 시 시간당 88만 토큰
- 결론: 추론 사업자에게는 사업자 등급(신뢰성)이 곧 마진 그 자체 — 프리미엄 사업자를 골라 굿풋 손실을 줄이는 것이 단위경제성(unit economics) 관점에서 결정적

---

**입력 값 (SemiAnalysis 산출):**

| 항목 | 값 | 성격 |
|---|---|---|
| H200 처리량 | 초당 326토큰/GPU | InferenceX 벤치마크 실측(딥시크 R1, 1k1k·8k1k, 인터랙티비티 100) |
| 유효 가동률 | 75% | 시나리오 가정 |
| 시간당 유효 토큰 처리량 | 약 88만 토큰(0.88M Tok/hr) | 위 두 값에서 산출 |
| 토큰 판매가 | 100만 토큰당 3달러 (블렌디드) | 시나리오 가정 |
| 정상 굿풋 비용 | 시간당 GPU당 1.90달러 | AI TCO 모델 산출 |
| 하이퍼스케일러 굿풋 비용 | 시간당 GPU당 3.10달러(약 1.59배 프리미엄) | AI TCO 모델 산출 |

```mermaid
flowchart TD
    Cost["시간당 GPU당 굿풋 비용"] --> Low["$1.90/hr (정상)<br/>→ 토큰당 3달러 판매가 기준<br/>매출총이익률 28%"]
    Cost --> Mid["$2.10/hr (약 20%↑)<br/>→ 매출총이익률 20%<br/>(8%포인트 하락)"]
    Cost --> High["$3.10/hr (하이퍼스케일러 프리미엄,<br/>약 1.59배)<br/>→ 매출총이익률 -18%<br/>(적자 전환)"]

    style Low fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style Mid fill:#fff7ed,stroke:#ea580c
    style High fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

📌 용어 풀이: 토큰당 비용을 어떻게 계산했나
> - 시간당 GPU당 굿풋 비용을 시간당 유효 토큰 처리량(0.88M Tok)으로 나누면 "토큰 100만 개를 만드는 데 드는 굿풋 비용"이 나옴
> - 이 비용이 판매가(토큰당 3달러)에서 차지하는 비중이 커질수록 매출총이익률은 그만큼 깎임 — 굿풋 비용이 판매가보다 커지면 마진은 마이너스로 전환

이 민감도 분석은 왜 추론 사업자들이 GPU 시간당 가격만 보고 사업자를 고르면 안 되는지 숫자로 보여줍니다. 굿풋 비용 1.59배 프리미엄이 마진에 거의 1대1로 전이되는 구조이기 때문에, 하이퍼스케일러의 표면 가격이 네오클라우드와 비슷해 보여도 실제 수익성은 크게 갈릴 수 있습니다.

---

*작성 진행률: 100% 완료*
*업데이트: 원문 전체 10개 섹션(서론\~굿풋-마진 민감도 분석) 변환 완료*
