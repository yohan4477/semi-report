---
categories: [ai-infra/compute, ai-infra/business]
---

# Cerebras's Next Generation CS-4: Fast Just Got Faster

> **출처**: [SemiAnalysis Newsletter](https://newsletter.semianalysis.com/p/cerebrass-next-generation-cs-4-fast)
> **저자**: Myron Xie, Bryan Shan, Wega Chu
> **발행일**: 2026-08-19

---

## 📑 목차

### 전체 섹션
1. [서론 - CS-4 공개와 토큰 두 배 시대](#1-서론---cs-4-공개와-토큰-두-배-시대)
2. [같은 웨이퍼 두 배 빠른 클럭](#2-같은-웨이퍼-두-배-빠른-클럭)
3. [네트워크 개선](#3-네트워크-개선)
4. [백팩 랙 - 전력과 연산을 나눈 모듈러 구조](#4-백팩-랙---전력과-연산을-나눈-모듈러-구조)
5. [병렬화 전략 - 파이프라인 대 텐서 전문가 병렬](#5-병렬화-전략---파이프라인-대-텐서-전문가-병렬)
6. [분리형 추론에 거는 베팅](#6-분리형-추론에-거는-베팅)
7. [로드맵 - Nexus와 CS-5](#7-로드맵---nexus와-cs-5)

---

## 🔑 용어 정리

본문을 순서대로 읽기 전에 알아두면 좋은 용어들입니다. 자세한 수치와 설명은 본문에서 처음 등장하는 위치에 나옵니다.

- **WSE-3(Wafer-Scale Engine 3, 3세대 웨이퍼 스케일 엔진)**: 반도체 웨이퍼(원판) 한 장을 자르지 않고 통째로 칩 하나로 쓰는 세레브라스의 핵심 설계 — CS-4는 이 웨이퍼를 CS-3와 그대로 재사용한다
- **오프웨이퍼 I/O(Off-wafer I/O)**: 웨이퍼 안에서 도는 데이터가 아니라, 웨이퍼 밖 네트워크로 실제 나가고 들어오는 데이터 통로 — 웨이퍼 내부 SRAM 대역폭과는 별개의 병목 지점
- **백팩(Backpack) 모듈**: CS-4에서 전력·냉각·입출력·웨이퍼를 각각 독립된 부품으로 분리해 랙 뒤쪽에 꽂아 넣는 세로형 함체 — 현장에서 웨이퍼만 꽂아 넣으면 되는 조립 방식
- **TDP(Thermal Design Power, 열설계전력)**: 시스템이 정상 작동 중 실제로 소비·방출하는 전력의 설계 기준치 — 랙 하나가 감당해야 할 전력·냉각 용량을 가늠하는 숫자
- **파이프라인 병렬화 vs 텐서·전문가 병렬화**: 큰 모델을 여러 칩에 나눠 담는 두 가지 방식 — 파이프라인 병렬화는 모델을 층(layer) 단위로 순서대로 나눠 칩마다 한 구간씩 맡기고, 텐서·전문가 병렬화는 같은 층 안의 연산이나 전문가(expert)를 여러 칩에 동시에 흩뿌린다. 세레브라스는 칩 간 통신 대역폭이 좁아 파이프라인 병렬화를 주로 쓴다
- **PDD vs AFD(Prefill-Decode Disaggregation vs Attention-FFN Disaggregation)**: 추론 과정을 서로 다른 하드웨어에 나눠 맡기는 두 가지 분리 방식 — PDD는 입력을 한번에 처리하는 프리필과 토큰을 하나씩 만드는 디코드를 나누고, AFD는 어텐션 계산과 피드포워드 신경망 계산을 나눠 각각에 유리한 칩에 맡긴다
- **KV 캐시(Key-Value Cache)**: 이전에 생성한 토큰들의 연산 결과를 저장해둬 다음 토큰을 만들 때 다시 계산하지 않도록 하는 메모리 — 동시 사용자 수와 문맥 길이(context window)에 비례해 필요한 용량이 커진다
- **Nexus**: 세레브라스가 차세대 웨이퍼와 함께 설계 중인 새 랙스케일 플랫폼 — CS-4의 랙 뼈대(섀시)를 다음 세대 칩까지 그대로 이어 쓰기 위한 기반

---

## 1. 서론 - CS-4 공개와 토큰 두 배 시대

**📌 핵심:**
- CS-4는 세레브라스의 4세대 랙 — 웨이퍼 자체는 CS-3와 똑같은 5나노 3세대 웨이퍼 스케일 엔진(WSE-3)을 그대로 쓰지만, 전력 소비와 클럭 속도를 올리고 랙 밀도를 개선해 성능을 두 배로 끌어올렸다
- 결과: 웨이퍼 한 장이 초당 사용자당 뽑아내는 토큰 수(tok/s/user)가 CS-3 대비 두 배로 늘었는데, 비용은 이전 세대와 비슷한 수준이다 — 같은 하드웨어 지출로 토큰 매출이 두 배가 되는 셈이라 고객 입장에선 안 살 이유가 없는 제안
- 랙 구조 자체도 모듈형으로 다시 설계돼 제조·배치가 빨라졌고, 새 입출력(I/O) 모듈은 세레브라스 웨이퍼를 다른 회사의 HBM(고대역폭메모리) 기반 칩과 섞어 쓰는 개방형·이종·분리형 추론 구조를 가능하게 한다
- 결론: 이 분리형 구조는 세레브라스 웨이퍼의 고질적 약점인 낮은 메모리 용량을 HBM 기반 시스템과 짝지어 우회하는 통로가 된다

---

```mermaid
flowchart TD
    CS4["CS-4 = 4세대 랙<br/>웨이퍼는 CS-3와 동일한<br/>5nm WSE-3"] --> Perf["성능 2배<br/>tok/s/user 2배"]
    CS4 --> Cost["비용은 이전 세대와 비슷"]
    CS4 --> Modular["랙 모듈화<br/>제조·배치 속도 개선"]

    style Perf fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style Cost fill:#eff6ff,stroke:#3b82f6
```

```mermaid
flowchart TD
    NewIO["새 I/O 모듈"] --> Open["개방형·이종·분리형<br/>추론 구조 가능"]
    Open --> Fix["HBM 기반 칩과 결합해<br/>CS-4의 낮은 메모리 용량<br/>한계를 우회"]

    style Fix fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

---

## 2. 같은 웨이퍼 두 배 빠른 클럭

**📌 핵심:**
- CS-4는 CS-3와 똑같은 5나노 WSE-3 웨이퍼를 그대로 쓰면서, 웨이퍼에 훨씬 많은 전력을 밀어넣고 클럭 속도를 두 배로 올려 성능을 뽑아낸다 — 이게 가능해진 건 전력 공급과 냉각 기술이 함께 개선됐기 때문
- 같은 실리콘을 재사용한다는 게 인상적이지 않아 보여도, 세레브라스가 가장 중요하게 보는 지표인 메모리 대역폭은 두 배가 됐다 — 다른 조건이 같다면 초당 사용자당 토큰 수(tok/s/user)도 거의 두 배로 이어진다는 뜻이라, 이게 바로 고객이 원하는 결과다
- 클럭이 두 배가 되면서 이론상 최대 연산능력(FLOPs)과 웨이퍼 밖으로 나가는 오프웨이퍼 I/O도 같이 두 배로 뛰어, CS-3의 초당 1.2테라비트(Tb/s)에서 CS-4는 초당 2.4Tb/s로 올라갔다
- 결론: 다만 웨이퍼 한 장당 SRAM(고속 메모리) 용량은 44GB로 그대로다 — 이 수치는 웨이퍼 위에 물리적으로 새길 수 있는 SRAM 비트셀 개수로 정해지는 값이라 클럭을 올린다고 늘어나지 않으며, 다음 세대 실리콘이 나와야 개선을 기대할 수 있다. 낮은 메모리 용량은 세레브라스 아키텍처가 안고 가는 고질적 트레이드오프다

---

```mermaid
flowchart TD
    SameWafer["CS-4 = CS-3와 동일한<br/>5nm WSE-3"] --> MorePower["웨이퍼에 더 많은<br/>전력 투입 + 냉각 개선"]
    MorePower --> Clock2x["클럭 속도 2배"]
    Clock2x --> BW2x["메모리 대역폭 2배"]
    Clock2x --> FLOPs2x["이론 최대 FLOPs 2배"]
    Clock2x --> IO2x["오프웨이퍼 I/O 2배<br/>1.2Tb/s → 2.4Tb/s"]

    style BW2x fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style IO2x fill:#f0fdf4,stroke:#16a34a
```

```mermaid
flowchart TD
    SRAM["SRAM 용량<br/>웨이퍼당 44GB"] --> Fixed["클럭 올려도 불변<br/>(비트셀 수로 결정)"]
    Fixed --> NextGen["다음 세대 실리콘<br/>나와야 개선"]
    Fixed --> Weak["세레브라스 구조의<br/>고질적 트레이드오프"]

    style Weak fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

세레브라스는 이전 기사(「세레브라스 — 더 빠른 토큰을 주세요」)에서 설명했듯이, SRAM 기반 구조는 연산 강도(Arithmetic Intensity, 메모리에서 1바이트를 가져올 때마다 몇 번 연산할 수 있는지 나타내는 지표)가 낮은 커널 — 즉 배치 크기가 작은 디코드(토큰을 하나씩 순차 생성하는 단계) — 처리에 특히 유리한 웨이퍼 아키텍처다.

---

## 3. 네트워크 개선

**📌 핵심:**
- 오프웨이퍼 I/O 대역폭이 두 배가 된 것 외에도, 세레브라스 고유 프로토콜을 표준 이더넷으로 바꿔주는 네트워크 카드(NIC 역할을 하는 업그레이드된 FPGA)인 새 Wafer I/O 인터페이스가 추가됐다 — 웨이퍼의 북쪽·남쪽 두 곳에 I/O 모듈이 붙고, 현장 교체가 가능해 랙을 다시 설계하지 않고도 새 네트워킹 표준으로 갈아탈 수 있다
- 이 모듈 덕분에 HBM 기반 가속기와 짝지어 어텐션 계산과 피드포워드 신경망 계산을 나누는 AFD(Attention-FFN Disaggregation) 구성이 가능해져 CS-4의 낮은 메모리 용량을 우회할 길이 열렸다 — 엔비디아가 Groq LPU(저지연 특화 추론 칩)를 포지셔닝하는 방식과 같은 접근이며, AWS의 EFA(탄력적 패브릭 어댑터) 네트워크 카드를 CS-4에 붙여 트레이니움 서버와 연동하는 용도로 설계된 것으로 보인다
- 2계층 fat-tree 네트워크(아리스타 이더넷 스위치 사용)의 지연시간은 새 저지연 패킷 처리 파이프라인 덕분에 CS-3의 5마이크로초(μs)에서 3μs로 줄었고, 스위치를 거치지 않는 웨이퍼 간 직접 연결까지 쓰면 2μs까지 더 줄어든다 — 다만 경쟁사들이 스위치 지연을 나노초 단위로 얘기하는 상황이라 "초고속"이라는 표현은 상대적이며, 개선 폭 자체는 적당한 수준이다
- 결론: 이 3μs 지연시간과 대역폭 한계는 여전히 발목을 잡는다 — 전문가(expert) 계층이 여러 웨이퍼에 걸쳐 나뉘는 EP(전문가 병렬)·ETP 같은 병렬화 방식을 못 쓰게 막아, 라우터에서 전문가로 토큰을 보내고 모으는 작업이 지연시간에 민감한 데다 전문가 부하 불균형 문제까지 겹치면서 파이프라인 병렬화만이 사실상 유일하게 쓸 수 있는 방식으로 남는다

---

```mermaid
flowchart TD
    WaferIO["새 Wafer I/O 인터페이스<br/>(업그레이드 FPGA, NIC 역할)"] --> Convert["세레브라스 고유 I/O →<br/>표준 이더넷 변환"]
    WaferIO --> Field["현장 교체 가능<br/>랙 재설계 불필요"]
    WaferIO --> AWS["AWS EFA NIC 연동 겨냥<br/>(트레이니움과 짝짓기)"]

    style AWS fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    Latency["2계층 fat-tree<br/>네트워크 지연시간"] --> CS3lat["CS-3: 5μs"]
    Latency --> CS4lat["CS-4 스위치 경유: 3μs"]
    Latency --> Direct["웨이퍼 직접연결: 2μs"]

    style Direct fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    Bottleneck["3μs 지연 + 대역폭 한계"] --> Block["전문가 계층이 여러 웨이퍼에<br/>걸치는 EP·ETP 병렬화 차단"]
    Block --> OnlyPP["파이프라인 병렬화만<br/>사실상 유일한 선택"]

    style Bottleneck fill:#fef2f2,stroke:#dc2626,stroke-width:2px
    style OnlyPP fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

---

## 4. 백팩 랙 - 전력과 연산을 나눈 모듈러 구조

**📌 핵심:**
- 시스템 레벨에서 가장 큰 변화는 물리적 구조다 — 세레브라스는 랙을 앞쪽(전력 공급 전담)과 뒤쪽(연산 전담)으로 나누고, 뒤쪽을 모듈형으로 꽂아 넣는 "백팩(backpack)" 단위로 포장했다. 백팩 하나에 웨이퍼 스케일 엔진 한 장이 들어가고, CS-4 랙 한 대는 백팩 3개(웨이퍼 3장)를 담아 CS-3의 랙당 웨이퍼 2장보다 늘었다
- 냉각용 펌프·열교환기는 랙에서 아예 빠졌다 — 요즘 데이터센터는 처음부터 완전 액체 냉각 랙을 지원하도록 지어지기 때문에, 이 부품들을 랙이 아니라 데이터센터 설비 쪽으로 넘긴 것
- 백팩은 전력·냉각·I/O·웨이퍼(WSE-3) 모듈을 각각 독립시킨 세로형 함체라 CS-3보다 제조가 훨씬 단순하다 — 웨이퍼는 세로로 서 있고 전력은 앞면, 냉각은 뒷면, I/O는 위아래 모서리에서 받는 구조. 고객은 전력 모듈로 랙을 먼저 세팅한 뒤 현장에서 웨이퍼 백팩만 꽂으면 되는 배치 방식이라 설치가 더 매끄럽다
- 결론: 웨이퍼 3장을 더 높은 클럭으로 돌리는 대가로 전력이 크게 늘었다 — CS-4 랙 한 대의 열설계전력(TDP)은 125\~135kW로, CS-3 한 대의 23kW 대비 거의 두 배다. 전력당 성능(성능/W)은 기껏해야 소폭 개선에 그친다

---

```mermaid
flowchart TD
    Backpack["CS-4 랙 구조"] --> Front["앞쪽: 전력 공급 전담"]
    Backpack --> Rear["뒤쪽: 연산(백팩 3개)"]
    Rear --> Wafer3["웨이퍼 3장/랙<br/>(CS-3는 2장)"]

    style Wafer3 fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    BackpackModule["백팩 = 세로형 함체"] --> PowerSide["전력: 앞면"]
    BackpackModule --> CoolSide["냉각: 뒷면"]
    BackpackModule --> IOSide["I/O: 위아래 모서리"]

    style BackpackModule fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    TDPCompare["랙 1대 TDP"] --> CS3TDP["CS-3: 23kW"]
    TDPCompare --> CS4TDP["CS-4: 125~135kW<br/>(약 2배)"]
    CS4TDP --> PerfW["성능/W:<br/>기껏해야 소폭 개선"]

    style CS4TDP fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

---

### 비용과 상호작용성 - 43 PB/s가 진짜 의미하는 것

세대를 거듭할수록 가장 큰 개선은 비용이 될 전망이다 — 냉각·전력이 늘면 부품 명세(BOM, 시스템 제작에 들어가는 부품과 원가 목록) 비용도 오르지만, 부품 수가 줄고 조립이 단순해진 만큼 이를 상쇄해 웨이퍼당 실효 BOM은 CS-3와 비슷한 수준에 머물 것으로 저자들은 본다. 고객 입장에서는 비슷한 총소유비용(TCO)으로 상호작용성과 토큰 매출이 거의 두 배가 되는 셈이라 매력적인 제안이다.

세레브라스가 CS-4를 소개할 때 가장 즐겨 쓰는 숫자는 온칩 메모리 대역폭 합계 초당 43페타바이트(PB/s)다 — 회사는 이를 엔비디아 루빈 대비 약 2,000배 많은 대역폭이라고 홍보하는데, 온웨이퍼 SRAM 대역폭이 전력 투입량에 비례해 늘어나는 구조라 클럭·전력을 올린 만큼 그대로 커진 수치다. 다만 실제 체감 상호작용성 개선 폭은 이보다 훨씬 보수적이다 — 회사 스스로도 GPU 대비 최대 30배의 "초고속(ultrafast)" 등급 상호작용성 개선을 내세운다.

```mermaid
flowchart TD
    BOM["비용 구조"] --> Raise["냉각·전력 증가<br/>→ BOM 상승 요인"]
    BOM --> Offset["부품 수 감소·조립 단순화<br/>→ 상쇄"]
    Offset --> SimilarBOM["웨이퍼당 실효 BOM<br/>CS-3와 비슷한 수준 전망"]

    style SimilarBOM fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    Marketing["43 PB/s 온칩 대역폭<br/>(루빈 대비 약 2,000배)"] --> RealClaim["실제 상호작용성 주장:<br/>GPU 대비 최대 30배"]
    RealClaim --> WhyGap["대역폭은 전력에 비례해<br/>커지지만 체감 속도는<br/>더 보수적으로 환산"]

    style Marketing fill:#fff7ed,stroke:#ea580c,stroke-width:2px
    style RealClaim fill:#f0fdf4,stroke:#16a34a
```

저자들의 추정으로는 프론티어 모델에서 CS-4가 초당 사용자당(tok/s/user) 약 4,000토큰에 근접하는 반면 CS-3는 2,000토큰에 그치고, 블랙웰 GPU는 이론상 최대치가 200토큰(실제로 이 값을 쓰는 곳은 없음)이며 현실적인 동시요청 수준에서는 100토큰 정도에 머문다. 그러면 CS-4는 블랙웰 대비 20\~40배 빠른 상호작용성이라는 계산이 나온다.

```mermaid
flowchart TD
    TokCompare["초당 사용자당 토큰<br/>(tok/s/user, 프론티어 모델)"] --> CS4tok["CS-4: 약 4,000"]
    TokCompare --> CS3tok["CS-3: 2,000"]
    TokCompare --> BWtok["블랙웰: 이론 200<br/>현실 약 100"]

    style CS4tok fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    Ratio["CS-4 vs 블랙웰 상호작용성"] --> R1["이론치 200 대비: 20배"]
    Ratio --> R2["현실치 100 대비: 40배"]
    R2 --> Fair["회사 주장 '최대 40배'는<br/>대역폭 2,000배보다<br/>훨씬 정직한 표현"]

    style Fair fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

---

## 5. 병렬화 전략 - 파이프라인 대 텐서 전문가 병렬

**📌 핵심:**
- 웨이퍼 한 장에는 모델 전체 가중치를 담을 SRAM이 부족하기 때문에, 세레브라스는 CS-4에서도 파이프라인 병렬화(모델을 층 단위로 나눠 웨이퍼마다 한 구간씩 맡기는 방식)에 집중한다 — MoE(전문가 혼합) 모델의 전문가(expert)는 웨이퍼 하나에 여러 개가 번갈아(interleaved) 배치된다. 큰 모델을 HBM에 욱여넣기 위해 텐서·전문가 병렬화를 주로 쓰는 GPU와는 기본 전제부터 다르다 — 세레브라스는 GPU의 HBM에 가중치를 두는 방식이 웨이퍼 한 장에서 다 처리하는 것보다 느리고 전력을 더 먹고 더 비싸다고 주장해왔다
- 웨이퍼 클러스터와 GPU 클러스터를 성능·전력·비용으로 비교한 결과는 어떤 병렬화 전략을 고르느냐에 크게 좌우된다 — GPU는 고처리량/저상호작용성부터 고상호작용성/저처리량까지 설정 폭이 넓은 반면, 웨이퍼는 사실상 고상호작용성/저처리량 한 지점만 쓴다. 엔비디아의 TileRT(GPU 클러스터에도 고처리량/저상호작용성 구성을 가능케 하는 신규 소프트웨어)를 함께 봐야 하는 이유가 여기 있다
- 웨이퍼가 44GB라 세레브라스는 지금 운영 중인 GPT 5.6 Sol 같은 프론티어 모델에서도 MoE 전문가 하나를 여러 웨이퍼로 쪼갤 필요가 아직 없었다 — 다만 긴 문맥(long context) 추론 수요 때문에, OpenAI 같은 고객이 웨이퍼 위에 KV 캐시(이전 토큰들의 연산 결과를 저장해두는 메모리)를 유지하는 비용을 아끼려고 5.6 Sol을 전체 100만(1M) 토큰이 아니라 25만 6천(256k) 토큰 문맥 창으로 돌릴 것으로 저자들은 예상한다
- 결론: 긴 문맥 추론 비용은 막대하다 — 한 번의 순전파(forward pass)에 필요한 메모리는 모델 파라미터 수에 비례하지만, KV 캐시에 필요한 메모리는 동시 사용자 수와 문맥 창 크기에 비례해 늘어난다는 사실은 여전히 덜 알려져 있다. 예컨대 1조 6천억(1.6T) 파라미터 규모의 DeepSeek V4 Pro를 100만 토큰 문맥으로 돌리는 데는 최소 세레브라스 시스템 약 20대, 동시요청 256건이라는 현실적 수준에서는 약 40대가 필요하다 — 첫 순전파를 얻기도 전에 2,000만 달러 이상의 설비투자와 1메가와트(MW)의 전력이 든다는 뜻이다 (분석 근거: 세레브라스 공개 톡노믹스 사이트 tokenomics.info/cerebras)

---

```mermaid
flowchart TD
    CerebrasPar["세레브라스 CS-4"] --> PP["파이프라인 병렬화<br/>(층 단위로 웨이퍼마다 분담)"]
    PP --> MoEInter["MoE 전문가<br/>웨이퍼 1장에 여러 개 배치"]

    style PP fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    GPUPar["GPU 클러스터"] --> TP["텐서·전문가 병렬화<br/>주로 사용"]
    TP --> WhyHBM["큰 모델을<br/>HBM에 담기 위함"]

    style TP fill:#fff7ed,stroke:#ea580c
```

```mermaid
flowchart TD
    ConfigRange["설정 폭 비교"] --> GPURange["GPU: 고처리량/저상호작용성<br/>~ 고상호작용성/저처리량<br/>폭넓게 조절 가능"]
    ConfigRange --> WaferRange["웨이퍼: 사실상<br/>고상호작용성/저처리량<br/>한 지점만"]

    style GPURange fill:#eff6ff,stroke:#3b82f6
    style WaferRange fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

```mermaid
flowchart TD
    LongCtx["긴 문맥 추론 비용"] --> Param["순전파 메모리:<br/>파라미터 수에 비례"]
    LongCtx --> KV["KV캐시 메모리:<br/>동시사용자 수 × 문맥 길이에 비례"]

    style KV fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

```mermaid
flowchart TD
    DeepSeekEx["예시: DeepSeek V4 Pro<br/>1.6T 파라미터"] --> Ctx1M["100만 토큰 문맥:<br/>최소 세레브라스 약 20대"]
    DeepSeekEx --> Ctx256["동시요청 256건:<br/>약 40대 필요"]
    Ctx256 --> CostPower["2,000만 달러+ CAPEX<br/>1MW 전력"]

    style CostPower fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

---

*작성 진행률: 약 70% 완료*
*업데이트: 백팩 랙(전력·연산 분리 구조, TDP·비용·상호작용성), 병렬화 전략(파이프라인 vs 텐서·전문가, 긴 문맥 비용) 섹션 작성 완료*
