---
categories: [ai-models/rl]
---

# RL Systems Mind the Gap: Matching Trainer and Generator Throughput

> **출처**: [https://newsletter.semianalysis.com/p/rl-systems-mind-the-gap-matching](https://newsletter.semianalysis.com/p/rl-systems-mind-the-gap-matching)
> **저자**: Kimbo Chen, Cheang Kang Wen, Dylan Patel
> **발행일**: 2026-06-16

---

## 📑 목차

### 전체 섹션
 1. [서론 - RL 학습 비용의 구조](#1-서론---rl-학습-비용의-구조)
 2. [세 주체 - 생성기·RL 환경·학습기](#2-세-주체---생성기rl-환경학습기)
 3. [RL 알고리즘 기초 - GRPO](#3-rl-알고리즘-기초---grpo)
 4. [동기식에서 비동기식으로 - PipelineRL과 정책 지연](#4-동기식에서-비동기식으로---pipelinerl과-정책-지연)
 5. [RL 환경과 샌드박스](#5-rl-환경과-샌드박스)
 6. [처리량 매칭 프레임워크 - 학습기와 생성기 처리량 정량화](#6-처리량-매칭-프레임워크---학습기와-생성기-처리량-정량화)
 7. [처리량을 제약하는 요인들](#7-처리량을-제약하는-요인들)
 8. [정책 지연과 움직이는 목표](#8-정책-지연과-움직이는-목표)
 9. [사례 연구 1 - 긴 응답과 초기 탐색 단계](#9-사례-연구-1---긴-응답과-초기-탐색-단계)
10. [사례 연구 2 - 잦은 도구 호출과 쉬운 과제](#10-사례-연구-2---잦은-도구-호출과-쉬운-과제)
11. [사례 연구 3 - 샌드박스 확장 문제](#11-사례-연구-3---샌드박스-확장-문제)
12. [사례 연구 4 - 부분 롤아웃과 상태 유지형 샌드박스](#12-사례-연구-4---부분-롤아웃과-상태-유지형-샌드박스)
13. [소프트웨어 사용 경험 - Prime RL·slime·Modal](#13-소프트웨어-사용-경험---prime-rlslimemodal)
14. [오픈소스 RL 프레임워크의 역사](#14-오픈소스-rl-프레임워크의-역사)
15. [결론과 향후 계획](#15-결론과-향후-계획)
16. [RL 학습 TCO 분석 - 서버 비용과 Tinker 비교](#16-rl-학습-tco-분석---서버-비용과-tinker-비교)

---

## 🔑 용어 정리

본문을 순서대로 읽기 전에 알아두면 좋은 용어들입니다. 자세한 수치와 설명은 본문에서 처음 등장하는 위치에 나옵니다.

- **세 주체 (생성기·RL 환경·학습기)**: RL(강화학습) 학습 시스템을 이루는 세 부품 — 생성기(Generator)는 모델이 답을 만들게 하고, RL 환경(RL Environment)은 그 답을 채점해 보상을 매기고, 학습기(Trainer)는 그 결과로 모델을 실제로 업데이트함
- **GRPO (Group-Relative Policy Optimization)**: 같은 문제에 여러 개의 답(롤아웃)을 만들게 한 뒤, 그룹 평균보다 잘한 답은 강화하고 못한 답은 억제하는 방식으로 모델을 학습시키는 오픈소스 표준 RL 알고리즘
- **PipelineRL과 비동기 RL**: 학습기가 다음 가중치를 다 만들 때까지 생성기가 기다리지 않고, 롤아웃이 진행 중인 와중에도 새 가중치를 받아 계속 작업하게 하는 방식 — 학습기와 생성기가 서로 다른 속도로 돌 수 있게 해줌
- **정책 지연 (Policy Staleness)**: 어떤 답(롤아웃)을 만들 때 쓰인 모델 버전과, 그 답으로 학습할 때 학습기가 실제로 쓰고 있는 모델 버전 사이의 간극 — 너무 벌어지면 학습이 불안정해짐
- **샌드박스 (Sandbox)**: 모델이 생성한 코드를 실제로 실행해보는 격리된 실행 환경 — 가벼운 마이크로 가상머신부터 완전한 가상머신까지 다양하며, 시작 지연·확장성·안정성이 RL 학습 속도를 좌우함
- **처리량 매칭 (Throughput Matching)과 MFU**: 생성기가 답을 만드는 속도와 학습기가 그 답을 소비하는 속도를 맞추는 것이 이 글 전체의 핵심 틀 — MFU(Model FLOPs Utilization)는 GPU가 이론상 낼 수 있는 연산 능력 중 실제로 학습에 쓰인 비율
- **PD 분리 (Prefill-Decode Disaggregation)**: 추론 과정을 프롬프트를 읽어들이는 단계(프리필)와 답을 한 토큰씩 만들어내는 단계(디코드)로 나눠 서로 다른 GPU에 맡기는 방식 — 두 단계의 연산 특성이 달라 따로 최적화하면 더 빠름
- **TCO(총소유비용)와 WACC**: TCO는 GPU 구매비뿐 아니라 전력·시설·감가상각까지 합친 실제 총비용, WACC(Weighted Average Cost of Capital, 가중평균자본비용)는 이 감가상각을 시간당 비용으로 환산할 때 쓰는 자본비용률

---

## 1. 서론 - RL 학습 비용의 구조

**📌 핵심:**
- 코딩 어시스턴트는 세계 최대 B2B SaaS 시장 — 상위 6개 업체 연매출(ARR)만 300억 달러 이상, 연말까지 1,000억 달러를 넘어설 전망(SemiAnalysis 토크노믹스 모델 추산)
- 이런 코딩 능력은 사전학습만으로 나오지 않고, 사전학습 이후에 진행하는 **강화학습(RL, Reinforcement Learning)**이 실제로 능력을 끌어냄 — 클로드 Opus 4.8은 SWE-bench Pro 69.2%, Terminal-Bench 2.1에서 74.6%를 기록했는데, 이 점수의 상당 부분이 RL 학습에서 나옴
- 앤트로픽 CEO 다리오 아모데이는 RL도 사전학습처럼 학습 시간을 늘릴수록 성능이 로그-선형으로 오르는 스케일링 법칙을 보인다고 설명 — 문제는 이 스케일링이 막대한 비용이 들어, RL 시스템을 얼마나 효율적으로 짜느냐가 "얼마나 많은 RL을 감당할 수 있는가", 나아가 "모델 능력이 어디까지 갈 수 있는가"를 직접 결정
- 결론: 이 글은 오픈소스 RL 프레임워크로 직접 학습 실험을 하고 가격을 호스팅형 RL 서비스인 Tinker(Thinking Machines Lab)와 비교해, RL 시스템 효율의 핵심이 **학습기(Trainer)와 생성기(Generator)의 처리량을 맞추는 것**임을 보임

---

```mermaid
flowchart TD
    Market["코딩 어시스턴트 시장<br/>상위 6개사 ARR 300억 달러+<br/>연말 1,000억 달러 전망"] --> Source["능력의 출처: 사전학습만으론 부족<br/>사후학습·강화학습(RL)이 실제로 끌어냄"]
    Source --> Example["클로드 Opus 4.8:<br/>SWE-bench Pro 69.2%<br/>Terminal-Bench 2.1 74.6%"]
    Source --> Scaling["RL도 사전학습처럼<br/>학습시간↑ → 성능 로그-선형↑<br/>(다리오 아모데이, 앤트로픽 CEO)"]
    Scaling --> Cost["막대한 비용 → RL 시스템 효율이<br/>감당 가능한 RL 양·모델 능력 한계를 결정"]

    style Cost fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

이 글의 핵심 질문은 "무엇이 RL 학습 시스템의 효율을 좌우하는가"입니다. SemiAnalysis는 오픈 모델·오픈소스 RL 프레임워크로 직접 학습 실험을 수행하고, 그 결과를 호스팅형 RL 학습 서비스인 Tinker의 가격과 비교했습니다. 그 결론은 시스템 효율이 **학습기와 생성기의 처리량을 서로 맞추는 것**으로 귀결된다는 것입니다.

---

## 2. 세 주체 - 생성기·RL 환경·학습기

**📌 핵심:**
- 오픈소스 RL 학습 시스템은 **생성기(Generator)·RL 환경(RL Environment)·학습기(Trainer)** 세 주체로 구성 — 사전학습과 달리 데이터셋이 정답을 미리 주지 않고, 시스템 스스로 학습 신호를 만들어냄
- 생성기는 데이터셋의 프롬프트로 추론(inference)을 수행해 **롤아웃(Rollout)**(프롬프트+모델 응답)을 만들고, RL 환경은 그 롤아웃을 채점해 보상(reward)을 매김 — 예를 들어 코딩 환경은 생성된 코드를 샌드박스에서 실제 실행해 테스트 케이스 통과율로 점수를 매김
- 학습기는 생성기가 만든 롤아웃과 보상을 받아 모델을 학습시켜 새 가중치(weights)를 만들고, 이를 다시 생성기에 전달 — 이 한 바퀴가 RL 학습의 한 스텝(step)을 완성
- 결론: 세 주체가 순환 고리를 이루기 때문에, 어느 한쪽이 느려지면 나머지가 기다려야 함 — 이 순환 고리의 속도를 맞추는 문제가 이 글 전체를 관통하는 주제

---

```mermaid
flowchart TD
    Gen["① 생성기(Generator)<br/>프롬프트로 추론 수행<br/>→ 롤아웃(프롬프트+응답) 생성"] --> Env["② RL 환경(RL Environment)<br/>롤아웃을 채점 → 보상(reward) 산출<br/>(예: 코드를 샌드박스에서 실행해<br/>테스트 통과율로 점수화)"]
    Env --> Train["③ 학습기(Trainer)<br/>롤아웃+보상으로 모델 학습<br/>→ 새 가중치 생성"]
    Train --> Gen

    style Gen fill:#eff6ff,stroke:#3b82f6
    style Env fill:#fff7ed,stroke:#ea580c
    style Train fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

사전학습은 인터넷 텍스트라는 정답(다음 토큰)이 이미 주어져 있지만, RL은 데이터셋이 프롬프트만 줄 뿐 정답을 주지 않습니다. 대신 시스템이 스스로 생성기·환경·학습기의 순환을 통해 학습 신호를 만들어내야 합니다.

---

## 3. RL 알고리즘 기초 - GRPO

**📌 핵심:**
- 사전학습이 다음 단어를 맞출 확률(로그우도)을 최대화하는 것과 달리, RL 학습은 기대 보상을 최대화 — 오픈소스 표준은 **GRPO(Group-Relative Policy Optimization)**로, 대부분의 오픈웨이트 모델이 이 계열의 변형을 사용
- GRPO는 같은 프롬프트에 여러 개의 응답(롤아웃)을 만들어 하나의 그룹을 구성하고, 각 롤아웃에 보상을 매긴 뒤 **어드밴티지(Advantage)**(그룹 평균 대비 얼마나 잘했는지)를 계산 — 평균보다 잘한 롤아웃은 강화하고 못한 롤아웃은 억제
- 그룹 안의 모든 롤아웃이 똑같은 보상을 받으면(문제가 너무 쉬워 전부 통과하거나, 너무 어려워 전부 실패) 어드밴티지가 전부 0이 돼 학습 신호가 사라짐 — 정답률(solve rate)이 100%나 0%에 가까울 때 이런 현상이 발생
- 결론: GRPO의 학습 신호는 그룹 내 보상의 분산에서 나오므로, 문제 난이도를 정답률이 극단으로 쏠리지 않는 "생산적인 중간 구간"으로 유지하는 것이 RL 학습 설계의 핵심 과제

---

```mermaid
flowchart TD
    Prompt["프롬프트 1개"] --> Rollouts["N개 응답(롤아웃) 그룹 생성<br/>(N=8 또는 16이 일반적)"]
    Rollouts --> Reward["각 롤아웃에 보상(reward) 부여"]
    Reward --> Advantage["어드밴티지(Advantage) 계산<br/>= 개별 보상 - 그룹 평균 보상"]
    Advantage --> Update["평균보다 잘한 롤아웃 강화<br/>못한 롤아웃 억제"]

    style Advantage fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

📌 용어 풀이: 왜 정답률이 극단으로 쏠리면 학습이 멈추나
> - 그룹 안의 모든 롤아웃이 같은 보상을 받으면, 각 롤아웃의 보상이 곧 그룹 평균과 같아져 어드밴티지가 0이 됨 — "다른 것보다 잘했다/못했다"라는 상대적 신호 자체가 없어지는 것
> - 극단적인 경우는 균일 분포(uniform distribution)일 때, 즉 문제가 너무 쉬워 전원 통과하거나 너무 어려워 전원 실패할 때(정답률 100%에 가깝거나 0%에 가까울 때) 발생

---

## 4. 동기식에서 비동기식으로 - PipelineRL과 정책 지연

**📌 핵심:**
- GRPO가 기반한 고전적 정책 경사(policy gradient) 알고리즘은 한 그룹의 롤아웃이 전부 같은 정책(같은 가중치)에서 나왔다고 가정 — 이 때문에 생성기는 현재 배치를 다 끝내기 전엔 가중치를 갱신할 수 없고, 학습기는 학습 스텝을 마쳐도 생성기를 기다려야 함(동기식 실행) → 시스템 효율이 크게 떨어짐
- **PipelineRL**은 롤아웃이 진행 중인 와중에도 학습기가 새 가중치를 생성기에 밀어 넣는 방식(in-flight weight update)으로 비동기성을 도입 — 학습기가 생성기의 롤아웃과 실행을 겹칠 수 있게 함
- 대가는 **정책 지연(Policy Staleness)**: 한 샘플(롤아웃+보상)이 신구 정책이 섞인 채로 만들어짐 — 너무 오래된(stale) 샘플은 모델 학습을 저해하므로, PipelineRL은 지연 정도에 상한(bound)을 두는 처리량 매칭 방식으로 작동
- 결론: 동기식 실행은 대규모에서 컴퓨트 낭비가 너무 커 실용적이지 않아 비동기 기법이 필수가 됐고, PipelineRL은 오픈소스 RL 학습의 사실상 표준 구현으로 자리잡음

---

```mermaid
flowchart TD
    Sync["동기식 실행<br/>학습기가 생성기의 배치 완료를 기다림"] --> Problem["학습기 스텝 완료 후에도<br/>생성기 대기 → 유휴시간 발생"]
    Problem --> Pipeline["PipelineRL: 롤아웃 진행 중에도<br/>새 가중치를 생성기에 즉시 전달<br/>(in-flight weight update)"]
    Pipeline --> Overlap["학습기·생성기 실행을 겹침<br/>→ 처리량 매칭 가능"]
    Overlap --> Staleness["대가: 정책 지연(Policy Staleness)<br/>샘플이 신구 정책 혼합으로 생성됨<br/>→ 지연 상한(budget)으로 통제"]

    style Pipeline fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
    style Staleness fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

📌 용어 풀이: 정책 지연은 왜 생기나
> - 학습기가 소비하는 단위인 "샘플"은 롤아웃과 그에 매겨진 보상으로 정의됨. in-flight 가중치 갱신을 쓰면 하나의 샘플이 생성되는 동안에도 정책(가중치)이 바뀔 수 있어, 신구 정책이 섞인 채로 샘플이 만들어짐
> - PipelineRL의 실험 결과 RL 알고리즘은 어느 정도의 정책 지연은 견딜 수 있지만, 너무 오래된 샘플은 학습을 저해함 — 즉 PipelineRL의 본질은 "지연 상한이 있는 처리량 매칭 기법"

---

## 5. RL 환경과 샌드박스

**📌 핵심:**
- RL 환경은 실무적으로 **샌드박스(Sandbox)**(코드 실행용 컨테이너화 런타임)로 구현 — 환경 복잡도에 따라 가벼운 Firecracker 마이크로 가상머신부터 완전한 QEMU 가상머신까지 다양
- 생성기와 RL 환경 사이의 상호작용 지연이 롤아웃 전체 지연에 직결되는데, 샌드박스 시작 지연(startup latency)이 주요 오버헤드 중 하나 — Modal 같은 샌드박스 서비스 업체는 콘텐츠 주소 지정 캐싱(content-addressed caching) 같은 기법으로 이 지연을 줄임
- 대규모 서빙도 난제: 샌드박스 개수는 동시 진행 중인 롤아웃 수에 비례해 늘어나고, 롤아웃이 잘리거나(pruned) 완료되면서 살아있는 샌드박스 수가 크게 출렁임 — 게다가 모델이 학습 중 파일을 백만 개 만드는 등 예상 밖 동작으로 샌드박스 자원을 고갈시킬 수 있어, 이런 장애를 감지·복구하는 오케스트레이션이 필요
- 결론: 샌드박스는 RL 학습의 숨은 병목 — 시작 지연·확장성·장애 복구 세 가지 모두 갖춰야 대규모 RL 학습이 안정적으로 돌아감

---

```mermaid
flowchart TD
    Sandbox["샌드박스(코드 실행용<br/>컨테이너화 런타임)"] --> Type["경량 Firecracker 마이크로 VM<br/>~ 완전한 QEMU VM"]
    Sandbox --> Challenge1["시작 지연(startup latency)<br/>→ 롤아웃 전체 지연에 직결<br/>(Modal 등은 콘텐츠 캐싱으로 완화)"]
    Sandbox --> Challenge2["대규모 서빙: 동시 롤아웃 수에 비례해<br/>샌드박스 수 급변동"]
    Sandbox --> Challenge3["장애 복구: 모델의 예상 밖 동작<br/>(예: 파일 100만 개 생성)으로<br/>자원 고갈 시 감지·복구 필요"]

    style Challenge1 fill:#fff7ed,stroke:#ea580c
    style Challenge3 fill:#fef2f2,stroke:#dc2626
```

---

## 6. 처리량 매칭 프레임워크 - 학습기와 생성기 처리량 정량화

**📌 핵심:**
- SemiAnalysis는 RL 학습 시스템을 하나의 큐(queue)로 봄 — 생성기가 롤아웃을 큐에 채워 넣고 학습기가 이를 꺼내 씀. 생성기가 학습기보다 느리면 큐가 비어 학습기가 굶주리고(idle), 생성기가 더 빠르면 큐가 쌓이며 샘플이 오래돼 정책 지연 문제가 생김
- 이상적인 시스템에서는 학습기의 소비율(consumption rate)과 생성기의 생산율(production rate)이 거의 같아야 함 — 학습기 쪽은 학습 스텝 시간(신규 샘플 대기 유휴 시간+연산 시간+가중치 전파 시간)을 재고, 이를 뒤집어 **샘플 처리량**(초당 샘플 수)을 구함. GPU당 초당 연산량(FLOP/s)과 **MFU(Model FLOPs Utilization)**로 하드웨어 효율도 함께 측정
- 생성기 쪽은 **스트래글러(Straggler)**(그룹 내에서 가장 오래 걸리는 롤아웃)가 그룹 전체의 완료 시각을 결정하므로, 종단 간(end-to-end) 지연을 측정해 평균 샘플 처리량을 추정 — 이 지연은 다시 LLM 추론 시간과 RL 환경 상호작용 시간(샌드박스 시작+실행)으로 쪼갤 수 있음
- 결론: "학습기 소비율 ≈ 생성기 생산율"이라는 큐 관점이 이 글 전체를 관통하는 분석틀 — 이어지는 사례 연구들은 전부 이 두 처리량이 왜, 얼마나 어긋났는지를 진단하는 구조

---

```mermaid
flowchart TD
    Queue["RL 학습 시스템 = 큐(Queue)<br/>생성기가 채우고 학습기가 소비"] --> Slow["생성기 < 학습기 속도<br/>→ 큐 비어 학습기 유휴(starve)"]
    Queue --> Fast["생성기 > 학습기 속도<br/>→ 큐 적체, 샘플 노후화(정책 지연↑)"]
    Queue --> Ideal["이상적 상태: 두 처리량이 거의 같음"]

    style Slow fill:#fef2f2,stroke:#dc2626
    style Fast fill:#fff7ed,stroke:#ea580c
    style Ideal fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    TrainerSide["학습기 소비율 측정"] --> T1["학습 스텝 시간<br/>(대기+연산+가중치 전파)"]
    T1 --> T2["샘플 처리량 = 샘플 수 / 연산 시간"]
    T1 --> T3["MFU = 실제 연산량 / 이론상 최대 연산량"]

    GenSide["생성기 생산율 측정"] --> G1["스트래글러(그룹 내 최장 롤아웃)가<br/>그룹 완료 시각을 결정"]
    G1 --> G2["종단 간 지연 = LLM 추론 시간<br/>+ RL 환경 상호작용 시간<br/>(샌드박스 시작+실행)"]
    G2 --> G3["샘플 처리량 = 샘플 수 / 종단 간 지연"]

    style T3 fill:#eff6ff,stroke:#3b82f6
    style G3 fill:#eff6ff,stroke:#3b82f6
```

📌 용어 풀이: 스트래글러(Straggler)가 왜 중요한가
> - 그룹 안의 롤아웃들은 응답 길이가 제각각인데, GRPO는 그룹 전체가 다 모여야 어드밴티지를 계산할 수 있음
> - 따라서 그룹에서 가장 늦게 끝나는 롤아웃(스트래글러) 하나가 그룹 전체의 완료 시각을 결정 — 응답 길이 편차가 클수록 스트래글러로 인한 지연 손실도 커짐

---

## 7. 처리량을 제약하는 요인들

**📌 핵심:**
- **학습기 소비율** = 스텝당 샘플 수 ÷ 학습 스텝 시간 — 스텝당 샘플 수는 그룹 크기(보통 8\~16개, GPU 커널 작성 같은 과제는 N=64까지 늘림)·보상 분포에 따른 어드밴티지 필터링(0-어드밴티지 샘플 제외 시 감소)·최소 유효 배치 크기 3가지가 좌우
- 학습 스텝 시간은 모델(크기·활성화 크기·정밀도가 메모리 사용량과 필요 최소 GPU 수를 결정)과 병렬화·메모리 설정(텐서/파이프라인/데이터/전문가 병렬화, FSDP, 메모리 오프로딩, 활성화 체크포인팅)이 좌우
- **생성기 생산율** = 동시 진행 롤아웃 수 ÷ 종단 간 지연 — 지연은 추론 처리량(병렬화 전략·KV 캐시 양자화·PD 분리·추측 디코딩)·샌드박스 지연·보상 모델 유형(경량 검증기 vs LLM 심사관)·보상 형태(구간별 채점 vs 전체 롤아웃 채점)가 좌우하고, 동시성 자체는 KV 캐시 메모리 용량을 평균 시퀀스 길이로 나눈 값으로 상한이 정해짐
- 결론: 생성된 롤아웃이 전부 학습에 쓰이는 건 아님 — **유효 생성 생산율 = 수락률(acceptance rate) × 생성 생산율**로 정의되며, 조기 잘라내기(early pruning)와 적응적 샘플링(온라인 어드밴티지 필터링)이 수락률을 낮춤

---

```mermaid
flowchart TD
    TrainerRate["학습기 소비율<br/>= 스텝당 샘플 수 / 학습 스텝 시간"] --> TSamples["스텝당 샘플 수 결정 요인"]
    TSamples --> TS1["그룹 크기<br/>(보통 8~16, 커널 과제는 N=64)"]
    TSamples --> TS2["보상 분포·어드밴티지 필터링<br/>(0-어드밴티지 샘플 제외 시 감소)"]
    TSamples --> TS3["최소 유효 배치 크기"]

    TrainerRate --> TTime["학습 스텝 시간 결정 요인"]
    TTime --> TT1["모델(크기·활성화·정밀도<br/>→ 최소 GPU 수 결정)"]
    TTime --> TT2["병렬화·메모리 설정<br/>(TP/PP/DP/EP, FSDP,<br/>오프로딩, 활성화 체크포인팅)"]

    style TrainerRate fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    GenRate["생성기 생산율<br/>= 동시 롤아웃 수 / 종단 간 지연"] --> GLatency["종단 간 지연 결정 요인"]
    GLatency --> GL1["추론 처리량<br/>(병렬화·KV캐시 양자화·<br/>PD분리·추측 디코딩)"]
    GLatency --> GL2["샌드박스 지연<br/>(시작+실행 시간)"]
    GLatency --> GL3["보상 모델 유형·형태<br/>(경량 검증기 vs LLM 심사관,<br/>구간별 vs 전체 채점)"]

    GenRate --> Concurrency["동시성 상한<br/>= KV캐시 메모리 용량 / 평균 시퀀스 길이"]

    Effective["유효 생성 생산율<br/>= 수락률 × 생성 생산율"] --> E1["수락률을 낮추는 요인:<br/>조기 잘라내기(early pruning)<br/>적응적 샘플링(온라인 어드밴티지 필터링)"]

    style GenRate fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
    style Effective fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

---

## 8. 정책 지연과 움직이는 목표

**📌 핵심:**
- 정책 지연은 세 단계에서 발생 — ① 궤적(trajectory) 수준(롤아웃 시작 시점 정책과 학습기가 실제 쓰는 정책 버전의 차이) ② 토큰 수준(롤아웃 도중 가중치가 갱신돼 한 응답 안에 서로 다른 정책 버전이 섞임) ③ 환경 상태 수준(12번 섹션에서 상세)
- **정책 지연 예산(Staleness Budget)**이 생성기가 학습기보다 몇 스텝까지 앞서갈 수 있는지 상한을 정함 — 이 상한이 곧 생성기 생산율과 학습기 소비율이 벌어질 수 있는 최대 폭을 결정
- 모델의 능력(**정답률, Solve Rate**)과 행동(응답 길이·도구 호출 패턴)은 RL 학습 중 계속 변하는 "움직이는 목표" — 정답률이 0%나 100%에 가까우면 보상 분포가 균일해져 학습 신호가 사라지므로, 이 변화가 그룹 크기·어드밴티지 필터링·커리큘럼(과제 난이도 순서) 설계를 계속 흔듦
- 결론: 모델이 사고 연쇄(Chain-of-Thought) 추론으로 긴 응답을 내놓기 시작하면 KV 캐시 사용량이 늘어 동시 롤아웃 수가 줄고, 도구 호출이 늘면 샌드박스 시간이 늘어남 — 시스템은 모델 능력·행동의 변화에 맞춰 계속 재적응해야 함

---

```mermaid
flowchart TD
    Staleness["정책 지연 발생 3단계"] --> S1["궤적 수준<br/>롤아웃 시작 시점 정책 ≠<br/>학습기가 실제 쓰는 정책"]
    Staleness --> S2["토큰 수준<br/>롤아웃 도중 가중치 갱신<br/>→ 한 응답에 신구 정책 혼재"]
    Staleness --> S3["환경 상태 수준<br/>(부분 롤아웃 재개 시 발생,<br/>12번 섹션 상세)"]

    Staleness --> Budget["정책 지연 예산이<br/>생성기가 학습기보다<br/>앞서갈 수 있는 최대 스텝 수 결정"]

    style Budget fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

```mermaid
flowchart TD
    Moving["움직이는 목표:<br/>모델 능력·행동이 학습 중 계속 변함"] --> Capability["능력 변화(정답률)<br/>0%·100%에 가까우면<br/>보상 분포 균일 → 학습신호 소멸"]
    Moving --> Behavior["행동 변화<br/>(응답 길이·도구 호출 패턴)"]

    Capability --> Curriculum["그룹 크기·어드밴티지 필터링·<br/>커리큘럼 설계에 영향"]
    Behavior --> KVImpact["긴 응답 → KV캐시 사용↑<br/>→ 동시 롤아웃 수↓"]
    Behavior --> SandboxImpact["도구 호출↑ → 샌드박스 시간↑"]

    style Capability fill:#fef2f2,stroke:#dc2626
    style Behavior fill:#fff7ed,stroke:#ea580c
```

---

## 9. 사례 연구 1 - 긴 응답과 초기 탐색 단계

**📌 핵심:**
- Qwen3-235B-A22B-Thinking-2507 모델로 학습(**BF16 정밀도**)·생성(**FP8 정밀도**)을 분리하는 RL 학습에서 흔한 설정을 사용 — 학습기 H200 64개(FSDP·EP8·CPU 오프로딩), 생성기 192개(24개 추론 인스턴스, 각 DP1/TP8/EP8), 배치 512개(그룹 크기 16×문제 32개), 최대 시퀀스 길이 32K, 최대 정책 지연 16스텝
- 모델의 긴 응답(길게 사고하는 추론 흔적)이 LLM 추론 시간을 지배해 샘플 생성 시간 대부분을 차지 — 응답 길이 편차가 커 그룹 내 꼬리 지연(tail latency)이 심해지자, 오버샘플링(학습기가 필요한 것보다 더 많은 롤아웃을 동시 실행하다 목표 개수 도달 즉시 미완료분 폐기)으로 대응했고 이번 실행에서는 실행 중이던 롤아웃의 60%를 폐기
- 커리큘럼(과제 난이도 순서)이 베이스 모델에 비해 다소 어려웠던 것으로 확인 — 첫 학습 스텝 직후 정답을 하나도 못 만든 롤아웃 그룹 수가 급증했으나 평균 보상과 pass@16은 서서히 회복, 초기 RL 탐색이 정책을 기존에 풀 수 있던 문제에서 밀어냈다가 다시 배우는 과정으로 해석
- 결론: 두 요인이 합쳐져 **생성 병목(Generation-Bound) 시스템**이 됨 — 학습기는 초당 2.75샘플을 소비하며 유휴시간이 전체의 30%, MFU 10.5%에 그친 반면, 생성기는 초당 1.95샘플을 내놓으며 학습기 대비 3배의 컴퓨트를 사용해 추론 효율이 RL 학습 속도에 얼마나 중요한지 보여줌

---

```mermaid
flowchart TD
    Setup["실험 설정: Qwen3-235B-A22B-Thinking<br/>학습 BF16 / 생성 FP8"] --> T["학습기: H200 64개<br/>FSDP·EP8·CPU오프로딩"]
    Setup --> G["생성기: H200 192개<br/>24개 인스턴스(DP1/TP8/EP8)"]
    Setup --> B["배치 512개<br/>(그룹크기16×문제32),<br/>최대 시퀀스 32K, 정책지연 상한 16스텝"]

    style Setup fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    LongResp["모델의 긴 사고 응답<br/>→ LLM 추론시간이 생성시간 대부분 차지"] --> Tail["응답 길이 편차↑<br/>→ 그룹 내 꼬리 지연 심화"]
    Tail --> Oversample["대응: 오버샘플링<br/>필요량보다 더 많이 실행 후<br/>목표 도달시 미완료분 폐기<br/>(이번 실행: 60% 폐기)"]

    Hard["커리큘럼이 다소 어려움<br/>→ 1스텝 후 무득점 그룹 급증<br/>→ 평균보상·pass@16 서서히 회복"] --> Bound["생성 병목(Generation-Bound) 시스템"]
    Oversample --> Bound

    Bound --> Result["학습기: 2.75샘플/s, 유휴 30%, MFU 10.5%<br/>생성기: 1.95샘플/s, 학습기 대비 3배 컴퓨트 사용"]

    style Oversample fill:#fff7ed,stroke:#ea580c
    style Bound fill:#fef2f2,stroke:#dc2626,stroke-width:2px
    style Result fill:#fef2f2,stroke:#dc2626
```

---

## 10. 사례 연구 2 - 잦은 도구 호출과 쉬운 과제

**📌 핵심:**
- GLM-5 모델(학습 BF16·생성 FP8)로, 학습기 H200 128개(FSDP·CP2·EP8·CPU 옵티마이저 오프로딩), 생성기 H200 128개를 64개짜리 추론 복제본 2개로 구성하고 **PD 분리**(프리필 GPU 32개·디코드 GPU 32개, 각각 DP32/EP32)를 적용, 노드당 1TB KV 캐시 오프로드
- 이번 실행에서는 두 요인이 시스템 효율을 결정 — ① 모델 행동 변화: 턴당 평균 응답 길이와 도구 호출 수(20\~51회)가 학습 진행에 따라 3배로 증가해 시퀀스 길이가 늘고 워크로드가 프리필 중심으로 이동(PD 분리 채택 이유와 부합) ② 커리큘럼이 모델에 비해 너무 쉬움
- 평균적으로 문제의 55%가 정답률 100%(그룹 내 모든 롤아웃이 통과)를 기록 — 그룹 내 모든 롤아웃이 같은 보상을 받으면 어드밴티지가 0이 돼 학습 신호가 사라지고, 평균 보상이 정체됨. 이 커리큘럼 불일치가 유효 학습 샘플 수와 유효 생성 생산율을 함께 낮춤
- 결론: 두 효과가 겹쳐 심한 **생성 병목** 상태가 됨 — 큐는 빠르게 채워지지만 상당 부분이 걸러져 나가고, LLM 추론이 종단 간 생성 시간의 대부분을 차지하며 학습이 진행될수록 계속 늘어남. 학습기는 대기 시간이 전체의 74%, 소비율이 생성기가 실제로 내놓는 생산율의 5배에 달함

---

```mermaid
flowchart TD
    Setup2["실험 설정: GLM-5<br/>학습 BF16 / 생성 FP8"] --> T2["학습기: H200 128개<br/>FSDP·CP2·EP8·옵티마이저 오프로딩"]
    Setup2 --> G2["생성기: H200 128개<br/>64GPU 복제본 2개<br/>PD분리(프리필32/디코드32)"]
    Setup2 --> B2["배치 512개, 그룹크기16,<br/>최대 시퀀스 64K, 정책지연 상한 16스텝"]

    style Setup2 fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    Behavior2["행동 변화: 응답 길이·도구호출(20~51회)<br/>학습 진행하며 3배 증가"] --> Prefill["워크로드가 프리필 중심으로 이동<br/>→ PD 분리 채택 정당화"]

    Easy["커리큘럼이 너무 쉬움<br/>문제 55%가 정답률 100%"] --> ZeroAdv["그룹 내 전원 통과<br/>→ 어드밴티지 0 → 평균보상 정체"]
    ZeroAdv --> LowEff["유효 학습샘플·유효 생성생산율 동반 하락"]

    Prefill --> Bound2["심한 생성 병목(Generation-Bound)"]
    LowEff --> Bound2
    Bound2 --> Result2["학습기: 대기 74%, 소비율=생성기 생산율의 5배"]

    style Easy fill:#fff7ed,stroke:#ea580c
    style Bound2 fill:#fef2f2,stroke:#dc2626,stroke-width:2px
    style Result2 fill:#fef2f2,stroke:#dc2626
```

---

## 11. 사례 연구 3 - 샌드박스 확장 문제

**📌 핵심:**
- Qwen3 235B A22B Instruct 2507(FP16, GB300 하드웨어, verl+uni-agent 롤아웃, GRPO, 최대 시퀀스 128K)로 3단계 학습 — 1단계는 학습기 32GPU(TP16/EP32)·생성기 24GPU(6복제본·TP4)·그룹크기8·동시롤아웃96, 2\~3단계는 학습기 48GPU(TP4/CP4/PP3/EP16)·생성기 동일 24GPU에 동시롤아웃을 256개(2단계)·360개(3단계)로 확대
- 다른 Qwen3 235B 실행들과 비슷하게 생성 병목이었음(학습기 유휴 30\~60%) — 응답 길이가 급격히 늘어 샘플의 3\~8%가 최대 시퀀스 길이 128K에 도달했고, 9번째 스텝에서는 눈에 띄는 스트래글러 1개 때문에 꼬리 지연이 7,500초까지 치솟음
- 이 실험의 독특한 부분은 동시 롤아웃 수를 96개에서 960개까지 밀어붙이며 샌드박스 확장 한계를 직접 겪은 것 — 960개 근처에서 계정에 설정된 한도를 넘자 샌드박스 초기화 데드락 오류와 최대 1시간짜리 스트래글러급 샌드박스 기동 지연이 발생, 다시 96개로 줄이면 이번엔 롤아웃 효율이 낮아지는 딜레마에 부딪힘
- 결론: 동시 롤아웃 확장은 배치 크기·보상 신호 분산 확대에 필수적이지만, 롤아웃 하나당 샌드박스 최소 1개가 필요해 샌드박스 인프라 안정성이 그대로 병목이 된다는 것을 실증 — Modal과 협업해 계정 자원 한도가 원인임을 확인하고 한도를 올려 고동시성에서도 안정성을 검증

---

```mermaid
flowchart TD
    Setup3["실험 설정: Qwen3 235B A22B Instruct<br/>FP16, GB300, verl+uni-agent, GRPO, 최대 128K"] --> Stage1["1단계: 학습기32GPU(TP16/EP32)<br/>생성기24GPU(6복제본·TP4)<br/>그룹8, 동시롤아웃96"]
    Setup3 --> Stage23["2~3단계: 학습기48GPU(TP4/CP4/PP3/EP16)<br/>동시롤아웃 256→360으로 확대"]

    style Setup3 fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    Push["동시 롤아웃 96 → 960개로 확대"] --> Limit["960 근처: 계정 한도 초과<br/>→ 샌드박스 초기화 데드락<br/>+ 최대 1시간 기동 지연"]
    Limit --> Rollback["96개로 축소<br/>→ 롤아웃 효율 저하"]
    Rollback --> Fix["Modal과 협업해 원인 규명<br/>(계정 자원 한도) → 한도 상향 후 안정성 확인"]

    style Limit fill:#fef2f2,stroke:#dc2626,stroke-width:2px
    style Fix fill:#f0fdf4,stroke:#16a34a
```

---

## 12. 사례 연구 4 - 부분 롤아웃과 상태 유지형 샌드박스

**📌 핵심:**
- Qwen3-235B-A22B-Thinking-2507-FP8(생성 FP8·학습 BF16), 학습기 H200 32개(TP4/CP4/PP2/EP8)·생성기 H200 64개(8복제본·TP8/EP8), 최대 시퀀스 32K·전역 배치 64, GSPO 알고리즘(그룹크기8), **slime** 프레임워크로 완전 비동기+부분 롤아웃 실행, SWE-Bench Verified·Lite 환경(OpenHands 하네스+Modal 샌드박스)
- 다른 Qwen3 235B 실행들과 비슷하게 생성 병목(학습기 대기 60%)에 턴당 12K 토큰의 긴 응답, 시퀀스당 평균 12회 도구 호출, 상대적으로 낮은 해결률을 보임 — 주목할 부분은 slime의 **부분 롤아웃(Partial Rollout)** 기능: 스트래글러 롤아웃을 버리지 않고 버퍼에 저장했다가 이후 배치에서 재개해 꼬리 지연을 완화
- 부분 롤아웃은 샌드박스 설계에 세 가지 요구사항을 추가 — ① 롤아웃 배치 간 상태 유지(SWE-Bench 문제는 파일을 절반만 수정한 상태일 수 있어 샌드박스 ID와 샘플 매핑 추적 필요) ② 지연 생성(도구 호출이 아예 없는 샘플에 샌드박스를 만드는 낭비 방지) ③ 장애 견고성(중단\~재개 사이 샌드박스가 죽으면 재개 대신 실패 처리)
- 결론: 재개된 롤아웃은 정책 간극뿐 아니라 **환경 상태 수준의 정책 지연**까지 떠안음 — 새 정책이 이전 정책이 만든 절반짜리 수정 상태를 이어받아야 하고, 이 문제가 부분 재개 발생이 잦았던 이번 실행의 낮은 해결률에 영향을 준 것으로 추정

---

```mermaid
flowchart TD
    Setup4["실험 설정: Qwen3-235B-Thinking-FP8<br/>학습기 H200 32개(TP4/CP4/PP2/EP8)<br/>생성기 H200 64개(8복제본·TP8/EP8)"] --> Frame["slime 프레임워크<br/>완전 비동기+부분 롤아웃<br/>SWE-Bench(OpenHands+Modal 샌드박스)"]

    style Setup4 fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    Partial["부분 롤아웃: 스트래글러를<br/>버리지 않고 버퍼 저장 후 재개"] --> Req1["요구사항①: 배치 간 상태 유지<br/>(반쯤 수정된 파일 추적)"]
    Partial --> Req2["요구사항②: 지연 생성<br/>(불필요한 샌드박스 생성 방지)"]
    Partial --> Req3["요구사항③: 장애 견고성<br/>(중단중 샌드박스 죽으면 실패 처리)"]

    Req1 --> StateStale["환경 상태 수준 정책 지연 발생<br/>새 정책이 옛 정책의 절반짜리<br/>수정 상태를 이어받음"]
    StateStale --> LowResolve["부분 재개 빈발 실행일수록<br/>해결률 낮음(추정)"]

    style StateStale fill:#fff7ed,stroke:#ea580c,stroke-width:2px
    style LowResolve fill:#fef2f2,stroke:#dc2626
```

📌 용어 풀이: slime의 KV 캐시 처리와 PD 분리의 연결고리
> - PipelineRL과 달리 slime의 SGLang 설정은 중단된 롤아웃의 KV 캐시를 그대로 비워버림(evict) — 재개된 롤아웃은 사실상 큰 규모의 프리필(prefill) 요청이 되는 셈
> - 이는 정확히 PD 분리(프리필과 디코드를 분리 처리)가 가장 큰 이득을 주는 워크로드 유형이라, 부분 롤아웃과 PD 분리 아키텍처가 서로 잘 맞물림

---

*작성 진행률: 약 75% 완료 (1\~12장)*
*업데이트: 사례 연구 2\~4(GLM-5 잦은 도구호출, GB300 샌드박스 확장, slime 부분 롤아웃) 작성 완료*
