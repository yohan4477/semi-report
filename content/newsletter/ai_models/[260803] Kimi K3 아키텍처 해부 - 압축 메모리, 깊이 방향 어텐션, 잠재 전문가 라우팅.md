---
categories: [ai-models]
---

# Kimi K3, The Manos, The Mythos, The Legendos

> **출처**: [SemiAnalysis Newsletter](https://newsletter.semianalysis.com/p/kimi-k3-the-manos-the-mythos-the)
> **저자**: Kimbo Chen, Shubham Choudhari, Bryan Shan, Dylan Patel
> **발행일**: 2026-08-03

---

## 📑 목차

### 전체 섹션
 1. [개요 - Kimi K3와 이 문서의 목적](#1-개요---kimi-k3와-이-문서의-목적)
 2. [Kimi Delta Attention의 계보 - 선형 어텐션에서 DeltaNet, Gated DeltaNet을 거쳐 KDA까지](#2-kimi-delta-attention의-계보---선형-어텐션에서-deltanet-gated-deltanet을-거쳐-kda까지)
 3. [FlashKDA 알고리즘과 연산 복잡도 - 프리필은 선형, 디코드는 상수](#3-flashkda-알고리즘과-연산-복잡도---프리필은-선형-디코드는-상수)
 4. [Kimi Linear 아키텍처 - KDA·MLA 하이브리드 비율과 MLA vs GQA 선택](#4-kimi-linear-아키텍처---kdamla-하이브리드-비율과-mla-vs-gqa-선택)
 5. [KV 캐시 효율 - KV 처리량이라는 새 지표](#5-kv-캐시-효율---kv-처리량이라는-새-지표)
 6. [KV 캐시 저장 계층과 Prefix 캐시 관리의 딜레마](#6-kv-캐시-저장-계층과-prefix-캐시-관리의-딜레마)
 7. [어텐션 잔차 - 깊이 방향으로도 어텐션을 적용하다](#7-어텐션-잔차---깊이-방향으로도-어텐션을-적용하다)
 8. [블록 어텐션 잔차 - 통신량 절감과 학습·추론 최적화](#8-블록-어텐션-잔차---통신량-절감과-학습추론-최적화)
 9. [LatentMoE - 라우팅 전 압축으로 통신량 줄이기](#9-latentmoe---라우팅-전-압축으로-통신량-줄이기)
10. [Quantile 부하 분산(QB) - 하이퍼파라미터 없는 라우팅 균형](#10-quantile-부하-분산qb---하이퍼파라미터-없는-라우팅-균형)
11. [서빙 성능과 TCO - InferenceX 실측 벤치마크](#11-서빙-성능과-tco---inferencex-실측-벤치마크)

---

## 🔑 용어 정리

본문을 순서대로 읽기 전에 알아두면 좋은 용어들입니다. 자세한 수치와 설명은 본문에서 처음 등장하는 위치에 나옵니다.

- **KDA (Kimi Delta Attention)**: Kimi K3가 쓰는 선형 어텐션 계열 기법 — 과거 정보를 압축된 하나의 메모리에 담아 두고 필요할 때 꺼내 쓰는 방식
- **선형 어텐션(Linear Attention)**: 기존 어텐션이 과거 토큰을 전부 하나하나 다시 훑는 것과 달리, 과거 정보를 하나의 고정 크기 메모리로 압축해두고 그 메모리만 갱신하는 방식
- **DeltaNet → Gated DeltaNet**: 압축 메모리가 무한정 커지는 문제를 고치기 위해, 필요 없는 정보를 지우는 규칙(DeltaNet)과 정보를 서서히 잊는 장치(Gated DeltaNet)를 차례로 추가한 계보
- **MLA vs GQA**: 둘 다 메모리를 아끼는 어텐션 방식이지만, MLA는 생성(디코드)에 유리하고 GQA는 입력 처리(프리필)에 유리해 워크로드 성격에 따라 유불리가 갈림
- **KV 캐시와 KV 처리량(KV Throughput)**: KV 캐시는 지금까지 나온 대화 내용을 기억해두는 임시 저장공간, KV 처리량은 이 저장공간 크기를 입력 처리 시간으로 나눈 값으로 실제 서빙 효율을 재는 지표
- **Prefix 캐시(접두사 캐시)**: 이미 처리한 적 있는 앞부분 대화 내용을 다시 계산하지 않고 재사용하는 캐싱 기법
- **어텐션 잔차(Attention Residuals)**: 신경망의 층(레이어)이 깊어질수록 앞쪽 정보가 희미해지는 문제를 풀기 위해, 각 층이 이전 모든 층의 결과를 필요한 만큼 골라 참조하게 하는 기법
- **LatentMoE**: 여러 GPU에 전문가(Expert)를 나눠 놓은 MoE 구조에서, 토큰을 전문가에게 보내기 전에 압축해 통신량을 줄이는 기법

---

## 1. 개요 - Kimi K3와 이 문서의 목적

**📌 핵심:**
- Kimi K3는 공개 직후 여러 리더보드를 휩쓸며 오픈소스 최고 모델(오픈 프론티어 모델)로 자리잡음 — 다만 그 성능을 뒷받침하는 기술이 이례적이어서 커뮤니티도 놀란 상황
- 이 문서는 Kimi K3 아키텍처의 핵심 기술을 해설하는 안내서 — ① 압축 메모리 방식의 새 어텐션(KDA) ② 층(레이어)을 가로지르는 어텐션(어텐션 잔차) ③ 전문가에게 보내기 전 압축하는 라우팅(LatentMoE), 이 세 가지 혁신이 실제로 통하는지를 ④ 서빙 성능·비용(TCO) 실측으로 검증
- 결론: Kimi K3는 널리 쓰이는 표준 설계를 그대로 따르지 않고 여러 지점에서 독자적인 선택을 했다는 것이 이 문서 전체를 관통하는 메시지

---

```mermaid
flowchart TD
    K3["Kimi K3 아키텍처<br/>3대 핵심 혁신"] --> A["① KDA(압축 메모리<br/>어텐션)"]
    K3 --> B["② 어텐션 잔차<br/>(층 방향 어텐션)"]
    K3 --> C["③ LatentMoE<br/>(라우팅 전 압축)"]

    style A fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
    style B fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
    style C fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    Validate["세 혁신이 실제로<br/>통하는지 검증"] --> Serve["④ 서빙 성능·TCO<br/>(InferenceX 실측 벤치마크)"]
    Serve --> Result["결과: 표준 설계 대신<br/>독자 선택을 곳곳에서 채택"]

    style Serve fill:#fff7ed,stroke:#ea580c,stroke-width:2px
    style Result fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

---

## 2. Kimi Delta Attention의 계보 - 선형 어텐션에서 DeltaNet, Gated DeltaNet을 거쳐 KDA까지

**📌 핵심:**
- 표준 어텐션(소프트맥스 어텐션)은 지금까지 나온 토큰(단어)을 전부 하나하나 다시 훑어야 해서 연산량이 토큰 수의 제곱에 비례 — 선형 어텐션은 소프트맥스 연산을 없애 과거 정보를 하나의 고정 크기 메모리(행렬 S)로 압축, 연산량을 토큰 수에 비례하는 수준으로 줄임
- 문제는 압축 메모리 S가 오래된 정보와 새 정보를 뒤섞으며 무한정 커져 학습이 불안정해진다는 것 — DeltaNet은 "관련 없는 연관을 지운다"는 규칙(Delta Rule)으로 S의 성장을 억제하고, Gated DeltaNet은 LSTM식 망각 게이트(alpha)를 추가해 메모리 수명 자체를 조절
- KDA(Kimi Delta Attention)는 이 alpha를 하나의 숫자가 아니라 채널마다 다른 값을 갖는 대각행렬로 확장 — 채널별로 세밀하게 망각 속도를 조절할 수 있게 되고, 이 덕분에 MLA의 위치 인코딩(RoPE) 없이도 스스로 위치를 인식하는 역할까지 겸함
- 결론: 표준 어텐션이 "모든 과거를 다 기억"이라면 KDA는 "필요한 것만 요약해 계속 갱신되는 메모장" — 압축으로 얻는 속도와 압축이 낳는 불안정성 사이의 균형을 이 계보 전체가 다루는 문제로 이해하면 됨

---

```mermaid
flowchart TD
    Soft["소프트맥스 어텐션<br/>(과거 토큰 전부 재조회,<br/>연산량 토큰수의 제곱)"] -->|"softmax 제거<br/>→ 계산량 선형화"| Lin["선형 어텐션<br/>(과거를 압축메모리 S<br/>하나로 압축)"]
    Lin -->|"S가 무한정 커지는<br/>문제 발생"| Delta["DeltaNet<br/>(Delta Rule로<br/>불필요한 연관 제거)"]
    Delta -->|"망각 게이트<br/>alpha 추가"| GDN["Gated DeltaNet<br/>(LSTM식 메모리<br/>수명 조절)"]
    GDN -->|"alpha를 채널별<br/>대각행렬로 확장"| KDA["KDA<br/>(채널별 세밀한 망각+<br/>위치 인식까지 겸함)"]

    style Lin fill:#eff6ff,stroke:#3b82f6
    style GDN fill:#fff7ed,stroke:#ea580c
    style KDA fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

**📌 용어 풀이: 압축 메모리를 "온라인 학습"으로 보는 관점**
> - 선형 어텐션의 갱신식은 "매 토큰마다 저장 행렬 S를 살짝 고쳐써서, 다음에 같은 키(key)로 물어봤을 때 더 정확한 값(value)을 꺼내도록 만드는" 학습 과정으로도 해석 가능 — 매 순간 조금씩 채점하고 고치는 셈
> - DeltaNet은 이 학습의 목표를 "값을 완벽히 재현"에서 "불필요하게 저장된 연관만 골라 지우기"로 바꿔, S가 계속 부풀어 오르는 것을 막음

---

## 3. FlashKDA 알고리즘과 연산 복잡도 - 프리필은 선형, 디코드는 상수

**📌 핵심:**
- Moonshot(Kimi 개발사)은 KDA 전용 커널 FlashKDA를 직접 개발해 오픈소스로 공개 — 문장을 한 토큰씩 순서대로 생성(디코드)할 때는 재귀식을 그대로 따르지만, 입력 처리(프리필) 시에는 토큰을 덩어리(청크)로 묶어 GPU에서 병렬 처리하도록 재구성
- FlashKDA는 커널 2개로 구성 — K1(청크 단위로 필요한 행렬을 미리 병렬 준비), K2(청크 단위로 순서대로 상태를 갱신) — 청크 안에서는 병렬, 청크 사이에서만 순서를 지키면 되어 GPU 활용도가 높아짐
- 복잡도 분석 결과 **프리필은 문장 길이에 비례(선형)**, **디코드는 문장 길이와 무관(상수)** — 표준 어텐션은 문장이 길어질수록 디코드 비용도 함께 늘어나는 것과 대조적으로, KDA는 문장이 아무리 길어져도 한 토큰을 생성하는 데 드는 비용이 일정하게 유지됨
- 결론: 이 "디코드가 상수 비용"이라는 성질이 KDA를 긴 문맥·에이전트형 워크로드에서 특히 유리하게 만드는 핵심 근거 — 다음 섹션(KV 캐시 효율)에서 이 성질이 실제 서빙에 어떤 영향을 주는지 다룸

---

```mermaid
flowchart TD
    Flash["FlashKDA 커널 구조"] --> K1["K1: 청크 단위<br/>행렬 병렬 준비"]
    Flash --> K2["K2: 청크 단위<br/>순차 상태 갱신"]
    K1 --> Parallel["청크 내부는 병렬,<br/>청크 사이만 순서 유지<br/>→ GPU 활용도 상승"]

    style Parallel fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    Complexity["연산 복잡도<br/>(문장 길이 대비)"] --> Prefill["프리필: 문장 길이에<br/>비례(선형)"]
    Complexity --> Decode["디코드: 문장 길이와<br/>무관(상수)"]
    Decode --> Contrast["표준 어텐션은 디코드도<br/>문장 길이에 비례해<br/>비용 증가 → KDA가 유리"]

    style Decode fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style Contrast fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

---

## 4. Kimi Linear 아키텍처 - KDA·MLA 하이브리드 비율과 MLA vs GQA 선택

**📌 핵심:**
- Moonshot은 Kimi Linear 모델을 KDA 설계를 검증하기 위한 사전 실험작으로 개발 — K3 기술 블로그와 비교해보면 공유 전문가(shared expert) 수, KDA·MLA 혼합 비율, 어텐션 모듈 설계를 그대로 물려받은 것으로 확인됨
- Kimi K3는 **KDA와 풀 어텐션 MLA를 3대1 비율로 번갈아 배치** — Kimi Linear 논문에서 이 비율이 성능과 효율의 균형점으로 검증됨, KDA가 MLA의 위치 인코딩(RoPE)까지 대신 담당해 위치 인식 능력이 강한 연산자 역할까지 겸함
- MLA를 풀 어텐션으로 유지한 것은 이례적 선택 — 다른 오픈소스 모델은 전부 GQA(그룹 쿼리 어텐션) 계열로 이동 중(DeepSeek V4의 CSA, GLM 5.2의 DSA, MiniMax M3의 MSA, MiMo V3의 HySparse) — MLA는 생성(디코드) 단계 계산을 줄이는 대신 입력 처리(프리필) 단계 계산이 늘어나는 트레이드오프가 있어, 대화형(추론 중심) 워크로드엔 유리하지만 프리필 비중이 큰 에이전트형 워크로드엔 불리
- 결론: 이 트레이드오프 때문에 저자들은 Moonshot의 다음 모델(가칭 Kimi K4)이 MLA를 대체할 어텐션 방식으로 전환할 가능성이 크다고 전망

---

```mermaid
flowchart TD
    Module["KDA 모듈 내부 연산"] --> QKV["Q·K·V: 선형변환+<br/>짧은 컨볼루션<br/>(인접 토큰 관계 포착)"]
    Module --> Norm["Q·K에 L2 정규화<br/>(학습 안정화)"]
    Module --> Gate["감쇠게이트 alpha(저랭크)·<br/>beta(다운프로젝션)"]
    Gate --> Out["출력: 헤드별 정규화+<br/>출력 망각게이트<br/>(K3는 선형변환으로 구현)"]

    style QKV fill:#eff6ff,stroke:#3b82f6
    style Out fill:#f0fdf4,stroke:#16a34a
```

```mermaid
flowchart TD
    Ratio["KDA·MLA 혼합 비율"] --> Pattern["KDA 3개 층마다<br/>MLA(풀 어텐션) 1개 층<br/>= 3대1 비율"]
    Pattern --> Role["KDA가 MLA의<br/>RoPE(위치 인코딩)까지<br/>대신 담당"]

    style Pattern fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    Choice["MLA vs GQA<br/>트레이드오프"] --> MLA["MLA: 디코드 계산 감소,<br/>프리필 계산 증가<br/>→ 대화형(추론중심)에 유리"]
    Choice --> GQA["GQA: 프리필에 유리<br/>→ 에이전트형(프리필비중↑)에 유리"]
    GQA --> Trend["DeepSeek V4·GLM 5.2·<br/>MiniMax M3·MiMo V3<br/>전부 GQA 계열로 전환"]

    style MLA fill:#fff7ed,stroke:#ea580c
    style Trend fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

---

## 5. KV 캐시 효율 - KV 처리량이라는 새 지표

**📌 핵심:**
- 저자들의 핵심 주장은 **KV 캐시 크기(공간복잡도)만 보고 효율을 판단하면 안 된다**는 것 — KV 캐시 크기는 모델 설계에 따라 달라지는 하나의 속성일 뿐, 실제로는 어떤 병렬화 방식으로 배포하는지(전문가를 널리 흩어 배치하는 방식 vs 텐서를 나눠 배치하는 방식)에 따라 KV 캐시에 남는 메모리 여유가 크게 달라짐
- 그래서 저자들은 **KV 처리량(KV Throughput)**이라는 새 지표를 제안 — "KV 캐시 크기 ÷ 프리필 시간(첫 토큰까지 걸리는 시간)"으로 정의, 분산 서빙(프리필과 생성을 다른 GPU 묶음에 맡기는 방식)에서 필요한 최소 대역폭을 알려주는 동시에 KV 캐시 효율을 종합적으로 재는 지표 역할
- 프리필 시간에는 모델 아키텍처의 효율이 그대로 반영되고, 문장이 길어질수록 메모리 병목 구간과 연산 병목 구간이 나뉘어 나타남 — 저자들이 실측한 결과 **문장이 길어질수록 KDA 같은 하이브리드 선형 어텐션의 이점이 더 뚜렷해지는 경향**이 확인됨
- 결론: 이 지표는 클러스터 안에서 KV 캐시를 여러 메모리 계층(다음 섹션 참고)으로 옮길 때 필요한 대역폭을 가늠하는 데도 그대로 활용 가능

---

```mermaid
flowchart TD
    Metric["KV 처리량(KV Throughput)<br/>= KV캐시 크기 ÷ 프리필시간"] --> Purpose1["분산서빙(PD분리) 최소<br/>필요 대역폭 산출"]
    Metric --> Purpose2["KV캐시 효율<br/>종합 지표 역할"]

    style Metric fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    Factor["KV캐시 효율에<br/>영향 주는 요소"] --> Arch["모델 아키텍처<br/>(프리필 시간에 반영)"]
    Factor --> Deploy["배포 방식<br/>(전문가 분산 vs<br/>텐서 분산)"]
    Deploy --> Length["문장이 길어질수록<br/>KDA 하이브리드의<br/>이점이 더 뚜렷해짐"]

    style Length fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

---

## 6. KV 캐시 저장 계층과 Prefix 캐시 관리의 딜레마

**📌 핵심:**
- KV 캐시가 저장되는 위치는 컴퓨터의 메모리 계층 구조와 똑같은 순서를 따름 — ① GPU의 HBM(가장 빠름, 모델 가중치가 쓰고 남은 용량만 사용) → ② 서버 DRAM(용량은 크지만 느림) → ③ SSD 디스크(더 크지만 더 느림) 순으로 넘침(spill)
- 분산 KV 캐시 프레임워크 Mooncake Store는 모든 작업자(worker)가 서로의 KV 캐시를 볼 수 있는 공유 풀을 제공 — DRAM과 이 풀 사이에 "쓰는 즉시 반영"(write-through) 방식을 적용하면 여러 서버 간 접두사 캐시 공유, 텐서 분산된 MLA의 중복 저장 방지, 서버 장애 시 캐시 이중화라는 세 가지 이점을 동시에 확보
- **Prefix 캐시의 딜레마**: 표준 어텐션은 앞부분이 몇 번째 토큰까지 같은지 몰라도 캐시를 저장해두면 되지만, KDA처럼 모든 과거를 압축한 하나의 상태값(recurrent state)만 남기는 방식은 그 경계를 모르면 매 토큰 위치마다 상태를 다 저장해야 함 — 그러면 KV 캐시가 다시 문장 길이에 비례해 커져 선형 어텐션을 쓰는 의미가 사라짐
- 결론: 이를 막기 위해 Moonshot과 vLLM은 거친 단위(예: 3만 2천 토큰마다)로만 상태를 저장하고, 에이전트형 대화는 보통 프롬프트 끝에서 새 턴이 시작된다는 점을 활용해 프롬프트 경계에서도 별도로 저장 — 다만 이 절충 때문에 KDA도 실제 서빙에서는 이론상 상수 크기가 아니라 어느 정도 문장 길이에 비례해 캐시가 늘어남을 저자들은 명시적으로 지적

---

```mermaid
flowchart TD
    Hierarchy["KV캐시 저장 계층"] --> HBM["① HBM(GPU, 최고속)<br/>가중치가 쓰고 남은 용량"]
    HBM --> DRAM["② 서버 DRAM<br/>(용량 크고 속도 낮음)"]
    DRAM --> SSD["③ SSD 디스크<br/>(용량 최대, 속도 최저)"]

    style HBM fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style SSD fill:#fef2f2,stroke:#dc2626
```

```mermaid
flowchart TD
    Mooncake["Mooncake Store<br/>write-through 정책"] --> Share["여러 서버 간<br/>접두사 캐시 공유"]
    Mooncake --> Dedup["텐서분산 MLA<br/>중복저장 방지"]
    Mooncake --> Redundancy["서버 장애 시<br/>캐시 이중화"]

    style Mooncake fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    Dilemma["KDA Prefix캐시 딜레마"] --> Problem["경계 모르면<br/>매 토큰마다 상태 저장<br/>→ 캐시가 다시 길이에 비례"]
    Problem --> Fix["해법: 3.2만 토큰마다+<br/>프롬프트 경계에서<br/>거칠게 저장"]
    Fix --> Reality["결과: 이론상 상수 크기지만<br/>실제 서빙에선<br/>길이에 어느정도 비례"]

    style Problem fill:#fef2f2,stroke:#dc2626
    style Reality fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

---

## 7. 어텐션 잔차 - 깊이 방향으로도 어텐션을 적용하다

**📌 핵심:**
- 잔차 연결(Residual Connection)은 신경망을 깊게 쌓을 수 있게 해준 핵심 발명 — 각 층이 입력에 "변화량"만 더하는 방식(출력 = 입력 + 그 층이 학습한 변화)으로, 앞쪽 층의 신호가 지워지지 않고 그대로 뒤쪽까지 전달되는 지름길(identity path) 역할
- 문제는 층이 깊어질수록 **앞쪽 층의 정보가 잔차 흐름을 지배**하고, 뒤쪽 층은 자신의 존재감을 내려고 출력을 점점 키우면서(gain 증가) 학습이 불안정해진다는 것 — 층마다 "어떤 이전 층의 정보를 골라 쓸지" 선택할 방법이 없다는 게 근본 원인
- 시퀀스(문장) 축에서는 이미 같은 문제를 겪은 적이 있음 — RNN도 이전 상태에 변화량만 더하는 같은 재귀식을 쓰다 문장이 길어질수록 신호가 희미해졌고, Transformer가 "과거 어느 토큰이든 어텐션으로 직접 조회"하는 방식으로 이 문제를 해결
- 결론: Kimi는 이 해법을 깊이(층) 축에 그대로 이식 — 각 층이 문장 속 과거 토큰이 아니라 **앞쪽 모든 층이 만든 결과물**을 대상으로 어텐션을 적용, 층마다 입력과 무관하게 학습되는 고정 쿼리(query)로 "어떤 이전 층의 결과를 얼마나 참조할지"를 선택 — 표준 잔차 연결보다 훨씬 세밀하고 선택적인 정보 접근이 가능해짐

---

```mermaid
flowchart TD
    Residual["표준 잔차 연결의 한계"] --> Dominate["앞쪽 층 정보가<br/>잔차 흐름 지배"]
    Dominate --> Gain["뒤쪽 층이 출력을<br/>점점 키움(gain 증가)<br/>→ 학습 불안정"]
    Gain --> NoChoice["층마다 이전 층 정보를<br/>선택적으로 고를 방법 없음"]

    style NoChoice fill:#fef2f2,stroke:#dc2626,stroke-width:2px
```

```mermaid
flowchart TD
    TimeAxis["시퀀스(문장) 축<br/>동일 문제 경험"] -->|"RNN도 신호 희미해짐<br/>→ Transformer가<br/>어텐션으로 해결"| Solved["Transformer:<br/>과거 어느 토큰이든<br/>직접 조회 가능"]
    Solved -->|"같은 해법을<br/>깊이(층) 축에 이식"| DepthAttn["Kimi 어텐션 잔차:<br/>각 층이 이전 모든 층의<br/>결과물에 어텐션 적용"]

    style Solved fill:#eff6ff,stroke:#3b82f6
    style DepthAttn fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

**📌 용어 풀이: 고정 쿼리(query)가 왜 특별한가**
> - 표준 어텐션은 지금 처리 중인 토큰 내용에 따라 쿼리가 매번 달라지지만, 어텐션 잔차의 쿼리는 **층마다 하나씩 학습되는 고정값** — "이 층은 항상 이런 종류의 과거 정보를 찾는다"는 성향 자체를 학습하는 셈
> - 이 덕분에 층마다 서로 다른 정보 선호도를 갖게 되어, 모든 층이 똑같은 잔차 흐름을 공유하던 기존 구조보다 표현력이 높아짐

---

## 8. 블록 어텐션 잔차 - 통신량 절감과 학습·추론 최적화

**📌 핵심:**
- 모든 층이 이전 모든 층에 어텐션을 거는 방식은 여러 GPU에 걸쳐 층을 나눠 배치한 대형 모델에서 통신량이 층 수(L)에 비례해 커지는 문제가 있음 — 블록 어텐션 잔차는 L개 층을 S개씩 묶어 N개 블록으로 나누고, **완결된 블록의 결과물에만 어텐션을 걸고 현재 블록은 진행 중인 부분합만 사용**해 통신량을 블록 수(N)에 비례하는 수준으로 줄임
- 실측 효과는 표준 잔차 연결 대비 **연산 효율 1.25배**, 검증 손실(validation loss)도 꾸준히 더 낮고 학습 후반(decay phase)으로 갈수록 격차가 벌어짐 — 표준 잔차는 층이 깊어질수록 출력 크기(magnitude)가 계속 커지는 데 반해, 블록 어텐션 잔차는 출력 크기가 일정 범위 안에 갇혀(bounded) 그래디언트 크기도 안정적으로 유지됨
- **학습 시 통신 문제**: N번째 층을 계산하려면 이전 N-1개 블록의 결과가 모두 필요해, 파이프라인 병렬화(여러 GPU에 층을 나눠 순서대로 처리하는 방식)에서 매번 전체를 다시 전송하면 비용이 제곱으로 커짐 — Kimi는 이미 계산된 블록을 각 GPU 로컬에 캐싱해두고 활성값 체크포인팅(중간 계산값을 저장해뒀다 재사용)까지 결합해, 이 통신 오버헤드를 표준 아키텍처 대비 **단 4%**까지 억제
- 결론: 추론(서빙) 단계에서도 2단계로 나눠 낭비를 줄임 — **1단계**는 완결된 블록들에 대해 여러 층의 쿼리를 한꺼번에 묶어 병렬로 어텐션을 걸고(배치 처리로 비용 상쇄), **2단계**는 디코드와 비슷하게 현재 진행 중인 블록만 순차 처리(온라인 소프트맥스로 중복 메모리 접근 제거) — 전체 I/O 부담은 표준 잔차 구조와 거의 비슷한 수준으로 유지됨

---

```mermaid
flowchart TD
    FullAttn["전체 층 어텐션:<br/>통신량이 층 수에 비례"] -->|"L개 층을 S개씩<br/>N개 블록으로 분할"| Block["블록 어텐션 잔차:<br/>완결 블록만 어텐션+<br/>현재 블록은 부분합"]
    Block --> Reduced["통신량이 블록 수(N)에<br/>비례하는 수준으로 감소"]

    style Reduced fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    Result2["블록 어텐션 잔차<br/>실측 효과"] --> Eff["연산 효율 1.25배"]
    Result2 --> Loss["검증 손실 감소,<br/>학습 후반 격차 확대"]
    Result2 --> Bound["출력 크기 bounded<br/>(표준 잔차는 깊이따라 증가)"]

    style Eff fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    Train["학습 시 파이프라인<br/>병렬화 통신 문제"] --> Naive["단순 구현: 매번 전체<br/>재전송(제곱 비용 증가)"]
    Train --> Cache["Kimi 해법: 로컬 캐싱+<br/>활성값 체크포인팅"]
    Cache --> Overhead["오버헤드<br/>표준 대비 단 4%"]

    style Overhead fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    Infer["추론 2단계 설계"] --> P1["1단계: 완결 블록<br/>병렬 어텐션<br/>(쿼리 배치 처리)"]
    Infer --> P2["2단계: 현재 블록<br/>순차 처리<br/>(온라인 소프트맥스)"]
    P2 --> IO["전체 I/O 부담<br/>표준 잔차와 유사한 수준"]

    style IO fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

---

## 9. LatentMoE - 라우팅 전 압축으로 통신량 줄이기

**📌 핵심:**
- LatentMoE는 MoE(여러 전문가 신경망 중 일부만 골라 쓰는 구조)에서 **토큰을 전문가에게 보내기(dispatch) 전에 압축하고, 전문가 처리 결과를 모을 때(aggregation) 다시 풀어내는** 방식 — Kimi K3의 Stable LatentMoE는 압축을 푸는 단계(up-projection) 직전에 정규화(RMSNorm)를 추가해 값의 스케일이 들쭉날쭉해지는 문제를 완화
- MoE의 GPU 간 통신량은 "토큰 수 × 활성 전문가 수(K) × 전문가 입력 차원(d)"에 비례하고 "전문가를 나눠 배치한 GPU 수(E)"에 반비례 — Kimi K2는 활성 전문가 8개·입력 차원 7168이었는데, K3는 **압축 차원을 절반(3584)으로 줄여 통신량 증가 없이 활성 전문가 수를 16개로 두 배 늘림**
- 다만 저자들은 **통신량 자체보다 "통신 시간 대비 연산 시간의 비율"이 실제 서빙 효율에 더 중요**하다고 강조 — 이 비율은 오직 "전문가 중간층 차원(m)" 하나로만 결정되며, m을 키우면 이 비율이 낮아져 통신을 연산 뒤에 숨길(오버랩) 여지가 늘어남
- 결론: 이 논리가 Kimi K2→K3뿐 아니라 최근 공개된 여러 오픈소스 모델(DeepSeek V4 Pro, MiniMax M3, MiMo V2.5 Pro, Inkling)이 일제히 전문가 중간층 차원을 3072로 키운 이유를 설명 — 하드웨어 연산 속도가 빨라지고 전문가 가중치를 더 낮은 정밀도로 저장해 메모리 여유가 생기면서, 통신 병목을 줄이는 손쉬운 레버로 이 차원 확장이 업계 전반에 퍼짐

---

```mermaid
flowchart TD
    LatentMoE["LatentMoE 흐름"] --> Compress["압축(라우팅 전)"]
    Compress --> Dispatch["전문가에 전송(dispatch)"]
    Dispatch --> Decompress["결과 취합 후<br/>압축 해제(up-projection)"]

    style Compress fill:#eff6ff,stroke:#3b82f6,stroke-width:2px
```

```mermaid
flowchart TD
    Compare["Kimi K2 vs K3<br/>압축 차원 전략"] --> K2v["K2: 활성전문가 8개,<br/>입력차원 7168"]
    Compare --> K3v["K3: 입력차원 절반(3584)<br/>→ 활성전문가 16개로 2배"]
    K3v --> SameComm["통신량은 그대로 유지"]

    style SameComm fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
```

```mermaid
flowchart TD
    Ratio2["통신시간 대비 연산시간<br/>비율의 유일 변수"] --> M["전문가 중간층<br/>차원(m)"]
    M -->|"m을 키우면<br/>비율 감소"| Overlap["통신을 연산 뒤에<br/>숨길 여지 확대"]
    Overlap --> Industry["업계 전반이<br/>m을 3072로 확장<br/>(DeepSeek V4 Pro·MiniMax M3 등)"]

    style Overlap fill:#f0fdf4,stroke:#16a34a,stroke-width:2px
    style Industry fill:#fff7ed,stroke:#ea580c,stroke-width:2px
```

---

*작성 진행률: 약 82% 완료 (11개 섹션 중 9개 완료)*
*업데이트: 어텐션 잔차, 블록 어텐션 잔차, LatentMoE 섹션 작성 완료*
