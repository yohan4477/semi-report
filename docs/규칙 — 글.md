# 규칙 — 글 (확정)

이 문서는 **확정 규칙**이다(2026-09-02 세움). 옛 규칙 문서·스킬과 어긋나면 이쪽이 이긴다.
고칠 때 그 장의 문서를 이쪽으로 맞춘다.

자매 문서 — 도해는 `docs/규칙 — 도해.md`, 화면은 `docs/규칙 — 화면.md`,
어디에 무엇이 있나는 `docs/글과 도해 — 확정 규칙.md`(색인).

## 1. 독자

반도체·AI 뉴스를 따라가는 사람. 제품·회사 이름(Blackwell·Groq·UALink)은 안 풀어도 되고,
용어(RTL·NUMA·KV 캐시)는 첫 등장에 괄호로 짧게 푼다.


## 2. 글

**자세** — 전략 컨설턴트 출신 애널리스트. 특정 회사의 의뢰를 받은 것이 아니고 종목을 추천하는
것도 아니다. 원문을 읽고 **업계에 무엇이 보이는지**를 쓴다. 회사가 무엇을 해야 하나가 아니다.
회사의 결정(팔까·좁힐까)은 다뤄도 좋지만 글의 축은 아니다.

**도구는 이름 없이** (G7) — 빠짐없이 겹침 없이 나누기, 순서로 보이기, 공급망을 따라 짚기는 하되
MECE·프로세스·밸류체인·대비·부분 나눔 같은 방법 이름은 글에 안 나온다. 구조는 절의 배열로만
드러난다. (밸류체인이 **대상**일 때 — 공급망 자체를 말할 때 — 는 그 말을 쓴다.)

**문체** (G2·G3) — 절마다 산문이 흐른다. 절은 다섯에서 일곱. 마지막 절은 「이 회차가 말하지 않은
것」과 무엇이 나오면 판단이 바뀌는지. 첫 절 앞에 여는 문단 둘셋 문장 — 거기서 진행자를 풀네임으로
한 번 부른다(「Austin Lyons(이하 진행자A)」). 라벨(물음·바탕·축)은 안 붙인다.

**길이** (G1) — 5,500~6,500자(공백 포함, 줄 번호 걷고). 1만 2천 자는 너무 길다. 모자라면 전사에
있는데 안 쓴 사실을 해당 절에 더한다 — 있는 문장을 다시 쓰지 않는다.

**중복 없음** — 같은 사실을 두 번 쓰지 않는다. 표가 바로 위 산문을 되풀이하면 표를 걷는다.
앞 절의 말을 뒤 절에서 다시 하지 않는다. 그림이 글보다 앞에 서서 같은 값을 보이는 것은
중복이 아니다(그림 먼저, 글 나중).

**구성을 말하면 ①②③** (G4) — 「둘이다·셋이다·두 구간」으로 열면 항마다 동그라미 숫자.

**인용은 한국어로** (G5) — 원문(영어)은 줄 번호로 원본 파일에서 따라간다. 화자가 단 단서
(I think·I believe)는 「~라고 봤다/기억했다」로 살린다 — 대조에서 가장 많이 걸리는 자리다.

**번역체 낱말** (G6) — 이날 걸린 것. 보이면 바꾼다.

```
값이 움직인다        →  새 수요가 생긴다 · 돈이 갈 자리가 생긴다 · 비싸게 팔린다/안 팔린다
값을 받는다 · 값을 매긴다 →  비싸게 팔린다 · 돈을 번다 · 가격을 정한다   (check_prose P16·P19)
가른다(구분 뜻)      →  나눈다 · 정한다 · 구분한다   (check_prose P19. 피동 「갈린다」는 정상)
(자리가) 열린다      →  새 수요가 생긴다 · 기회가 생긴다
자기 몫 · HBM 몫     →  전용 HBM · 따로 둔다 · 전력 · 부분
단(段)               →  밸류체인 · 자리   (「아래 단」「세 단」처럼 공급망 층을 뜻할 때)
```
이미 `check_prose` 가 막는 것(돈을 댄다 P18, 은유 주어 P14, 「A 가 아니라 B」 밀도 P9,
대시 밀도 P8)은 그대로 살아 있다.

**맨 「값」을 홀로 세우지 않는다** (G6-1, 2026-09-09 확정) — 앞에 아무것도 없이 조사만
붙은 「값이·값은·값을」은 무엇의 값인지를 지운다. 주가인지 계약가인지 마진인지 단가인지
몸값인지가 한 글자로 뭉개진다. 대시보드 575장에서 「값」 8,628건 가운데 758건이 이 꼴이었다.

```
값이 올랐다          →  D램 고정가가 올랐다 · 주가가 올랐다
값을 만든 것은        →  수익을 만든 것은 · 마진을 만든 것은
값은 뒤로 밀린다       →  이익이 뒤 공정으로 옮겨 간다
값을 정하는 관문       →  약값을 정하는 관문 · 단가를 정하는 관문
```

앞에 이름이나 관형절이 붙으면 그대로 쓴다 — 「국채값이 오르고」·「메모리 값이 뛰어」·
「잰 값」·「그 값」·「같은 값」은 문장이 이미 무엇의 값인지 말한다. `check_prose` P20 이
잡는다. 「X는 값이 …」 꼴은 안 잡는다 — 앞머리가 대상을 말했고, 「적은 값이」와 글자 꼴이
같아 기계가 못 가른다. 자가시험은 `python insights/check_prose.py --selftest`.

**정확성** (G8) — 값·이름·인용은 원문에 있는 것만. 진행자와 발표자, 말한 사람과 인용된 기사를
구분한다. 문장 끝에 (L줄번호)를 붙여 두고, 받은 뒤 **다른 에이전트**가 원문과 대조해 어긋난
것을 고치고 frontmatter `fixed` 에 적는다. `fixed` 가 없는 글은 대조를 안 거친 글이다. 줄이거나
다듬은 뒤에는 커밋본에 없던 값·이름이 끼지 않았는지 기계로 견준다.


## 3. 받는 절차

```
0  모델 (2026-09-03 확정) — 규칙이 대신하는 것은 기계가 잴 수 있는 것까지다. 검사기가 못 보는 판단 자리에만
   위 모델을 쓴다.
     글 쓰기(논지·절 얼개·무엇을 버릴지)   opus 한 콜. 2026-09-03 전력 벽 회차로 견줬다 — sonnet 은 두 번
                                         돌아(진행자 표기·길이 미달) 43만 토큰, 광통신 대목을 버리고 잡담 셋을
                                         더했다. opus 는 한 번에 13.6만, 회차 전체를 덮고 여는 문단이 물음으로
                                         섰다. 대조는 둘 다 어긋남 0. 값이 비슷하고 글은 opus 가 낫다 — 글 쓰기는 opus
     다시 쓰기 · 늘리기·줄이기            sonnet 한 콜씩(본보기를 준다). haiku 는 윤문에서 뜻을 깎았다(아래)
     전사 대조                           haiku 로 된다(2026-09-03 광 규제·텐서다인·할라페뇨) — 인용 줄에 화자
                                         라벨을 붙여 주면 줄 맞추기라 값·귀속·유보를 잡는다. 편당 10만 토큰 안팎
     낱말 치환 · 형식 · 줄 표기            스크립트(파이썬). 모델을 안 쓴다
   편당 30만 토큰 안팎이 목표(실험 편은 80만). 대조에는 전사 전문 대신 `cited_lines.py` 가 뽑은 인용 줄만.
   메인 세션이 가장 비싸다 — 본문·전사·긴 검사기 출력을 메인이 읽지 않고 에이전트·스크립트 요약만 받는다
1  전사 전문을 읽힌다(요약본이 아니라). 자세·독자·꼴·정확성 규칙을 프롬프트에 그대로 준다
2  글을 받는다. 답장에는 절 목록·글자 수·도해 후보만 (파일 내용은 다시 안 붙인다)
3  다른 에이전트가 원문과 대조한다 — 값·인용·귀속. 어긋난 것을 고치고 fixed 에 적는다.
   에이전트가 한도에 걸리면 고침은 메인이 직접 한다
4  길면 같은 에이전트에게 줄이게 한다 — 사실을 더하지 말고 지우기만. 짧으면 전사에서 더한다 —
   있는 문장은 안 건드리고 문단 앞머리(도해 열쇠)는 그대로. 커밋본과 값·이름을 기계로 견준다
4.5 한국어로 다시 쓰기 — 문장 윤문(`humanize-korean`, 변경률 3~5%)만으로는 「전혀 한국말 같지 않다」가 안
   풀렸다(2026-09-03 Grok 편, 사용자). 기준은 **문단째 다시 쓰기**다: 사실·값·(L)·①②③·인용의 뜻·절 제목·
   문단 수와 순서는 그대로 두고, 문장 순서를 한국어 순서(상황 → 판단)로, 「자리·대목·못 박는다·받았다」 같은
   낱말을 걷고, 물음은 물음으로, 인용은 입말로. 본보기는 `insights/semidoped/2026-08-24-grok-bots-cpu-strategy.md`
   (메인이 씀, 열쇠는 `rekey_figs.py`). 다른 편은 이 본보기를 주고 sonnet 이 쓰고 인용 줄 대조로 사실을 잡는다.
   아홉 편 실측(2026-09-03): sonnet 여섯 편·메인 셋(sonnet 한도 때). 다시 쓴 뒤 인용 줄 대조에서 편당 어긋남
   0~2, 유보 탈락 0~5, 글쓴이 프레임에 표시 빠짐 0~6 — 다시 쓰기는 사실을 거의 안 건드리고 유보와 판단 표시를
   떨어뜨리는 쪽으로 틀린다. 그래서 대조는 꼭 뒤에 온다.
   (옛 4.5) 문장 윤문 — `humanize-korean` 윤문 한 콜(sonnet)로 번역투를 걷는다. 진단은 첫 실험에서 나온 지배 패턴 여섯을
   프롬프트에 박아 건너뛰고, 마무리 대조는 기계 게이트로 갈음한다. 지배 패턴은 「~라는
   것이 X의 말이다」 결말 · 형식명사 머리 · 「A가 아니라 B」 · 대시 부연이었다(2026-09-03 CDS 실험 —
   변경률 5.1%, 대시 25→3, 「것이다」 9→1, 드리프트 0). 윤문 뒤 `scratchpad/humanize_gate.py` 로
   (L)·숫자·인용·①②③·문단 앞머리 보존을 재고, 다시 check_prose 를 돈다. 문단 첫 여덟 글자를
   묶어 두면 문단 머리 틀(「Vik이 짚은 것은」)은 못 걷는다 — 그건 쓰는 프롬프트에서 막는다
   **haiku 는 윤문에 못 쓴다**(2026-09-03 PicoJool 시험) — 「A가 아니라 B」의 앞 절을 통째로 지워 뜻을 깎았고
   (「최고 성능이 아니라 확보 가능성」→「확보 가능성」), (L79) 하나를 떨어뜨렸다. 대구 규칙은 「부정항을 긍정
   서술로 되돌린다」인데 haiku 는 「지운다」로 읽는다. 윤문은 sonnet 이상, haiku 는 낱말 치환·늘리기·줄이기만
5  도해를 그린다(메인). 생성 → 검사 → 스크린샷 → 커밋 → 푸시 → 라이브 확인
```

