# Value Chain Intelligence Platform — 설계 (2026-09-11)

## 0. 감사 결과

### 저장소 (재사용 가능한 것)
- `package.json` 없다. npm 빌드 없음. 대시보드는 **파이썬 생성기 → 단일 HTML** 이 관례다.
- `scripts/gen_vcexplorer.py` (783줄) → `대시보드/밸류체인 탐색기.html` (709줄).
  이미 있다: React Flow 11 + dagre(둘 다 CDN UMD), 회사/그룹 커스텀 노드, 검색, 기간 선택(PERIODS/LATEST),
  confidence 필터, official-only 필터, 드로어, 접기·펴기(open state), 중심 회사 전환(setCenter).
  없다: URL 상태, 포커스 모드(hop 제한·opacity), breadcrumb, 타임 스크러버 애니메이션,
  BOM 뷰, methodology 패널, 스윔레인 타임라인, lane 분리, edge-level role.
- `scripts/gen_valuechain.py` (281줄) — 정적 SVG 여섯 층 판. 인터랙티브와 무관. **보존, 손대지 않는다.**
- `scripts/gen_site.py:75` 에 `밸류체인 탐색기.html` 이 `/valuechain` 슬러그로 등록돼 있다.
- `data/valuechain/` 이미 존재하고 6파일 104KB, NVIDIA 사슬만 남아 있다(커밋 37d7faf7).
  `companies.json`(49) `relationships.json`(57) `relationship_metrics.json`(39)
  `evidence.json`(94) `sources.json`(13) `identity_hypotheses.json`(16).
- 기존 스키마는 이미 마스터 프롬프트 모델과 구조가 같다. 관계에 share 를 박지 않고
  `relationship_metrics` 가 period·basis·estimate_type·method 를 들고 있다.
  즉 **재설계가 아니라 확장**이면 된다.
- `insights/chains/` 는 씨모어 전사에서 뽑은 별개 체계(`체인 대시보드.html`). 건드리지 않는다.

### Google Drive (research repository)
폴더 `Bloom Energy` = `1Rp6x_81fSv0YW9TLEvaFTZId723pAQGK`
- `01_조사 추적과정` `1Cc4MBgofRzINVGwMtdvWN4DPLVX7J0TGiWbE7eTx700` — 아직 안 읽음, Phase 2 에서 methodology 원천으로 읽는다
- `02_Bloom Energy Value Chain` `1bWf25JYMIk71UYJJIk-FtPALrOu7fjbBpp76sZWNsoM` — 읽음. 15절 + Deep Research 업데이트 + 2024/2025/2026 시계열 재구성
- `03_Evidence & Sources` `1TmF0PFCWFWHYx1gASzKqizUBZMAhJ4MjwwmFgPwHc6M` — 읽음. CLM001~CLM018, SRC001~SRC015
- `04_Entities & Edges` `1u250_aBLn8yzhxJrSLuWBxHUQ1MuCJotVjNkhi467XY` — 읽음. E001~E026, ED001~ED019

**중요한 불일치**: 04 의 엔티티 26개에는 Porite·Stackpole·Plus Metal·Kaori·AcBel·Sanmina·Nash·Texon·
Cypress·Bloom India·CUMI·Yongzhou Mingrui·Unicorn·Protechnic 이 없다. 이들은 02 의 Deep Research 절에만 있다.
따라서 데이터셋은 04 를 베끼는 것이 아니라 **02 + 03 + 04 를 합쳐서** 만든다.
Drive 는 런타임에 읽지 않는다. Git JSON 이 source of truth 다.

## 1. 데이터 아키텍처

전역 레지스트리 + focal 별 팩. NVIDIA 와 Bloom 이 같은 엔티티 풀을 쓰고 관계만 focal 로 나눈다.

```
data/valuechain/
  entities.json                 전역. 회사·JV·프로젝트 SPV·금융기관·유틸리티
  sources.json                  전역
  methods.json                  전역. 계산 절차(재사용됨)
  chains/<focal>/
    relationships.json          지속적인 관계. share 를 담지 않는다
    observations.json           시점 종속 수치(구 relationship_metrics)
    claims.json                 문장 단위 주장
    evidence.json               인용문 ↔ source 연결
    hypotheses.json             익명 고객 식별 가설(구 identity_hypotheses)
    bom/<period>.json
    financials/<period>.json
    projects.json
```

기존 6파일은 `chains/nvidia/` 로 옮기고 `companies.json` 은 `entities.json` 으로 승격한다.
카테고리 노드(Interconnect·Hotbox 따위)는 저장하지 않는다. `subsystem` 필드에서 **파생**한다.

### entity
```json
{"id":"porite-taiwan","name":"Porite Taiwan","name_ko":"포리테 대만",
 "entity_type":"company","country":"TW","categories":["Interconnect"]}
```
`entity_type`: company | jv | project_spv | fund_jv | financial_institution | utility | end_user

### relationship — 변하는 숫자를 여기 넣지 않는다
```json
{"id":"porite-bloom-interconnect","source_entity":"porite-taiwan","target_entity":"bloom-energy",
 "relationship_type":"SUPPLIES","lane":"MANUFACTURING_BOM",
 "component":"SOFC interconnect plate","subsystem":"Interconnect",
 "source_role":"component supplier","target_role":"integrator",
 "valid_from":"2024-01-01","valid_to":null,"status":"ACTIVE","evidence_level":"CONFIRMED"}
```
`lane`: MANUFACTURING_BOM | MANUFACTURING_EQUIPMENT | SITE_ELECTRICAL_BOP | OPERATIONAL_INPUT | DOWNSTREAM
role 은 엔티티가 아니라 **엣지에** 둔다. AEP 는 유틸리티이자 채널이자 금융이다.

### observation — 변하는 숫자는 전부 여기
```json
{"relationship_id":"porite-bloom-interconnect","metric":"sourcing_share","value":0.50,
 "unit":"percent","period":"2024","as_of_date":"2024-10-01",
 "denominator":"Bloom interconnect plate sourcing volume","status":"HISTORICAL",
 "evidence_level":"ESTIMATED","confidence":0.7,
 "method_id":"method-porite-share-2024","source_ids":["SRC-cw-porite-2024"]}
```
`denominator` 없는 percent 는 검증기가 FAIL 시킨다.

### method — 계산 패널의 원천
```json
{"id":"method-porite-content-per-mw","title":"Porite content per MW",
 "steps":[{"label":"plates per 650kW unit","value":30000},
          {"label":"÷ 0.65 MW","value":46154,"unit":"plates/MW"},
          {"label":"× ~$6/plate","value":277000,"unit":"USD/MW"}],
 "result_status":"ESTIMATED","source_ids":["SRC-cw-porite-2024"]}
```

### 두 축의 중요도
`economic_importance` 와 `capacity_criticality` 를 관계에 따로 둔다. UI 에서 섞지 않는다.

## 2. 데이터 규칙 (검증기가 강제)
- 모든 percent 에 denominator + period + as_of_date.
- 과거 관측을 current 로 승격 금지. `status`: CURRENT | HISTORICAL | HISTORICAL_CURRENT_UNKNOWN | NOT_YET_ACTIVE.
- 값이 없으면 UNKNOWN. 임의 숫자로 채우지 않는다.
- 계약액에 MW/unit 이 없으면 $/MW 강제 산출 금지. 시나리오 감도만 허용.
- 운영 연료·제조 장비·사이트 전기 BoP 는 제품 BOM lane 과 합치지 않는다.
- 계약상 고객 / 중개 / 프로젝트 SPV / 최종 사용자를 별도 엔티티로.
- `source_ids` 가 `sources.json` 에 없으면 FAIL.

## 3. UI
`scripts/gen_vcexplorer.py` 를 확장한다. 새 파일을 만들지 않는다.
Bloom 고유 사실을 컴포넌트에 박지 않는다 — 전부 JSON 에서 읽는다.

- **Current / Timeline / Evidence** 모드 선택기(헤더 우측).
- 첫 화면은 카테고리 레벨만. `subsystem` 으로 묶은 그룹 노드 → focal → downstream lane 3개.
- 노드 클릭 = 드로어. `Expand upstream` / `Expand downstream` 는 1홉씩. 자동 무한 확장 없다.
- Focus 모드: 중심 ±2홉만 진하게, 나머지 opacity 낮춤, breadcrumb 유지.
- 타임 스크러버: 2024 — 2025 — 2026 Current. 레이아웃 고정, 노드 위치 유지, 새 공급사 fade-in + NEW.
- Evidence: CONFIRMED 실선 / ESTIMATED 실선+배지 / INFERRED 파선 / UNDISCLOSED 점선+흐림 /
  HISTORICAL_CURRENT_UNKNOWN 배지+저투명. 색만으로 구분하지 않는다.
- 드로어 폭 32%. 역할·부품·share·denominator·기준일·근거등급·confidence·methodology·출처.
- BOM 뷰: 가로 누적 막대. 블록 클릭 → 그 subsystem 공급사로 drill-down. 전부 Estimated 배지.
- 타임라인 뷰: 스윔레인. 거대 Sankey 금지.
- URL 상태: `?focal=bloom-energy&year=2026&focus=interconnect&sel=porite-taiwan`. 뒤로가기 동작.
- 성능: focal 기준 1홉만 로드, 확장 시 +1홉, 최대 2홉.

## 4. 검증기
`insights/check_chain.py` 를 새로 쓴다(현재 없다). FAIL 0 이어야 푸시.
- denominator 없는 percent, period 없는 observation, 끊어진 source_ids/relationship_id,
  entities 에 없는 엔티티 참조, lane 누락, BOM 합계가 총액 범위를 벗어남,
  HISTORICAL 관측이 current 뷰에 노출되는 구성.

## 5. 진행 순서
1. (완료) 저장소·Drive 감사, 설계 — `eddd0c45`
2. (완료) 스키마 이행과 `insights/check_chain.py` — `bd4299da`
3. (완료) Bloom 데이터셋 — `ac54e231`. 엔티티 60·관계 62·관측 41·주장 18·가설 3·방법 11
4~10. (완료) 탐색기 전면 재작성 — `9b82b443`. 계통 노드·점진적 확장·포커스·breadcrumb·
   URL 상태·타임 스크러버·근거 서랍·계산 패널·원가 뷰·스윔레인·반응형

## 6. 05_UIUX & Implementation Guide 대조 (2026-09-11)

Drive `05_UIUX & Implementation Guide` `1uI0DGQMlZ29I349v9lJNehCRrO9SSkrsi0P_qdmr7dQ`.
조항별로 지금 구현이 어디에 서 있는지.

| 조항 | 상태 |
| --- | --- |
| §3·§15 다운스트림 다층 | 맞춤. target_tier 네 층, 갈래마다 깊이가 다르다 |
| §16 업스트림 다층 | 맞춤. source_tier 네 층, 소재 노드 둘을 새로 세웠다 |
| §4 첫 화면 계통만 | 맞춤. 제품 BOM 계통이 앞, 장비·부지 전기·운영 투입은 lane 이름을 달고 뒤 |
| §5 점진적 확장 | 맞춤. 묶음을 눌러야 열리고 사슬은 두 홉까지만 자동, 더는 버튼 |
| §6 포커스·breadcrumb | 맞춤 |
| §7·§8 시점 | 맞춤. 값은 전부 관측에 있고 자리는 모든 해를 합쳐 한 번 잡는다 |
| §9 현재·시점 뷰 | 맞춤. 스윔레인, 거대 Sankey 없음 |
| §10·§11 근거 | 맞춤. 굵기·꼴·배지로 나누고 서랍에 분모·기준일·확신도·계산 단계 |
| §12 분모 | 맞춤. 검사기가 분모 없는 비중을 FAIL |
| §13 BOM | 맞춤. 전부 추정 배지, 막대에서 계통으로 내려간다 |
| §14 중요도 두 축 | 맞춤. economic·capacity·integration 셋을 따로 둔다 |
| §17 다중 역할 | 맞춤. 역할은 엣지에 있다 (AEP) |
| §19 관계·관측 분리 | 맞춤 |
| §20 재귀 탐색 | 맞춤. 회사 이름을 코드에 안 박는다 |
| §21 URL 상태 | 맞춤. focal·year·mode·open·sel |
| §23 성능 | 맞춤. 중심 둘레만 그린다 |
| §28 매출원 층 (2026-09-11 추가) | 맞춤. 다운스트림 첫 층이 제품·설치·서비스·전력과
  「배분 미상」이고 값은 기간마다 바뀐다. 눌러야 그 매출원에 근거가 붙는 고객이 열린다.
  총매출 기준 고객 집중도는 `denominator_scope` 로 갈라 선에 안 적고 서랍에만 쓴다 |
| §18 디렉터리 | **다르다.** 05 는 /data/companies·relationships·observations 를 권한다.
  우리는 전역 레지스트리(entities·sources·methods) + `chains/<사슬>/` 로 나눴다. 회사가
  여럿이 되면 같은 엔티티를 여러 사슬이 함께 쓰기 때문이다. 파일 종류는 05 와 같다 |
| §5 「View timeline」 | **덜 됐다.** 지금은 그 회사로 중심을 옮기고 시점 뷰를 연다.
  고른 엣지의 관측만 따로 뽑아 주지는 않는다 |

남은 것
- `01_조사 추적과정` 의 미확인 항목을 관측에 더 넣는다(현재는 방법과 가설까지만 반영).
- AEP 오하이오 발표는 URL 이 없어 `check_chain` 이 WARN 한다. 원문 주소를 찾아 채운다.
- 다음 회사(TSMC·NVIDIA 등)를 실을 때는 `SHIP` 에 사슬 이름만 더한다. 코드는 안 고친다.
