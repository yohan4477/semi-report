# 돈 고리 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 통합 인사이트를 카드 41장에서, 돈이 도는 고리를 따라가는 글 8편으로 다시 짠다.

**Architecture:** 축(axis)을 자료로 두고 글이 자기 frontmatter에 `cell:`로 자리를 밝힌다. 축은 칸 목록만 갖고 글 목록은 안 갖는다 — 진실의 출처를 하나로 둔다. 새 글은 `insights/loop/`에 살고, 옛 글 41장은 편이 완성될 때마다 그 편이 흡수한 만큼 지워진다. 이행 기간에는 두 층이 공존한다.

**Tech Stack:** Python 3(표준 라이브러리만) · pytest 7.4.4 · 생성물은 정적 HTML

**Spec:** `docs/superpowers/specs/2026-08-20-돈-고리-design.md`

## Global Constraints

- 검사기 여섯이 전부 FAIL 0이어야 푸시한다: `PYTHONIOENCODING=utf-8 python scripts/build_all.py`
- 콘솔이 cp949다. 파이썬 실행에 `PYTHONIOENCODING=utf-8`을 붙인다
- **pytest 출력이 이 환경에서 삼켜진다.** 반드시 파일로 받아 읽는다: `python -m pytest <파일> -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt`. pytest는 `insights/`를 작업 디렉터리로 돌린다
- **③ 포스트 본문을 쓰거나 고치기 전에 `insight-review` 스킬을 연다.** 예외 없다. Task 6~13 전부 해당
- 용어는 남기고 첫 등장에 괄호로 푼다. 쉬운 말로 치환하지 않는다
- 은유를 주어 자리에 두지 않는다 — `check_prose` P14가 FAIL로 막는다
- 일반론 금지. 숫자·명명된 주체·비직관적인 것으로 쓴다
- 의미 단위마다 커밋·푸시한다. 몰아서 하지 않는다
- 41장 전량(브리핑 17 + 교차 24)이 여덟 편 중 정확히 한 편에 한 번씩 들어간다
- 대시보드 HTML은 생성물이다. 손으로 고치지 않는다

## File Structure

**새로 만든다**

| 파일 | 책임 |
|---|---|
| `insights/axes.py` | 축 정의. 칸 id·이름·설명만. 글 목록은 안 갖는다 |
| `insights/check_axes.py` | A1~A3·S1·L1~L2 검사 |
| `insights/loop/` | 새 글 8편이 사는 곳 |
| `insights/test_li_signal.py` | `classify()` 배제 판정 테스트 |
| `insights/test_axes.py` | 축 자료구조·기준일 파서 테스트 |

**고친다**

| 파일 | 무엇을 |
|---|---|
| `insights/paths.py` | `LOOP` 상수 추가 |
| `insights/li_signal.py` | `classify()`를 포함 판정에서 배제 판정으로 뒤집는다 |
| `insights/notes_lib.py` | 링크드인 인용의 기준일을 읽는 `li_basis()` 추가 |
| `insights/gen_insightview.py` | KINDS에 loop 추가 · 고리 SVG · `period()`의 링크드인 분기 · 타일 제거(마지막) |
| `insights/check_cite.py` `check_fresh.py` `check_notes.py` `check_prose.py` `check_read.py` | 훑는 디렉터리에 `paths.LOOP` 추가 |
| `scripts/build_all.py` | `check_merge` → `check_axes` |

**지운다**

| 파일 | 언제 |
|---|---|
| `insights/merges.py` · `insights/check_merge.py` | Task 4에서 `axes.py`·`check_axes.py`로 대체 |
| `insights/briefs/*.md` 17장 · `insights/synth/*.md` 24장 | Task 6~13에서 편마다 그 편이 흡수한 만큼 |

---

### Task 1: 링크드인 — 포함 판정을 배제 판정으로 뒤집는다

**Files:**
- Modify: `insights/li_signal.py:23-32`(정규식), `insights/li_signal.py:classify()`
- Test: `insights/test_li_signal.py`(신규)

**Interfaces:**
- Consumes: 없음
- Produces: `classify(text, slug, posted, pub, has_video=False) -> (kind, usable, basis_date, lag_days, push)` — 반환 튜플의 모양은 그대로. `usable`의 뜻만 「수치가 있다」에서 「배제 목록에 없다」로 바뀐다. `scripts/gen_li_source.py`가 같은 함수를 부르므로 시그니처를 바꾸면 안 된다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_li_signal.py`:

```python
# -*- coding: utf-8 -*-
import li_signal as ls

PUB = {'some-newsletter': '2026-08-01'}


def test_meme_is_excluded():
    kind, usable, _b, _l, _p = ls.classify(
        '밈: 셀카에 “Gemini, who?” 자막을 얹은 사진 한 장.', None, '2026-08-14', PUB)
    assert usable is False
    assert kind == '밈·농담'


def test_event_is_excluded():
    kind, usable, _b, _l, push = ls.classify(
        '8월 27일 목요일 저녁 뉴욕 미드타운에서 초청제 모임을 연다고 알렸다.',
        None, '2026-08-18', PUB)
    assert usable is False
    assert push is True


def test_past_recollection_is_excluded():
    _k, usable, _b, _l, _p = ls.classify(
        '젠슨 황이 90년대 Dreamcast GPU 계약으로 맺은 인연을 회고했다.',
        None, '2026-07-16', PUB)
    assert usable is False


def test_repromotion_is_excluded():
    kind, usable, basis, lag, push = ls.classify(
        '지난 리포트를 다시 소개한다.', 'some-newsletter', '2026-09-01', PUB)
    assert kind == '재홍보'
    assert usable is False
    assert basis == '2026-08-01'
    assert lag > ls.LAG_MAX
    assert push is True


def test_fresh_newsletter_link_is_not_a_citation_target():
    """원본 뉴스레터를 인용하면 되므로 링크드인은 인용 대상이 아니다."""
    kind, usable, basis, _l, _p = ls.classify(
        '새 리포트를 냈다.', 'some-newsletter', '2026-08-03', PUB)
    assert kind == '신규 발행 알림'
    assert usable is False
    assert basis == '2026-08-01'


def test_commentary_without_numbers_is_now_usable():
    """137건이 잔여물이었다 — 숫자가 없어도 명명된 주체의 검증 가능한 주장은 쓴다."""
    _k, usable, _b, _l, _p = ls.classify(
        'AMD의 최상위 AI 엔지니어 대부분이 상하이에 있다. MoRI 그룹, '
        'KV 캐시 오프로딩·풀링 팀이 여기 몰려 있다.', None, '2026-08-06', PUB)
    assert usable is True


def test_inch_units_are_usable():
    """좁은 NUM 정규식이 「2~4인치 웨이퍼」를 떨어뜨렸다."""
    _k, usable, _b, _l, _p = ls.classify(
        'III-V 업계는 아직 2~4인치 웨이퍼가 표준이라 12인치 CMOS와 비교하면 다르다.',
        None, '2026-07-31', PUB)
    assert usable is True
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd insights && python -m pytest test_li_signal.py -q > /tmp/pt.txt 2>&1; tail -30 /tmp/pt.txt
```

기대: `test_meme_is_excluded` 는 `AssertionError`(지금은 kind가 `논평·서술`), `test_commentary_without_numbers_is_now_usable` 과 `test_inch_units_are_usable` 도 실패(지금은 usable False).

- [ ] **Step 3: 배제 정규식을 나눠 쓴다**

`insights/li_signal.py`의 `PROMO`(32행)를 넷으로 가른다. `NUM`·`COMPARE`는 지운다 — 포함 판정에 쓰던 것이라 더 이상 필요 없다.

```python
# 배제 목록 — 여기 걸리면 근거로 못 쓴다. 걸리지 않으면 전부 인용 후보다.
# 「수치가 있나」로 자동 판정하지 않는다. 기계가 「새 사실인가」를 못 가른다는 것을
# 논평·서술 137건이 증명했다 — 밈과 InP 레이저 글이 같은 칸에 앉아 있었다.
MEME = re.compile(r'밈\s*[—:-]|밈:|농담|풍자|짤')
EVENT = re.compile(r'모임|컨퍼런스|콘퍼런스|행사|웨비나|구독|초청|등록에 승인')
HIRE = re.compile(r'채용|합류|모집|팔로우|계정을 열|구독을 권')
BROADCAST = re.compile(r'팟캐스트|Podcast|에피소드|Ep\.|출연|방송 자막')
PAST = re.compile(r'작년|지난해|20(1\d|2[0-4])년|당시|그때|돌아보면|회고')
```

- [ ] **Step 4: `classify()`를 뒤집는다**

```python
def classify(text, slug, posted, pub, has_video=False):
    """게시물 하나의 자격 판정. 이 규칙은 여기 한 벌만 둔다 —
    scripts/gen_li_source.py도 이 함수를 부른다.

    **배제 판정이다.** 배제 목록에 걸리지 않으면 전부 인용 후보다. 고를 사람은
    글 쓰는 쪽이고, 기계는 배제된 줄을 인용하면 막는 일만 한다(check_axes L1).

    반환: (kind, usable, basis_date, lag_days, push)
    """
    if slug:
        src = pub.get(slug)
        if src is None:
            return '뉴스레터 링크(발행일 미상)', False, posted, None, False
        lag = (datetime.date.fromisoformat(posted) - datetime.date.fromisoformat(src)).days
        if lag > LAG_MAX:
            return '재홍보', False, src, lag, True
        # 원본 뉴스레터가 있으니 그쪽을 인용한다. 링크드인은 인용 대상이 아니다
        return '신규 발행 알림', False, src, lag, False
    if MEME.search(text):
        return '밈·농담', False, posted, None, True
    if EVENT.search(text):
        return '행사·모임', False, posted, None, True
    if HIRE.search(text):
        return '채용·권유', False, posted, None, True
    if BROADCAST.search(text) or has_video:
        return '방송·팟캐스트', False, posted, None, True
    if PAST.search(text):
        return '과거 회고', False, posted, None, False
    return '자체 발화', True, posted, None, False
```

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

```bash
cd insights && python -m pytest test_li_signal.py -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt
```

기대: `7 passed`

- [ ] **Step 6: 자격 분포가 어떻게 바뀌었는지 센다**

```bash
PYTHONIOENCODING=utf-8 python insights/li_signal.py
```

기대: 「사용 가능」이 107건에서 늘어난다. 정확한 수를 커밋 메시지에 적는다. `신규 발행 알림` 53건이 usable에서 빠지고 `논평·서술` 137건 중 배제 안 된 것이 들어오므로, 대략 150건 안팎이 된다.

- [ ] **Step 7: 전체 빌드가 안 깨지는지 확인한다**

```bash
PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: 마지막 줄 `전부 통과`. `scripts/gen_li_source.py`가 같은 `classify()`를 쓰므로 링크드인 원문 파일의 `kind`·`usable` 표기가 새 값으로 다시 생성된다.

- [ ] **Step 8: 커밋**

```bash
git add insights/li_signal.py insights/test_li_signal.py "content/linkedin" insights/views/li_signals.json
git commit -m "$(cat <<'EOF'
fix(링크드인): 포함 판정을 배제 판정으로 뒤집는다

「논평·서술」 137건은 판정이 아니라 정규식 넷이 다 안 걸린 잔여물이었다.
수치 정규식에 인치·개·홉·시간이 없어 InP 레이저 웨이퍼 규격(2~4인치 대 12인치)이
밈과 같은 칸에 앉았다. 기계가 「새 사실인가」를 못 가른다는 것이 137건이 증명한 것이다.

배제 목록(밈·행사·채용·방송·회고·재홍보·뉴스레터 링크)에 걸리지 않으면 전부
인용 후보다. 고를 사람은 글 쓰는 쪽이고 기계는 배제된 줄의 인용만 막는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 2: 링크드인 기준일을 읽는다

**Files:**
- Modify: `insights/notes_lib.py`(끝에 추가), `insights/gen_insightview.py:period()`
- Test: `insights/test_axes.py`(신규 — 기준일 파서 부분만 먼저)

**Interfaces:**
- Consumes: Task 1의 `classify()`가 정한 기준일이 원문 파일에 박혀 있다
- Produces: `nl.li_basis(rel_path, line_no) -> 'YYYY-MM-DD' | None` — 링크드인 원문 파일에서 그 줄이 속한 게시물 블록의 기준일을 돌려준다. 링크드인 파일이 아니면 `None`

**배경:** 인용 파일명이 `content/linkedin/[2608] 링크드인 게시물.md`로 월 4자리다. `period()`는 근거 발행일 범위를 `\[(\d{6})\]` 여섯 자리로 찾으니 링크드인이 안 걸린다. 링크드인만 인용한 글은 근거 범위가 `as_of`로 떨어지고 `check_fresh`가 못 본다. 답은 파일 안에 있다 — 게시물 블록마다 `- 기준일 2026-08-01 (게시일)` 줄이 있다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_axes.py`:

```python
# -*- coding: utf-8 -*-
import io
import os
import notes_lib as nl
import paths

LI = 'content/linkedin/[2608] 링크드인 게시물.md'


def _abs(rel):
    return os.path.join(paths.ROOT, rel.replace('/', os.sep))


def test_li_basis_reads_the_block_the_line_belongs_to():
    text = io.open(_abs(LI), encoding='utf-8').read().split('\n')
    # 「기준일」 줄을 하나 찾고, 그보다 아래 줄의 기준일이 같은 값이어야 한다
    idx = next(i for i, l in enumerate(text) if l.startswith('- 기준일 '))
    want = text[idx].split('기준일 ')[1].split()[0]
    assert nl.li_basis(LI, idx + 3) == want


def test_li_basis_returns_none_for_non_linkedin():
    assert nl.li_basis('content/newsletter/ai_infra/x.md', 10) is None
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd insights && python -m pytest test_axes.py -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt
```

기대: `AttributeError: module 'notes_lib' has no attribute 'li_basis'`

- [ ] **Step 3: `li_basis()`를 쓴다**

`insights/notes_lib.py` 끝에 붙인다:

```python
LI_DIR = 'content/linkedin/'
LI_BASIS = re.compile(r'^-\s*기준일\s*(\d{4}-\d{2}-\d{2})')


def li_basis(rel_path, line_no):
    """링크드인 원문에서 그 줄이 속한 게시물 블록의 기준일.

    파일명이 [2608]처럼 월 단위라 파일명에서는 날짜를 못 뽑는다. 게시물마다
    붙은 기준일 줄을 위로 거슬러 찾는다 — 게시일이 아니라 li_signal 이 정한
    basis_date 다(뉴스레터 링크가 있으면 그 원문의 발행일)."""
    p = (rel_path or '').replace('\\', '/')
    if not p.startswith(LI_DIR):
        return None
    ap = abspath(p)
    if not os.path.isfile(ap):
        return None
    found = None
    with io.open(ap, encoding='utf-8', errors='replace') as f:
        for i, line in enumerate(f, 1):
            if i > line_no:
                break
            m = LI_BASIS.match(line.strip())
            if m:
                found = m.group(1)
    return found
```

- [ ] **Step 4: 테스트 통과를 확인한다**

```bash
cd insights && python -m pytest test_axes.py -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt
```

기대: `2 passed`

- [ ] **Step 5: `period()`가 링크드인 기준일을 세게 한다**

`insights/gen_insightview.py`의 `period()`에서 `ds`를 만드는 자리를 고친다. 지금은 인용한 문서의 파일명에서 `[\d{6}]`만 뽑는다. 링크드인은 인용된 줄 번호가 있어야 기준일을 아는데, `period()`는 줄 번호를 안 갖고 있다. 그래서 `nl.cite_refs(body, src)`로 인용 목록을 받아 링크드인 건의 기준일을 더한다.

`period(meta, src)`의 시그니처를 `period(meta, src, body='')`로 넓히고, `one()`에서 `period(meta, src, body)`로 부른다. 함수 안 `ds` 계산 뒤에 붙인다:

```python
    # 링크드인은 파일명이 [2608]처럼 월 단위라 위에서 안 잡힌다.
    # 인용된 줄이 속한 게시물의 기준일을 대신 센다.
    for ref in nl.cite_refs(body, src):
        if not (ref.get('file') and ref.get('lines')):
            continue
        b = nl.li_basis(ref['file'], ref['lines'][0])
        if b:
            ds.append(b[2:4] + b[5:7] + b[8:10])
    ds = sorted(ds)
```

`ds`가 `if not ds:` 분기보다 앞에서 채워지도록 순서를 확인한다.

- [ ] **Step 6: 링크드인만 인용한 글의 근거 범위가 잡히는지 확인한다**

```bash
PYTHONIOENCODING=utf-8 python insights/gen_insightview.py
python - <<'PY'
import io, re
h = io.open('대시보드/통합 인사이트.html', encoding='utf-8').read()
print('as_of 로 떨어진 카드', h.count('class="asof">as_of'))
PY
```

기대: 재생성이 성공하고, `as_of` 표기 카드 수가 이전보다 줄거나 같다.

- [ ] **Step 7: 전체 검사**

```bash
PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: `전부 통과`

- [ ] **Step 8: 커밋**

```bash
git add insights/notes_lib.py insights/gen_insightview.py insights/test_axes.py "대시보드"
git commit -m "$(cat <<'EOF'
fix(근거 범위): 링크드인 인용의 기준일을 게시물 블록에서 읽는다

인용 파일명이 [2608]처럼 월 4자리라 period() 의 여섯 자리 정규식에 안 걸렸다.
링크드인만 인용한 글은 근거 범위가 as_of 로 떨어져 check_fresh 가 못 봤다.
게시물마다 박혀 있는 기준일 줄을 거슬러 읽는다 — 게시일이 아니라 basis_date 다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 3: 새 글이 살 자리를 만든다

**Files:**
- Create: `insights/loop/.gitkeep`
- Modify: `insights/paths.py`, `insights/gen_insightview.py:58-60`, `insights/check_cite.py:104-105`, `insights/check_fresh.py:174-175`, `insights/check_notes.py:69`, `insights/check_prose.py:394-433`, `insights/check_read.py:196-197`

**Interfaces:**
- Consumes: 없음
- Produces: `paths.LOOP` — `insights/loop` 절대경로. 새 글 8편의 frontmatter 스키마(아래)

**왜 새 디렉터리인가:** `check_prose`는 `SYNTH`에만 절 순서 검사(`check_order`)와 머리 검사(`check_head`)를 건다(`check_prose.py:431-432`). 새 여덟 편을 `synth/`에 넣으면 「주장 / 그래서 무엇이 달라지나 / 근거 / 조건 충돌 / 아직 모르는 것」 틀에 묶인다 — 그 틀이 지금 문제의 하나다(「어느 카드를 열어도 같은 문서를 읽는 느낌」). 그리고 이행 기간에 41장과 8편이 공존해야 한다.

**새 글 frontmatter 스키마**

```yaml
---
view: loop
axis: money          # 어느 축인가
cell: capital        # 그 축의 어느 칸인가 — axes.py 의 칸 id
headline: 조달한 돈은 아직 장부 밖에 있다
subhead: 한 줄 요약
section: biz         # 대표 주제. period() 의 STALE_DAYS 와 sources_rank 가 쓴다
as_of: 2026-08-21
merged:              # 이 편이 흡수한 옛 글의 headline. A3 가 이걸로 전량을 센다
  - "빅테크는 쓴 돈만큼 장부에 담지 않는다"
  - "같은 보증 계약을 한쪽은 다리로, 한쪽은 부풀림으로 센다"
  - "같은 날 메타는 코어위브의 고객이자 경쟁자가 됐다"
sources:
  - {file: "content/newsletter/...", note: "무엇을 주장하는 문서인지"}
---
```

**새 글의 절 규칙** — 순서를 강제하지 않는다. 단 두 가지만 지킨다.

1. 첫 문단이 이 칸에서 무슨 일이 벌어지는지를 인과로 말한다
2. 마지막에 `## 아직 모르는 것` 절이 하나 있다

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_axes.py`에 덧붙인다:

```python
def test_loop_path_is_under_insights():
    assert paths.LOOP == os.path.join(paths.ROOT, 'insights', 'loop')
    assert os.path.isdir(paths.LOOP)
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd insights && python -m pytest test_axes.py -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt
```

기대: `AttributeError: module 'paths' has no attribute 'LOOP'`

- [ ] **Step 3: 디렉터리와 상수를 만든다**

```bash
mkdir -p insights/loop && touch insights/loop/.gitkeep
```

`insights/paths.py`의 `THESES` 줄 아래에 붙인다:

```python
# 돈 고리 여덟 편. 교차 인사이트(어긋남 하나)와 절 구성이 달라 디렉터리를 가른다
LOOP = os.path.join(HERE, 'loop')
```

- [ ] **Step 4: 검사기 다섯이 새 디렉터리를 훑게 한다**

`insights/check_cite.py:104-105`, `insights/check_fresh.py:174-175`, `insights/check_read.py:196-197` — 세 파일 모두 같은 모양이다. `glob.glob(os.path.join(paths.LOOP, '*.md')) +` 를 목록에 더한다. 예:

```python
    files = sorted(glob.glob(os.path.join(paths.BRIEFS, '*.md')) +
                   glob.glob(os.path.join(paths.LOOP, '*.md')) +
                   glob.glob(os.path.join(paths.SYNTH, '*.md')))
```

`insights/check_notes.py:69`:

```python
    for d in (paths.NOTES, paths.TRACKS, paths.SYNTH, paths.THESES, paths.BRIEFS, paths.LOOP):
```

`insights/check_prose.py` — 브리핑 루프(407행) 바로 아래에 loop 루프를 새로 둔다. **`check_order`·`check_head`를 부르지 않는다.**

```python
    # 돈 고리 여덟 편. 절 순서를 강제하지 않는다 — 그 틀이 카드 41장의 문제였다.
    # 문체·용어·길이·번역투만 보고, headline 만 필수로 건다.
    loops = sorted(glob.glob(os.path.join(paths.LOOP, '*.md')))
    for p in loops:
        where = os.path.basename(p)
        raw = io.open(p, encoding='utf-8').read()
        body = strip_refs(raw)
        meta, _ = parse_synth(raw)
        check_density(body, where)
        check_glossary(body, where, gloss)
        check_length(body, where)
        check_translationese(body, where)
        if not (meta or {}).get('headline'):
            add('FAIL', where, 'P7', 'headline 없음 — 카드에 제목이 안 붙는다')
```

요약 줄(444행)에 편수를 더한다:

```python
    print('요약: 인사이트 %d건 / 고리 %d편 / 노트 %d장 / 대시보드 %d장 / FAIL %d / WARN %d'
          % (len(files), len(loops), len(notes), len(dashes), fails, warns))
```

- [ ] **Step 5: KINDS에 loop를 더한다**

`insights/gen_insightview.py:58-60`:

```python
# (디렉터리, 배지 이름, 탭 id) — 탭은 이 순서로 선다.
# loop 가 맨 앞이다. 여덟 편이 다 서면 나머지 셋은 비고 탭도 사라진다
KINDS = ((paths.LOOP, '고리', 'loop'),
         (paths.BRIEFS, '브리핑', 'brief'),
         (paths.SYNTH, '교차 인사이트', 'cross'),
         (paths.THESES, '종합 판단', 'thesis'))
```

- [ ] **Step 6: 테스트와 전체 검사를 돌린다**

```bash
cd insights && python -m pytest test_axes.py test_paths.py -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt
cd .. && PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: pytest `3 passed`, build_all `전부 통과`. 고리 디렉터리가 비어 있으므로 `고리 0편`이 찍힌다.

- [ ] **Step 7: 커밋**

```bash
git add insights/paths.py insights/loop insights/check_*.py insights/gen_insightview.py insights/test_axes.py
git commit -m "$(cat <<'EOF'
feat(고리): 새 글 여덟 편이 살 insights/loop 를 연다

교차 인사이트는 절 순서를 강제받는다(check_prose 의 check_order·check_head).
「주장 / 달라지는 것 / 근거 / 충돌 / 모르는 것」 틀이 어느 카드를 열어도 같은
문서를 읽는 느낌을 만든 원인의 하나라, 새 여덟 편은 그 틀을 안 쓴다.
문체·용어·길이·번역투만 보고 headline 만 필수로 건다.

이행 기간에 카드 41장과 새 글이 공존해야 하는 것도 디렉터리를 가른 이유다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 4: 축 자료구조와 검사기

**Files:**
- Create: `insights/axes.py`, `insights/check_axes.py`
- Delete: `insights/merges.py`, `insights/check_merge.py`
- Modify: `insights/gen_insightview.py`(merges 참조를 axes로), `scripts/build_all.py`

**Interfaces:**
- Consumes: `paths.LOOP`(Task 3), `nl.li_basis`(Task 2)
- Produces:
  - `axes.AXES` — 축 목록
  - `axes.cell_of(axis_id, cell_id) -> (이름, 설명) | None`
  - `axes.cells(axis_id) -> [(cell_id, 이름, 설명, 열)]`  열은 `'loop'` 또는 `'outside'`
  - `check_axes.py` — A1~A3·S1·L1~L2

**설계 변경 한 가지:** `merges.py`는 축이 글 목록(headline 문자열)을 들고 있었다. 그러면 진실의 출처가 둘이 된다 — 글의 frontmatter와 축의 목록. 새 구조는 **축이 칸만 갖고, 글이 자기 `cell:`로 자리를 밝힌다.**

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_axes.py`에 덧붙인다:

```python
import axes


def test_money_axis_has_six_loop_cells_and_two_outside():
    rows = axes.cells('money')
    loop = [r for r in rows if r[3] == 'loop']
    out = [r for r in rows if r[3] == 'outside']
    assert [r[0] for r in loop] == ['capital', 'chip', 'power', 'run', 'sell', 'back']
    assert [r[0] for r in out] == ['money_cost', 'estate']


def test_cell_of_returns_name_and_gloss():
    name, gloss = axes.cell_of('money', 'capital')
    assert name == '조달'
    assert gloss


def test_cell_of_unknown_is_none():
    assert axes.cell_of('money', 'nope') is None
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd insights && python -m pytest test_axes.py -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt
```

기대: `ModuleNotFoundError: No module named 'axes'`

- [ ] **Step 3: `insights/axes.py`를 쓴다**

```python
# -*- coding: utf-8 -*-
"""축 — 글 여럿이 하나의 사슬 위 어디에 서는가.

섹션 타일은 글을 주제별로 **나눈다.** 축은 반대로 주제를 **가로질러** 사슬 하나로
꿴다. 설계는 docs/superpowers/specs/2026-08-20-돈-고리-design.md.

**축은 칸만 갖는다.** 어느 글이 어느 칸에 서는지는 글이 자기 frontmatter 의
cell: 로 밝힌다 — 목록을 양쪽에 두면 진실의 출처가 둘이 된다.

축은 여럿일 수 있다. 41장을 다시 읽으면 최소 셋이 보인다(돈 고리 · 세는 기준 ·
병목의 이동). 지금 세우는 것은 돈 고리 하나이고 나머지는 자료구조만 열어 둔다.
같은 글이 여러 축에 서는 것은 허용한다 — 한 축 안에서 두 칸에 서는 것만 막는다.
"""

AXES = [{
    'id': 'money',
    'title': '돈이 도는 고리',
    'lede': '조달한 돈이 설비와 컴퓨트를 거쳐 매출이 되고, 그 매출이 다시 조달을 '
            '정당화하는지까지를 한 바퀴로 따라간다',
    'loop': [
        ('capital', '조달', '돈이 어디서 오나'),
        ('chip', '칩', '그 돈이 무엇으로 바뀌나'),
        ('power', '전기', '꽂을 데가 있나'),
        ('run', '가동', '산 것이 일하나'),
        ('sell', '판매', '얼마 남나'),
        ('back', '되돌아옴', '그래서 조달이 정당한가'),
    ],
    'outside': [
        ('money_cost', '돈값', '고리 전체가 서 있는 바깥 조건 — 금리와 환율'),
        ('estate', '부동산', '따로 서 있는 묶음 — 짓고 사고 파는 쪽'),
    ],
}]

BY_ID = dict((a['id'], a) for a in AXES)


def cells(axis_id):
    """(칸 id, 이름, 설명, 열) — 그리는 쪽과 검사하는 쪽이 같은 순서를 본다."""
    a = BY_ID.get(axis_id)
    if not a:
        return []
    out = [(c, n, g, 'loop') for c, n, g in a['loop']]
    out += [(c, n, g, 'outside') for c, n, g in a['outside']]
    return out


def cell_of(axis_id, cell_id):
    for c, n, g, _col in cells(axis_id):
        if c == cell_id:
            return (n, g)
    return None


def all_cell_ids():
    out = set()
    for a in AXES:
        for c, _n, _g, _col in cells(a['id']):
            out.add((a['id'], c))
    return out
```

- [ ] **Step 4: 테스트 통과를 확인한다**

```bash
cd insights && python -m pytest test_axes.py -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt
```

기대: `6 passed`

- [ ] **Step 5: `insights/check_axes.py`를 쓴다**

```python
# -*- coding: utf-8 -*-
"""축 검사 — 사슬이 실제 글과 맞나, 그리고 못 쓸 근거를 인용했나.

  A1  글이 밝힌 axis/cell 이 축에 없다
  A2  한 축 안에서 같은 글이 두 칸에 섰다
  A3  옛 글 41장이 여덟 편에 정확히 한 번씩 안 들어갔다 (이행 중에는 WARN)
  S1  충돌을 다루는 절이 있는데 「누가 더 가까이서 봤나」가 없다 (당분간 WARN)
  L1  배제된 링크드인 게시물의 줄을 인용했다
  L2  링크드인 인용의 기준일을 못 읽는다

  py -3.13 insights/check_axes.py
"""
import glob
import io
import json
import os
import re
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import axes
import notes_lib as nl
import paths

FINDINGS = []
NEAR = '누가 더 가까이서 봤나'
CLASH = re.compile(r'^##\s*(조건 충돌|어긋나는 자리|갈리는 자리)', re.M)
LI_DIR = 'content/linkedin/'


def add(level, rule, where, msg):
    FINDINGS.append((level, rule, where, msg))


def li_excluded():
    """배제된 게시물의 기준일 집합. li_signal 이 usable=False 로 표시한 것."""
    p = os.path.join(paths.HERE, 'views', 'li_signals.json')
    if not os.path.isfile(p):
        return set()
    d = json.load(io.open(p, encoding='utf-8'))
    return set((s.get('basis_date'), s.get('id')) for s in d.get('signals', [])
               if not s.get('usable'))


def li_usable_dates():
    p = os.path.join(paths.HERE, 'views', 'li_signals.json')
    if not os.path.isfile(p):
        return set()
    d = json.load(io.open(p, encoding='utf-8'))
    return set(s.get('basis_date') for s in d.get('signals', []) if s.get('usable'))


def loop_files():
    return sorted(glob.glob(os.path.join(paths.LOOP, '*.md')))


def old_files():
    return sorted(glob.glob(os.path.join(paths.BRIEFS, '*.md')) +
                  glob.glob(os.path.join(paths.SYNTH, '*.md')))


def main():
    known = axes.all_cell_ids()
    ok_dates = li_usable_dates()
    seen_cell = {}
    merged = []

    for f in loop_files():
        where = os.path.basename(f)
        raw = io.open(f, encoding='utf-8').read()
        meta, body = nl.parse_front(raw)
        aid, cid = meta.get('axis', ''), meta.get('cell', '')
        if (aid, cid) not in known:
            add('FAIL', 'A1', where, '축에 없는 자리다: axis=%r cell=%r' % (aid, cid))
        key = (aid, cid)
        if key in seen_cell:
            add('FAIL', 'A2', where, '%s 와 같은 칸에 두 편이 섰다' % seen_cell[key])
        seen_cell[key] = where

        merged += re.findall(r'^\s*-\s*"(.+?)"\s*$',
                             meta.get('_head', '').split('merged:')[-1].split('sources:')[0], re.M)

        if CLASH.search(body) and NEAR not in body:
            add('WARN', 'S1', where, '충돌 절이 있는데 「%s」가 없다' % NEAR)

        src = nl.sources_of(meta)
        for ref in nl.cite_refs(body, src):
            f2 = (ref.get('file') or '').replace('\\', '/')
            if not f2.startswith(LI_DIR) or not ref.get('lines'):
                continue
            b = nl.li_basis(f2, ref['lines'][0])
            if not b:
                add('FAIL', 'L2', where, '링크드인 인용의 기준일을 못 읽는다: L%d'
                    % ref['lines'][0])
            elif b not in ok_dates:
                add('FAIL', 'L1', where,
                    '배제된 링크드인 게시물을 인용했다: %s L%d' % (b, ref['lines'][0]))

    # A3 — 옛 글이 정확히 한 번씩 흡수됐나
    old = []
    for f in old_files():
        meta, _b = nl.parse_front(io.open(f, encoding='utf-8').read())
        old.append(meta.get('headline') or os.path.basename(f)[:-3])
    dup = sorted(h for h in set(merged) if merged.count(h) > 1)
    ghost = sorted(h for h in merged if h not in old)
    left = sorted(h for h in old if h not in merged)
    for h in dup:
        add('FAIL', 'A3', '-', '두 편이 같은 글을 흡수했다: %r' % h)
    for h in ghost:
        add('FAIL', 'A3', '-', '흡수했다는 글이 없다: %r' % h)
    if left:
        add('WARN', 'A3', '-', '아직 안 흡수된 옛 글 %d장' % len(left))

    for level, rule, where, msg in FINDINGS:
        print('%s %s  %s  %s' % (level, rule, where, msg))
    fails = sum(1 for f in FINDINGS if f[0] == 'FAIL')
    warns = len(FINDINGS) - fails
    print('요약: 축 %d개 / 고리 글 %d편 / 남은 옛 글 %d장 / FAIL %d / WARN %d'
          % (len(axes.AXES), len(loop_files()), len(left), fails, warns))
    return 1 if fails else 0


if __name__ == '__main__':
    sys.stdout.reconfigure(encoding='utf-8')
    sys.exit(main())
```

- [ ] **Step 6: 옛 지도를 지우고 참조를 옮긴다**

```bash
git rm insights/merges.py insights/check_merge.py
```

`insights/gen_insightview.py`에서 `import merges`를 `import axes`로 바꾸고, `merges.cell_index()`·`merges.cells()`·`merge_svg()`·`merge_layer()`가 `axes`를 쓰도록 고친다. 글이 `cell:`을 스스로 밝히므로 `cell_index()`는 없어지고, `one()`의 `cells` 인자는 `meta`에서 바로 온다:

```python
            cellid = '%s:%s' % (meta.get('axis', ''), meta.get('cell', ''))
            cells = (cellid,) if meta.get('cell') else ()
```

`scripts/build_all.py`의 CHECK 목록에서 이름을 바꾼다:

```python
    ('축과 근거', ['insights/check_axes.py']),
```

- [ ] **Step 7: 전체 검사**

```bash
cd insights && python -m pytest test_axes.py -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt
cd .. && PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: pytest `6 passed`. build_all `전부 통과`, 축 검사 줄에 `FAIL 0 / WARN 1`(아직 안 흡수된 옛 글 41장).

- [ ] **Step 8: 커밋**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(축): merges 를 axes 로 승격하고 진실의 출처를 하나로 둔다

merges.py 는 축이 글 목록(headline 문자열)을 들고 있었다. 글의 frontmatter 와
축의 목록 둘이 같은 사실을 말해 어긋날 수 있었다. 이제 축은 칸만 갖고,
글이 자기 cell: 로 자리를 밝힌다.

check_axes 가 여섯을 본다 — A1 없는 자리, A2 한 칸에 두 편, A3 옛 글 전량 흡수,
S1 충돌 절의 근접 판단, L1 배제된 링크드인 인용, L2 못 읽는 기준일.
A3 와 S1 은 이행 중이라 WARN 이다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 5: 고리 SVG — 페이지 맨 위 한 장, 글마다 작게 한 장

**Files:**
- Modify: `insights/gen_insightview.py`(`merge_svg`→`loop_svg`, `merge_layer`→`axis_layer`, `MRG_CSS`)

**Interfaces:**
- Consumes: `axes.cells('money')`(Task 4)
- Produces:
  - `loop_svg(axis_id, active=None, mini=False) -> str` — `active`가 주어지면 그 칸만 불이 켜지고 나머지는 흐려진다. `mini=True`면 누를 수 없고 폭이 줄어든다
  - `axis_layer() -> str` — 페이지 맨 위 층

**모양:** 여섯 칸이 가로로 서고, 마지막 칸에서 첫 칸으로 돌아오는 곡선 하나가 위를 지난다. 그 곡선에 「그 매출이 다시 조달을 정당화하나」를 얹는다. 고리 밖 두 칸은 아래에 따로 선다. 아직 안 쓴 칸은 점선 테두리에 「아직 안 씀」.

- [ ] **Step 1: 칸 상태를 세는 헬퍼를 쓴다**

`insights/gen_insightview.py`에 붙인다:

```python
def written_cells():
    """이미 글이 선 칸. 아직 안 쓴 칸은 점선으로 그린다."""
    out = {}
    for p in sorted(glob.glob(os.path.join(paths.LOOP, '*.md'))):
        meta, _b = nl.parse_front(io.open(p, encoding='utf-8').read())
        if meta.get('axis') and meta.get('cell'):
            out['%s:%s' % (meta['axis'], meta['cell'])] = {
                'head': meta.get('headline', ''), 'anchor': anchor(meta.get('headline', ''))}
    return out
```

- [ ] **Step 2: `loop_svg()`를 쓴다**

`merge_svg()`를 지우고 대신 넣는다. 좌표는 칸 개수에서 계산한다 — 손으로 적지 않는다.

```python
_BW, _BH, _GAP = 96, 46, 14        # 칸 폭·높이·사이


def loop_svg(axis_id, active=None, mini=False):
    rows = axes.cells(axis_id)
    loop = [r for r in rows if r[3] == 'loop']
    out = [r for r in rows if r[3] == 'outside']
    done = written_cells()
    n = len(loop)
    W = n * _BW + (n - 1) * _GAP
    top = 34                                   # 되돌아오는 곡선 자리
    H = top + _BH + (0 if mini else 58)
    h = ['<svg viewBox="0 0 %d %d" class="loopsvg%s" role="group" '
         'aria-label="%s">' % (W, H, ' is-mini' if mini else '', nl.esc(axis_id))]
    # 되돌아오는 곡선 — 마지막 칸 위에서 첫 칸 위로
    h.append('<path class="mflow lp-back" d="M%d %d C %d 2, %d 2, %d %d"/>'
             % (W - _BW / 2, top, W - _BW / 2, _BW / 2, _BW / 2, top))
    if not mini:
        h.append('<text x="%d" y="12" text-anchor="middle" class="m-col">'
                 '그 매출이 다시 조달을 정당화하나</text>' % (W / 2))
    for i, (cid, name, gloss, _col) in enumerate(loop):
        x = i * (_BW + _GAP)
        key = '%s:%s' % (axis_id, cid)
        got = done.get(key)
        cls = 'mcell'
        if active is not None:
            cls += ' is-on' if key == active else ' is-off'
        if not got:
            cls += ' is-todo'
        h.append('<g class="%s" data-cell="%s" data-label="%s" %s>'
                 % (cls, nl.esc(key), nl.esc(name),
                    '' if mini else 'role="button" tabindex="0"'))
        h.append('<title>%s — %s</title>' % (nl.esc(name), nl.esc(gloss)))
        h.append('<rect x="%d" y="%d" width="%d" height="%d" rx="9"/>'
                 % (x, top, _BW, _BH))
        h.append('<text x="%d" y="%d" text-anchor="middle" class="m-lab">%s</text>'
                 % (x + _BW / 2, top + 20, nl.esc(name)))
        h.append('<text x="%d" y="%d" text-anchor="middle" class="lp-sub">%s</text>'
                 % (x + _BW / 2, top + 35, nl.esc(gloss if got else '아직 안 씀')))
        h.append('</g>')
        if i < n - 1:
            h.append('<path class="mflow" d="M%d %d L%d %d"/>'
                     % (x + _BW, top + _BH / 2, x + _BW + _GAP, top + _BH / 2))
    if not mini:
        h.append('<text x="0" y="%d" class="m-col">고리 밖</text>' % (top + _BH + 24))
        for j, (cid, name, gloss, _col) in enumerate(out):
            x = 58 + j * (_BW + _GAP)
            key = '%s:%s' % (axis_id, cid)
            got = done.get(key)
            cls = 'mcell is-out' + ('' if got else ' is-todo')
            h.append('<g class="%s" data-cell="%s" data-label="%s" role="button" '
                     'tabindex="0"><title>%s</title>' % (cls, nl.esc(key),
                                                         nl.esc(name), nl.esc(gloss)))
            h.append('<rect x="%d" y="%d" width="%d" height="%d" rx="9"/>'
                     % (x, top + _BH + 10, _BW, 28))
            h.append('<text x="%d" y="%d" text-anchor="middle" class="m-lab">%s</text>'
                     % (x + _BW / 2, top + _BH + 29, nl.esc(name)))
            h.append('</g>')
    h.append('</svg>')
    return ''.join(h)
```

- [ ] **Step 3: 층과 글 앞 미니 지도를 붙인다**

`merge_layer()`를 `axis_layer()`로 바꾼다. 접지 않는다 — 이것이 페이지의 첫 화면이다.

```python
def axis_layer():
    """페이지 맨 위 한 장. 「지금 무슨 일이 벌어지는 중인가」를 말하는 자리다."""
    a = axes.AXES[0]
    return ('<section class="axis"><p class="ax-t">%s</p><p class="ax-l">%s</p>%s'
            '<p class="mg-n">칸을 누르면 그 편으로 갑니다.</p></section>'
            % (nl.esc(a['title']), nl.esc(a['lede']), loop_svg(a['id'])))
```

`one()`에서 loop 글이면 본문 맨 앞에 미니 지도를 세운다:

```python
    minimap = ''
    if meta.get('axis') and meta.get('cell'):
        minimap = ('<div class="minimap">%s</div>'
                   % loop_svg(meta['axis'], active='%s:%s' % (meta['axis'], meta['cell']),
                              mini=True))
```

`nl.md_body(...)` 앞에 `minimap +`을 붙인다.

- [ ] **Step 4: CSS를 더한다**

`MRG_CSS`에 붙인다:

```css
  .axis{margin:0 0 20px;padding:16px;border:1px solid var(--line);border-radius:10px;
    background:var(--card)}
  .ax-t{margin:0 0 3px;font-size:15px;font-weight:850;color:var(--ink)}
  .ax-l{margin:0 0 12px;font-size:12.5px;line-height:1.6;color:var(--sub)}
  .loopsvg{width:100%;height:auto;display:block}
  .loopsvg.is-mini{max-width:420px;margin:0 0 14px}
  .lp-sub{font-size:9.5px;fill:var(--faint)}
  .lp-back{stroke-dasharray:none}
  .mcell.is-todo rect{stroke-dasharray:4 3;stroke:var(--faint)}
  .mcell.is-off{opacity:.35}
  .mcell.is-on rect{fill:var(--soft);stroke:var(--accent);stroke-width:2.4}
  .minimap{margin:0 0 10px}
```

- [ ] **Step 5: 만들고 눈으로 확인한다**

```bash
PYTHONIOENCODING=utf-8 python insights/gen_insightview.py
python - <<'PY'
import io, re
h = io.open('대시보드/통합 인사이트.html', encoding='utf-8').read()
print('축 층', h.count('class="axis"'))
print('칸', h.count('class="mcell'))
print('아직 안 씀', h.count('아직 안 씀'))
print('축 층이 타일보다 위', h.find('class="axis"') < h.find('class="sgrid"'))
PY
```

기대: 축 층 1, 칸 8, 「아직 안 씀」 6(고리 칸 여섯이 아직 비었다), 마지막 줄 `True`.

- [ ] **Step 6: 전체 검사와 커밋**

```bash
PYTHONIOENCODING=utf-8 python scripts/build_all.py
git add -A
git commit -m "$(cat <<'EOF'
feat(고리): 페이지 맨 위에 고리 한 장, 글마다 앞에 같은 그림을 작게

입구가 카드 더미라는 지적에 답하는 자리다. 여섯 칸이 가로로 서고 마지막에서
첫 칸으로 곡선이 돌아온다. 아직 안 쓴 칸은 점선에 「아직 안 씀」이라 적는다 —
이행 중 어디까지 왔는지가 화면에 그대로 보인다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 6: ① 조달 편을 쓴다

**Files:**
- Create: `insights/loop/money-01-조달.md`
- Delete: `insights/briefs/AI-투자-회계-지금-상태.md`, `insights/synth/cross-2026-08-17-보증은-다리인가-부풀림인가.md`, `insights/synth/cross-2026-08-17-메타는-고객이면서-경쟁자다.md`

**Interfaces:**
- Consumes: Task 3의 frontmatter 스키마, Task 4의 `cell: capital`
- Produces: 나머지 일곱 편이 따라 쓸 형식. **이 편이 형식을 정한다**

**흡수할 세 장**

| headline | 무엇을 갖고 있나 |
|---|---|
| 빅테크는 쓴 돈만큼 장부에 담지 않는다 | 감가상각 3년 대 6년, 오프발란스 9개사 약 3조 달러, 메타 미개시 리스 약 2,790억·비취소성 계약 약 3,493억 달러 |
| 같은 보증 계약을 한쪽은 다리로, 한쪽은 부풀림으로 센다 | 코어위브 무담보 회사채 약 10% 대 메타 보증 대출 고정 5.9%, 1년 임대 내부수익률 25.4% 대 40.7%, 얽힌 거래 8,000억 달러 초과, 엔비디아 잉여현금흐름 970억 달러, 닷컴기 루슨트 81억·노텔 31억·시스코 24억 달러 |
| 같은 날 메타는 코어위브의 고객이자 경쟁자가 됐다 | 메타 계약 용량 거의 10GW, 코어위브 임차분 2032년까지 재판매 불가, 발표일 메타 +10%·코어위브 −10%·네비우스 −12% |

- [ ] **Step 1: `insight-review` 스킬을 연다**

```
Skill(skill="insight-review")
```

스킬의 절차를 따른다. 이 편이 다른 편과 겹치는지, 주장이 자기 사례를 덮는지는 그 절차로만 걸린다.

- [ ] **Step 2: 세 장의 원문과 근거를 읽는다**

```bash
cat "insights/briefs/AI-투자-회계-지금-상태.md"
cat "insights/synth/cross-2026-08-17-보증은-다리인가-부풀림인가.md"
cat "insights/synth/cross-2026-08-17-메타는-고객이면서-경쟁자다.md"
```

세 장의 `sources:` 를 합쳐 새 글의 `sources:` 로 옮긴다. **인용(`파일 L줄`)을 하나도 잃지 않는다** — `check_cite` 가 새 글의 숫자가 원문 그 줄에 있는지 다시 본다.

- [ ] **Step 3: 새 글을 쓴다**

`insights/loop/money-01-조달.md`. frontmatter는 Task 3의 스키마 그대로, `cell: capital`, `merged:` 에 위 세 headline.

본문 구성:

1. **첫 문단** — 이 칸에서 벌어지는 일을 인과로. 「빅테크가 AI에 쓴 돈은 현금으로 이미 나갔는데 장부에는 나뉘어 들어가고, 모자란 만큼은 채권과 보증으로 메운다. 그 보증이 다시 매출로 잡히면서 같은 돈이 여러 장부에 겹쳐 앉는다」 같은 사슬 한 줄
2. **장부 밖에 있는 것** — 감가상각 3년 대 6년이 연간 비용을 20억 대 10억 달러로 가르는 대목, 오프발란스 약 3조 달러
3. **보증이 무엇을 바꾸나** — 10%가 5.9%로 내려가는 대목과, 그 대가로 수익률 상단이 40.7%에서 25.4%로 깎이는 대목
4. **같은 계약을 두 셈이 다르게 센다** — 다리로 보는 쪽과 중복 집계로 보는 쪽. **여기 「누가 더 가까이서 봤나」 한 문장을 반드시 넣는다**(S1). SemiAnalysis는 수익률을 직접 재고 회계사는 공시를 직접 읽는다 — 둘은 다른 것을 센다
5. **고리 안과 밖** — 메타가 고객이면서 공급자가 된 사건, 재판매가 막힌 임차 물량
6. **`## 아직 모르는 것`** — 보증 없이 조달한 계약의 비중, 실제로 최소 매출을 채워 준 사례, 8,000억 달러의 산출 방식

- [ ] **Step 4: 옛 세 장을 지운다**

```bash
git rm "insights/briefs/AI-투자-회계-지금-상태.md" \
       "insights/synth/cross-2026-08-17-보증은-다리인가-부풀림인가.md" \
       "insights/synth/cross-2026-08-17-메타는-고객이면서-경쟁자다.md"
```

- [ ] **Step 5: 검사기 여섯을 돌린다**

```bash
PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: `전부 통과`. 축 검사 줄의 「남은 옛 글」이 41 → 38로 줄고 「고리 글 1편」이 찍힌다. `check_cite`의 확인 필요 건수가 늘지 않아야 한다 — 늘면 옮기다 숫자를 흘린 것이다.

- [ ] **Step 6: 화면을 확인한다**

```bash
python - <<'PY'
import io
h = io.open('대시보드/통합 인사이트.html', encoding='utf-8').read()
print('아직 안 씀', h.count('아직 안 씀'))
print('미니 지도', h.count('class="minimap"'))
PY
```

기대: 「아직 안 씀」 5(한 칸이 채워졌다), 미니 지도 1.

- [ ] **Step 7: 커밋**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(고리): ① 조달 — 장부 밖에 있는 돈과 보증이 바꾸는 값

카드 세 장(장부에 안 담긴 3조 달러 · 보증은 다리인가 부풀림인가 · 메타는
고객이자 경쟁자)을 한 편으로 합쳤다. 세 장은 지웠고 인용은 전부 옮겼다.

여덟 편의 형식을 이 편에서 잡는다 — 첫 문단이 사슬을 말하고, 충돌 자리에는
누가 더 가까이서 봤는지를 쓰고, 끝에 아직 모르는 것을 남긴다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 7: ⑥ 되돌아옴 편을 쓴다

**Files:**
- Create: `insights/loop/money-06-되돌아옴.md`
- Delete: `insights/synth/cross-2026-08-01-GW당-매출.md`, `insights/synth/cross-2026-08-18-이익은-최대인데-배수는-5배-밑.md`, `insights/briefs/SK하이닉스-밸류에이션-지금-상태.md`, `insights/synth/cross-2026-08-19-같은-근거에-다른-프리미엄.md`, `insights/synth/cross-2026-08-19-한쪽은-표로-한쪽은-한-줄로.md`

**Interfaces:**
- Consumes: Task 6이 정한 형식
- Produces: 고리가 닫힌다. ①의 형식이 맞는지는 여기서만 알 수 있다

**흡수할 다섯 장:** 같은 1GW인데 매출이 열 배 다르다 / 수혜 SK하이닉스·삼성·마이크론·KLA: 이익은 최대인데 PER 5배 밑 / SK하이닉스는 사상 최대 이익에도 멀티플 5배가 안 된다 / 한 회계사가 같은 범위의 중간값이라 적고 서로 다른 프리미엄을 쓴다 / 한 회계사가 경쟁사 두 곳의 할인율을 한쪽은 표로 깔고 한쪽은 한 줄로 놓는다

- [ ] **Step 1: `insight-review` 스킬을 연다**

```
Skill(skill="insight-review")
```

- [ ] **Step 2: 다섯 장을 읽고 인용을 모은다**

```bash
for f in "insights/synth/cross-2026-08-01-GW당-매출.md" \
         "insights/synth/cross-2026-08-18-이익은-최대인데-배수는-5배-밑.md" \
         "insights/briefs/SK하이닉스-밸류에이션-지금-상태.md" \
         "insights/synth/cross-2026-08-19-같은-근거에-다른-프리미엄.md" \
         "insights/synth/cross-2026-08-19-한쪽은-표로-한쪽은-한-줄로.md"; do
  echo "=== $f"; cat "$f"; done
```

- [ ] **Step 3: `insights/loop/money-06-되돌아옴.md`를 쓴다**

`cell: back`. 본문 구성:

1. **첫 문단** — 고리를 닫는 자리. 「①에서 조달한 돈이 여기서 매출로 돌아오는데, 같은 1GW가 층에 따라 여덟에서 열 배 다른 매출을 낸다. 그래서 조달이 정당했는지는 어느 층에 서 있느냐로 갈린다」
2. **같은 단위가 다른 것을 잰다** — 전기를 파는 층 1GW당 연 100~120억 달러, 토큰을 파는 층 연 1,000억 달러 초과
3. **이익이 최대인데 배수는 바닥이다** — 합산 영업이익 150조원, HBM 점유율 58%, 낙폭 40~50%, 선행 PER 5배 밑
4. **값을 매기는 사람의 손이 흔들리는 자리** — 같은 범위의 중간값에 5.5%와 6.0%가 같이 나오는 대목, 한쪽은 표로 한쪽은 한 줄로 놓아 1%p가 주당 2만 1천원을 움직이는 대목. **「누가 더 가까이서 봤나」 한 문장을 넣는다**(S1)
5. **`## 아직 모르는 것`**

- [ ] **Step 4: 옛 다섯 장을 지운다**

```bash
git rm "insights/synth/cross-2026-08-01-GW당-매출.md" \
       "insights/synth/cross-2026-08-18-이익은-최대인데-배수는-5배-밑.md" \
       "insights/briefs/SK하이닉스-밸류에이션-지금-상태.md" \
       "insights/synth/cross-2026-08-19-같은-근거에-다른-프리미엄.md" \
       "insights/synth/cross-2026-08-19-한쪽은-표로-한쪽은-한-줄로.md"
```

- [ ] **Step 5: 검사와 확인**

```bash
PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: `전부 통과`. 남은 옛 글 38 → 33, 고리 글 2편.

- [ ] **Step 6: ①의 형식이 맞는지 되본다**

두 편을 나란히 읽고 판단한다. 고리의 시작과 끝이 같은 말을 두 번 하는지, ①의 「보증」과 ⑥의 「배수」가 사실은 한 이야기인지. 형식을 고쳐야 하면 **여기서 고친다** — 나머지 여섯 편을 쓴 뒤에는 비싸다.

- [ ] **Step 7: 커밋**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(고리): ⑥ 되돌아옴 — 같은 1GW가 층마다 열 배 다른 매출을 낸다

카드 다섯 장을 한 편으로 합쳐 고리를 닫았다. 조달이 정당했는지는 어느 층에
서 있느냐로 갈린다는 것이 이 편의 사슬이다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 8: ⑤ 판매 편을 쓴다

**Files:**
- Create: `insights/loop/money-05-판매.md`
- Delete: `insights/briefs/모델랩-재무-지금-상태.md`, `insights/briefs/추론-경제-지금-상태.md`, `insights/synth/cross-2026-08-17-싸진-값과-오른-청구서.md`

**Interfaces:**
- Consumes: Task 6·7이 굳힌 형식
- Produces: `cell: sell`

**흡수할 세 장:** 앤트로픽은 흑자, 오픈AI는 매출만큼 적자 / AI 값은 내려가는데 파는 쪽 마진은 오른다 / 토큰 값은 1,000분의 1인데 최신 모델 청구서는 2배다

- [ ] **Step 1: `insight-review` 스킬을 연다**

```
Skill(skill="insight-review")
```

- [ ] **Step 2: 세 장을 읽는다**

```bash
for f in "insights/briefs/모델랩-재무-지금-상태.md" \
         "insights/briefs/추론-경제-지금-상태.md" \
         "insights/synth/cross-2026-08-17-싸진-값과-오른-청구서.md"; do
  echo "=== $f"; cat "$f"; done
```

- [ ] **Step 3: `insights/loop/money-05-판매.md`를 쓴다**

`cell: sell`. 사슬: 「받는 값은 내려가는데 남는 몫은 는다. 원가가 더 빨리 떨어지기 때문이고, 그 원가를 깎는 것은 하드웨어가 아니라 서빙 소프트웨어다. 다만 같은 성능을 고정했을 때와 프론티어를 좇을 때 청구서가 반대로 간다.」

들어갈 숫자: 앤트로픽 3분기 영업이익 10억 달러 대 오픈AI 영업이익률 −100%, 앤트로픽 매출의 75~85%가 기업용 API, 오픈AI 무료 사용자 9억 명 이상에 1인당 월 약 0.70달러, Opus 4.5가 직전 세대 3분의 1 값, 이익률 38%에서 70%대, B300 한 대의 처리량이 GPU당 초당 1천에서 1만 4천 토큰, 토큰 값 1,000분의 1 대 최신 모델 청구서 2배.

끝에 `## 아직 모르는 것`. 충돌 자리가 있으면 「누가 더 가까이서 봤나」를 넣는다.

- [ ] **Step 4: 옛 세 장을 지운다**

```bash
git rm "insights/briefs/모델랩-재무-지금-상태.md" \
       "insights/briefs/추론-경제-지금-상태.md" \
       "insights/synth/cross-2026-08-17-싸진-값과-오른-청구서.md"
```

- [ ] **Step 5: 검사**

```bash
PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: `전부 통과`. 남은 옛 글 33 → 30, 고리 글 3편.

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(고리): ⑤ 판매 — 받는 값은 내려가는데 남는 몫은 는다

카드 세 장을 합쳤다. 원가를 깎는 것이 하드웨어가 아니라 서빙 소프트웨어라는
것과, 같은 성능을 고정했을 때와 프론티어를 좇을 때 청구서가 반대로 간다는 것이
이 편의 두 축이다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 9: ④ 가동 편을 쓴다

**Files:**
- Create: `insights/loop/money-04-가동.md`
- Delete: `insights/synth/cross-2026-08-18-사-오는-과제와-노는-GPU.md`, `insights/synth/cross-2026-08-02-배수의-기준선.md`, `insights/briefs/AMD-엔비디아-지금-상태.md`, `insights/synth/cross-2026-08-18-같은-칩에서-더-뽑는-쪽.md`, `insights/briefs/모델-학습-지금-상태.md`

**Interfaces:**
- Consumes: 굳은 형식
- Produces: `cell: run`

**흡수할 다섯 장:** 사 오는 과제가 병목이라더니 산 GPU가 74% 놀았다 / 같은 칩을 두고 2배와 100배가 같이 나온다 / AMD 칩은 싸졌는데 왜 아직 안 팔리나 / 수혜 엔비디아·AMD, 위기 세레브라스·Groq: 소프트웨어가 1.9배 / 모델을 키우는 돈이 사람 손으로 만드는 과제로 간다

- [ ] **Step 1: `insight-review` 스킬을 연다**

```
Skill(skill="insight-review")
```

- [ ] **Step 2: 다섯 장을 읽는다**

```bash
for f in "insights/synth/cross-2026-08-18-사-오는-과제와-노는-GPU.md" \
         "insights/synth/cross-2026-08-02-배수의-기준선.md" \
         "insights/briefs/AMD-엔비디아-지금-상태.md" \
         "insights/synth/cross-2026-08-18-같은-칩에서-더-뽑는-쪽.md" \
         "insights/briefs/모델-학습-지금-상태.md"; do
  echo "=== $f"; cat "$f"; done
```

- [ ] **Step 3: `insights/loop/money-04-가동.md`를 쓴다**

`cell: run`. 사슬: 「산 칩이 저절로 일하지 않는다. 같은 칩에서 나오는 값이 소프트웨어로 갈리고, 그래서 「몇 배 빠르다」는 문장이 2배에서 100배까지 흔들린다. 병목이라던 자리도 실제로 재 보면 다른 곳이었다.」

들어갈 숫자: 깃허브 PR 45만 개 중 채택 2만 1,336개(수율 4.7%), 환경 하나 약 2만 달러, Qwen3-235B 학습기 유휴 30%·MFU 10.5%, GLM-5 학습기 대기 74%, 프레임워크 교체로 100만 토큰당 총소유비용 4.86배 대 2.01배, 루빈 2배·5.4배·10배와 4비트 대 8비트 100배(같은 8비트끼리 65배), TileRT B200 8장 노드 초당 340토큰 대 GB300 NVL72 181.4토큰(1.9배), FP8로 맞추면 초당 108토큰·백만 토큰당 35달러 대 340토큰·13.56달러(61% 절감), 세레브라스 CS-3 코어 약 90만 개·온칩 SRAM 44GB·대역폭 초당 21페타바이트.

AMD 링크드인 인용(`[2608] 링크드인 L251`)을 살린다 — **측정 조건이 공개되지 않았다는 단서를 붙인다.**

- [ ] **Step 4: 옛 다섯 장을 지운다**

```bash
git rm "insights/synth/cross-2026-08-18-사-오는-과제와-노는-GPU.md" \
       "insights/synth/cross-2026-08-02-배수의-기준선.md" \
       "insights/briefs/AMD-엔비디아-지금-상태.md" \
       "insights/synth/cross-2026-08-18-같은-칩에서-더-뽑는-쪽.md" \
       "insights/briefs/모델-학습-지금-상태.md"
```

- [ ] **Step 5: 검사**

```bash
PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: `전부 통과`. 남은 옛 글 30 → 25, 고리 글 4편. 링크드인 인용이 들어갔으므로 축 검사의 L1·L2가 처음으로 실제로 걸린다 — FAIL이 나면 그 게시물이 배제 대상이거나 기준일을 못 읽는 것이다.

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(고리): ④ 가동 — 산 칩이 저절로 일하지 않는다

카드 다섯 장을 합쳤다. 같은 칩에서 나오는 값을 소프트웨어가 가르고, 그래서
「몇 배 빠르다」가 2배에서 100배까지 흔들린다. 병목이라던 자리도 재 보면
다른 곳이었다 — 학습기 대기 74%.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 10: ② 칩 편을 쓴다

**Files:**
- Create: `insights/loop/money-02-칩.md`
- Delete: `insights/briefs/메모리-지금-상태.md`, `insights/synth/cross-2026-08-17-밀어낸-쪽과-사가는-쪽이-같다.md`, `insights/briefs/칩-만들기-지금-상태.md`, `insights/briefs/자체설계-칩-지금-상태.md`, `insights/synth/cross-2026-08-18-연산-다이-바깥에서-뽑는다.md`

**Interfaces:**
- Consumes: 굳은 형식
- Produces: `cell: chip`

**흡수할 다섯 장:** AI가 부른 HBM 수요가 일반 D램 생산까지 갉아먹는다 / 일반 D램을 밀어낸 쪽과 사가는 쪽이 같다 / 트랜지스터가 안 줄어드니 성능을 패키지와 메모리에서 뽑는다 / 직접 만든 칩은 엔비디아 청구서를 깎는다 / 수혜 마벨·코히어런트·루멘텀·AOI: 성능이 다이 바깥에서 나온다

- [ ] **Step 1: `insight-review` 스킬을 연다**

```
Skill(skill="insight-review")
```

- [ ] **Step 2: 다섯 장을 읽는다**

```bash
for f in "insights/briefs/메모리-지금-상태.md" \
         "insights/synth/cross-2026-08-17-밀어낸-쪽과-사가는-쪽이-같다.md" \
         "insights/briefs/칩-만들기-지금-상태.md" \
         "insights/briefs/자체설계-칩-지금-상태.md" \
         "insights/synth/cross-2026-08-18-연산-다이-바깥에서-뽑는다.md"; do
  echo "=== $f"; cat "$f"; done
```

- [ ] **Step 3: `insights/loop/money-02-칩.md`를 쓴다**

`cell: chip`. 사슬: 「조달한 돈이 먼저 칩으로 간다. 그런데 트랜지스터가 안 줄어들어 성능이 연산 다이 안에서 안 나오고, 그래서 값이 붙는 자리가 다이 바깥의 메모리와 광 연결로 옮겨 갔다. 같은 이유로 HBM이 일반 D램 생산을 밀어낸다.」

들어갈 숫자: 루빈 GPU 다이 면적의 약 16%가 HBM 관련 회로, 마벨 커스텀 HBM이 그 면적 약 60% 절감, 구글 광 회선 스위치 전력 40% 절감, 오픈AI가 쓰기도 전에 청구서 30% 절감, 소비자용 감산으로 풀리는 3%. 3나노 트랜지스터 2,000억 개·설계 규칙 25,000개.

링크드인 TPU ICI 토폴로지 인용(`[2608] 링크드인 L331`)을 살린다 — **원문이 소문이라 밝혔다는 단서를 붙인다.**

- [ ] **Step 4: 옛 다섯 장을 지운다**

```bash
git rm "insights/briefs/메모리-지금-상태.md" \
       "insights/synth/cross-2026-08-17-밀어낸-쪽과-사가는-쪽이-같다.md" \
       "insights/briefs/칩-만들기-지금-상태.md" \
       "insights/briefs/자체설계-칩-지금-상태.md" \
       "insights/synth/cross-2026-08-18-연산-다이-바깥에서-뽑는다.md"
```

- [ ] **Step 5: 검사와 커밋**

```bash
PYTHONIOENCODING=utf-8 python scripts/build_all.py
git add -A
git commit -m "$(cat <<'EOF'
feat(고리): ② 칩 — 값이 붙는 자리가 연산 다이 바깥으로 옮겨 갔다

카드 다섯 장을 합쳤다. 트랜지스터가 안 줄어드니 성능이 다이 안에서 안 나오고,
그래서 주문이 쌓이는 곳이 메모리 인터페이스와 광 연결이 됐다. 같은 이유로
HBM 이 일반 D램 생산을 밀어낸다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

기대: 남은 옛 글 25 → 20, 고리 글 5편.

---

### Task 11: ③ 전기 편을 쓴다

**Files:**
- Create: `insights/loop/money-03-전기.md`
- Delete: `insights/briefs/데이터센터-전력-지금-상태.md`, `insights/briefs/랙-냉각-전력-지금-상태.md`, `insights/synth/cross-2026-08-03-병목이-어디인가.md`, `insights/synth/cross-2026-08-04-병목은-풀리나.md`, `insights/briefs/전기요금-지금-상태.md`, `insights/synth/cross-2026-08-17-태양광-3배의-값을-누가-무나.md`, `insights/briefs/에너지안보-지금-상태.md`, `insights/briefs/재생전환-지금-상태.md`, `insights/synth/cross-2026-08-18-이미-서-있는-설비가-먼저-받는다.md`

**Interfaces:**
- Consumes: 굳은 형식
- Produces: `cell: power`

**흡수할 아홉 장** — 이 편이 제일 크다. 데이터센터 전력 / 랙 한 대가 먹는 전력 / 병목 답이 셋 / 병목은 풀리나 / 전기요금은 가장 비싼 발전기가 정한다 / 태양광 3배의 값을 누가 무나 / 해협 하나가 막히자 / 비싸도 자국에서 짓는 쪽으로 / 선 설비가 먼저

**갈라야 하면 갈라도 된다.** 쓰다 보아 한 편에 안 들어가면 `money-03-전기.md`(전력망·랙·병목)와 `money-03b-연료와-요금.md`(요금·태양광·해협·자국 건설)로 나눈다. 나눌 경우 `axes.py`의 `loop` 목록에 칸을 하나 더하고 Task 4의 테스트 기대값(`['capital', 'chip', 'power', 'run', 'sell', 'back']`)을 같이 고친다.

- [ ] **Step 1: `insight-review` 스킬을 연다**

```
Skill(skill="insight-review")
```

- [ ] **Step 2: 아홉 장을 읽는다**

```bash
for f in "insights/briefs/데이터센터-전력-지금-상태.md" \
         "insights/briefs/랙-냉각-전력-지금-상태.md" \
         "insights/synth/cross-2026-08-03-병목이-어디인가.md" \
         "insights/synth/cross-2026-08-04-병목은-풀리나.md" \
         "insights/briefs/전기요금-지금-상태.md" \
         "insights/synth/cross-2026-08-17-태양광-3배의-값을-누가-무나.md" \
         "insights/briefs/에너지안보-지금-상태.md" \
         "insights/briefs/재생전환-지금-상태.md" \
         "insights/synth/cross-2026-08-18-이미-서-있는-설비가-먼저-받는다.md"; do
  echo "=== $f"; cat "$f"; done
```

- [ ] **Step 3: 편을 쓴다(필요하면 둘로 나눈다)**

`cell: power`. 사슬: 「칩을 샀어도 꽂을 데가 없으면 못 쓴다. 전력망을 기다리는 대신 스스로 짓는 쪽으로 돌아섰고, 그러자 값을 정하는 것이 발전 원가가 아니라 그 순간 가장 비싼 발전기가 됐다. 그래서 이미 서 있는 설비가 먼저 값을 받는다.」

들어갈 숫자: PJM 경매가 더 낸 116억 달러, 수주잔고 18억 달러, 접속 5년과 터빈 5년, 도매가 121원 대 생산원가 122원 대 반도체 회사 180원, 저장 설비 5.2GW, 태양광 원가 12배 하락.

「무엇이 먼저 막히나 답이 셋」과 「병목은 풀리나」는 **총량으로 보느냐 지역으로 보느냐가 결론을 갈랐다**는 대목이라 「누가 더 가까이서 봤나」가 반드시 필요하다.

링크드인 Oracle 스타게이트 지연 인용을 살릴 수 있으면 살린다 — 가스 파이프라인·발전 허가 병목으로 1~2년.

- [ ] **Step 4: 옛 아홉 장을 지운다**

```bash
git rm "insights/briefs/데이터센터-전력-지금-상태.md" \
       "insights/briefs/랙-냉각-전력-지금-상태.md" \
       "insights/synth/cross-2026-08-03-병목이-어디인가.md" \
       "insights/synth/cross-2026-08-04-병목은-풀리나.md" \
       "insights/briefs/전기요금-지금-상태.md" \
       "insights/synth/cross-2026-08-17-태양광-3배의-값을-누가-무나.md" \
       "insights/briefs/에너지안보-지금-상태.md" \
       "insights/briefs/재생전환-지금-상태.md" \
       "insights/synth/cross-2026-08-18-이미-서-있는-설비가-먼저-받는다.md"
```

- [ ] **Step 5: 검사와 커밋**

```bash
PYTHONIOENCODING=utf-8 python scripts/build_all.py
git add -A
git commit -m "$(cat <<'EOF'
feat(고리): ③ 전기 — 값을 정하는 것은 그 순간 가장 비싼 발전기다

카드 아홉 장을 합쳤다. 전력망을 기다리는 대신 스스로 짓는 쪽으로 돌아섰고,
그래서 이미 서 있는 설비가 먼저 값을 받는다. 에너지 네 장을 따로 세우지 않고
여기 합친 것은 해협·태양광·요금이 전부 「전기가 얼마에 오나」라서다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

기대: 남은 옛 글 20 → 11, 고리 글 6편. 고리 여섯 칸이 다 찼으므로 화면에서 「아직 안 씀」이 0이 된다(고리 밖 둘은 남는다).

---

### Task 12: 고리 밖 · 돈값 편을 쓴다

**Files:**
- Create: `insights/loop/money-out-돈값.md`
- Delete: `insights/briefs/금리환율-지금-상태.md`, `insights/briefs/시장국면-지금-상태.md`

**Interfaces:**
- Consumes: 굳은 형식
- Produces: `cell: money_cost`

**흡수할 두 장:** 금리와 환율을 국내 수급으로 설명하는 방식이 깨졌다 / 값을 붙잡아 주던 쪽이 빠지고 흐름을 보는 돈만 남았다

- [ ] **Step 1: `insight-review` 스킬을 연다**

```
Skill(skill="insight-review")
```

- [ ] **Step 2: 두 장을 읽는다**

```bash
for f in "insights/briefs/금리환율-지금-상태.md" \
         "insights/briefs/시장국면-지금-상태.md"; do echo "=== $f"; cat "$f"; done
```

- [ ] **Step 3: 편을 쓴다**

`cell: money_cost`. 사슬: 「고리 전체가 이 조건 위에 서 있다. 국채와 주식이 같이 움직이기 시작했고, 원화 약세는 원화가 아니라 달러 쪽에서 왔다. 값을 붙잡아 주던 쪽이 빠지자 흐름을 보는 돈만 남았다.」

들어갈 숫자: 물가 가중치가 성장보다 1.5배, 달러 인덱스에 원화가 없다는 사실, 내부자 매수 21년 최저, 되사기 연료가 6월에 소진.

**출처 등급을 여기서 정직하게 쓴다.** 이 편의 근거는 류상철(전 한국은행 국장, `market` 관측) 두 건을 빼면 회계사·미주사의 해석과 전달이다. 그 사실을 글 안에 적는다 — 「가장 가까이서 본 사람의 말이 두 건뿐이다」.

- [ ] **Step 4: 옛 두 장을 지우고 검사·커밋**

```bash
git rm "insights/briefs/금리환율-지금-상태.md" "insights/briefs/시장국면-지금-상태.md"
PYTHONIOENCODING=utf-8 python scripts/build_all.py
git add -A
git commit -m "$(cat <<'EOF'
feat(고리): 고리 밖 · 돈값 — 원화 약세는 원화 쪽에서 오지 않았다

카드 두 장을 합쳤다. 고리 전체가 서 있는 바깥 조건이다.
이 편의 근거가 관측 두 건과 나머지 전달·해석으로 서 있다는 사실을 글에 적었다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

기대: 남은 옛 글 11 → 9, 고리 글 7편.

---

### Task 13: 고리 밖 · 부동산 편을 쓰고 부동산 대시보드를 맞춘다

**Files:**
- Create: `insights/loop/money-out-부동산.md`
- Delete: `insights/synth/cross-2026-08-15-무엇을-세느냐로-갈린다.md`, `insights/synth/cross-2026-08-15-내놓는-공급이-모자란-집이-아니다.md`, `insights/synth/cross-2026-08-15-공사비를-올린-건-자재값이-아니다.md`, `insights/synth/cross-2026-08-17-집이-왜-안-팔리나.md`, `insights/briefs/부동산-세제-지금-상태.md`, `insights/synth/cross-2026-08-15-만기-다음이-비어-있다.md`, `insights/synth/cross-2026-08-15-공장은-옮겨도-병원은-못-옮긴다.md`, `insights/synth/cross-2026-08-15-기준에-숫자가-없다.md`, `insights/synth/cross-2026-08-15-깨는-문턱이-더-낮다.md`
- Modify: `insights/gen_insightview.py:export()`·`export_sections()`, `scratchpad/gen_realestate_dashboard.py`

**Interfaces:**
- Consumes: 굳은 형식
- Produces: `cell: estate`. 부동산 대시보드가 싣는 조각의 새 모양

**딸려 오는 일:** 부동산 대시보드는 통합 인사이트의 부동산 카드 9장을 `export()`로 그대로 싣는다. 9장이 한 편이 되면 저쪽도 바뀐다. **이 단계의 완료 조건이다.**

- [ ] **Step 1: `insight-review` 스킬을 연다**

```
Skill(skill="insight-review")
```

- [ ] **Step 2: 아홉 장과 저쪽 실림 방식을 읽는다**

```bash
ls insights/synth/cross-2026-08-1*.md insights/briefs/부동산-세제-지금-상태.md
sed -n '/^def export(/,/^def guide/p' insights/gen_insightview.py
grep -n "export" scratchpad/gen_realestate_dashboard.py | head
```

- [ ] **Step 3: 편을 쓴다**

`cell: estate`. 사슬: 「무엇을 세느냐에서 시작한다 — 같은 서울 입주 물량이 4천 호에서 2만 9천 호까지 나온다. 세는 기준이 갈리니 공급도 값도 갈리고, 그 위에 공사비와 세금과 아직 안 정해진 규칙이 얹힌다.」

들어갈 숫자: 입주 물량 4천~2만 9천 호, 용도변경 5년 실적, 나란히 30% 비싼 대안 둘, 월 1만 건이 3~4천 건으로, 6억 넘으면 자금조달계획서, 장기전세·토지임대부의 빈 규칙 20년.

- [ ] **Step 4: 부동산 대시보드가 새 모양을 싣게 한다**

한 편을 통째로 실으면 저쪽 첫 화면에서 너무 커진다. **그 편의 앞머리(첫 문단 + 미니 지도)만 싣고 본문은 통합 인사이트로 보낸다.** `export()`에 `head_only=True` 분기를 더하고 부동산 생성기가 그걸 쓴다.

- [ ] **Step 5: 옛 아홉 장을 지우고 검사한다**

```bash
git rm "insights/synth/cross-2026-08-15-무엇을-세느냐로-갈린다.md" \
       "insights/synth/cross-2026-08-15-내놓는-공급이-모자란-집이-아니다.md" \
       "insights/synth/cross-2026-08-15-공사비를-올린-건-자재값이-아니다.md" \
       "insights/synth/cross-2026-08-17-집이-왜-안-팔리나.md" \
       "insights/briefs/부동산-세제-지금-상태.md" \
       "insights/synth/cross-2026-08-15-만기-다음이-비어-있다.md" \
       "insights/synth/cross-2026-08-15-공장은-옮겨도-병원은-못-옮긴다.md" \
       "insights/synth/cross-2026-08-15-기준에-숫자가-없다.md" \
       "insights/synth/cross-2026-08-15-깨는-문턱이-더-낮다.md"
PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: `전부 통과`. **남은 옛 글 0, 고리 글 8편.** `briefs/`와 `synth/`가 비었다.

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(고리): 고리 밖 · 부동산 — 무엇을 세느냐에서 다 갈린다

카드 아홉 장을 합쳤다. 같은 서울 입주 물량이 집계하는 곳마다 4천 호에서
2만 9천 호까지 나온다는 것이 이 편의 시작이다.

부동산 대시보드는 이제 이 편의 앞머리만 싣고 본문은 통합 인사이트로 보낸다.
카드 아홉 장을 그대로 싣던 자리다.

옛 글 41장이 전부 여덟 편으로 들어갔다 — briefs/ 와 synth/ 가 비었다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

### Task 14: 이행을 닫는다 — A3를 FAIL로 올리고 타일을 걷는다

**Files:**
- Modify: `insights/check_axes.py`(A3 WARN→FAIL), `insights/gen_insightview.py`(타일·탭·읽는 순서 제거), `insights/paths.py`·`insights/check_*.py`(빈 디렉터리 정리)

**Interfaces:**
- Consumes: Task 6~13이 끝나 옛 글이 0장
- Produces: 최종 화면 — 고리 한 장 + 글 여덟 편

- [ ] **Step 1: 옛 글이 정말 0장인지 확인한다**

```bash
ls insights/briefs/*.md insights/synth/*.md 2>&1 | tail -3
PYTHONIOENCODING=utf-8 python insights/check_axes.py
```

기대: `No such file`. 축 검사 요약의 「남은 옛 글 0장」.

- [ ] **Step 2: A3를 FAIL로 올린다**

`insights/check_axes.py`의 마지막 A3 분기를 바꾼다:

```python
    if left:
        add('FAIL', 'A3', '-', '여덟 편에 안 들어간 옛 글 %d장' % len(left))
```

- [ ] **Step 3: 타일·탭·읽는 순서를 걷는다**

여덟 편이면 타일로 고를 것이 없고, 종류가 하나라 탭도 없다. `COURSE`(읽는 순서)는 고리 SVG가 대신한다.

`insights/gen_insightview.py`에서 지운다: `sectiles()`, `tabs()`, `course()`, `COURSE`, `COURSE_LEDE`, `COURSE_CSS`, `GROUPS`·`SECTIONS`의 타일용 쓰임. `build()`의 치환을 고친다:

```python
                .replace('__GUIDE__', guide(per))
                .replace('__TOP__', '')
                .replace('__TABS__', axis_layer())
```

`KINDS`를 하나로 줄인다:

```python
KINDS = ((paths.LOOP, '고리', 'loop'),)
```

`TAB_JS`에서 `sbar`·`kbar` 관련 분기를 걷고 `cell` 필터만 남긴다.

- [ ] **Step 4: 빈 디렉터리를 정리한다**

```bash
git rm -r insights/briefs insights/synth insights/theses
```

`insights/paths.py`에서 `SYNTH`·`BRIEFS`·`THESES`를 지우고, 다섯 검사기(`check_cite` `check_fresh` `check_notes` `check_prose` `check_read`)에서 그 참조를 걷는다. `check_prose`의 synth 전용 루프(`check_order`·`check_head` 호출)도 같이 지운다 — 대상이 없다.

`insights/test_paths.py`에 남은 참조가 있으면 같이 고친다.

- [ ] **Step 5: 전부 돌린다**

```bash
cd insights && python -m pytest -q > /tmp/pt.txt 2>&1; tail -20 /tmp/pt.txt
cd .. && PYTHONIOENCODING=utf-8 python scripts/build_all.py
```

기대: pytest 전부 통과, build_all `전부 통과`.

- [ ] **Step 6: 화면을 확인한다**

```bash
python - <<'PY'
import io
h = io.open('대시보드/통합 인사이트.html', encoding='utf-8').read()
for k in ('class="axis"', 'class="sgrid"', 'class="itabs"', 'class="intro"',
          'class="minimap"', '아직 안 씀'):
    print('%-18s %d' % (k, h.count(k)))
PY
```

기대: `class="axis"` 1, `sgrid` 0, `itabs` 0, `intro` 0, `minimap` 8, `아직 안 씀` 0.

- [ ] **Step 7: 커밋**

```bash
git add -A
git commit -m "$(cat <<'EOF'
refactor(고리): 이행을 닫는다 — 타일과 탭과 읽는 순서를 걷는다

옛 글 41장이 전부 여덟 편에 들어갔다. A3 를 WARN 에서 FAIL 로 올려 앞으로
빠뜨리면 빌드가 멈추게 했다.

여덟 편이면 타일로 고를 것이 없고 종류가 하나라 탭도 없다. 읽는 순서는
고리 SVG 가 대신한다 — 그것이 애초에 COURSE 가 하려던 일이었다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
git push origin main
```

---

## 자기 점검

**스펙 커버리지**

| 스펙 요구 | 태스크 |
|---|---|
| 고리 여섯 칸 + 밖 둘 | Task 4(자료구조) · 6~13(집필) |
| 축이 여럿일 수 있게 | Task 4 — `AXES`가 목록이고 `all_cell_ids()`가 축을 가로지른다 |
| 41장 전량이 정확히 한 번씩 | Task 4 A3 · Task 14에서 FAIL로 승격 |
| 페이지 맨 위 고리 SVG | Task 5 |
| 글마다 앞에 미니 지도 | Task 5 |
| 섹션 타일 제거 | Task 14 |
| 41장 삭제·인용 보존 | Task 6~13 각 Step 4~5, `check_cite`가 지킨다 |
| 출처 등급 | 이미 들어감(`sources_rank.py`, 근거표 점, 범례) |
| 링크드인 배제 판정 | Task 1 |
| 링크드인 기준일 | Task 2 |
| L1 배제 줄 인용 금지 | Task 4 |
| S1 누가 더 가까이서 봤나 | Task 4(WARN) · 집필 태스크에서 채움 |
| 재홍보를 근거에서 뺀다 | Task 1 — `push=True`, `usable=False` |
| 부동산 대시보드 딸림 | Task 13 Step 4 |
| 집필 순서 ①→⑥→⑤→④→②→③→돈값→부동산 | Task 6~13 순서 그대로 |

**빈자리 점검:** 스펙의 「③이 9장이라 갈릴 수 있다」는 Task 11에 갈랐을 때 고칠 곳(`axes.py` 목록과 Task 4 테스트 기대값)까지 적어 뒀다.

**이름 일관성:** `loop_svg`·`axis_layer`·`written_cells`·`li_basis`·`cells`·`cell_of`·`all_cell_ids`가 태스크를 가로질러 같은 이름으로 쓰였다. `merge_svg`·`merge_layer`·`cell_index`는 Task 4~5에서 전부 없어진다.

---

계획이 `docs/superpowers/plans/2026-08-20-돈-고리.md`에 저장됐다. 실행 방법 둘.

**1. 서브에이전트 주도(권장)** — 태스크마다 새 서브에이전트를 붙이고 사이사이 검토. 빠르게 돈다

**2. 이 세션에서 직접** — `executing-plans`로 체크포인트를 두고 묶음 실행

어느 쪽으로 갈까?
