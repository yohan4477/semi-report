# 인사이트 합성 아키텍처 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 성장하는 코퍼스 위에서 통합 인사이트를 증분/전체로 관리하는 데이터 계층(매니페스트·provenance 클러스터·커버리지)을 구축하고, 기존 언더스탠딩 4클러스터를 이관한다.

**Architecture:** `insights/gen_manifest.py`가 `content/newsletter/**`+`content/understanding/**`를 스캔해 `manifest.json`(소스 인벤토리+hash)을 만든다. 인사이트는 `insights/clusters/*.md`에 provenance(sources·source_hashes·categories·covered_through) 프론트매터로 저장. `insights/coverage.py`가 둘을 diff해 stale/uncovered/ok를 판정한다. `validate_insights.py`가 provenance 무결성을 검사한다.

**Tech Stack:** Python 3.13(`py`), pytest, 표준 라이브러리만(hashlib·json·glob·re). 정적 파일.

## Global Constraints

- Python 3.13으로 실행: `py -m ...`. 기본 `python`(3.9) 금지.
- Windows 콘솔 cp949 → 실행 시 `PYTHONUTF8=1` 접두, 파일 I/O 항상 `encoding='utf-8'`.
- 외부 의존 금지(pytest만). stdlib만.
- 작업 디렉터리 `C:\Users\y\semianalysis`, 브랜치 main.
- 소스 id 규칙: `<corpus약칭>:<category>:<slug>` — corpus약칭 semi|und, category=소스 폴더명, slug=파일명(확장자·`[YYMMDD]` 접두 제거 후 정규화).
- `content/understanding/통합/`(= Layer2 인사이트)와 `insights/`는 매니페스트 스캔에서 **제외**.
- 커밋 메시지 끝:
  ```
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01E96zxxWkFv9RygYJ1yvUye
  ```

## 파일 구조

- Create: `insights/gen_manifest.py` — 스캔→manifest.json (단일 책임: 소스 인벤토리).
- Create: `insights/coverage.py` — manifest↔clusters diff (stale/uncovered/ok).
- Create: `insights/validate_insights.py` — provenance 무결성.
- Create: `insights/test_insights.py` — pytest(세 스크립트 순수 함수).
- Create: `insights/clusters/*.md` — 이관된 클러스터(Task 5).
- Generated: `insights/manifest.json`, `insights/coverage_report.md`.

---

### Task 1: gen_manifest 순수 함수 (id·category·date·hash)

**Files:**
- Create: `insights/gen_manifest.py`
- Test: `insights/test_insights.py`

**Interfaces:**
- Produces:
  - `slug(filename: str) -> str`
  - `parse_date(filename: str, body: str) -> str | None`
  - `body_hash(text: str) -> str`
  - `source_id(corpus: str, category: str, filename: str) -> str`

- [ ] **Step 1: 실패 테스트 작성** — `insights/test_insights.py`
```python
import gen_manifest as gm

def test_slug_strips_datecode_and_ext():
    assert gm.slug('[260711] 컴퓨트 통합 리포트.md') == '컴퓨트-통합-리포트'

def test_parse_date_from_datecode():
    assert gm.parse_date('[250214] 냉각.md', '') == '2025-02-14'

def test_parse_date_from_frontmatter():
    assert gm.parse_date('제목.md', 'published: 2026-01-02\n') == '2026-01-02'

def test_parse_date_none():
    assert gm.parse_date('제목.md', '내용') is None

def test_body_hash_changes_with_content():
    assert gm.body_hash('a') != gm.body_hash('b')
    assert len(gm.body_hash('a')) == 12

def test_source_id_shape():
    assert gm.source_id('semianalysis', 'cooling', '[250214] 냉각 시스템.md') == 'semi:cooling:냉각-시스템'
```

- [ ] **Step 2: 실패 확인**

Run: `cd insights && PYTHONUTF8=1 py -m pytest test_insights.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'gen_manifest'`. (pytest 없으면 `py -m pip install pytest`)

- [ ] **Step 3: 구현** — `insights/gen_manifest.py`
```python
# 소스 매니페스트 생성 — content/newsletter + content/understanding 스캔
import os, re, io, json, hashlib, glob

ROOT = r"C:\Users\y\semianalysis"
BASES = [(os.path.join(ROOT, "content", "newsletter"), "semianalysis", "semi"),
         (os.path.join(ROOT, "content", "understanding"), "understanding", "und")]
OUT = os.path.join(ROOT, "insights", "manifest.json")
EXCLUDE_DIRS = ("통합",)  # understanding/통합 = Layer2 인사이트, 소스 아님

def slug(filename):
    s = re.sub(r'\.md$', '', filename)
    s = re.sub(r'^\[\d{6}\]\s*', '', s)                 # [YYMMDD] 접두 제거
    s = re.sub(r'[\s]+', '-', s.strip())
    s = re.sub(r'[^0-9A-Za-z가-힣\-]', '', s)
    return re.sub(r'-+', '-', s).strip('-')[:60]

def parse_date(filename, body):
    m = re.search(r'\[(\d{2})(\d{2})(\d{2})\]', filename)
    if m: return "20%s-%s-%s" % (m.group(1), m.group(2), m.group(3))
    m = re.search(r'^(?:published|updated):\s*(\d{4}-\d{2}-\d{2})', body, re.M)
    return m.group(1) if m else None

def body_hash(text):
    return hashlib.sha1(text.encode('utf-8')).hexdigest()[:12]

def source_id(corpus, category, filename):
    abbr = "semi" if corpus == "semianalysis" else "und"
    return "%s:%s:%s" % (abbr, category, slug(filename))
```

- [ ] **Step 4: 통과 확인**

Run: `cd insights && PYTHONUTF8=1 py -m pytest test_insights.py -q`
Expected: PASS (6 passed).

- [ ] **Step 5: 커밋**
```bash
cd "C:/Users/y/semianalysis" && git add insights/gen_manifest.py insights/test_insights.py && git commit -m "feat(insights): gen_manifest 순수 함수(slug·date·hash·id)"
```

---

### Task 2: gen_manifest 스캔 + main

**Files:**
- Modify: `insights/gen_manifest.py`
- Test: `insights/test_insights.py`

**Interfaces:**
- Consumes: `slug`, `parse_date`, `body_hash`, `source_id`.
- Produces: `scan(bases: list) -> list[dict]` — 소스 dict 목록(id·corpus·category·date·path·hash); `main()` — manifest.json 기록.

- [ ] **Step 1: 실패 테스트 추가** — `test_insights.py` 끝에
```python
import os, io, tempfile

def _mkfile(root, rel, text):
    p = os.path.join(root, rel)
    os.makedirs(os.path.dirname(p), exist_ok=True)
    io.open(p, 'w', encoding='utf-8').write(text)
    return p

def test_scan_ids_and_excludes_통합(tmp_path):
    root = str(tmp_path)
    _mkfile(root, 'content/newsletter/ai_infra/cooling/[250214] 냉각.md', 'x')
    _mkfile(root, 'content/understanding/권효재 대표/미국 데이터센터.md', 'published: 2026-07-24\n')
    _mkfile(root, 'content/understanding/통합/A-이란.md', 'insight body')  # 제외돼야
    bases = [(os.path.join(root, 'content', 'newsletter'), 'semianalysis', 'semi'),
             (os.path.join(root, 'content', 'understanding'), 'understanding', 'und')]
    out = gm.scan(bases, root)
    ids = {s['id'] for s in out}
    assert 'semi:cooling:냉각' in ids
    assert any(s['id'].startswith('und:권효재 대표:') for s in out)
    assert not any(':통합:' in s['id'] for s in out)   # 통합 폴더 제외
    cool = next(s for s in out if s['id'] == 'semi:cooling:냉각')
    assert cool['corpus'] == 'semianalysis' and cool['date'] == '2025-02-14'
    assert cool['path'] == 'content/newsletter/ai_infra/cooling/[250214] 냉각.md'
```

- [ ] **Step 2: 실패 확인**

Run: `cd insights && PYTHONUTF8=1 py -m pytest test_insights.py -q`
Expected: FAIL — `AttributeError: module 'gen_manifest' has no attribute 'scan'`.

- [ ] **Step 3: 구현 추가** — `insights/gen_manifest.py` 끝에
```python
def scan(bases, root=ROOT):
    out = []
    for base, corpus, _ in bases:
        for p in glob.glob(os.path.join(base, '**', '*.md'), recursive=True):
            rel = os.path.relpath(p, base).replace('\\', '/')
            parts = rel.split('/')
            if any(d in EXCLUDE_DIRS for d in parts[:-1]):   # 통합 등 제외
                continue
            category = parts[-2] if len(parts) >= 2 else 'root'
            name = parts[-1]
            body = io.open(p, encoding='utf-8').read()
            out.append({
                'id': source_id(corpus, category, name),
                'corpus': corpus, 'category': category,
                'date': parse_date(name, body),
                'path': os.path.relpath(p, root).replace('\\', '/'),
                'hash': body_hash(body),
            })
    return sorted(out, key=lambda s: s['id'])

def main():
    import datetime
    os.makedirs(os.path.dirname(OUT), exist_ok=True)
    data = {'generated': datetime.date.today().isoformat(), 'sources': scan(BASES)}
    io.open(OUT, 'w', encoding='utf-8').write(json.dumps(data, ensure_ascii=False, indent=1))
    print('OK: %d sources -> %s' % (len(data['sources']), OUT))

if __name__ == '__main__':
    main()
```

- [ ] **Step 4: 통과 확인**

Run: `cd insights && PYTHONUTF8=1 py -m pytest test_insights.py -q`
Expected: PASS (7 passed).

- [ ] **Step 5: 커밋**
```bash
cd "C:/Users/y/semianalysis" && git add insights/gen_manifest.py insights/test_insights.py && git commit -m "feat(insights): gen_manifest scan+main(통합 제외·정렬)"
```

---

### Task 3: coverage 분류 + validate

**Files:**
- Create: `insights/coverage.py`
- Create: `insights/validate_insights.py`
- Test: `insights/test_insights.py`

**Interfaces:**
- Produces (coverage):
  - `parse_cluster(md_text: str) -> dict` — frontmatter(cluster_id·categories·sources·source_hashes) 파싱.
  - `classify(manifest: dict, clusters: list[dict]) -> dict` — `{stale:{id:reasons}, uncovered:[src_id], ok:[id]}`.
- Produces (validate):
  - `validate(manifest: dict, clusters: list[dict]) -> list[str]` — 오류 메시지 목록(빈=정상).

- [ ] **Step 1: 실패 테스트 추가** — `test_insights.py` 끝에
```python
import coverage as cov
import validate_insights as vi

MAN = {'sources': [
    {'id': 'und:oil:밥엘만데브', 'category': 'oil', 'hash': 'h1'},
    {'id': 'und:oil:하르그섬', 'category': 'oil', 'hash': 'h2'},
    {'id': 'semi:robotics:unitree', 'category': 'robotics', 'hash': 'h3'},
]}

def test_parse_cluster_frontmatter():
    md = '---\ncluster_id: A\ncategories: [oil]\nsources: ["und:oil:밥엘만데브"]\nsource_hashes: {"und:oil:밥엘만데브":"h1"}\n---\n본문'
    c = cov.parse_cluster(md)
    assert c['cluster_id'] == 'A' and c['categories'] == ['oil']
    assert c['sources'] == ['und:oil:밥엘만데브']
    assert c['source_hashes']['und:oil:밥엘만데브'] == 'h1'

def test_classify_stale_new_category_source():
    # A는 oil인데 하르그섬(oil 신규)이 sources에 없음 → stale(c)
    A = {'cluster_id': 'A', 'categories': ['oil'], 'sources': ['und:oil:밥엘만데브'],
         'source_hashes': {'und:oil:밥엘만데브': 'h1'}}
    r = cov.classify(MAN, [A])
    assert 'A' in r['stale']
    assert 'semi:robotics:unitree' in r['uncovered']  # 어느 카테고리에도 안 걸림

def test_classify_stale_changed_hash():
    A = {'cluster_id': 'A', 'categories': ['oil'],
         'sources': ['und:oil:밥엘만데브', 'und:oil:하르그섬'],
         'source_hashes': {'und:oil:밥엘만데브': 'OLD', 'und:oil:하르그섬': 'h2'}}
    r = cov.classify(MAN, [A])
    assert 'A' in r['stale']  # 밥엘만데브 hash 변경

def test_classify_ok():
    A = {'cluster_id': 'A', 'categories': ['oil'],
         'sources': ['und:oil:밥엘만데브', 'und:oil:하르그섬'],
         'source_hashes': {'und:oil:밥엘만데브': 'h1', 'und:oil:하르그섬': 'h2'}}
    r = cov.classify(MAN, [A])
    assert r['stale'] == {} and 'A' in r['ok']

def test_validate_flags_missing_source():
    A = {'cluster_id': 'A', 'categories': ['oil'], 'sources': ['und:oil:없는것'],
         'source_hashes': {}}
    errs = vi.validate(MAN, [A])
    assert any('없는것' in e for e in errs)
```

- [ ] **Step 2: 실패 확인**

Run: `cd insights && PYTHONUTF8=1 py -m pytest test_insights.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'coverage'`.

- [ ] **Step 3: 구현** — `insights/coverage.py`
```python
# 커버리지 diff — manifest ↔ clusters
import os, re, io, json, glob, sys

ROOT = r"C:\Users\y\semianalysis"
MAN_PATH = os.path.join(ROOT, "insights", "manifest.json")
CLUSTERS = os.path.join(ROOT, "insights", "clusters")
REPORT = os.path.join(ROOT, "insights", "coverage_report.md")

def parse_cluster(md_text):
    m = re.match(r'^---\n(.*?)\n---', md_text, re.DOTALL)
    fm = m.group(1) if m else ''
    def scal(k):
        mm = re.search(r'^%s:\s*(.*)$' % k, fm, re.M)
        return mm.group(1).strip().strip('"') if mm else ''
    def lst(k):
        mm = re.search(r'^%s:\s*\[(.*)\]\s*$' % k, fm, re.M)
        if not mm: return []
        return [x.strip().strip('"').strip("'") for x in mm.group(1).split(',') if x.strip()]
    def hashes():
        mm = re.search(r'^source_hashes:\s*(\{.*\})\s*$', fm, re.M)
        if not mm: return {}
        try: return json.loads(mm.group(1))
        except Exception: return {}
    return {'cluster_id': scal('cluster_id'), 'categories': lst('categories'),
            'sources': lst('sources'), 'source_hashes': hashes()}

def load_clusters():
    out = []
    for p in sorted(glob.glob(os.path.join(CLUSTERS, '*.md'))):
        out.append(parse_cluster(io.open(p, encoding='utf-8').read()))
    return out

def classify(manifest, clusters, full=False):
    by_id = {s['id']: s for s in manifest['sources']}
    covered = set()
    stale = {}
    for c in clusters:
        covered |= set(c['sources'])
        reasons = []
        for sid in c['sources']:
            if sid not in by_id: reasons.append('삭제:%s' % sid)
            elif c['source_hashes'].get(sid) != by_id[sid]['hash']: reasons.append('변경:%s' % sid)
        for s in manifest['sources']:
            if s['category'] in c['categories'] and s['id'] not in c['sources']:
                reasons.append('신규:%s' % s['id'])
        if reasons: stale[c['cluster_id']] = reasons
    cat_covered = set()
    for c in clusters: cat_covered |= set(c['categories'])
    uncovered = [s['id'] for s in manifest['sources']
                 if s['id'] not in covered and s['category'] not in cat_covered]
    ok = [c['cluster_id'] for c in clusters if c['cluster_id'] not in stale]
    if full:  # 전체 재합성: 모든 클러스터 검토 대상
        stale = {c['cluster_id']: ['--full'] for c in clusters}
        ok = []
    return {'stale': stale, 'uncovered': sorted(uncovered), 'ok': ok}

def main():
    full = '--full' in sys.argv
    manifest = json.load(io.open(MAN_PATH, encoding='utf-8'))
    r = classify(manifest, load_clusters(), full=full)
    lines = ['# 커버리지 리포트\n']
    lines.append('## STALE (재합성 필요)')
    lines += ['- %s: %s' % (k, ', '.join(v)) for k, v in r['stale'].items()] or ['- (없음)']
    lines.append('\n## UNCOVERED (새 클러스터 후보)')
    lines += ['- %s' % s for s in r['uncovered']] or ['- (없음)']
    lines.append('\n## OK')
    lines += ['- %s' % s for s in r['ok']] or ['- (없음)']
    if len(r['uncovered']) >= 5:
        lines.insert(1, '> ⚠️ uncovered %d건 — 새 클러스터/전체 재합성 권장\n' % len(r['uncovered']))
    io.open(REPORT, 'w', encoding='utf-8').write('\n'.join(lines))
    print('STALE %d · UNCOVERED %d · OK %d -> %s' % (len(r['stale']), len(r['uncovered']), len(r['ok']), REPORT))
    sys.exit(1 if (r['stale'] or r['uncovered']) else 0)

if __name__ == '__main__':
    main()
```

- [ ] **Step 3b: 구현** — `insights/validate_insights.py`
```python
# provenance 무결성 검사
import os, io, json, sys
import coverage as cov

ROOT = r"C:\Users\y\semianalysis"
MAN_PATH = os.path.join(ROOT, "insights", "manifest.json")

def validate(manifest, clusters):
    ids = {s['id'] for s in manifest['sources']}
    errs = []
    for c in clusters:
        if not c['cluster_id']:
            errs.append('cluster_id 없음(frontmatter 깨짐)')
        for sid in c['sources']:
            if sid not in ids:
                errs.append('%s: 존재하지 않는 소스 %s' % (c['cluster_id'] or '?', sid))
    return errs

def main():
    manifest = json.load(io.open(MAN_PATH, encoding='utf-8'))
    errs = validate(manifest, cov.load_clusters())
    for e in errs: print('ERROR', e)
    print('validate: %d errors' % len(errs))
    sys.exit(1 if errs else 0)

if __name__ == '__main__':
    main()
```

- [ ] **Step 4: 통과 확인**

Run: `cd insights && PYTHONUTF8=1 py -m pytest test_insights.py -q`
Expected: PASS (12 passed).

- [ ] **Step 5: 커밋**
```bash
cd "C:/Users/y/semianalysis" && git add insights/coverage.py insights/validate_insights.py insights/test_insights.py && git commit -m "feat(insights): coverage 분류(stale/uncovered/ok)·validate provenance"
```

---

### Task 4: 매니페스트 베이스라인 생성

**Files:**
- Generated: `insights/manifest.json`

- [ ] **Step 1: 매니페스트 생성**

Run: `cd "C:/Users/y/semianalysis" && PYTHONUTF8=1 py insights/gen_manifest.py`
Expected: `OK: N sources -> …manifest.json` (N = newsletter+understanding md 수, 통합 제외).

- [ ] **Step 2: 멱등 확인**

Run: `cd "C:/Users/y/semianalysis" && cp insights/manifest.json /tmp/m1.json 2>/dev/null; PYTHONUTF8=1 py insights/gen_manifest.py && diff <(python -c "import json;print(json.dumps(json.load(open('insights/manifest.json',encoding='utf-8')).get('sources'),ensure_ascii=False,sort_keys=True))") <(python -c "import json;print(json.dumps(json.load(open('/tmp/m1.json',encoding='utf-8')).get('sources'),ensure_ascii=False,sort_keys=True))") && echo IDEMPOTENT`
Expected: `IDEMPOTENT`(sources 동일; `generated` 날짜만 다를 수 있음).

- [ ] **Step 3: 커밋**
```bash
cd "C:/Users/y/semianalysis" && git add insights/manifest.json && git commit -m "chore(insights): 매니페스트 베이스라인 생성"
```

---

### Task 5: 언더스탠딩 4클러스터 이관 (메인 세션 저술)

> 소스 id 매핑·provenance 판단이 필요해 **메인 세션이 직접**. 서브에이전트 위임 금지.

**Files:**
- Create: `insights/clusters/A-이란전쟁.md`, `B-원유의존.md`, `C-재생비용.md`, `D-AI전력.md`

- [ ] **Step 1: 소스 id 매핑 확인**

Run: `cd "C:/Users/y/semianalysis" && PYTHONUTF8=1 py -c "import json,io; [print(s['id'],'|',s['path']) for s in json.load(io.open('insights/manifest.json',encoding='utf-8'))['sources'] if s['corpus']=='understanding']"`
Expected: understanding 소스 id↔path 목록 출력 → 각 클러스터 `sources`에 쓸 id 확인.

- [ ] **Step 2: 4클러스터 md 작성**

기존 `content/understanding/통합/*.md` 4편의 본문(통합논지·공통진단·연결인과·상충이견·함의)을 그대로 옮기되, frontmatter를 provenance 형식으로:
```yaml
---
cluster_id: A-이란전쟁
title: "이란 전쟁과 원유 수급 충격"
subtitle: "…"
corpus_scope: und
categories: []          # understanding는 인물 폴더라 자동 라우팅 미사용(수동 배정)
sources: ["und:권효재 대표:…", "und:언더스탠딩 백브리핑:…", …]
source_hashes: {}       # Step 3에서 채움
covered_through: "2026-07-25"
---
```
`sources` id는 Step 1 목록에서 정확히 골라 넣을 것. (기존 `content/understanding/통합/`은 그대로 두고 병행 — 소비자 UI 스펙에서 정리.)

- [ ] **Step 3: source_hashes 자동 채움 + 검증**

Run:
```bash
cd "C:/Users/y/semianalysis" && PYTHONUTF8=1 py -c "
import json,io,re,glob,sys; sys.path.insert(0,'insights'); import coverage as cov
man={s['id']:s['hash'] for s in json.load(io.open('insights/manifest.json',encoding='utf-8'))['sources']}
for p in glob.glob('insights/clusters/*.md'):
    t=io.open(p,encoding='utf-8').read(); c=cov.parse_cluster(t)
    h={sid:man.get(sid,'MISSING') for sid in c['sources']}
    t2=re.sub(r'^source_hashes:.*$', 'source_hashes: '+json.dumps(h,ensure_ascii=False), t, flags=re.M)
    io.open(p,'w',encoding='utf-8').write(t2)
    print(p, '| MISSING' if 'MISSING' in h.values() else 'ok')
"
```
Expected: 각 클러스터 `ok`(MISSING 없어야 — 있으면 Step 2 sources id 오타 수정).

- [ ] **Step 4: validate + coverage**

Run: `cd "C:/Users/y/semianalysis" && PYTHONUTF8=1 py insights/validate_insights.py; PYTHONUTF8=1 py insights/coverage.py; cat insights/coverage_report.md | iconv -f utf-8 -t utf-8 -c | head -30`
Expected: validate `0 errors`. coverage: 4 클러스터가 stale 아님(OK)이어야(그 소스 hash 일치). semi 뉴스레터는 UNCOVERED로 대량 표시(예상 — 베이스라인).

- [ ] **Step 5: 커밋·푸시**
```bash
cd "C:/Users/y/semianalysis" && git add insights/clusters/ insights/coverage_report.md && git commit -m "content(insights): 언더스탠딩 4클러스터 이관 + provenance" && git push origin main
```

---

## Self-Review 결과

- **스펙 커버리지**: §3 데이터모델→Task1-2·5, §4 알고리즘→Task3, §5 스크립트→Task1-3, §6 마이그레이션(understanding)→Task5, §8 테스트→Task1-3 픽스처·Task4 멱등. semi 클러스터 작성은 §9 경계상 후속(이 플랜은 understanding 이관까지). 커버됨.
- **플레이스홀더**: 없음(코드·명령·기대값 실체화). Task5 본문은 기존 md 이관+저술이라 코드 아님 — 매핑·검증 명령 제공.
- **타입 일관성**: `scan(bases,root)->list`, `parse_cluster->dict(cluster_id·categories·sources·source_hashes)`, `classify(manifest,clusters,full)->{stale,uncovered,ok}`, `validate(manifest,clusters)->list[str]` 전 Task 일치. coverage/validate가 `cov.load_clusters` 공유.
