# 통합 인사이트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 언더스탠딩 대시보드 상단에 23개 문서를 4개 주제 클러스터로 합성한 "통합 인사이트" 섹션을 정적으로 생성한다.

**Architecture:** 클러스터별 합성 원문은 `content/understanding/통합/*.md`(진실원천). 빌드 스크립트 `scratchpad/gen_insights.py`가 md를 읽어 (1) 대시보드 각 카드 `<h2>`에 앵커 id 부여, (2) 본문 md→HTML 렌더, (3) `sources` 제목을 카드 앵커로 링크, (4) nav 위에 통합 섹션을 멱등 주입한다. `regroup_dashboard.py` 다음에 마지막으로 실행.

**Tech Stack:** Python 3.13(`py` 런처), pytest, 표준 라이브러리만(외부 md 라이브러리 없음), 정적 HTML.

## Global Constraints

- Python 3.13으로 실행: 명령은 `py -m ...` / `py 파일.py`. 기본 `python`은 3.9라 사용 금지.
- Windows 콘솔이 cp949라 한글/기호 출력 크래시 방지: 스크립트 실행 시 `PYTHONUTF8=1` 접두, 파일 입출력은 항상 `encoding='utf-8'`.
- 외부 의존 추가 금지(pytest 제외). md→HTML은 서브셋 자체 렌더러.
- 유료 원문 전문 재수록 금지 — 통합 인사이트는 합성 창작, 링크만 원본/요약 카드로.
- 대시보드 파일: `대시보드/언더스탠딩 대시보드.html`. 통합 md: `content/understanding/통합/`.
- 빌드는 멱등: 재실행해도 결과 동일. 통합 섹션은 `<!-- INSIGHTS:START -->`…`<!-- INSIGHTS:END -->` 센티넬로 감싼다.
- 실행 순서: `regroup_dashboard.py` → `gen_insights.py`(마지막).

---

## 파일 구조

- Create: `scratchpad/gen_insights.py` — 통합 섹션 빌드 스크립트(단일 책임: 통합 md → 대시보드 주입).
- Create: `scratchpad/test_gen_insights.py` — pytest 단위 테스트(순수 함수).
- Create: `content/understanding/통합/A-이란전쟁-원유수급.md` 외 3개 — 합성 원문.
- Modify: `대시보드/언더스탠딩 대시보드.html` — 카드 id + 통합 섹션(스크립트가 자동).

---

### Task 1: 순수 함수 — slugify · 프론트매터 · md→HTML

**Files:**
- Create: `scratchpad/gen_insights.py`
- Test: `scratchpad/test_gen_insights.py`

**Interfaces:**
- Produces:
  - `slugify(title: str) -> str` — 태그 제거 후 카드 앵커용 슬러그.
  - `parse_front_matter(raw: str) -> dict` — `key: val` + `sources` 리스트 파싱.
  - `md_to_html(body: str) -> str` — `##`·`-`·`**bold**`·문단 서브셋 렌더.

- [ ] **Step 1: pytest 준비 + 실패 테스트 작성**

Create `scratchpad/test_gen_insights.py`:
```python
import gen_insights as g

def test_slugify_strips_tags_and_symbols():
    assert g.slugify('호남 반도체 산단, 4년 <span>x</span>') == '호남-반도체-산단-4년-x'

def test_parse_front_matter_scalars_and_sources():
    raw = 'cluster_id: A\ntitle: "이란 전쟁"\nsources:\n  - "카드 하나"\n  - "카드 둘"\nupdated: 2026-07-25'
    fm = g.parse_front_matter(raw)
    assert fm['cluster_id'] == 'A'
    assert fm['title'] == '이란 전쟁'
    assert fm['sources'] == ['카드 하나', '카드 둘']

def test_md_to_html_headings_bullets_bold():
    html = g.md_to_html('## 공통 진단\n- 첫째 **강조**\n- 둘째\n')
    assert '<h4>공통 진단</h4>' in html
    assert '<li>첫째 <b>강조</b></li>' in html
```

- [ ] **Step 2: 실패 확인**

Run: `cd scratchpad && PYTHONUTF8=1 py -m pytest test_gen_insights.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'gen_insights'` (없으면 `py -m pip install pytest` 먼저).

- [ ] **Step 3: 순수 함수 구현**

Create `scratchpad/gen_insights.py`:
```python
# 통합 인사이트 빌드 — content/understanding/통합/*.md → 대시보드 상단 섹션 주입
import re, io, os, glob, sys

DASH = r"C:\Users\y\semianalysis\대시보드\언더스탠딩 대시보드.html"
INS_DIR = r"C:\Users\y\semianalysis\content\understanding\통합"

def slugify(title):
    s = re.sub(r'<[^>]+>', '', title)                 # 태그 제거
    s = re.sub(r'[\s,·—…()\[\]?]+', '-', s.strip())    # 공백·기호 → 하이픈
    s = re.sub(r'[^0-9A-Za-z가-힣\-]', '', s)
    return re.sub(r'-+', '-', s).strip('-')[:60]

def parse_front_matter(raw):
    fm = {}
    key = None
    for line in raw.splitlines():
        m_item = re.match(r'^\s*-\s+(.*)$', line)
        if m_item and key:
            fm.setdefault(key, []).append(m_item.group(1).strip().strip('"'))
            continue
        m = re.match(r'^(\w+):\s*(.*)$', line)
        if m:
            key = m.group(1)
            val = m.group(2).strip().strip('"')
            fm[key] = val if val else []
    return fm

def _inline(t):
    return re.sub(r'\*\*(.+?)\*\*', r'<b>\1</b>', t)

def md_to_html(body):
    out, in_ul = [], False
    for line in body.splitlines():
        h = re.match(r'^##\s+(.*)$', line)
        li = re.match(r'^-\s+(.*)$', line)
        if h:
            if in_ul: out.append('</ul>'); in_ul = False
            out.append('<h4>%s</h4>' % _inline(h.group(1).strip()))
        elif li:
            if not in_ul: out.append('<ul>'); in_ul = True
            out.append('<li>%s</li>' % _inline(li.group(1).strip()))
        elif line.strip():
            if in_ul: out.append('</ul>'); in_ul = False
            out.append('<p>%s</p>' % _inline(line.strip()))
    if in_ul: out.append('</ul>')
    return '\n'.join(out)
```

- [ ] **Step 4: 통과 확인**

Run: `cd scratchpad && PYTHONUTF8=1 py -m pytest test_gen_insights.py -q`
Expected: PASS (3 passed).

- [ ] **Step 5: 커밋**

```bash
git add scratchpad/gen_insights.py scratchpad/test_gen_insights.py
git commit -m "feat(insights): gen_insights 순수 함수(slugify·frontmatter·md_to_html)"
```

---

### Task 2: 카드 id 주입 + 근거 링크 해석

**Files:**
- Modify: `scratchpad/gen_insights.py`
- Test: `scratchpad/test_gen_insights.py`

**Interfaces:**
- Consumes: `slugify`.
- Produces:
  - `card_titles(html: str) -> list[str]` — 대시보드 카드 `<h2>` 텍스트(태그 제거) 목록.
  - `inject_card_ids(html: str) -> tuple[str, dict]` — 각 카드 `<h2>`에 `id="card-<슬러그>"` 부여(있으면 유지), `{정규화제목: id}` 맵 반환.
  - `resolve_sources(titles: list[str], id_map: dict) -> tuple[list[tuple[str,str]], list[str]]` — `[(제목, '#id')]`와 미매칭 제목 목록.

- [ ] **Step 1: 실패 테스트 추가**

Append to `scratchpad/test_gen_insights.py`:
```python
CARD = '<div class="ucard"><h2>이란 전쟁 <span>(1부)</span></h2></div>'

def test_inject_card_ids_adds_anchor_and_map():
    out, m = g.inject_card_ids(CARD)
    assert 'id="card-이란-전쟁-1부"' in out
    assert m['이란 전쟁 (1부)'] == '#card-이란-전쟁-1부'

def test_inject_card_ids_idempotent():
    out1, _ = g.inject_card_ids(CARD)
    out2, _ = g.inject_card_ids(out1)
    assert out1 == out2

def test_resolve_sources_matches_and_warns():
    _, m = g.inject_card_ids(CARD)
    ok, miss = g.resolve_sources(['이란 전쟁 (1부)', '없는 카드'], m)
    assert ok == [('이란 전쟁 (1부)', '#card-이란-전쟁-1부')]
    assert miss == ['없는 카드']
```

- [ ] **Step 2: 실패 확인**

Run: `cd scratchpad && PYTHONUTF8=1 py -m pytest test_gen_insights.py -q`
Expected: FAIL — `AttributeError: module 'gen_insights' has no attribute 'inject_card_ids'`.

- [ ] **Step 3: 구현 추가**

Append to `scratchpad/gen_insights.py`:
```python
def _text_of(h2_inner):
    return re.sub(r'\s+', ' ', re.sub(r'<[^>]+>', '', h2_inner)).strip()

def card_titles(html):
    return [_text_of(m) for m in re.findall(r'<h2>(.*?)</h2>', html, re.DOTALL)]

def inject_card_ids(html):
    id_map = {}
    seen = {}
    def repl(m):
        inner = m.group(1)
        title = _text_of(inner)
        if title in id_map:                      # 이미 이번 실행서 처리
            return m.group(0)
        base = 'card-' + slugify(title)
        sid = base
        n = seen.get(base, 0)
        if n: sid = '%s-%d' % (base, n + 1)
        seen[base] = n + 1
        id_map[title] = '#' + sid
        return '<h2 id="%s">%s</h2>' % (sid, inner)
    # 기존 id 있는 <h2 id=...>는 건드리지 않되 맵에 등록
    def keep(m):
        sid, inner = m.group(1), m.group(2)
        id_map[_text_of(inner)] = '#' + sid
        return m.group(0)
    html = re.sub(r'<h2 id="([^"]+)">(.*?)</h2>', keep, html, flags=re.DOTALL)
    html = re.sub(r'<h2>(.*?)</h2>', repl, html, flags=re.DOTALL)
    return html, id_map

def resolve_sources(titles, id_map):
    ok, miss = [], []
    for t in titles:
        key = re.sub(r'\s+', ' ', t).strip()
        if key in id_map: ok.append((t, id_map[key]))
        else: miss.append(t)
    return ok, miss
```

- [ ] **Step 4: 통과 확인**

Run: `cd scratchpad && PYTHONUTF8=1 py -m pytest test_gen_insights.py -q`
Expected: PASS (6 passed).

- [ ] **Step 5: 커밋**

```bash
git add scratchpad/gen_insights.py scratchpad/test_gen_insights.py
git commit -m "feat(insights): 카드 id 주입·근거 링크 해석(멱등)"
```

---

### Task 3: 통합 섹션 조립 + 멱등 주입 + main

**Files:**
- Modify: `scratchpad/gen_insights.py`
- Test: `scratchpad/test_gen_insights.py`

**Interfaces:**
- Consumes: `parse_front_matter`, `md_to_html`, `inject_card_ids`, `resolve_sources`.
- Produces:
  - `render_block(fm: dict, body: str, id_map: dict) -> tuple[str, list[str]]` — 클러스터 1블록 HTML + 미매칭 목록.
  - `build_section(mds: list[tuple[dict,str]], id_map: dict) -> tuple[str, list[str]]` — 센티넬 감싼 전체 섹션 + nav 칩 조각.
  - `strip_section(html: str) -> str` — 기존 센티넬 블록·nav 칩 제거(멱등).
  - `main()` — 파일 읽고 카드 id 주입·섹션 주입·검증 후 저장.

- [ ] **Step 1: 실패 테스트 추가**

Append to `scratchpad/test_gen_insights.py`:
```python
def test_render_block_has_thesis_and_source_chip():
    fm = {'cluster_id': 'A', 'title': '이란 전쟁', 'subtitle': '부제', 'sources': ['이란 전쟁 (1부)']}
    _, m = g.inject_card_ids(CARD)
    html, miss = g.render_block(fm, '## 통합 논지\n한 줄.\n', m)
    assert '이란 전쟁' in html and '한 줄.' in html
    assert 'href="#card-이란-전쟁-1부"' in html
    assert miss == []

def test_strip_section_idempotent():
    wrapped = 'A<!-- INSIGHTS:START -->x<!-- INSIGHTS:END -->B'
    assert g.strip_section(wrapped) == 'AB'
```

- [ ] **Step 2: 실패 확인**

Run: `cd scratchpad && PYTHONUTF8=1 py -m pytest test_gen_insights.py -q`
Expected: FAIL — `has no attribute 'render_block'`.

- [ ] **Step 3: 구현 추가**

Append to `scratchpad/gen_insights.py`:
```python
def render_block(fm, body, id_map):
    ok, miss = resolve_sources(fm.get('sources', []), id_map)
    chips = '\n'.join(
        '        <a class="ins-src" href="%s">%s</a>' % (href, re.sub(r'\s*\(.*?\)\s*$', '', t))
        for t, href in ok)
    return (
        '    <article class="ins-card" id="ins-%s">\n'
        '      <span class="ins-id">%s</span>\n'
        '      <h3>%s</h3>\n'
        '      <p class="ins-sub">%s</p>\n'
        '      <div class="ins-body">\n%s\n      </div>\n'
        '      <p class="ins-src-label">근거 카드</p>\n'
        '      <div class="ins-srcs">\n%s\n      </div>\n'
        '    </article>' % (
            fm.get('cluster_id', ''), fm.get('cluster_id', ''),
            fm.get('title', ''), fm.get('subtitle', ''),
            md_to_html(body), chips)
    ), miss

def build_section(mds, id_map):
    blocks, misses = [], []
    for fm, body in sorted(mds, key=lambda x: x[0].get('cluster_id', '')):
        h, miss = render_block(fm, body, id_map)
        blocks.append(h); misses += miss
    sec = (
        '<!-- INSIGHTS:START -->\n'
        '  <section id="sec-insights">\n'
        '    <div class="sec-head"><span class="sec-num">🧭</span>'
        '<h2 class="sec-title">통합 인사이트</h2></div>\n'
        '    <p class="sec-sub">여러 문서가 합쳐서 말하는 것 — 주제별 크로스-문서 합성</p>\n'
        + '\n'.join(blocks) + '\n  </section>\n'
        '<!-- INSIGHTS:END -->\n\n')
    return sec, misses

def strip_section(html):
    return re.sub(r'<!-- INSIGHTS:START -->.*?<!-- INSIGHTS:END -->\n\n?', '', html, flags=re.DOTALL)

def main():
    html = io.open(DASH, encoding='utf-8').read()
    html = strip_section(html)
    html, id_map = inject_card_ids(html)
    mds = []
    for p in sorted(glob.glob(os.path.join(INS_DIR, '*.md'))):
        raw = io.open(p, encoding='utf-8').read()
        m = re.match(r'^---\n(.*?)\n---\n(.*)$', raw, re.DOTALL)
        mds.append((parse_front_matter(m.group(1)), m.group(2)))
    if not mds:
        io.open(DASH, 'w', encoding='utf-8').write(html)
        print('통합 md 없음 — 섹션 스킵'); return
    sec, misses = build_section(mds, id_map)
    # nav 칩 맨 앞에 삽입
    chip = '    <a href="#sec-insights">🧭 통합인사이트 <b>%d</b></a>\n' % len(mds)
    html = re.sub(r'(<nav class="sec-nav">\n)', r'\1' + chip, html, count=1)
    # nav 앞에 섹션 주입
    html = re.sub(r'(  <nav class="sec-nav">)', sec + r'\1', html, count=1)
    io.open(DASH, 'w', encoding='utf-8').write(html)
    print('OK: 클러스터 %d개' % len(mds))
    if misses: print('경고 미매칭 sources:', misses)

if __name__ == '__main__':
    main()
```

- [ ] **Step 4: 통과 확인**

Run: `cd scratchpad && PYTHONUTF8=1 py -m pytest test_gen_insights.py -q`
Expected: PASS (8 passed).

- [ ] **Step 5: 커밋**

```bash
git add scratchpad/gen_insights.py scratchpad/test_gen_insights.py
git commit -m "feat(insights): 통합 섹션 조립·nav 칩·멱등 주입·main"
```

---

### Task 4: 통합 섹션 CSS

**Files:**
- Modify: `대시보드/언더스탠딩 대시보드.html` (`<style>` 내 `.sec-nav` 규칙 근처)

**Interfaces:**
- Consumes: 기존 CSS 토큰(`--accent`, `--surface`, `--line`, `--ink-2` 등).

- [ ] **Step 1: CSS 추가**

`대시보드/언더스탠딩 대시보드.html`의 `/* ── 섹션 점프 내비 ── */` 주석 바로 앞에 삽입:
```css
  /* ── 통합 인사이트 ── */
  #sec-insights .sec-num { background:var(--accent); color:var(--surface); }
  .ins-card { background:var(--surface); border:1px solid var(--line); border-left:3px solid var(--accent);
    border-radius:12px; padding:20px 22px; margin-bottom:16px; box-shadow:var(--shadow); }
  .ins-card .ins-id { font-size:11px; font-weight:800; letter-spacing:.1em; color:var(--accent); }
  .ins-card h3 { font-size:20px; font-weight:850; letter-spacing:-.02em; margin:4px 0 2px; color:var(--ink); }
  .ins-sub { font-size:13.5px; color:var(--ink-3); margin:0 0 14px; }
  .ins-body h4 { font-size:12.5px; font-weight:800; color:var(--accent-ink); margin:14px 0 6px;
    text-transform:uppercase; letter-spacing:.04em; }
  .ins-body p { font-size:14.5px; color:var(--ink); line-height:1.6; margin:0 0 8px; }
  .ins-body ul { margin:0 0 8px; padding-left:18px; }
  .ins-body li { font-size:13.5px; color:var(--ink-2); line-height:1.58; margin-bottom:4px; }
  .ins-src-label { font-size:10.5px; font-weight:800; color:var(--ink-3); margin:14px 0 6px; }
  .ins-srcs { display:flex; flex-wrap:wrap; gap:6px; }
  .ins-srcs a { font-size:11.5px; padding:5px 10px; border-radius:999px; background:var(--sunk);
    border:1px solid var(--line); color:var(--ink-2); text-decoration:none; }
  .ins-srcs a:hover { border-color:var(--accent); color:var(--accent); }
```

- [ ] **Step 2: 커밋**

```bash
git add "대시보드/언더스탠딩 대시보드.html"
git commit -m "style(insights): 통합 인사이트 카드 CSS"
```

---

### Task 5: 합성 원문 4편 작성 (메인 세션 저술)

> 도메인 깊이가 필요해 **메인 세션이 직접 저술**(23편을 이미 읽음). 서브에이전트에 위임 금지.

**Files:**
- Create: `content/understanding/통합/A-이란전쟁-원유수급.md`
- Create: `content/understanding/통합/B-원유의존-다변화벽.md`
- Create: `content/understanding/통합/C-재생전환-진짜비용.md`
- Create: `content/understanding/통합/D-AI반도체-전력수요.md`

- [ ] **Step 1: 4편 작성**

각 파일은 §2 스키마(프론트매터 `cluster_id·title·subtitle·sources·updated` + 본문 `## 통합 논지/공통 진단/연결·인과/상충·이견/한국 함의`). `sources` 제목은 대시보드 카드 `<h2>` 텍스트와 정확히 일치시킬 것(불일치 시 Task 6 빌드가 경고). 클러스터·근거 배정은 설계 §3 표 참조.

- [ ] **Step 2: 제목 일치 사전 점검**

Run: `PYTHONUTF8=1 py -c "import sys; sys.path.insert(0,'scratchpad'); import gen_insights as g; import io; html=io.open(g.DASH,encoding='utf-8').read(); print('\n'.join(g.card_titles(html)))"`
Expected: 카드 제목 목록 출력 → 각 md `sources`가 이 목록에 있는지 눈으로 대조.

- [ ] **Step 3: 커밋**

```bash
git add "content/understanding/통합/"
git commit -m "content(insights): 통합 인사이트 4편(이란전쟁·원유의존·재생비용·AI전력)"
```

---

### Task 6: 파이프라인 실행 · 검증 · 발행

**Files:**
- Modify: `대시보드/언더스탠딩 대시보드.html` (스크립트가 자동)

- [ ] **Step 1: 빌드 실행(순서 준수)**

Run:
```bash
cd "C:/Users/y/semianalysis" && PYTHONUTF8=1 py scratchpad/regroup_dashboard.py && PYTHONUTF8=1 py scratchpad/gen_insights.py
```
Expected: `OK: ai=2 …` 이어서 `OK: 클러스터 4개`. **"경고 미매칭 sources" 줄이 없어야 함**(있으면 Task 5로 돌아가 제목 수정).

- [ ] **Step 2: 구조 검증**

Run:
```bash
cd "C:/Users/y/semianalysis" && f="대시보드/언더스탠딩 대시보드.html" && echo "div:$(grep -o '<div' "$f"|wc -l)/$(grep -o '</div>' "$f"|wc -l) insrt:$(grep -c 'INSIGHTS:START' "$f")/$(grep -c 'INSIGHTS:END' "$f") blocks:$(grep -c 'class=\"ins-card\"' "$f") navchips:$(grep -c 'href=\"#sec-' "$f")"
```
Expected: `div` 좌우 동일, `insrt 1/1`, `blocks 4`, `navchips 7`(통합1+산업6).

- [ ] **Step 3: 앵커 무결성 검증**

Run:
```bash
cd "C:/Users/y/semianalysis" && PYTHONUTF8=1 py -c "import re,io; h=io.open(r'대시보드/언더스탠딩 대시보드.html',encoding='utf-8').read(); ids=set(re.findall(r'id=\"(card-[^\"]+)\"',h)); refs=set(re.findall(r'href=\"#(card-[^\"]+)\"',h)); miss=refs-ids; print('MISSING' if miss else 'OK', miss)"
```
Expected: `OK set()`.

- [ ] **Step 4: 아티팩트 재발행**

Artifact 도구로 `대시보드/언더스탠딩 대시보드.html`을 `url=https://claude.ai/code/artifact/178afa89-49aa-43bc-8a3a-e52c7a094686`, `favicon=🎧`, `force=true`로 재발행.

- [ ] **Step 5: 커밋·푸시**

```bash
cd "C:/Users/y/semianalysis" && git add "대시보드/언더스탠딩 대시보드.html" && git commit -m "feat(insights): 통합 인사이트 섹션 대시보드 반영(4클러스터)" && git push origin main
```

---

## Self-Review 결과

- **스펙 커버리지**: §3 클러스터→Task5, §4 스키마→Task5, §5 빌드/id/링크/주입→Task1-3, CSS→Task4, §6 에러(미매칭 경고·멱등·빈 디렉터리)→Task2-3·6, §7 검증(div·센티넬·앵커)→Task6, §8 갱신→플랜 재실행. 커버됨.
- **플레이스홀더**: 없음(코드·명령·기대값 실체화). Task5 본문은 창작 저술이라 코드 아님 — 스키마·점검 명령 제공.
- **타입 일관성**: `inject_card_ids`→`(html, id_map)`, `resolve_sources(titles,id_map)`→`(ok,miss)`, `render_block`→`(html,miss)`, `build_section`→`(sec,misses)` 전 Task 일치.
