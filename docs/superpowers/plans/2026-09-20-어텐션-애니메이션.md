# 어텐션 애니메이션 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 어텐션 계산 한 번을 일곱 단계로 늦춰 보이는 공개 장 하나를 세운다.

**Architecture:** 파이썬 생성기 둘로 쪼갠다. `attn_trace.py` 가 실제 모델을 한 번 돌려 값을 `data/attn_trace.json` 으로 굽고, `gen_anim_page.py` 가 그 JSON 만 읽어 HTML 을 낸다. 페이지는 `data-step` 속성 하나로 도는 상태 기계이고 그 사이는 CSS `transition` 이 메운다. 바깥 라이브러리를 안 쓴다.

**Tech Stack:** Python 3.9 · torch 2.8.0+cpu · transformers 4.56.2 · 인라인 SVG + CSS · Playwright(검사용)

**Spec:** `docs/superpowers/specs/2026-09-20-어텐션-애니메이션-design.md`

## Global Constraints

- 콘솔이 cp949 다. 파이썬을 돌릴 때 `PYTHONIOENCODING=utf-8` 을 붙인다
- 화면에 뜨는 숫자는 전부 `data/attn_trace.json` 에 있어야 한다. 지어낸 값 금지(도해 규칙 1)
- 판 위에 남기는 글자는 토큰·값 라벨뿐이고 설명은 판 밖 한 줄이다(도해 규칙 3)
- 색은 회색조. 강조색은 하나만 — 지금 보는 자리와 방금 계산한 막대
- 모바일 폭 360 에서 가로 스크롤이 생기면 안 된다
- 검사기는 배치를 눈으로 안 본다. `scratchpad/check_fig.py` 에 먹인다
- 커밋 메시지 끝에 붙인다:
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_013ZeLyU1QpQdxypbbREemyJ`
- 푸시는 검사기 전부 FAIL 0 을 본 뒤에 한다
- 생성물(`대시보드/애니메이션 — 어텐션.html`)을 손으로 고치지 않는다

## File Structure

| 파일 | 맡는 일 |
|---|---|
| `scratchpad/attn_trace.py` | 모델 한 번 돌려 값 굽기. 헤드 고르기·스키마 검증 |
| `data/attn_trace.json` | 구운 값. 커밋한다 |
| `scratchpad/gen_anim_page.py` | JSON → HTML. `check_ui()` 규약, `--selftest` |
| `대시보드/애니메이션 — 어텐션.html` | 생성물 |
| `scratchpad/check_anim.py` | 스텝 일곱을 Playwright 로 찍어 `check_fig` 에 먹인다 |
| `scratchpad/anim_sweep_probe.py` | 스텝 3 의 연속 훑기가 실제로 도는지 본다 |
| `docs/규칙 — 애니메이션.md` | 이 장의 규칙 |
| `scripts/gen_site.py` | PAGES 에 한 줄 |

---

### Task 1: 실제 어텐션 값 굽기

**Files:**
- Create: `scratchpad/attn_trace.py`
- Create: `data/attn_trace.json` (스크립트가 낸다)

**Interfaces:**
- Consumes: 없음
- Produces: `data/attn_trace.json` — 아래 키를 갖는다.
  `model`(str) `sentence`(str) `tokens`(list[str]) `query_pos`(int) `noun_pos`(int)
  `layer`(int) `head`(int) `picked_weight`(float) `picked_by`(str) `dim`(int)
  `scores_raw`(list[float], 길이 query_pos+1) `scores_softmax`(list[float], 같은 길이, 합 1)
  `q_preview`(list[float], 8) `k_preview`(list[list[float]], 각 8) `v_preview`(같은 꼴)
  `out_preview`(list[float], 8)

- [ ] **Step 1: 검증 함수를 먼저 쓴다 (실패하는 시험)**

`scratchpad/attn_trace.py` 에 검증만 먼저 넣는다.

```python
# -*- coding: utf-8 -*-
"""어텐션 애니메이션이 쓸 실제 값을 모델에서 한 번 뽑아 굽는다.

브라우저에서 모델을 돌리지 않는다. 화면에 뜨는 숫자는 전부 이 파일이 낸 JSON 에 있어야
한다(도해 규칙 1 — 원문에 없는 값을 그리지 않는다). 그래서 굽는 자리와 그리는 자리를
갈라 두고, 그리는 쪽(gen_anim_page.py)은 이 JSON 말고 아무 데서도 숫자를 못 가져온다.
"""
import io, os, sys, json, math

OUT = os.path.join('data', 'attn_trace.json')
MODEL_ID = 'Qwen/Qwen2.5-0.5B-Instruct'
SENTENCE = '어제 산 고양이 사료를 오늘 열었는데, 그것이 벌써 눅눅했다'
MAX_TOKENS = 12
PREVIEW = 8


def validate(tr):
    """구운 값이 그림이 기대하는 꼴인가. 어기면 굽기가 실패한다."""
    n = len(tr['tokens'])
    assert 2 <= n <= MAX_TOKENS, '토큰이 %d개다 — %d개를 넘으면 막대가 화면에 안 들어간다' % (n, MAX_TOKENS)
    assert 0 <= tr['noun_pos'] < tr['query_pos'] < n, '보는 자리가 앞 명사보다 뒤에 있어야 한다'
    m = tr['query_pos'] + 1
    assert len(tr['scores_raw']) == m, '점수 길이가 보는 자리까지여야 한다'
    assert len(tr['scores_softmax']) == m, '소프트맥스 길이가 점수와 같아야 한다'
    assert abs(sum(tr['scores_softmax']) - 1.0) < 1e-4, '소프트맥스 합이 1이 아니다'
    assert len(tr['q_preview']) == PREVIEW, 'Q 미리보기가 %d개여야 한다' % PREVIEW
    for row in tr['k_preview'] + tr['v_preview']:
        assert len(row) == PREVIEW, 'K·V 미리보기가 %d개여야 한다' % PREVIEW
    assert len(tr['k_preview']) == m and len(tr['v_preview']) == m, 'K·V 줄 수가 점수와 같아야 한다'
    assert len(tr['out_preview']) == PREVIEW, '출력 미리보기가 %d개여야 한다' % PREVIEW
    assert tr['picked_weight'] == max(tr['scores_softmax']), \
        '고른 헤드의 근거 값이 소프트맥스 최댓값과 달라선 안 된다'
    # 화면은 「점수를 소프트맥스하면 이 비율이 된다」고 말한다. 두 값이 안 이어지면
    # 그 말이 거짓이 된다 — RoPE 나 스케일링이 빠지면 여기서 걸린다.
    ex = [math.exp(v - max(tr['scores_raw'])) for v in tr['scores_raw']]
    recomputed = [e / sum(ex) for e in ex]
    gap = max(abs(a - b) for a, b in zip(recomputed, tr['scores_softmax']))
    assert gap < 1e-4, \
        'scores_raw 를 소프트맥스해도 scores_softmax 가 안 나온다 (최대 %.2e)' % gap
    return True


if __name__ == '__main__':
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
    print('아직 굽는 코드가 없다')
```

- [ ] **Step 2: 돌려서 실패를 본다**

Run: `PYTHONIOENCODING=utf-8 python scratchpad/attn_trace.py`
Expected: `아직 굽는 코드가 없다` 만 찍히고 `data/attn_trace.json` 이 안 생긴다.

- [ ] **Step 3: 굽는 코드를 쓴다**

`validate` 아래에 더한다.

```python
def pick_head(attns, query_pos, noun_pos):
    """보는 자리가 앞 명사를 가장 세게 되짚는 (층, 헤드)를 고른다.

    층·헤드를 사람이 고르면 「그렇게 보이는 것만 골랐다」는 말을 듣는다. 기준을 코드에
    박아 두고 그 기준과 값을 JSON 에 같이 남긴다.
    """
    best = (-1, -1, -1.0)
    for li, a in enumerate(attns):                 # a: (1, heads, seq, seq)
        w = a[0, :, query_pos, noun_pos]           # 헤드마다 그 자리 → 명사 가중치
        for hi in range(w.shape[0]):
            v = float(w[hi])
            if v > best[2]:
                best = (li, hi, v)
    return best


def build_trace():
    import torch
    from transformers import AutoTokenizer, AutoModelForCausalLM
    tok = AutoTokenizer.from_pretrained(MODEL_ID)
    model = AutoModelForCausalLM.from_pretrained(
        MODEL_ID, torch_dtype=torch.float32, attn_implementation='eager')
    model.eval()

    enc = tok(SENTENCE, return_tensors='pt')
    ids = enc['input_ids'][0]
    tokens = [tok.decode([int(i)]) for i in ids]
    assert len(tokens) <= MAX_TOKENS, \
        '토큰이 %d개다 — SENTENCE 를 줄여 다시 뽑는다: %s' % (len(tokens), tokens)

    # 보는 자리와 되짚을 앞 명사. 문장을 바꾸면 이 두 줄을 같이 고친다.
    query_pos = max(i for i, t in enumerate(tokens) if '그것' in t)
    noun_pos = max(i for i, t in enumerate(tokens[:query_pos]) if '사료' in t or '고양이' in t)

    with torch.no_grad():
        out = model(**enc, output_attentions=True, output_hidden_states=True)
    layer, head, picked = pick_head(out.attentions, query_pos, noun_pos)

    # 그 층·헤드의 Q·K·V 를 직접 다시 만든다. 어텐션 가중치만으로는 Q·K 를 못 보인다.
    hs = out.hidden_states[layer][0]               # (seq, hidden)
    attn = model.model.layers[layer].self_attn
    ln = model.model.layers[layer].input_layernorm
    x = ln(hs)
    n_heads = model.config.num_attention_heads
    n_kv = getattr(model.config, 'num_key_value_heads', n_heads)
    dim = model.config.hidden_size // n_heads
    kv_head = head // (n_heads // n_kv)            # GQA — 여러 Q 헤드가 K·V 하나를 나눠 쓴다

    # Qwen2 는 RoPE 모델이다. 투영한 Q·K 를 그대로 내적하면 실제 모델이 낸 점수와
    # 다른 값이 나온다 — 회전을 먹인 뒤에 내적해야 scores_raw 를 소프트맥스했을 때
    # scores_softmax 가 나온다. 위 validate() 의 정합성 단언이 그것을 강제한다.
    from transformers.models.qwen2.modeling_qwen2 import apply_rotary_pos_emb
    qs = attn.q_proj(x).view(1, -1, n_heads, dim).transpose(1, 2)
    ks = attn.k_proj(x).view(1, -1, n_kv, dim).transpose(1, 2)
    pos = torch.arange(x.shape[0]).unsqueeze(0)
    cos, sin = model.model.rotary_emb(x.unsqueeze(0), pos)
    qs, ks = apply_rotary_pos_emb(qs, ks, cos, sin)
    q = qs[0, head]                                        # (seq, dim)
    k = ks[0, kv_head]
    v = attn.v_proj(x).view(-1, n_kv, dim)[:, kv_head, :]  # V 는 회전하지 않는다

    m = query_pos + 1
    raw = [float(torch.dot(q[query_pos], k[j]) / math.sqrt(dim)) for j in range(m)]
    soft = [float(w) for w in out.attentions[layer][0, head, query_pos, :m]]
    soft = [w / sum(soft) for w in soft]           # 부동소수점 오차만 턴다(causal 이라 버리는 질량은 없다)
    outv = [sum(soft[j] * float(v[j][d]) for j in range(m)) for d in range(PREVIEW)]

    return {
        'model': MODEL_ID,
        'sentence': SENTENCE,
        'tokens': tokens,
        'query_pos': query_pos,
        'noun_pos': noun_pos,
        'layer': layer,
        'head': head,
        'picked_weight': max(soft),
        'picked_by': '보는 자리가 앞 명사 토큰에 준 가중치가 가장 큰 층·헤드',
        'dim': dim,
        'scores_raw': raw,
        'scores_softmax': soft,
        'q_preview': [float(x) for x in q[query_pos][:PREVIEW]],
        'k_preview': [[float(x) for x in k[j][:PREVIEW]] for j in range(m)],
        'v_preview': [[float(x) for x in v[j][:PREVIEW]] for j in range(m)],
        'out_preview': outv,
    }
```

`__main__` 을 갈아 끼운다.

```python
if __name__ == '__main__':
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
    tr = build_trace()
    validate(tr)
    os.makedirs('data', exist_ok=True)
    io.open(OUT, 'w', encoding='utf-8').write(json.dumps(tr, ensure_ascii=False, indent=1))
    print('토큰 %d개 · %d층 %d번 헤드 · 그 자리가 「%s」에 준 가중치 %.3f'
          % (len(tr['tokens']), tr['layer'], tr['head'],
             tr['tokens'][tr['noun_pos']], tr['picked_weight']))
```

- [ ] **Step 4: 돌려서 통과를 본다**

Run: `PYTHONIOENCODING=utf-8 python scratchpad/attn_trace.py`
Expected: 토큰 수·층·헤드·가중치가 한 줄로 찍히고 `data/attn_trace.json` 이 생긴다.

걸릴 만한 자리 셋. ① `validate` 의 토큰 수에서 걸리면 `SENTENCE` 를 줄여 다시 돌린다 — 열둘이 상한이다. ② `query_pos`/`noun_pos` 를 찾는 줄이 `ValueError: max() arg is an empty sequence` 로 죽으면 토크나이저가 「그것」·「사료」를 다른 조각으로 쪼갠 것이다. `print(tokens)` 로 실제 조각을 보고 그 두 줄의 검색어를 조각에 맞춘다. ③ 모델을 처음 받을 때 네트워크가 필요하다. 받아 둔 뒤로는 캐시에서 뜬다.

- [ ] **Step 5: 커밋**

```bash
git add scratchpad/attn_trace.py data/attn_trace.json
git commit -m "feat(애니메이션): 어텐션 실제 값 굽기 — 헤드는 기준을 코드에 박아 고른다

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013ZeLyU1QpQdxypbbREemyJ"
```

---

### Task 2: 페이지 뼈대와 규약 검사

**Files:**
- Create: `scratchpad/gen_anim_page.py`
- Create: `대시보드/애니메이션 — 어텐션.html` (생성물)

**Interfaces:**
- Consumes: `data/attn_trace.json` (Task 1 의 키 전부)
- Produces:
  `load_trace(path='data/attn_trace.json') -> dict`
  `STEPS -> list[tuple[str, str]]` — (키, 설명 한 줄) 일곱
  `svg_board(tr) -> str` — 판 SVG 한 덩이
  `render(tr) -> str` — 페이지 전체 HTML
  `check_ui(html, tr) -> None` — 어기면 AssertionError
  `selftest() -> None` — 규약마다 결함을 넣어 무는지 센다

- [ ] **Step 1: 규약 검사와 자가 시험을 먼저 쓴다**

```python
# -*- coding: utf-8 -*-
"""어텐션 애니메이션 장 생성기. 아카이브 부품(dash_common)을 안 쓴다.

왜 따로 짰나. 아카이브는 카드가 쌓이는 장의 부품이고 이 장은 카드가 없다 — 한 계산을
일곱 단계로 늦춰 보이는 것이 전부다. 접힘·타일·목록이 그 사이를 막는다.

규약은 check_ui() 가 생성 때 검사한다. 규약을 우회하려고 나온 장이 규약이 없는 장이
되면 다음 사람이 같은 자리를 다시 판다(워치 장에서 배운 것).
"""
import io, os, re, sys, json

TRACE = os.path.join('data', 'attn_trace.json')
OUT = os.path.join('대시보드', '애니메이션 — 어텐션.html')

STEPS = [
    ('sentence', '이 자리에서 다음 토큰을 만든다 — 그러려면 앞선 토큰들을 얼마나 볼지 정해야 한다'),
    ('query',    '지금 토큰이 무엇을 찾는지가 Q다 — 이 자리 것 하나뿐이다'),
    ('keys',     '앞선 토큰마다 K가 이미 캐시에 있다 — 새로 만들지 않는다'),
    ('score',    'Q와 K를 하나씩 곱해 점수를 낸다 — 막대가 그 점수다'),
    ('softmax',  '점수를 합 1의 비율로 바꾼다 — 이제 막대는 어느 토큰을 얼마나 볼지의 몫이다'),
    ('mix',      '그 비율대로 V를 섞는다 — 많이 보기로 한 토큰의 V가 많이 들어간다'),
    ('out',      '섞인 결과 하나가 이 자리의 어텐션 출력이다'),
]


def load_trace(path=TRACE):
    return json.load(io.open(path, encoding='utf-8'))


def _screen_numbers(html):
    """화면에 뜬 숫자를 전부 긁는다. 태그 속성이 아니라 사람이 읽는 글자만 본다."""
    text = re.sub(r'<[^>]+>', ' ', html)
    return set(re.findall(r'-?\d+\.\d+', text))


def check_ui(html, tr):
    """이 장의 규약. 설계 §5 와 한 줄씩 맞물린다."""
    # 1. 스텝 수와 설명 줄 수가 같다
    n_desc = html.count('class="stepdesc"')
    assert n_desc == len(STEPS), \
        '규약 위반: 설명 줄이 %d개, 스텝은 %d개다' % (n_desc, len(STEPS))
    # 2. 모든 스텝이 정지 상태를 갖는다
    for i, (key, _d) in enumerate(STEPS):
        assert '[data-step="%d"]' % i in html, \
            '규약 위반: 스텝 %d(%s)에 대응하는 CSS 상태가 없다' % (i, key)
    # 3. 화면에 뜬 숫자가 전부 구운 값에 있다
    known = set()
    for v in tr['scores_raw'] + tr['scores_softmax'] + tr['q_preview'] + tr['out_preview']:
        known.add('%.2f' % v)
        known.add('%.3f' % v)
    for row in tr['k_preview'] + tr['v_preview']:
        for v in row:
            known.add('%.2f' % v)
            known.add('%.3f' % v)
    stray = _screen_numbers(html) - known
    assert not stray, '규약 위반: 구운 값에 없는 숫자가 화면에 있다 — %s' % sorted(stray)[:5]
    # 4. 자동재생을 멈출 수단이 있다
    assert 'id="btn-pause"' in html, '규약 위반: 자동재생을 멈출 버튼이 없다'
    # 5. 가로 스크롤이 안 생긴다
    flat = html.replace(' ', '')
    assert 'overflow-x:hidden' in flat, '규약 위반: 가로 스크롤을 막는 선언이 없다'
    assert 'width:100%' in flat and 'viewBox' in html, \
        '규약 위반: 판이 폭에 맞춰 줄어들지 않는다'


def selftest():
    """규칙을 세울 때는 결함을 일부러 넣어 무는지 먼저 본다(CLAUDE.md)."""
    tr = load_trace()
    good = render(tr)
    check_ui(good, tr)                      # 멀쩡한 것은 통과해야 한다
    cases = [
        ('설명 줄 하나 빼기', good.replace('class="stepdesc"', 'class="x"', 1)),
        # 이 줄만 개수를 안 준다 — Task 3 이 [data-step="3"] 을 여러 줄로 늘리므로
        # 하나만 바꾸면 나머지가 남아 규약 2번이 안 문다
        ('스텝 상태 하나 빼기', good.replace('[data-step="3"]', '[data-step="99"]')),
        ('없는 숫자 넣기', good.replace('</body>', '<p>9.87</p></body>', 1)),
        ('멈춤 버튼 빼기', good.replace('id="btn-pause"', 'id="x"', 1)),
        ('가로 스크롤 허용', good.replace('overflow-x:hidden', 'overflow-x:auto', 1)),
    ]
    bites = 0
    for name, broken in cases:
        try:
            check_ui(broken, tr)
        except AssertionError:
            bites += 1
        else:
            print('FAIL 결함을 안 물었다 —', name)
    print('selftest: 규약 %d개 중 %d개가 결함을 물었다' % (len(cases), bites))
    assert bites == len(cases)


if __name__ == '__main__':
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
    if '--selftest' in sys.argv:
        selftest()
    else:
        tr = load_trace()
        html = render(tr)
        check_ui(html, tr)
        io.open(OUT, 'w', encoding='utf-8').write(html)
        print('%s — 스텝 %d개 · 토큰 %d개' % (OUT, len(STEPS), len(tr['tokens'])))
```

- [ ] **Step 2: 돌려서 실패를 본다**

Run: `PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py`
Expected: `NameError: name 'render' is not defined`

- [ ] **Step 3: 스텝 ①만 그리는 최소 `render` 를 쓴다**

`check_ui` 위에 더한다. 문장 줄과 껍데기, 그리고 스텝 일곱 전부의 CSS 상태를 미리 깐다 — 판의 칸은 다음 과제에서 채운다.

```python
CSS = '''
:root{--ink-1:#222;--ink-3:#8a8a8a;--bg-1:#fafafa;--bg-2:#f4f4f4;--bg-3:#e6e6e6;--hot:#2f8f6b}
@media (prefers-color-scheme:dark){:root{--ink-1:#e8e8e8;--ink-3:#9a9a9a;
  --bg-1:#181818;--bg-2:#202020;--bg-3:#2a2a2a}}
*{box-sizing:border-box}
html,body{margin:0;overflow-x:hidden;background:var(--bg-1);color:var(--ink-1);
  font-family:system-ui,-apple-system,"Segoe UI",sans-serif}
.wrap{max-width:900px;margin:0 auto;padding:24px 16px 56px}
.sent{display:flex;flex-wrap:wrap;gap:6px;margin:18px 0}
.tk{padding:6px 10px;border:1px solid var(--ink-3);border-radius:6px;font-size:13px;
  background:var(--bg-2);transition:border-color .25s ease,background .25s ease}
.tk.q{border-color:var(--hot);border-width:2px;background:var(--bg-3)}
svg{width:100%;height:auto;display:block}
.stepdesc{display:none;font-size:14px;line-height:1.7;margin:0}
.bar{transition:height .35s ease,y .35s ease,fill .25s ease}
.fade{opacity:0;transition:opacity .3s ease}
button{font:inherit;font-size:13px;padding:7px 12px;border:1px solid var(--ink-3);
  border-radius:6px;background:var(--bg-2);color:var(--ink-1);cursor:pointer}
'''

# 스텝마다 무엇이 보이나. 여기 한 줄이 곧 check_ui 2번이 찾는 상태다.
STEP_CSS = '\n'.join(
    '[data-step="%d"] .stepdesc[data-i="%d"]{display:block}' % (i, i)
    for i in range(len(STEPS)))


def svg_board(tr):
    """판. Task 3~5 에서 칸이 들어찬다."""
    return '<svg viewBox="0 0 900 320" role="img" aria-label="어텐션 계산 한 번"></svg>'


def render(tr):
    tks = ''.join(
        '<span class="tk%s">%s</span>' % (' q' if i == tr['query_pos'] else '', t.strip() or '_')
        for i, t in enumerate(tr['tokens']))
    descs = ''.join('<p class="stepdesc" data-i="%d">%s</p>' % (i, d)
                    for i, (_k, d) in enumerate(STEPS))
    return '''<!doctype html><html lang="ko"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>애니메이션 — 어텐션</title><style>%s
%s</style></head><body><div class="wrap" id="stage" data-step="0">
<h1 style="font-size:20px;margin:0 0 6px">어텐션 계산 한 번을 일곱 걸음으로</h1>
<p style="font-size:13px;color:var(--ink-3);margin:0">%s · %d층 %d번 헤드 · 값은 그 모델을 실제로 돌려 뽑은 것이다</p>
<div class="sent">%s</div>
%s
<div style="min-height:68px;margin-top:14px">%s</div>
<div style="display:flex;gap:8px;margin-top:10px;flex-wrap:wrap">
<button type="button" id="btn-prev">이전</button>
<button type="button" id="btn-next">다음</button>
<button type="button" id="btn-pause">자동재생 멈춤</button>
<button type="button" id="btn-reset">처음부터</button></div>
</div><script>
(function(){var st=document.getElementById('stage'),n=%d,i=0,t=null;
function go(k){i=Math.max(0,Math.min(n-1,k));st.setAttribute('data-step',i);}
function stop(){if(t){clearInterval(t);t=null;}}
document.getElementById('btn-next').onclick=function(){stop();go(i+1)};
document.getElementById('btn-prev').onclick=function(){stop();go(i-1)};
document.getElementById('btn-reset').onclick=function(){stop();go(0)};
document.getElementById('btn-pause').onclick=stop;
t=setInterval(function(){if(i>=n-1){stop();}else{go(i+1);}},1800);})();
</script></body></html>''' % (CSS, STEP_CSS, tr['model'], tr['layer'], tr['head'],
                              tks, svg_board(tr), descs, len(STEPS))
```

- [ ] **Step 4: 생성과 자가 시험을 돌린다**

Run: `PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py`
Expected: `대시보드/애니메이션 — 어텐션.html — 스텝 7개 · 토큰 N개`

Run: `PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py --selftest`
Expected: `selftest: 규약 5개 중 5개가 결함을 물었다`

하나라도 안 물면 그 규약이 헐거운 것이다. 검사를 고친다 — 결함 쪽을 고치지 않는다.

- [ ] **Step 5: 커밋**

```bash
git add scratchpad/gen_anim_page.py "대시보드/애니메이션 — 어텐션.html"
git commit -m "feat(애니메이션): 어텐션 장 뼈대 — 규약 다섯과 결함 시험

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013ZeLyU1QpQdxypbbREemyJ"
```

---

### Task 3: 스텝 ②③ — Q 상자와 K 칸

**Files:**
- Modify: `scratchpad/gen_anim_page.py` (`svg_board`, `STEP_CSS`, `check_ui`, `selftest`)

**Interfaces:**
- Consumes: `load_trace()` 의 `q_preview` `k_preview` `tokens` `query_pos`
- Produces: `svg_board(tr)` 가 `id="q-box"` 하나와 `class="kcell"` 칸 `query_pos+1` 개를 낸다

- [ ] **Step 1: 규약을 먼저 더한다 (실패하는 시험)**

`check_ui` 의 5번 뒤에 붙인다.

```python
    # 6. 판에 Q 상자 하나와 K 칸이 보는 자리 수만큼 있다
    m = tr['query_pos'] + 1
    assert 'id="q-box"' in html, '규약 위반: Q 상자가 없다'
    assert html.count('class="kcell') == m, \
        '규약 위반: K 칸이 %d개다 (기대 %d개)' % (html.count('class="kcell'), m)
```

`selftest` 의 `cases` 에 한 줄 더한다. 기대치는 `len(cases)` 로 세니 따로 안 고친다.

```python
        ('K 칸 하나 빼기', good.replace('class="kcell', 'class="x', 1)),
```

- [ ] **Step 2: 돌려서 실패를 본다**

Run: `PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py`
Expected: `AssertionError: 규약 위반: Q 상자가 없다`

- [ ] **Step 3: 판에 Q 상자와 K 칸을 그린다**

`svg_board` 를 갈아 끼운다.

```python
BW, BG, BX = 62, 8, 60       # K 칸 폭 · 사이 · 왼쪽 시작
QY, KY = 60, 150             # Q 상자 윗변 · K 칸 윗변


def svg_board(tr):
    m = tr['query_pos'] + 1
    w = max(900, BX + m * (BW + BG) + 130)
    soft = tr['scores_softmax']
    p = ['<svg viewBox="0 0 %d 400" role="img" aria-label="어텐션 계산 한 번">' % w]
    # Q — 지금 토큰 것 하나
    p.append('<g id="q-box" class="fade" data-show="1">'
             '<rect x="%d" y="%d" width="%d" height="40" rx="6" fill="var(--bg-3)" '
             'stroke="var(--hot)" stroke-width="2"/>'
             '<text x="%d" y="%d" font-size="13" text-anchor="middle" fill="var(--ink-1)">Q</text>'
             '<text x="%d" y="%d" font-size="11" text-anchor="middle" fill="var(--ink-3)">%.2f</text>'
             '</g>' % (BX, QY, BW, BX + BW // 2, QY + 18, BX + BW // 2, QY + 33,
                       tr['q_preview'][0]))
    # K — 앞선 토큰마다 하나
    for j in range(m):
        x = BX + j * (BW + BG)
        p.append('<g class="kcell fade" data-show="2">'
                 '<rect x="%d" y="%d" width="%d" height="38" rx="5" fill="var(--bg-2)" '
                 'stroke="var(--ink-3)"/>'
                 '<text x="%d" y="%d" font-size="12" text-anchor="middle" fill="var(--ink-1)">K</text>'
                 '<text x="%d" y="%d" font-size="10" text-anchor="middle" fill="var(--ink-3)">%.2f</text>'
                 '</g>' % (x, KY, BW, x + BW // 2, KY + 16, x + BW // 2, KY + 31,
                           tr['k_preview'][j][0]))
        p.append('<text x="%d" y="%d" font-size="11" text-anchor="middle" fill="var(--ink-3)">%s</text>'
                 % (x + BW // 2, KY + 54, (tr['tokens'][j].strip() or '_')[:4]))
    p.append('</svg>')
    return '\n'.join(p)
```

`data-show="i"` 는 「스텝 i 부터 보인다」는 뜻이다. `STEP_CSS` 에 그 규칙을 더한다.

```python
STEP_CSS = '\n'.join(
    '[data-step="%d"] .stepdesc[data-i="%d"]{display:block}' % (i, i)
    for i in range(len(STEPS))) + '\n' + '\n'.join(
    '[data-step="%d"] [data-show="%d"]{opacity:1}' % (i, s)
    for i in range(len(STEPS)) for s in range(0, i + 1))
```

- [ ] **Step 4: 돌려서 통과를 본다**

```bash
PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py
PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py --selftest
```
Expected: 페이지가 생기고 `selftest: 규약 6개 중 6개가 결함을 물었다`

- [ ] **Step 5: 커밋**

```bash
git add scratchpad/gen_anim_page.py "대시보드/애니메이션 — 어텐션.html"
git commit -m "feat(애니메이션): 스텝 2·3 — Q 상자와 K 칸

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013ZeLyU1QpQdxypbbREemyJ"
```

---

### Task 4: 스텝 ④⑤ — 점수 막대와 소프트맥스

**Files:**
- Modify: `scratchpad/gen_anim_page.py` (`svg_board`, `render` 의 `go()`, `check_ui`, `selftest`)
- Create: `scratchpad/anim_sweep_probe.py` (스텝 3 이 차례로 서는지 한 번 본다)

**Interfaces:**
- Consumes: `scores_raw` `scores_softmax`
- Produces: `class="bar"` 막대 `query_pos+1` 개. 각 막대가 `data-raw`·`data-soft` 두 높이를 들고 있고, 스텝 4 이상에서 `data-soft` 로 바뀐다

- [ ] **Step 1: 규약을 먼저 더한다**

`check_ui` 의 6번 뒤에 붙인다.

```python
    # 7. 점수 막대가 K 칸 수만큼 있고, 가장 센 비율이 화면에 적혀 있다
    assert html.count('class="bar') == m, \
        '규약 위반: 점수 막대가 %d개다 (기대 %d개)' % (html.count('class="bar'), m)
    top = '%.2f' % max(tr['scores_softmax'])
    assert top in html, '규약 위반: 가장 센 비율 %s 가 화면에 없다' % top
```

`selftest` 의 `cases` 에 한 줄 더한다.

```python
        ('막대 하나 빼기', good.replace('class="bar', 'class="x', 1)),
```

- [ ] **Step 2: 돌려서 실패를 본다**

Run: `PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py`
Expected: `AssertionError: 규약 위반: 점수 막대가 0개다`

- [ ] **Step 3: 막대를 그린다**

`svg_board` 의 `p.append('</svg>')` 앞에 넣는다.

```python
    # 점수 막대 — 스텝 3 은 원점수, 스텝 4 부터는 합 1 비율
    raw = tr['scores_raw']
    lo, hi = min(raw), max(raw)
    span = (hi - lo) or 1.0
    BASE, MAXH = 290, 80
    for j in range(m):
        x = BX + j * (BW + BG)
        h_raw = int(6 + MAXH * (raw[j] - lo) / span)
        h_soft = int(6 + MAXH * soft[j] / (max(soft) or 1.0))
        hot = ' data-top="1"' if soft[j] == max(soft) else ''
        p.append('<rect class="bar fade" data-show="3"%s x="%d" y="%d" width="%d" height="%d" '
                 'rx="3" fill="var(--bg-3)" stroke="var(--ink-3)" data-raw="%d" data-soft="%d"/>'
                 % (hot, x + 8, BASE - h_raw, BW - 16, h_raw, h_raw, h_soft))
        p.append('<text class="softval fade" data-show="4" x="%d" y="%d" font-size="10" '
                 'text-anchor="middle" fill="var(--ink-3)">%.2f</text>'
                 % (x + BW // 2, BASE + 14, soft[j]))
```

막대 높이를 스텝에 따라 바꾸는 일은 CSS 가 못 한다 — `render` 의 `go()` 를 이렇게 갈아 끼운다.

스텝 3(점수 매기기)은 이 장에서 유일하게 연속이다. 한꺼번에 세우지 않고 **왼쪽부터 토큰 하나당
0.35초로 훑는다**(설계 §3). 되감아 들어올 수도 있으니 훑는 중이던 타이머는 스텝이 바뀔 때 끊는다.

```javascript
var sweep=null;
function setBar(r,v,hot){r.setAttribute('height',v);r.setAttribute('y',290-v);
  r.setAttribute('fill',hot?'var(--hot)':'var(--bg-3)');}
function go(k){i=Math.max(0,Math.min(n-1,k));st.setAttribute('data-step',i);
  if(sweep){clearTimeout(sweep);sweep=null;}
  var bars=st.querySelectorAll('.bar');
  if(i<3){for(var a=0;a<bars.length;a++){setBar(bars[a],0,false);}return;}
  if(i===3){                      // 왼쪽부터 하나씩 — 이 장에서 유일한 연속 구간
    var j=0;(function next(){if(j>=bars.length){sweep=null;return;}
      setBar(bars[j],+bars[j].getAttribute('data-raw'),false);j++;
      sweep=setTimeout(next,350);})();return;}
  for(var b=0;b<bars.length;b++){var r=bars[b];   // 스텝 4 부터는 비율로 한꺼번에
    setBar(r,+r.getAttribute('data-soft'),!!r.getAttribute('data-top'));}}
```

`i<3` 에서 막대를 0 으로 눕히는 줄이 있어야 되감기가 성립한다 — 스텝 5 에서 2 로 돌아왔을 때
막대가 남아 있으면 「아직 점수를 안 냈다」는 그 스텝의 말과 화면이 어긋난다.

- [ ] **Step 4: 돌려서 통과를 본다**

```bash
PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py
PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py --selftest
```
Expected: `selftest: 규약 7개 중 7개가 결함을 물었다`

그리고 연속 구간이 실제로 도는지 브라우저에서 한 번 본다. 막대 수만큼 0.35초가 걸리니
토큰이 아홉이면 3초 안팎이다. `scratchpad/anim_sweep_probe.py` 를 만들어 돌린다.

```python
# -*- coding: utf-8 -*-
"""스텝 3 이 한꺼번에 서지 않고 왼쪽부터 차례로 서는지만 본다. 한 번 쓰고 남겨 둔다."""
import io, os, sys
from playwright.sync_api import sync_playwright
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
PAGE = os.path.abspath(os.path.join('대시보드', '애니메이션 — 어텐션.html'))
COUNT = 'els => els.filter(x => +x.getAttribute("height") > 0).length'
with sync_playwright() as pw:
    b = pw.chromium.launch()
    pg = b.new_page(viewport={'width': 1200, 'height': 900})
    pg.goto('file:///' + PAGE.replace('\', '/'))
    pg.evaluate("document.getElementById('btn-pause').click()")
    pg.evaluate("document.getElementById('btn-reset').click()")
    for _ in range(3):
        pg.evaluate("document.getElementById('btn-next').click()")
    pg.wait_for_timeout(400)
    early = pg.eval_on_selector_all('.bar', COUNT)
    pg.wait_for_timeout(4000)
    late = pg.eval_on_selector_all('.bar', COUNT)
    total = pg.eval_on_selector_all('.bar', 'els => els.length')
    print('0.4초 뒤 선 막대 %d · 4.4초 뒤 %d · 전체 %d' % (early, late, total))
    assert late == total, '끝까지 안 섰다 — 훑기가 중간에 끊겼다'
    assert early < total, '한꺼번에 섰다 — go() 의 i===3 갈래가 안 돈다'
    print('연속 구간 OK')
    b.close()
```

Run: `PYTHONIOENCODING=utf-8 python scratchpad/anim_sweep_probe.py`
Expected: `연속 구간 OK`


- [ ] **Step 5: 커밋**

```bash
git add scratchpad/gen_anim_page.py scratchpad/anim_sweep_probe.py "대시보드/애니메이션 — 어텐션.html"
git commit -m "feat(애니메이션): 스텝 4·5 — 막대가 왼쪽부터 서고 소프트맥스에서 비율로 다시 선다

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013ZeLyU1QpQdxypbbREemyJ"
```

---

### Task 5: 스텝 ⑥⑦ — V 가중합과 출력

**Files:**
- Modify: `scratchpad/gen_anim_page.py`

**Interfaces:**
- Consumes: `v_preview` `out_preview` `scores_softmax`
- Produces: `class="vcell"` 칸 `query_pos+1` 개와 `id="out-box"` 하나

- [ ] **Step 1: 규약을 먼저 더한다**

`check_ui` 의 7번 뒤에 붙인다.

```python
    # 8. V 칸이 K 칸 수만큼, 출력 상자가 하나 있다
    assert html.count('class="vcell') == m, \
        '규약 위반: V 칸이 %d개다 (기대 %d개)' % (html.count('class="vcell'), m)
    assert 'id="out-box"' in html, '규약 위반: 어텐션 출력 상자가 없다'
```

`selftest` 의 `cases` 에 한 줄 더한다.

```python
        ('출력 상자 빼기', good.replace('id="out-box"', 'id="x"', 1)),
```

- [ ] **Step 2: 돌려서 실패를 본다**

Run: `PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py`
Expected: `AssertionError: 규약 위반: V 칸이 0개다`

- [ ] **Step 3: V 칸과 출력 상자를 그린다**

`svg_board` 의 `</svg>` 앞에 넣는다. viewBox 높이는 Task 3 에서 이미 400 으로 잡아 뒀다.

```python
    # V — 비율만큼 진하게. 진하기가 곧 그 토큰이 결과에 들어간 몫이다
    VY = 320
    for j in range(m):
        x = BX + j * (BW + BG)
        op = 0.15 + 0.85 * soft[j] / (max(soft) or 1.0)
        p.append('<g class="vcell fade" data-show="5">'
                 '<rect x="%d" y="%d" width="%d" height="34" rx="5" fill="var(--hot)" '
                 'fill-opacity="%.3f" stroke="var(--ink-3)"/>'
                 '<text x="%d" y="%d" font-size="11" text-anchor="middle" fill="var(--ink-1)">V</text>'
                 '</g>' % (x, VY, BW, op, x + BW // 2, VY + 21))
    # 출력 — 섞인 결과 하나
    ox = BX + m * (BW + BG) + 20
    p.append('<g id="out-box" class="fade" data-show="6">'
             '<rect x="%d" y="%d" width="%d" height="34" rx="6" fill="var(--bg-3)" '
             'stroke="var(--hot)" stroke-width="2"/>'
             '<text x="%d" y="%d" font-size="11" text-anchor="middle" fill="var(--ink-1)">출력 %.2f</text>'
             '</g>' % (ox, VY, BW + 30, ox + (BW + 30) // 2, VY + 21, tr['out_preview'][0]))
```

`fill-opacity` 의 `%.3f` 가 화면 글자가 아니라 속성이라 규약 3번(구운 값에 없는 숫자)에 안 걸린다 — `_screen_numbers` 가 태그를 걷어 내고 보기 때문이다.

- [ ] **Step 4: 돌려서 통과를 본다**

```bash
PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py
PYTHONIOENCODING=utf-8 python scratchpad/gen_anim_page.py --selftest
```
Expected: `selftest: 규약 8개 중 8개가 결함을 물었다`

- [ ] **Step 5: 커밋**

```bash
git add scratchpad/gen_anim_page.py "대시보드/애니메이션 — 어텐션.html"
git commit -m "feat(애니메이션): 스텝 6·7 — V 가중합과 어텐션 출력

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013ZeLyU1QpQdxypbbREemyJ"
```

---

### Task 6: 스텝 일곱을 기계가 본다

**Files:**
- Create: `scratchpad/check_anim.py`

**Interfaces:**
- Consumes: `대시보드/애니메이션 — 어텐션.html`
- Produces: `main() -> int` (FAIL 건수). 스텝마다 PNG 를 `<TMP>/anim_shots/<화면>_step<i>.png` 로 남긴다

- [ ] **Step 1: 검사기를 쓴다**

```python
# -*- coding: utf-8 -*-
"""어텐션 애니메이션 — 스텝 일곱을 실제로 눌러 보고 배치를 잰다.

눈으로 보고 「안 겹친다」고 보고하지 않는다(CLAUDE.md). 스텝마다 화면을 찍고, 판 SVG 를
check_fig 에 먹인다. 모바일 폭에서 가로로 넘치는지도 같은 자리에서 본다.
"""
import io, os, sys
sys.path.insert(0, 'scratchpad')
import check_fig
from playwright.sync_api import sync_playwright

PAGE = os.path.abspath(os.path.join('대시보드', '애니메이션 — 어텐션.html'))
OUTDIR = os.path.join(os.environ.get('TMP', '.'), 'anim_shots')
N_STEPS = 7


def main():
    os.makedirs(OUTDIR, exist_ok=True)
    fails = []
    with sync_playwright() as pw:
        b = pw.chromium.launch()
        for width, tag in [(1200, 'pc'), (360, 'mobile')]:
            pg = b.new_page(viewport={'width': width, 'height': 900})
            pg.goto('file:///' + PAGE.replace('\\', '/'))
            pg.evaluate("document.getElementById('btn-pause').click()")
            pg.evaluate("document.getElementById('btn-reset').click()")
            for i in range(N_STEPS):
                if i:
                    pg.evaluate("document.getElementById('btn-next').click()")
                pg.wait_for_timeout(500)
                pg.screenshot(path=os.path.join(OUTDIR, '%s_step%d.png' % (tag, i)))
                step = pg.get_attribute('#stage', 'data-step')
                if step != str(i):
                    fails.append('%s 스텝 %d: data-step 이 %s 다' % (tag, i, step))
                shown = pg.eval_on_selector_all(
                    '.stepdesc',
                    'els=>els.filter(e=>getComputedStyle(e).display!=="none").length')
                if shown != 1:
                    fails.append('%s 스텝 %d: 보이는 설명 줄이 %d개다' % (tag, i, shown))
            over = pg.evaluate('document.documentElement.scrollWidth - '
                               'document.documentElement.clientWidth')
            if over > 0:
                fails.append('%s: 가로로 %dpx 넘친다' % (tag, over))
            pg.close()
        b.close()
    page = io.open(PAGE, encoding='utf-8').read()
    svg = page[page.find('<svg'):page.find('</svg>') + 6]
    for h in (check_fig.hits(svg) or []):
        fails.append('배치: ' + h)
    for f in fails:
        print('FAIL', f)
    print('요약: 스텝 %d개 · 화면 2벌 / FAIL %d' % (N_STEPS, len(fails)))
    return len(fails)


if __name__ == '__main__':
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
    sys.exit(1 if main() else 0)
```

- [ ] **Step 2: 돌린다**

Run: `PYTHONIOENCODING=utf-8 python scratchpad/check_anim.py`
Expected: `요약: 스텝 7개 · 화면 2벌 / FAIL 0`

FAIL 이 나오면 그 스텝 PNG 를 열어 보고 `svg_board` 의 좌표를 고친다. 검사를 느슨하게 고치지 않는다. 자주 걸릴 자리 둘 — ① 토큰 라벨이 K 칸보다 넓어 「칸 밖으로 삐짐」이 뜨면 `[:4]` 를 `[:3]` 으로 줄인다. ② 모바일에서 가로로 넘치면 `BW` 를 줄이는 대신 viewBox 폭을 그대로 두고 `svg{width:100%}` 가 먹는지 본다 — 판은 줄어들고 글자만 작아지는 것이 맞다.

- [ ] **Step 3: 커밋**

```bash
git add scratchpad/check_anim.py
git commit -m "test(애니메이션): 스텝 일곱을 눌러 보고 배치·가로넘침을 잰다

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013ZeLyU1QpQdxypbbREemyJ"
```

---

### Task 7: 규칙 문서와 사이트 등록

**Files:**
- Create: `docs/규칙 — 애니메이션.md`
- Modify: `scripts/gen_site.py` (PAGES 에 한 줄)
- Modify: `대시보드/개념 지도 — LLM 계층·병렬화.html` (어텐션 패널에 링크 한 줄)

**Interfaces:**
- Consumes: Task 2~6 이 세운 규약과 검사기 이름
- Produces: 공개 사이트에 뜨는 장 하나

- [ ] **Step 1: 규칙 문서를 쓴다**

`docs/규칙 — 애니메이션.md` 에 아래를 그대로 쓴다. 한 절에 한 규칙이고, 규칙마다 그것을 보는 검사기 이름을 같이 적는다.

```markdown
# 규칙 — 애니메이션

2026-09-20 신설. 첫 편은 어텐션이다.

## 1. 한 편이 한 계산이다
편 하나가 계산 하나를 늦춰 보인다. 여러 계산을 한 편에 잇지 않는다.

## 2. 스텝마다 정지 상태가 있다
`data-step="i"` 하나로 상태가 정해지고, 어느 스텝에서 멈춰도 화면이 말이 된다.
되돌리기가 공짜여야 한다. — `gen_anim_page.check_ui` 2번

## 3. 설명은 판 밖 한 줄이다
판 위에 남는 글자는 토큰·값 라벨뿐이다. 스텝 수와 설명 줄 수가 같다. — `check_ui` 1번

## 4. 화면에 뜨는 숫자는 구운 값에만 있다
모델을 실제로 돌려 `data/*.json` 으로 굽고, 생성기는 그 JSON 말고 어디서도 숫자를
안 가져온다. 브라우저에서 모델을 돌리지 않는다. — `check_ui` 3번

## 5. 자동재생은 멈출 수 있다
들어오면 한 바퀴 돌고 멈춘다. 멈춤 버튼이 화면에 있다. — `check_ui` 4번

## 6. 모바일에서 가로로 안 넘친다
폭 360 에서 가로 스크롤이 생기면 안 된다. — `check_anim.py`

## 7. 배치는 눈으로 안 본다
스텝마다 찍어 `check_fig` 에 먹인다. — `check_anim.py`

## 8. 규칙을 세울 때는 결함을 넣어 문다
`gen_anim_page.py --selftest` 가 규약마다 결함을 하나씩 넣어 실제로 걸리는지 센다.
```

- [ ] **Step 2: 사이트 PAGES 에 등록한다**

`scripts/gen_site.py` 의 `PAGES` 에서 `'용어사전.html'` 항목 바로 뒤에 넣는다. 용어사전과 같은 자리(읽다 막힌 사람이 짚는 장)라 잠그지 않는다.

```python
    # 뉴스를 따라가다 「어텐션이 뭔데」에서 막힌 사람이 짚는 장. 계산 한 번을 일곱 걸음으로
    # 늦춰 보인다. 값은 모델을 실제로 돌려 구운 것이라 잠글 이유가 없다
    ('애니메이션 — 어텐션.html', 'anim-attention', '어텐션 애니메이션', '🎞️',
     '어텐션 계산 한 번을 일곱 걸음으로 — 점수가 비율이 되고 V가 섞이는 자리를 눌러 가며 본다', False),
```

- [ ] **Step 3: 개념 지도에서 링크를 건다**

어텐션을 다루는 패널을 찾는다.

```bash
grep -n "어텐션" "대시보드/개념 지도 — LLM 계층·병렬화.html" | head
```

그 패널 안에 한 줄을 더한다.

```html
<p style="margin:8px 0 0"><a href="애니메이션 — 어텐션.html">이 계산을 일곱 걸음으로 늦춰 보기 →</a></p>
```

- [ ] **Step 4: 검사기를 전부 돌린다**

`CLAUDE.md` 의 검사기를 **전부** 돌린다. 일부만 돌리고 푸시해 사고가 난 이력이 있다. 이 과제에서 새로 걸릴 자리는 셋이다.

```bash
PYTHONIOENCODING=utf-8 python insights/check_prose.py     # 대시보드 HTML 까지 본다
PYTHONIOENCODING=utf-8 python scratchpad/check_flatlist.py
PYTHONIOENCODING=utf-8 python scratchpad/check_anim.py
```

Expected: 셋 다 FAIL 0. `check_prose` 가 새 장의 산문을 물면 설명 줄을 고친다 — 번역투 낱말 넷·「돈을 댄다」·은유 주어가 그 자리다. `check_flatlist` 는 최신순 목록 장을 훑는 검사기라 이 장을 대상으로 안 잡을 수 있다. 그때는 그대로 두고 통과로 친다.

- [ ] **Step 5: 커밋하고 푸시한다**

```bash
git add "docs/규칙 — 애니메이션.md" scripts/gen_site.py "대시보드/개념 지도 — LLM 계층·병렬화.html"
git commit -m "feat(애니메이션): 규칙 문서와 사이트 등록 — 개념 지도에서 링크를 건다

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013ZeLyU1QpQdxypbbREemyJ"
git push origin main
```

---

## 남은 것 (이 계획 밖)

- 배칭·연속 배칭 편, 엔그램 조회 편 — 같은 장에 편이 쌓인다. 각각 새 계획이다
- 편이 셋을 넘으면 장 첫 화면에 편 고르는 줄이 필요해진다. 그때 규칙 문서에 절을 더한다
