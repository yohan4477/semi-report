# 쟁점 — 토론의 장 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 같은 물음에 화자들이 다르게 답한 것을 쟁점 한 장에 모으고, 화자 말(인용)과 진행자 말(판단)을 기계가 갈라 지키게 한다.

**Architecture:** 쟁점 하나가 마크다운 파일 하나(`insights/debate/`)다. `debate_lib.py` 가 그 파일을 구조로 바꾸고, `check_debate.py` 가 규칙 일곱을 검사하며, `gen_debate.py` 가 `dash_common.render()` 로 대시보드 한 장을 조립한다. 인용 검증은 기존 `check_cite` 가 그대로 맡는다 — 그래서 frontmatter `sources:` 를 교차 인사이트와 같은 모양으로 둔다.

**Tech Stack:** Python 3.13 표준 라이브러리만. 테스트는 pytest 7.4.4. 기존 모듈 재사용 — `insights/notes_lib.py`(frontmatter·인용 해석), `insights/paths.py`(경로 상수), `scripts/card_lib.py`(카드 마크업), `scratchpad/dash_common.py`(대시보드 조립).

**Spec:** `docs/superpowers/specs/2026-08-27-쟁점-토론의-장-design.md`

## Global Constraints

- 콘솔이 cp949 다. 파이썬 실행에 `PYTHONIOENCODING=utf-8` 을 붙인다.
- **테스트는 PowerShell 로, `insights/` 안에서 돈다.** 테스트 파일이 `import notes_lib` 처럼 맨이름으로 불러서다. Bash 도구로 pytest 를 돌리면 출력 없이 exit 1 이 난다 — 쓰지 않는다.
  `$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis\insights; python -m pytest <파일> -q`
- 파일은 전부 UTF-8, 개행 LF 로 쓴다.
- `git add` 에 경로를 찍는다. `git add -A` 는 금지 — 세션이 저장소를 공유한다.
- 의미 단위마다 커밋·푸시한다. 몰아서 하지 않는다.
- 관계 값은 넷뿐이다: `충돌` `동의` `결다름` `단독`.
- 진행자 절은 셋뿐이다: `물음` `갈리는 자리` `무엇을 보면 갈리나`.
- 대시보드 조립은 `dash_common.render()` 만 거친다. 첫 화면은 섹션 타일이고 그 앞에 관문 버튼을 두지 않는다.
- 산문은 기존 규칙을 그대로 받는다 — 용어는 남기고 첫 등장에 괄호로 풀고, 은유를 주어 자리에 두지 않고, 「돈을 댄다」로 뭉개지 않는다.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `insights/paths.py` (수정) | `DEBATE` 상수 한 줄 |
| `insights/debate_lib.py` (신규) | 쟁점 파일 → 구조. 파싱만 한다. 판정하지 않는다 |
| `insights/test_debate_lib.py` (신규) | 파서 테스트 |
| `insights/check_debate.py` (신규) | 규칙 D1–D7 |
| `insights/test_check_debate.py` (신규) | 규칙 테스트 |
| `insights/check_cite.py` (수정) | 글롭에 `paths.DEBATE` |
| `insights/check_prose.py` (수정) | 글롭에 `paths.DEBATE` |
| `insights/debate/*.md` (신규) | 쟁점 본문. 판단이 산출물이라 메인 세션이 쓴다 |
| `scripts/card_lib.py` (수정) | `debate_html()` + `card_html` 분기 |
| `insights/test_cards.py` (수정) | 카드 마크업 테스트 |
| `scratchpad/gen_debate.py` (신규) | 쟁점 → `대시보드/쟁점 대시보드.html` |
| `CLAUDE.md` (수정) | 검사기 목록·스킬 안내 |
| `.claude/skills/insight-debate/SKILL.md` (신규) | 쟁점 세우는 절차 |

`debate_lib` 와 `check_debate` 를 가르는 이유가 있다. 파서는 「무엇이 적혀 있나」만 답하고, 검사기는 「그게 규칙에 맞나」만 답한다. 붙여 두면 생성기가 검사기를 끌고 들어온다.

---

### Task 1: 쟁점 파서

**Files:**
- Modify: `insights/paths.py`
- Create: `insights/debate_lib.py`
- Test: `insights/test_debate_lib.py`

**Interfaces:**
- Consumes: `notes_lib.parse_front(text) -> (meta, body)`, `notes_lib.sources_of(meta) -> [{file, base, note}]`
- Produces:
  - `paths.DEBATE` — `insights/debate` 절대경로
  - `debate_lib.STANCES` — `('충돌', '동의', '결다름', '단독')`
  - `debate_lib.MODERATOR` — `('물음', '갈리는 자리', '무엇을 보면 갈리나')`
  - `debate_lib.parse(text) -> dict` — 키는 `meta`(dict), `sources`(list), `sections`(dict: h2 제목 → 본문 문자열), `voices`(list)
  - `voices` 원소 — `{'actor': str, 'said': str, 'title': str, 'stance': str, 'against': str, 'claim': str, 'body': str}`. `against` 는 `단독` 이면 빈 문자열
  - `debate_lib.voice_key(v) -> str` — `'엘곰 08-20 「바이백」'` 꼴. 관계 대상과 짝을 맞추는 데 쓴다

- [ ] **Step 1: `paths.py` 에 상수 추가**

`insights/paths.py` 의 `LOOP` 줄 아래에 붙인다.

```python
# 쟁점 — 같은 물음에 화자들이 갈린 자리. 화자 말은 인용이고 진행자 말은 판단이라
# 절 구성이 교차 인사이트와 다르다
DEBATE = os.path.join(HERE, 'debate')
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`insights/test_debate_lib.py` 를 만든다.

```python
# -*- coding: utf-8 -*-
import debate_lib as dl

DOC = '''---
view: debate
question: AI 설비투자가 금리를 미나, 금리가 투자를 정하나?
as_of: 2026-08-27
closes_when: {what: "엔비디아 실적 가이던스", by: 2026-08-27}
sources:
  - {file: "content/understanding/회계사/[260820] 바이백을 두 배로 늘렸다 - 엘곰.md", note: "가"}
  - {file: "content/understanding/회계사/[260820] 삼성전자 9% 급등 - 엘곰.md", note: "나"}
---

## 물음

두 글이 인과를 반대로 놓는다.

## 발언

### 엘곰 · 2026-08-20 · 「삼성전자 9% 급등」
- 관계: 충돌 ↔ 엘곰 08-20 「바이백」
- 주장: 금리가 투자를 정한다
금리가 내리면 조달금리가 따라 내린다 ([260820] 삼성전자 9% L23).

### 엘곰 · 2026-08-20 · 「바이백」
- 관계: 단독
- 주장: 투자가 금리를 정한다
회사채 발행이 매수 여력을 두고 경쟁한다 ([260820] 바이백을 두 배로 L17).

## 답하지 않은 화자

- SemiAnalysis — 같은 기간 조달비용을 다룬 글이 없다

## 갈리는 자리

금리 수준이 임계다.

## 무엇을 보면 갈리나

30년물이 5.3%를 넘긴 뒤 청약배수가 1.6배 아래로 가는지.
'''


def test_parse_reads_frontmatter_and_sources():
    d = dl.parse(DOC)
    assert d['meta']['view'] == 'debate'
    assert len(d['sources']) == 2
    assert d['sources'][0]['base'].startswith('[260820] 바이백')


def test_parse_splits_five_sections():
    d = dl.parse(DOC)
    assert set(d['sections']) == {
        '물음', '발언', '답하지 않은 화자', '갈리는 자리', '무엇을 보면 갈리나'}
    assert '두 글이 인과를' in d['sections']['물음']


def test_parse_reads_two_voices():
    d = dl.parse(DOC)
    v = d['voices']
    assert len(v) == 2
    assert v[0]['actor'] == '엘곰'
    assert v[0]['said'] == '2026-08-20'
    assert v[0]['title'] == '삼성전자 9% 급등'
    assert v[0]['stance'] == '충돌'
    assert v[0]['against'] == '엘곰 08-20 「바이백」'
    assert v[0]['claim'] == '금리가 투자를 정한다'
    assert '조달금리가 따라 내린다' in v[0]['body']


def test_solo_voice_has_no_target():
    d = dl.parse(DOC)
    assert d['voices'][1]['stance'] == '단독'
    assert d['voices'][1]['against'] == ''


def test_voice_body_excludes_label_lines():
    d = dl.parse(DOC)
    assert '관계:' not in d['voices'][0]['body']
    assert '주장:' not in d['voices'][0]['body']


def test_voice_key_matches_relation_target():
    d = dl.parse(DOC)
    assert dl.voice_key(d['voices'][1]) == '엘곰 08-20 「바이백」'
    assert d['voices'][0]['against'] == dl.voice_key(d['voices'][1])
```

- [ ] **Step 3: 테스트가 실패하는지 돌려 본다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis\insights; python -m pytest test_debate_lib.py -q
```

기대: `ModuleNotFoundError: No module named 'debate_lib'`

- [ ] **Step 4: 파서를 쓴다**

`insights/debate_lib.py`.

```python
# -*- coding: utf-8 -*-
"""쟁점 파일 한 장을 구조로 바꾼다.

파싱만 한다. 규칙에 맞는지는 check_debate.py 가 본다 — 붙여 두면 생성기가
검사기를 끌고 들어온다.
"""
import os
import re
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import notes_lib as nl

STANCES = ('충돌', '동의', '결다름', '단독')
MODERATOR = ('물음', '갈리는 자리', '무엇을 보면 갈리나')

SEC_RE = re.compile(r'^##[ \t]+(.+?)[ \t]*$', re.M)
# 제목줄 — 화자 · 날짜 · 「글 제목」. 가운뎃점을 구분자로 쓴다
VOICE_RE = re.compile(
    r'^###[ \t]+(.+?)[ \t]*·[ \t]*(\d{4}-\d{2}-\d{2})[ \t]*·[ \t]*「(.+?)」[ \t]*$', re.M)
REL_RE = re.compile(r'^[-*][ \t]*관계:[ \t]*(\S+)(?:[ \t]*↔[ \t]*(.+?))?[ \t]*$')
CLAIM_RE = re.compile(r'^[-*][ \t]*주장:[ \t]*(.+?)[ \t]*$')


def split_sections(body):
    """h2 제목 → 그 절의 본문."""
    out = {}
    hits = list(SEC_RE.finditer(body))
    for i, m in enumerate(hits):
        end = hits[i + 1].start() if i + 1 < len(hits) else len(body)
        out[m.group(1)] = body[m.end():end].strip()
    return out


def parse_voices(sec):
    """「발언」 절을 발언 목록으로."""
    out = []
    hits = list(VOICE_RE.finditer(sec))
    for i, m in enumerate(hits):
        end = hits[i + 1].start() if i + 1 < len(hits) else len(sec)
        stance, against, claim, lines = '', '', '', []
        for line in sec[m.end():end].split('\n'):
            r = REL_RE.match(line)
            c = CLAIM_RE.match(line)
            if r:
                stance = r.group(1)
                against = (r.group(2) or '').strip()
            elif c:
                claim = c.group(1)
            else:
                lines.append(line)
        out.append({'actor': m.group(1).strip(),
                    'said': m.group(2),
                    'title': m.group(3).strip(),
                    'stance': stance,
                    'against': against,
                    'claim': claim,
                    'body': '\n'.join(lines).strip()})
    return out


def voice_key(v):
    """관계 대상과 짝을 맞추는 열쇠 — 화자 MM-DD 「제목」."""
    return '%s %s 「%s」' % (v['actor'], v['said'][5:], v['title'])


def parse(text):
    meta, body = nl.parse_front(text)
    sections = split_sections(body)
    return {'meta': meta,
            'sources': nl.sources_of(meta),
            'sections': sections,
            'voices': parse_voices(sections.get('발언', ''))}
```

- [ ] **Step 5: 테스트가 통과하는지 돌려 본다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis\insights; python -m pytest test_debate_lib.py -q
```

기대: `6 passed`

- [ ] **Step 6: 커밋·푸시**

```bash
git add insights/paths.py insights/debate_lib.py insights/test_debate_lib.py
git commit -m "feat(쟁점): 쟁점 파일을 화자 발언 목록으로 읽는다"
git push
```

---

### Task 2: 규칙 검사기

**Files:**
- Create: `insights/check_debate.py`
- Test: `insights/test_check_debate.py`

**Interfaces:**
- Consumes: `debate_lib.parse`, `debate_lib.voice_key`, `debate_lib.STANCES`, `debate_lib.MODERATOR`, `paths.DEBATE`, `paths.NOTES`, `notes_lib.CITE`
- Produces:
  - `check_debate.check(path, text, today) -> [(level, rule, msg)]` — `level` 은 `'FAIL'` 또는 `'WARN'`. `today` 는 `datetime.date`
  - `check_debate.VERDICT` — 판정어 튜플
  - `check_debate.main() -> int` — FAIL 이 있으면 1

규칙 일곱은 스펙의 표 그대로다.

| 규칙 | 잡는 것 | 등급 |
|---|---|---|
| D1 | 화자 절에 인용 마커 없는 문장 (`- 관계:` `- 주장:` 두 줄은 뺀다) | FAIL |
| D2 | `관계` 대상이 이 파일에 없는 발언 | FAIL |
| D3 | `관계` 가 넷 밖의 값 | FAIL |
| D4 | `closes_when.by` 가 오늘보다 이르고 `as_of` 가 그 날짜 이하 | FAIL |
| D5 | 진행자 절에 판정어 | FAIL |
| D6 | 발언이 하나인데 「답하지 않은 화자」가 빔 | WARN |
| D7 | `sources:` 의 원문을 가리키는 노트가 `insights/notes/` 에 없음 | FAIL |

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_check_debate.py`.

```python
# -*- coding: utf-8 -*-
import datetime
import check_debate as cd

HEAD = '''---
view: debate
question: 무엇이 먼저인가?
as_of: 2026-08-27
closes_when: {what: "엔비디아 실적", by: 2026-12-31}
sources:
  - {file: "content/understanding/회계사/[260820] 바이백을 두 배로 늘렸다 - 엘곰.md", note: "가"}
---
'''

BODY_OK = '''
## 물음

두 글이 인과를 반대로 놓는다.

## 발언

### 엘곰 · 2026-08-20 · 「바이백」
- 관계: 단독
- 주장: 투자가 금리를 정한다
회사채 발행이 매수 여력을 두고 경쟁한다 ([260820] 바이백을 두 배로 L17).

## 답하지 않은 화자

- SemiAnalysis — 같은 기간 조달비용을 다룬 글이 없다

## 갈리는 자리

금리 수준이 임계다.

## 무엇을 보면 갈리나

30년물이 5.3%를 넘기는지.
'''

TODAY = datetime.date(2026, 8, 27)


def rules(found):
    return [r for _, r, _ in found]


def no_d7(found):
    """D7 은 저장소의 노트 파일을 읽는다 — 단위 테스트에서는 빼고 본다."""
    return [(lv, r, m) for lv, r, m in found if r != 'D7']


def test_clean_doc_has_no_findings():
    assert no_d7(cd.check('x.md', HEAD + BODY_OK, TODAY)) == []


def test_d1_flags_moderator_sentence_inside_voice():
    bad = BODY_OK.replace(
        '회사채 발행이 매수 여력을 두고 경쟁한다 ([260820] 바이백을 두 배로 L17).',
        '회사채 발행이 매수 여력을 두고 경쟁한다 ([260820] 바이백을 두 배로 L17).\n'
        '이 설명이 더 그럴듯하다.')
    assert 'D1' in rules(cd.check('x.md', HEAD + bad, TODAY))


def test_d1_ignores_label_lines():
    assert 'D1' not in rules(cd.check('x.md', HEAD + BODY_OK, TODAY))


def test_d2_flags_dangling_relation_target():
    bad = BODY_OK.replace('- 관계: 단독', '- 관계: 충돌 ↔ 이선엽 08-19 「없는 글」')
    assert 'D2' in rules(cd.check('x.md', HEAD + bad, TODAY))


def test_d3_flags_unknown_stance():
    bad = BODY_OK.replace('- 관계: 단독', '- 관계: 반박')
    assert 'D3' in rules(cd.check('x.md', HEAD + bad, TODAY))


def test_d4_flags_stale_as_of_after_close_date():
    bad = (HEAD.replace('by: 2026-12-31', 'by: 2026-08-20')
               .replace('as_of: 2026-08-27', 'as_of: 2026-08-19'))
    assert 'D4' in rules(cd.check('x.md', bad + BODY_OK, TODAY))


def test_d4_passes_when_as_of_moved_past_close_date():
    ok = HEAD.replace('by: 2026-12-31', 'by: 2026-08-20')
    assert 'D4' not in rules(cd.check('x.md', ok + BODY_OK, TODAY))


def test_d5_flags_verdict_word_in_moderator_section():
    bad = BODY_OK.replace('금리 수준이 임계다.', '엘곰이 맞았다.')
    assert 'D5' in rules(cd.check('x.md', HEAD + bad, TODAY))


def test_d5_ignores_verdict_word_inside_voice():
    bad = BODY_OK.replace(
        '회사채 발행이 매수 여력을 두고 경쟁한다',
        '앞선 예측이 맞았다고 적는다')
    assert 'D5' not in rules(cd.check('x.md', HEAD + bad, TODAY))


def test_d6_warns_when_single_voice_and_no_silent_list():
    bad = BODY_OK.replace(
        '- SemiAnalysis — 같은 기간 조달비용을 다룬 글이 없다', '')
    found = cd.check('x.md', HEAD + bad, TODAY)
    assert ('WARN', 'D6') in [(lv, r) for lv, r, _ in found]
```

- [ ] **Step 2: 테스트가 실패하는지 돌려 본다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis\insights; python -m pytest test_check_debate.py -q
```

기대: `ModuleNotFoundError: No module named 'check_debate'`

- [ ] **Step 3: 검사기를 쓴다**

`insights/check_debate.py`.

```python
# -*- coding: utf-8 -*-
"""쟁점 — 화자 말과 진행자 말이 섞였는지 본다.

이 장은 만들어진 구성물이다. 화자들은 서로를 안 읽는다. 그래서 지어낸 대사가
들어가는 것을 기계가 막는다 — 화자 절의 문장은 전부 인용 마커를 달아야 하고,
진행자는 누가 맞았는지 적지 못한다.

  PYTHONIOENCODING=utf-8 python insights/check_debate.py
"""
import datetime
import glob
import io
import os
import re
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import debate_lib as dl
import notes_lib as nl
import paths

# 진행자가 못 쓰는 말. 승패를 적는 순간 이 장은 검증 대장이 되고, 거기 규칙은
# 판정을 사후에 끼워 넣지 말라는 것이다
VERDICT = ('맞았', '틀렸', '옳다', '옳았', '승자', '패자',
           '이겼', '졌다', '정답', '적중')
DATE_RE = re.compile(r'(\d{4}-\d{2}-\d{2})')
BY_RE = re.compile(r'by:\s*(\d{4}-\d{2}-\d{2})')
# 문장 끝 기호로 자른다. 인용 마커가 문장 안에 있는지 보려면 문장 단위여야 한다
SENT_RE = re.compile(r'[^.!?\n]+[.!?]?')
_notes_cache = []


def _date(s):
    m = DATE_RE.search(s or '')
    return datetime.date(*[int(x) for x in m.group(1).split('-')]) if m else None


def _notes_heads():
    """노트 앞머리를 한 번만 읽어 둔다 — 쟁점마다 349장을 다시 열면 느리다."""
    if not _notes_cache:
        for p in sorted(glob.glob(os.path.join(paths.NOTES, '*.md'))):
            _notes_cache.append(io.open(p, encoding='utf-8',
                                        errors='replace').read(1200))
    return _notes_cache


def _note_exists(src_file):
    """이 원문을 source: 로 가리키는 노트가 있나."""
    base = os.path.basename(src_file)
    return any(base in head for head in _notes_heads())


def check(path, text, today):
    out = []
    d = dl.parse(text)
    meta, secs, voices = d['meta'], d['sections'], d['voices']
    keys = {dl.voice_key(v) for v in voices}

    for v in voices:
        who = '%s %s' % (v['actor'], v['said'])
        # D1 — 화자 절 문장은 전부 인용을 달아야 한다
        for sent in SENT_RE.findall(v['body']):
            if len(sent.strip()) < 10:
                continue
            if not nl.CITE.search(sent):
                out.append(('FAIL', 'D1',
                            '%s 발언에 인용 없는 문장이 있다: %s…'
                            % (who, sent.strip()[:40])))
        # D3 — 관계 값
        if v['stance'] not in dl.STANCES:
            out.append(('FAIL', 'D3',
                        '%s 의 관계 「%s」는 %s 밖이다'
                        % (who, v['stance'], ' · '.join(dl.STANCES))))
        # D2 — 관계 대상
        elif v['stance'] != '단독' and v['against'] not in keys:
            out.append(('FAIL', 'D2',
                        '%s 가 가리킨 「%s」가 이 쟁점에 없다'
                        % (who, v['against'])))

    # D4 — 닫힐 날짜가 지났는데 as_of 를 안 옮겼다
    m = BY_RE.search(meta.get('closes_when', ''))
    by = _date(m.group(1)) if m else None
    as_of = _date(meta.get('as_of', ''))
    if by and by < today and as_of and as_of <= by:
        out.append(('FAIL', 'D4',
                    '닫힐 날짜 %s 가 지났다 — 답이 나왔는지 확인하고 as_of 를 옮긴다'
                    % by))

    # D5 — 진행자 절 판정어
    for name in dl.MODERATOR:
        body = secs.get(name, '')
        for w in VERDICT:
            if w in body:
                out.append(('FAIL', 'D5',
                            '진행자 절 「%s」에 판정어 「%s」가 있다 — 진행자는 심판이 아니다'
                            % (name, w)))
                break

    # D6 — 한 명만 답했는데 답 안 한 화자를 안 적었다
    if len(voices) <= 1 and not secs.get('답하지 않은 화자', '').strip():
        out.append(('WARN', 'D6',
                    '발언이 하나인데 답하지 않은 화자가 비었다 — 한 명만 답한 것도 결과다'))

    # D7 — 원문을 가리키는 노트
    for s in d['sources']:
        if not _note_exists(s['file']):
            out.append(('FAIL', 'D7',
                        '%s 를 가리키는 노트가 insights/notes/ 에 없다'
                        % os.path.basename(s['file'])))
    return out


def main():
    try:
        sys.stdout.reconfigure(encoding='utf-8')
    except Exception:
        pass
    today = datetime.date.today()
    files = sorted(glob.glob(os.path.join(paths.DEBATE, '*.md')))
    fails = 0
    for p in files:
        text = io.open(p, encoding='utf-8').read()
        for level, rule, msg in check(p, text, today):
            fails += level == 'FAIL'
            print('%s %s [%s] %s' % (level, os.path.basename(p), rule, msg))
    print('\n요약: 쟁점 %d장 / FAIL %d' % (len(files), fails))
    return 1 if fails else 0


if __name__ == '__main__':
    sys.exit(main())
```

- [ ] **Step 4: 테스트가 통과하는지 돌려 본다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis\insights; python -m pytest test_check_debate.py -q
```

기대: `10 passed`

D7 은 저장소 노트를 읽어서 단위 테스트로 가두기 어렵다. `no_d7()` 로 걸러 두고, 진짜 확인은 Task 4 에서 실제 쟁점으로 한다.

- [ ] **Step 5: 빈 디렉터리에서 검사기가 도는지 본다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis; python insights/check_debate.py
```

기대: `요약: 쟁점 0장 / FAIL 0`, exit 0

- [ ] **Step 6: 커밋·푸시**

```bash
git add insights/check_debate.py insights/test_check_debate.py
git commit -m "feat(쟁점): 화자 말과 진행자 말이 섞이는 것을 막는다"
git push
```

---

### Task 3: 기존 검사기에 연결

**Files:**
- Modify: `insights/check_cite.py:104-106`
- Modify: `insights/check_prose.py:508` 근처
- Modify: `CLAUDE.md`

**Interfaces:**
- Consumes: `paths.DEBATE` (Task 1)
- Produces: 없음. 기존 검사기가 쟁점 파일을 자기 대상에 넣는다

`check_prose` 는 이미 `대시보드/*.html` 을 전부 글롭한다. 새 대시보드는 자동으로 문체 게이트에 들어간다 — 거기는 고칠 데가 없다. 고칠 것은 마크다운 쪽뿐이다.

- [ ] **Step 1: `check_cite` 글롭에 넣는다**

`insights/check_cite.py` 의 `main()`:

```python
    files = sorted(glob.glob(os.path.join(paths.BRIEFS, '*.md')) +
                   glob.glob(os.path.join(paths.LOOP, '*.md')) +
                   glob.glob(os.path.join(paths.DEBATE, '*.md')) +
                   glob.glob(os.path.join(paths.SYNTH, '*.md')))
```

- [ ] **Step 2: `check_prose` 마크다운 대상에 넣는다**

`insights/check_prose.py:521` 의 브리핑 반복문 바로 아래에 붙인다. 브리핑과 같은 검사 넷만 받는다 — 절 순서(P5~P7)는 안 본다. 쟁점은 교차 인사이트와 절 구성이 다르다.

브리핑이 `headline` 을 필수로 걸듯, 쟁점은 `question` 을 필수로 건다. 카드 제목이 거기서 오기 때문이다.

```python
    # 쟁점은 화자 발언과 진행자 절로 나뉘어 절 구성이 인사이트와 다르다.
    # 문체·용어·길이·번역투만 보고, question 만 필수로 건다
    for p in sorted(glob.glob(os.path.join(paths.DEBATE, '*.md'))):
        where = os.path.basename(p)
        raw = io.open(p, encoding='utf-8').read()
        body = strip_refs(raw)
        meta, _ = parse_synth(raw)
        check_density(body, where)
        check_glossary(body, where, gloss)
        check_length(body, where)
        check_translationese(body, where)
        if not (meta or {}).get('question'):
            add('FAIL', where, 'P7', 'question 없음 — 카드에 물음이 안 붙는다')
```

맨 끝 `print('요약: …')` 의 세는 항목에 쟁점 장수도 더한다.

- [ ] **Step 3: 둘 다 돌려 본다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis; python insights/check_cite.py; python insights/check_prose.py
```

기대: 쟁점이 아직 없으니 둘 다 지금과 같은 결과. 새 FAIL 이 생기면 글롭을 잘못 붙인 것이다.

- [ ] **Step 4: `check_deps` 를 돌린다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis; python scripts/check_deps.py
```

기대: FAIL 0. 추적된 코드가 부르는 파일이 추적되는지 본다 — 새 모듈 둘이 커밋돼 있어야 통과한다.

- [ ] **Step 5: `CLAUDE.md` 검사기 목록에 한 줄 더한다**

`insights/check_val.py` 줄 아래에 붙인다.

```
PYTHONIOENCODING=utf-8 python insights/check_debate.py   # 쟁점 — 화자 말과 진행자 말이 섞였나
```

- [ ] **Step 6: 커밋·푸시**

```bash
git add insights/check_cite.py insights/check_prose.py CLAUDE.md
git commit -m "feat(쟁점): 인용·문체 검사기가 쟁점도 본다"
git push
```

---

### Task 4: 첫 쟁점 한 장 — 스키마를 실전에서 시험한다

화면을 만들기 전에 쟁점을 먼저 쓴다. 스키마가 안 버티면 여기서 드러나고, 그때 고치는 편이 생성기까지 짓고 고치는 것보다 싸다.

**본문은 메인 세션이 쓴다.** 어느 발언이 그 물음에 답했는지, 둘이 충돌인지 결다름인지는 판단이고, 이 저장소는 판단을 하위 모델에 넘기지 않는다.

**Files:**
- Create: `insights/debate/issue-2026-08-27-금리와-AI설비투자-무엇이-앞에-서나.md`

**Interfaces:**
- Consumes: `debate_lib.parse` 가 읽는 형식 (Task 1), `check_debate` 의 규칙 일곱 (Task 2)
- Produces: 뒤 과제가 쓰는 실제 쟁점 파일 한 장

**재료 (이미 확인된 원문).** 교차 인사이트 `insights/synth/cross-2026-08-21-투자가-자기-조달비용을-민다.md` 가 같은 재료를 이미 다뤘다. 그 카드는 **글 대 글**이고 이 쟁점은 **화자 대 화자**다 — 겹치는 것이 아니라 축이 다르다. 그 파일의 `sources:` 블록을 출발점으로 쓴다.

| 화자 | 원문 | 놓는 방향 |
|---|---|---|
| 엘곰 | `[260820] 삼성전자 9%·SK하이닉스 12% 급등 …` L23 | 금리가 투자를 정한다 |
| 엘곰 | `[260820] 바이백을 두 배로 늘리자 30년물이 9bp 내렸다 …` L17 | 투자가 금리를 정한다 |
| 엘곰 | `[260807] AI CAPEX가 잉여현금을 삼켰다 …` L31, L43 | 두 번째에 붙는 크기 |
| 미국주식 사관학교 | `[260726] 숏커버링 연료 고갈과 AI의 자리 이동 …` L34, L35 | 방향이 바뀌었다 |

- [ ] **Step 1: 후보 노트를 긁는다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis; python -c "import io,glob,os; [print(os.path.basename(p)) for p in sorted(glob.glob('insights/notes/*.md')) if any(k in io.open(p,encoding='utf-8').read(1500) for k in ('조달비용','CAPEX','설비투자','30년물'))]"
```

나온 목록에서 **그 물음에 실제로 답한 것만** 남긴다. 관련 있어 보인다고 넣지 않는다.

- [ ] **Step 2: 쟁점 파일을 쓴다**

`insights/debate/issue-2026-08-27-금리와-AI설비투자-무엇이-앞에-서나.md`.

frontmatter 는 스펙의 예시 그대로 — `view` `question` `subhead` `section` `as_of` `closes_when` `sources`. 본문은 절 다섯: 물음 / 발언 / 답하지 않은 화자 / 갈리는 자리 / 무엇을 보면 갈리나.

지킬 것:
- 발언 절의 문장은 전부 인용 마커를 단다. `([260820] 삼성전자 9% L23)` 꼴이고, 라벨은 `sources:` 파일명 앞머리와 맞아야 `notes_lib.resolve()` 가 찾는다.
- 인용한 문장의 숫자는 그 줄에 실제로 있어야 한다. `check_cite` 가 대조한다.
- 진행자 절에 판정어를 안 쓴다.
- 「무엇을 보면 갈리나」는 관측 지점으로 적는다. 예측이 아니다.
- `closes_when` 은 관측 가능한 사건과 날짜로 적는다.
- `section` 은 `gen_debate.py` 가 세울 섹션 id 다. 첫 쟁점은 `market` 으로 둔다.

- [ ] **Step 3: 검사기를 전부 돌린다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis
python insights/check_debate.py
python insights/check_cite.py
python insights/check_prose.py
python insights/check_notes.py
python insights/check_read.py
python insights/check_fresh.py
python insights/check_report.py
python insights/check_val.py
python scripts/check_deps.py
```

기대: 전부 FAIL 0. **일부만 돌리지 않는다.**

`check_cite` 가 「확인 필요」를 내면 원문 줄을 열어 사람이 확인한다 — 기계가 잡는 것은 절반이다.

- [ ] **Step 4: 스키마가 버텼는지 판정한다**

셋 중 하나라도 걸리면 스펙으로 돌아간다.

- 관계 셋(충돌·동의·결다름)으로 이 재료가 안 나뉘나
- 진행자 절 셋으로 하고 싶은 말이 안 담기나
- `closes_when` 을 관측 가능한 사건으로 못 적나

- [ ] **Step 5: 커밋·푸시**

```bash
git add "insights/debate/issue-2026-08-27-금리와-AI설비투자-무엇이-앞에-서나.md"
git commit -m "feat(쟁점): 금리와 AI 설비투자 중 무엇이 앞에 서나"
git push
```

---

### Task 5: 카드 모양

**Files:**
- Modify: `scripts/card_lib.py` (`card_html` 분기 + `debate_html`)
- Test: `insights/test_cards.py`

**Interfaces:**
- Consumes: Task 1 의 `voices` 원소 구조
- Produces:
  - `card_lib.debate_html(d) -> str` — `d` 는 `{'question', 'moderator': {절이름: html}, 'voices': [...], 'silent': [{'actor','why'}], 'figs': (...)}`
  - `card_html(c)` 가 `c.get('debate')` 를 보고 이 분기로 간다
  - 마크업 약속 — 진행자 블록은 `class="uc-mod"`, 화자 블록은 `class="uc-voice"`, 관계는 `class="uc-stance is-<관계>"`

**진행자와 화자를 화면에서 가르는 것이 이 과제의 전부다.** 같은 모양으로 나가면 인용과 판단이 섞이고, 그러면 자료구조로 갈라 놓은 것이 화면에서 무너진다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_cards.py` 에 붙인다.

```python
def test_debate_card_separates_moderator_from_voices():
    import sys, os
    sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(
        os.path.abspath(__file__))), 'scripts'))
    import card_lib

    h = card_lib.debate_html({
        'question': '무엇이 먼저인가?',
        'moderator': {'물음': '<p>두 글이 반대로 놓는다.</p>',
                      '갈리는 자리': '<p>금리 수준이 임계다.</p>',
                      '무엇을 보면 갈리나': '<p>청약배수를 본다.</p>'},
        'voices': [{'actor': '엘곰', 'said': '2026-08-20', 'title': '바이백',
                    'stance': '단독', 'against': '',
                    'claim': '투자가 금리를 정한다',
                    'body': '<p>발행이 경쟁한다.</p>'}],
        'silent': [{'actor': 'SemiAnalysis', 'why': '다룬 글이 없다'}],
        'figs': (),
    })
    assert 'uc-mod' in h
    assert 'uc-voice' in h
    # 화자 이름·날짜·관계가 다 보인다
    assert '엘곰' in h and '2026-08-20' in h and 'is-단독' in h
    # 답하지 않은 화자도 화면에 선다
    assert 'SemiAnalysis' in h and '다룬 글이 없다' in h
    # 진행자 블록이 화자 블록보다 앞에 선다 — 물음이 먼저다
    assert h.index('uc-mod') < h.index('uc-voice')
    # 진행자 절 셋이 다 나온다
    for k in ('물음', '갈리는 자리', '무엇을 보면 갈리나'):
        assert k in h
```

- [ ] **Step 2: 테스트가 실패하는지 돌려 본다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis\insights; python -m pytest test_cards.py -q
```

기대: `AttributeError: module 'card_lib' has no attribute 'debate_html'`

- [ ] **Step 3: `debate_html` 을 쓴다**

`scripts/card_lib.py` 의 `post_html` 옆에 더한다.

```python
def debate_html(d):
    """쟁점 카드 본문 — 진행자와 화자를 갈라 낸다.

    화자 블록은 인용이고 진행자 블록은 판단이다. 같은 모양으로 내보내면
    자료구조로 갈라 놓은 것이 화면에서 도로 붙는다.
    """
    h = []
    h.append('<div class="uc-mod"><span class="uc-mod-tag">진행자</span>')
    h.append('<h3>물음</h3>%s' % d['moderator'].get('물음', ''))
    h.append('</div>')
    for f in d.get('figs', ()):
        h.append(f)
    for v in d['voices']:
        rel = v['stance'] if v['stance'] == '단독' else '%s ↔ %s' % (
            v['stance'], v['against'])
        h.append('<div class="uc-voice">')
        h.append('<div class="uc-voice-head"><b>%s</b>'
                 '<span class="uc-voice-when">%s</span>'
                 '<span class="uc-voice-src">「%s」</span>'
                 '<span class="uc-stance is-%s">%s</span></div>'
                 % (v['actor'], v['said'], v['title'], v['stance'], rel))
        if v.get('claim'):
            h.append('<p class="uc-voice-claim">%s</p>' % v['claim'])
        h.append(v['body'])
        h.append('</div>')
    if d.get('silent'):
        h.append('<div class="uc-silent"><h3>답하지 않은 화자</h3><ul>')
        for s in d['silent']:
            h.append('<li><b>%s</b> — %s</li>' % (s['actor'], s['why']))
        h.append('</ul></div>')
    h.append('<div class="uc-mod"><span class="uc-mod-tag">진행자</span>')
    h.append('<h3>갈리는 자리</h3>%s' % d['moderator'].get('갈리는 자리', ''))
    h.append('<h3>무엇을 보면 갈리나</h3>%s'
             % d['moderator'].get('무엇을 보면 갈리나', ''))
    h.append('</div>')
    return ''.join(h)
```

`card_lib.py` 는 이스케이프를 하지 않는다 — 내용이 전부 이 저장소가 쓴 마크다운이라 기존 `post_html` · `report_html` 도 그대로 끼워 넣는다. 여기서만 다르게 하지 않는다.

`uc-mod` · `uc-voice` · `uc-stance` 의 CSS 는 `dash_common.css()` 에 더한다. 진행자 블록은 배경을 깔고 왼쪽에 굵은 선을 세워 화자 블록과 확실히 갈라 놓는다. 관계 배지는 모양으로도 갈린다 — 색만으로 가르지 않는다.

- [ ] **Step 4: `card_html` 에 분기를 넣는다**

`card_html(c)` 안, `if c.get('post'):` 분기 바로 앞에 둔다.

```python
    if c.get('debate'):
        if c.get('verdict'):
            h.append('<p class="uc-verdict"><b>한줄 코멘트.</b> %s</p>' % c['verdict'])
        h.append(debate_html(c['debate']))
        h.append(_links_html(c))
        h.append('</div></div>')
        return ''.join(h)
```

- [ ] **Step 5: 테스트가 통과하는지 돌려 본다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis\insights; python -m pytest test_cards.py -q
```

기대: 전부 통과. 기존 카드 테스트도 같이 통과해야 한다 — 분기를 잘못 넣으면 기존 카드가 깨진다.

- [ ] **Step 6: 커밋·푸시**

```bash
git add scripts/card_lib.py scratchpad/dash_common.py insights/test_cards.py
git commit -m "feat(쟁점): 진행자 블록과 화자 블록을 갈라 낸다"
git push
```

---

> **2026-08-28 변경.** 쟁점을 전용 장이 아니라 `대시보드/통합 인사이트.html` 에
> 올리기로 했다(사용자 결정). Task 6 은 아래 내용대로 한 번 구현됐고
> (`scratchpad/gen_debate.py`, 커밋 116e870), Task 7 의 관계도까지 붙었다
> (커밋 ea79fed). 그 둘은 Task 6b 에서 통합 인사이트로 옮기고 전용 장은 지운다.
> 아래 본문은 그때의 기록으로 남긴다.

### Task 6: 대시보드

**Files:**
- Create: `scratchpad/gen_debate.py`
- Create (생성물): `대시보드/쟁점 대시보드.html`

**Interfaces:**
- Consumes: `debate_lib.parse` (Task 1), `card_lib.card_html` 의 `debate` 분기 (Task 5), `dash_common.render` / `check_ui` / `check_labels`
- Produces: `gen_debate.py` 를 다시 돌리면 같은 HTML 이 나온다

- [ ] **Step 1: 생성기를 쓴다**

`scratchpad/gen_debate.py`.

```python
# -*- coding: utf-8 -*-
"""쟁점 → 대시보드 한 장.

  PYTHONIOENCODING=utf-8 python scratchpad/gen_debate.py
"""
import glob
import io
import os
import re
import sys

HERE = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(HERE)
sys.path.insert(0, HERE)
sys.path.insert(0, os.path.join(ROOT, 'insights'))
sys.path.insert(0, os.path.join(ROOT, 'scripts'))

import dash_common as dc
import debate_lib as dl
import notes_lib as nl
import paths

SILENT_RE = re.compile(r'^[-*][ \t]*(.+?)[ \t]*—[ \t]*(.+?)[ \t]*$')


def silent_of(sec):
    out = []
    for line in sec.split('\n'):
        m = SILENT_RE.match(line)
        if m:
            out.append({'actor': m.group(1), 'why': m.group(2)})
    return out


def card_of(path):
    text = io.open(path, encoding='utf-8').read()
    d = dl.parse(text)
    meta, secs, src = d['meta'], d['sections'], d['sources']
    voices = []
    for v in d['voices']:
        v = dict(v)
        v['body'] = nl.md_body(v['body'], src)
        voices.append(v)
    mod = {k: nl.md_body(secs.get(k, ''), src) for k in dl.MODERATOR}
    actors = []
    for v in d['voices']:
        if v['actor'] not in actors:
            actors.append(v['actor'])
    return {
        'title': meta.get('question', '').strip().strip('"'),
        'gain': meta.get('subhead', '').strip().strip('"'),
        'section': meta.get('section', 'market').strip(),
        'meta': [' · '.join(actors), meta.get('as_of', '')],
        'debate': {'question': meta.get('question', ''),
                   'moderator': mod,
                   'voices': voices,
                   'silent': silent_of(secs.get('답하지 않은 화자', '')),
                   'figs': ()},
    }


def main():
    try:
        sys.stdout.reconfigure(encoding='utf-8')
    except Exception:
        pass
    cards = [card_of(p) for p in sorted(glob.glob(
        os.path.join(paths.DEBATE, '*.md')))]
    dc.check_labels(cards)
    out = os.path.join(ROOT, '대시보드', '쟁점 대시보드.html')
    dc.render(cards, title='쟁점',
              header='같은 물음에 화자들이 갈린 자리',
              footer='화자 말은 원문 인용이고 진행자 말은 판단이다. '
                     '화자들은 서로를 읽지 않는다 — 이 자리는 그것을 나란히 놓은 것이다.',
              out=out, newest_first=True)
    html = io.open(out, encoding='utf-8').read()
    dc.check_ui(html, has_top=False)
    print('쟁점 %d장 → %s' % (len(cards), out))
    return 0


if __name__ == '__main__':
    sys.exit(main())
```

`nl.md_body` 의 인자는 `insights/notes_lib.py:114` 를 열어 맞춘다 — `md_body(body, sources, h='h2', cls='tsec', dot=None)` 이다. 화자 본문 안에서는 h2 가 안 나오므로 기본값으로 둔다.

`dc.render` 와 `dc.check_ui` 의 인자 이름은 `scratchpad/dash_common.py:959`, `:1152` 를 열어 맞춘다. `render` 의 섹션 이름은 카드의 `section` 값에서 온다 — 섹션 이름표를 따로 넘겨야 하면 기존 생성기(`scratchpad/gen_undreport_dashboard.py`)가 어떻게 하는지 보고 같은 방식으로 한다.

- [ ] **Step 2: 돌려 본다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis; python scratchpad/gen_debate.py
```

기대: `쟁점 1장 → …\대시보드\쟁점 대시보드.html`. `check_ui` 나 `check_labels` 가 막으면 그 규약을 어긴 것이다 — 우회하지 말고 맞춘다.

- [ ] **Step 3: 문체 게이트를 받는다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis; python insights/check_prose.py
```

`check_prose` 는 `대시보드/*.html` 을 전부 글롭하므로 새 장이 자동으로 들어간다. 기대: FAIL 0.

- [ ] **Step 4: 화면을 눈으로 본다**

브라우저로 `대시보드/쟁점 대시보드.html` 을 연다. 확인할 것 셋:

- 첫 화면이 섹션 타일인가. 관문 버튼이 없나
- 진행자 블록과 화자 블록이 한눈에 구별되나
- 접힌 카드에 물음과 화자 이름이 보이나

- [ ] **Step 5: 커밋·푸시**

```bash
git add scratchpad/gen_debate.py "대시보드/쟁점 대시보드.html"
git commit -m "feat(쟁점): 쟁점 대시보드를 연다"
git push
```

---

### Task 7: 관계도

**Files:**
- Modify: `scratchpad/gen_debate.py` (`rel_svg` 추가, `figs` 를 채운다)

**Interfaces:**
- Consumes: Task 6 의 `card_of`, Task 1 의 `voices`
- Produces: `gen_debate.rel_svg(voices) -> str` — 인라인 SVG 한 덩이

**먼저 `insight-figure` 스킬을 연다.** 도해 규칙은 거기 있다. 배치는 `scratchpad/check_fig.py` 가 검사한다.

지킬 것 하나가 이 도해에 특히 걸린다 — **없는 값을 그리지 않는다.** 화자 사이 거리·선 굵기·노드 크기에 뜻을 싣지 않는다. 원문에 그런 수가 없다. 이 그림이 나르는 것은 관계 종류 하나뿐이다.

- [ ] **Step 1: `insight-figure` 스킬을 읽는다**

- [ ] **Step 2: `rel_svg` 를 쓴다**

화자를 원으로 놓고 관계를 선으로 잇는다. **선 모양으로 종류를 가른다** — 충돌은 실선, 동의는 점선, 결다름은 파선. 색으로만 가르지 않는다. 같은 화자의 여러 발언은 한 원 안에 묶지 말고 각각 놓는다 — 엘곰 두 글이 서로 어긋나는 것이 이 장의 출발점이라 한 점으로 뭉치면 그 어긋남이 사라진다.

범례를 그림 안에 둔다.

- [ ] **Step 3: `card_of` 에서 `figs` 에 넣는다**

```python
        'debate': {'question': meta.get('question', ''),
                   'moderator': mod,
                   'voices': voices,
                   'silent': silent_of(secs.get('답하지 않은 화자', '')),
                   'figs': (rel_svg(d['voices']),)},
```

`debate_html` 이 진행자 물음 바로 뒤에 그림을 놓는다 (Task 5). 물음을 읽고 관계를 보고 발언으로 들어가는 순서다.

- [ ] **Step 4: 다시 만들고 검사한다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis
python scratchpad/gen_debate.py
python scratchpad/check_fig.py
python insights/check_prose.py
```

기대: FAIL 0.

- [ ] **Step 5: 커밋·푸시**

```bash
git add scratchpad/gen_debate.py "대시보드/쟁점 대시보드.html"
git commit -m "feat(쟁점): 화자 사이 관계를 그림으로 놓는다"
git push
```

---

### Task 8: 쟁점 둘 더 — 스키마를 다른 모양으로 밀어 본다

첫 쟁점은 충돌이 뚜렷하고 화자가 여럿인 쉬운 경우다. 어려운 두 모양을 시험한다.

**Files:**
- Create: `insights/debate/issue-2026-08-27-반도체-하락이-추세의-시작인가.md`
- Create: `insights/debate/issue-2026-08-27-삼성전자-주주환원이-이미-반영됐나.md`

**Interfaces:**
- Consumes: Task 1–7 전부
- Produces: 쟁점 셋이 선 대시보드

**쟁점 2 — 한 명만 답한 경우.** 엘곰 `[260825] 한국은 전날 이미 3.12% 빠졌다 …` 가 이 물음에 답한다(L13, L20, L46). 다른 화자가 같은 물음에 답한 글이 있는지 찾고, 없으면 「답하지 않은 화자」에 이유와 함께 적는다. **여기서 빈칸이 정직한지 본다** — 없는 반박을 채우고 싶어지면 그게 설계 실패 신호다. `closes_when` 은 엔비디아 실적 발표(2026-08-27)다. 그날이 지나면 D4 가 걸리므로, 답이 나왔는지 확인하고 `as_of` 를 옮기는 흐름을 실제로 한 번 밟는다.

**쟁점 3 — 결다름이 나오는 경우.** 삼성전자 주주환원 실망이 8월 20일 급등과 8월 25일 하락 양쪽에 나온다(`[260820] 삼성전자 9% …`, `[260825] 한국은 전날 이미 … L51`). 같은 사실을 두고 「이미 반영됐다」와 「아직 남았다」가 갈리는지, 아니면 그냥 시점이 다른 것인지 — 후자면 관계는 `결다름`이다. **충돌로 부풀리지 않는다.**

- [ ] **Step 1: 쟁점 2 를 쓴다**

Task 4 의 Step 1–2 와 같은 절차. 후보 긁기 → 실제로 답한 것만 → 관계 → 진행자 절.

- [ ] **Step 2: 쟁점 3 을 쓴다**

- [ ] **Step 3: 검사기를 전부 돌린다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis
python insights/check_debate.py
python insights/check_cite.py
python insights/check_prose.py
python insights/check_notes.py
python insights/check_read.py
python insights/check_fresh.py
python insights/check_report.py
python insights/check_val.py
python scripts/check_deps.py
```

기대: 전부 FAIL 0. D6 이 WARN 으로 뜨면 「답하지 않은 화자」를 채웠는지 본다.

- [ ] **Step 4: 대시보드를 다시 만든다**

```
$env:PYTHONIOENCODING='utf-8'; Set-Location C:\Users\y\semianalysis; python scratchpad/gen_debate.py
```

기대: `쟁점 3장`

- [ ] **Step 5: 스키마 판정**

셋이 다 서고 검사기가 전부 통과하면 스키마가 버틴 것이다. 안 버티면 스펙을 고치고 Task 1–2 로 돌아간다.

- [ ] **Step 6: 커밋·푸시**

```bash
git add insights/debate/ "대시보드/쟁점 대시보드.html"
git commit -m "feat(쟁점): 한 명만 답한 것과 결이 다른 것을 세운다"
git push
```

---

### Task 9: 절차를 스킬로 굳힌다

**Files:**
- Create: `.claude/skills/insight-debate/SKILL.md`
- Modify: `CLAUDE.md`

**Interfaces:**
- Consumes: Task 1–8 에서 실제로 밟은 절차
- Produces: 다음 쟁점을 세울 때 여는 스킬

스킬은 Task 4·8 을 하고 나서 쓴다. 해 보기 전에 쓰면 안 밟은 절차를 적게 된다.

- [ ] **Step 1: 스킬을 쓴다**

`.claude/skills/insight-debate/SKILL.md`. frontmatter 의 `description` 은 언제 여는지를 적는다 — 「쟁점 세워줘」 / 「이 물음에 누가 뭐라 했나」 / 「토론에 올려」 / 새 노트가 기존 쟁점에 답할 때.

절차 일곱:

1. 후보 긁기 — `actors` · `topics` 로 관련 노트를 모은다
2. 거르기 — 그 물음에 실제로 답한 것만. **여기가 판단이고 메인 세션이 한다**
3. 관계 매기기 — 충돌 · 동의 · 결다름 · 단독. 충돌로 부풀리지 않는다
4. 진행자 절 셋. 판정어를 안 쓴다
5. 도해 — `insight-figure` 규칙
6. 검사기 전부 — FAIL 0
7. 커밋 · 푸시

같이 적을 것:
- 화자 말과 진행자 말이 왜 갈려 있는지 (화자들은 서로를 안 읽는다)
- 빈칸이 정직하다 — 답 안 한 화자를 채우려고 발언을 지어내지 않는다
- `sources:` 는 노트가 아니라 원문을 가리킨다. `notes_lib.resolve()` 가 파일명 앞머리로 찾아서다
- 새 쟁점이 아니라 기존 쟁점에 발언을 더하는 경우의 절차
- 닫힐 날짜가 지난 쟁점을 만났을 때 무엇을 하나 (답이 나왔는지 확인 → `as_of` 를 옮긴다)

- [ ] **Step 2: `CLAUDE.md` 에 안내를 넣는다**

「반드시 지킬 것」 절, `insight-report` 안내 옆에 붙인다.

```
**같은 물음에 화자들이 갈린 자리를 모을 때는 `insight-debate`.** 화자 말은 원문 인용이고 진행자 말은 판단이다 — 둘을 섞으면 없는 논쟁을 그린 것이 된다.
```

「이 저장소」 절의 디렉터리 그림에도 한 줄 더한다.

```
③ 포스트 insights/synth/cross-*.md        교차 인사이트
          insights/debate/issue-*.md       쟁점 — 화자들이 갈린 자리
          insights/briefs/*-지금-상태.md   현황 브리핑
```

- [ ] **Step 3: 검사기를 전부 돌린다**

Task 8 Step 3 과 같은 목록. 기대: FAIL 0.

- [ ] **Step 4: 커밋·푸시**

```bash
git add .claude/skills/insight-debate/SKILL.md CLAUDE.md
git commit -m "docs(쟁점): 쟁점 세우는 절차를 스킬로 굳힌다"
git push
```

---

## 안 만드는 것

스펙의 「범위 밖」 그대로다. 구현 중에 넣고 싶어지면 스펙으로 돌아가 먼저 고친다.

- 적중률 · 화자 점수판 — 진행자는 심판이 아니다
- 발언 유통기간 · 말 바꿈 취소선 · 쟁점 상태 라벨 — 시의성은 날짜와 닫힐 조건 하나로
- 노트 349장 소급 태깅
- 공개 도메인 배포. `insight-dashboard.com` 은 대시보드 넉 장만 낸다
