# 개체 색인 1단계 판정

측정일: 2026-08-27
측정 자리: 워크트리 `.claude/worktrees/entity-index` (커밋 `b0303df`)
코퍼스: `content/**/*.md` 473편 · 89,396줄
사전: 개체 449종이 색인에 잡혔다 (`entities.json` 474종 중)

계획서는 코퍼스를 548편으로 적었는데 실제로 추적되는 파일은 이 브랜치에서 473편,
`main`에서도 478편이다. 548은 다른 세션의 미커밋 작업이 섞인 체크아웃에서 잰 값으로 보인다.
아래 숫자는 전부 워크트리에서 다시 쟀다.

## 측정값

| 개체 | grep | 색인 | 색인만 | 놓친 것 |
|---|---|---|---|---|
| 램리서치 | 36 | 36 | 0 | 0 |
| 도쿄일렉트론 | 15 | 15 | 0 | 0 |
| 엔비디아 | 743 | 1,385 | 642 | 0 |
| 앤트로픽 | 490 | 635 | 145 | 0 |
| TSMC | 466 | 466 | 1 | 1 |

전수 훑기 0.81초 · 색인 조회 0.054초.

## 기준 셋

- **회수** — 놓친 것이 0인가: 실질적으로 예. 한 건 나온 TSMC의 누락은
  `content/newsletter/semiconductors/[260108] 애플-TSMC - 현대 반도체를 만든 파트너십.md#L242`이고,
  그 줄의 `TSMCSide`는 mermaid 도해의 노드 이름이다. 영문 별칭에 단어 경계를 요구하는 규칙이
  옳게 뺐고 grep 쪽이 오탐이다. 사람이 읽는 문장에서 놓친 줄은 없다
- **정밀** — 색인만 잡은 788줄에서 20줄을 고르게 뽑아 읽었다. 오탐 **0줄**. 기준은 3줄 이하다.
  뽑힌 20줄은 전부 영문 표기(`Nvidia`·`NVIDIA`·`Anthropic`)이거나 한글 본문 속 영문 혼용이었다.
  한글 표기 하나만 떠올린 grep이 못 보는 자리가 정확히 여기다
- **속도** — 전수 훑기 0.81초 대 색인 조회 0.054초. 15배

**판정: 4단으로 간다.** 기준 셋을 모두 만족했다.

## 재는 동안 고친 것

첫 색인에서 `델`이 2,444줄로 엔비디아(1,385줄)를 제치고 1위였다. 한글 별칭을 부분 문자열로
잡는 규칙이 「모델」에 걸린 것이다. 낱말 202종 중 진짜 Dell 언급은 11줄뿐이었다.
`deny`에 「모델·필라델피아·델타·나델라·시타델·램스델·델라웨어·피델리티」를 넣어 14줄로 줄였다.
같은 이유로 `인도`에 「인도네시아·본인도·요인도」 등을 넣어 157줄에서 97줄로 줄였다.
색인 전체 hit은 22,575에서 20,085로 내려갔다.

## 남은 한계

`커서`는 `deny`로 못 고친다. 88줄 중 다수가 「차이가 커서」·「분자량이 커서」처럼
크다는 뜻의 서술어이고, 진짜 Cursor 언급(「커서(Cursor) 인수」·「스페이스X/커서」)과 섞여 있다.
`deny`는 문자열을 통째로 지목하는 장치라 「앞말이 형용사면 버린다」를 적을 수 없다.
짧은 한글 이름이 흔한 서술어와 겹치는 경우가 이 규칙의 사각지대다.
4단에서 앞뒤 형태소를 보는 규칙을 붙이거나, 그 개체의 한글 별칭을 빼고 영문만 남기는 쪽으로 간다.
