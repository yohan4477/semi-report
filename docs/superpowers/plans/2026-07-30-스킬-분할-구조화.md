# 스킬 분할 + 문서 구조화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 인사이트 파이프라인을 주기별로 두 스킬로 나누고, 문서 유형(정량/논증)을 판별해 억지 원자를 막고, 구조 중복제거를 의미 기준으로 옮기고, 문장이 읽히는 한국어인지 검사하는 게이트를 붙인다.

**Architecture:** 기존 `insight-atomize` 스킬에서 문서를 가로지르는 판단(대조·승격)을 떼어 새 `insight-review` 스킬로 옮긴다. 문서 유형 판별과 구조 추출은 이미 문서를 읽는 `insight-atomizer` 에이전트가 함께 낸다. 검사는 스크립트 두 개로 갈라 둔다 — `check_atoms.py`(원자·인사이트 좌표, C1~C19)와 신설 `check_prose.py`(문체 P1~P6). 구조 기록은 `structures.py`가 검증하고, 의미 묶음은 `insight-review`가 만들어 `structure_groups.json`에 남긴다.

**Tech Stack:** Python 3.13(`py` 런처), pytest 9.1.1, 표준 라이브러리만. 마크다운 스킬·에이전트 정의.

## Global Constraints

- 저장소 루트는 `C:\Users\y\semianalysis`. 스크립트의 경로 상수는 기존 파일과 같은 형식(`import check_atoms as ca` 후 `ca.ROOT` 재사용)을 쓴다.
- 테스트는 `insights/` 디렉터리에서 실행한다: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_prose.py -q`. Bash 도구를 쓰면 `cd /c/Users/y/semianalysis/insights && PYTHONUTF8=1 py -m pytest test_prose.py -q`.
- 스타일은 `insights/test_insights.py`·`insights/test_crosscheck.py`와 같게 — 평범한 함수, 클래스 없음, 모듈 직접 import.
- 콘솔 기본 인코딩이 cp949다. 한글을 찍는 스크립트의 `main()` 첫 줄에 `sys.stdout.reconfigure(encoding='utf-8')`을 try/except로 넣는다(`crosscheck.py`가 그렇게 하고 있다). 검증 실행에는 `PYTHONIOENCODING=utf-8`을 붙인다.
- 외부 라이브러리 추가 금지. 표준 라이브러리 + pytest만.
- `.claude/skills/`는 `.gitignore`에 걸려 있으나 프로젝트 자작 스킬은 추적한다 — `git add -f`로 넣는다(`insight-atomize`가 그 선례).
- 커밋 메시지는 한국어 본문 + `feat:`/`fix:`/`docs:` 접두어, 끝에 두 줄:
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq`
- 금지어 목록(P1): `벤더`, `진영`, `커스텀 실리콘`, `헤지`, `익스포저`.
- 절 규정 순서(P6): 주장 → 그래서 무엇이 달라지나 → 되돌릴 수 없는 지점 → 근거 → 조건 충돌 → 아직 모르는 것 → 검토 후 무관.
- 문서 유형 판별 문턱(②): 뽑은 원자 중 `value` 없는 비율이 **40%를 넘으면** 추출 중단.
- 구조 승격 문턱(③): 묶음의 `members`가 **2편 이상**일 때만 후보. 자동 좌표 변경은 하지 않는다.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `insights/check_prose.py` (신규) | 문체 게이트 P1~P6. 위반 지점만 찍고 고치지 않는다 |
| `insights/views/glossary.json` (신규) | P2가 보는 용어 사전. 여기 없는 용어는 검사하지 않는다 |
| `insights/test_prose.py` (신규) | `check_prose.py` 단위 테스트. 픽스처는 함수 안에서 문자열로 만든다 |
| `insights/structures.py` (수정) | `kind_of_doc`·`thesis`·`legacy_atoms` 검증 추가 |
| `insights/views/structures.json` (수정) | `[260313]`에 `kind_of_doc: argument`·`thesis`·`legacy_atoms` 추가 |
| `insights/views/structure_groups.json` (신규) | 의미가 같다고 판정한 구조 묶음. `insight-review`가 쓴다 |
| `.claude/agents/insight-atomizer.md` (수정) | 문서 유형 판별 + 구조 추출을 정의에 넣는다 |
| `.claude/skills/insight-atomize/SKILL.md` (수정) | 대조 단계 제거, 구조 추출 추가, 보고에 다음 단계 안내 |
| `.claude/skills/insight-review/SKILL.md` (신규) | 여러 문서용 절차 — 검사·대조·의미 중복제거·보고 |
| `docs/superpowers/specs/2026-07-30-스킬-분할-구조화-design.md` (수정) | 완주 실측 기록 |

Task 1(게이트 FAIL 규칙) → Task 2(게이트 WARN 규칙·CLI) → Task 3(구조 기록 검증 확장) → Task 4(에이전트 정의) → Task 5(스킬 둘) → Task 6(완주 검증) 순서. 1~3은 코드라 테스트가 있고, 4~5는 정의 파일이라 Task 6의 완주가 그 검증이다.

---

### Task 1: 문체 게이트 — P1 금지어 · P2 용어 사전

기계가 확실히 판정하는 두 규칙만 먼저. FAIL 판정이라 오탐이 나면 아프므로 사전 기반으로 좁게 만든다.

**Files:**
- Create: `insights/check_prose.py`
- Create: `insights/views/glossary.json`
- Create: `insights/test_prose.py`

**Interfaces:**
- Consumes: `check_atoms.ROOT`, `check_atoms.SYNTH` (기존 상수).
- Produces:
  - `strip_refs(text: str) -> str` — 원자 id 인용과 frontmatter를 떨어낸 서술만.
  - `sentences(text: str) -> list[str]`
  - `load_glossary() -> dict[str, str]`
  - `check_banned(text, where) -> None` / `check_glossary(text, where, gloss) -> None` — `findings`에 append.
  - `findings: list[tuple[str, str, str, str]]` — `(level, where, rule, msg)`. `check_atoms.py`와 같은 모양이다.
  - Task 2의 CLI와 Task 3이 이 이름들을 쓴다.

- [ ] **Step 1: 용어 사전을 만든다**

`insights/views/glossary.json` 신규. 값은 그 용어를 풀어 쓴 말이다.

```json
{
  "_note": "P2가 보는 용어 사전. 여기 있는 용어는 인사이트 본문 첫 등장 문장에 괄호 설명이나 풀어쓴 말이 있어야 한다. 사전에 없는 용어는 검사하지 않는다 — 사전을 늘리는 것이 규칙을 늘리는 방법이다.",
  "공랭": "공기로 식히는 방식",
  "액체 냉각": "칩에 물을 직접 흘려 식히는 방식",
  "칠러": "냉각수를 강제로 식히는 냉동기",
  "드라이쿨러": "냉동기 없이 바깥 공기로만 식히는 장치",
  "CDU": "냉각수를 랙마다 나눠 주는 장치",
  "RDHx": "랙 뒤에 붙이는 열교환기",
  "TDP": "발열 한도",
  "SKU": "같은 세대의 별도 제품",
  "PUE": "전체 전력",
  "랙 전력밀도": "랙 하나가 먹는 전력",
  "HBM": "여러 층 쌓은 메모리",
  "열저항": "열이 빠져나가는 것을 막는 저항",
  "최선단 공정": "가장 앞선 미세 공정",
  "캐파": "생산능력",
  "콜로케이션": "데이터센터를 지어 임대하는 회사",
  "네오클라우드": "데이터센터를 지어 임대하는 회사",
  "전환률": "넘어간 비율"
}
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`insights/test_prose.py` 신규:

```python
import check_prose as cp


def _run(fn, text, gloss=None):
    cp.findings.clear()
    if gloss is None:
        fn(text, 'x.md')
    else:
        fn(text, 'x.md', gloss)
    return [(f[0], f[2]) for f in cp.findings]


def test_strip_refs_removes_atom_ids():
    t = '공랭은 41kW에서 막힌다(A-250214-07). 다음(A-260226-05, A-260226-08).'
    out = cp.strip_refs(t)
    assert 'A-250214-07' not in out
    assert 'A-260226-05' not in out
    assert '공랭은 41kW에서 막힌다' in out


def test_strip_refs_removes_frontmatter():
    t = '---\nview: stack\natoms: [A-1]\n---\n\n## 주장\n**x**\n'
    out = cp.strip_refs(t)
    assert 'view: stack' not in out
    assert '## 주장' in out


def test_sentences_splits_korean():
    s = cp.sentences('첫 문장이다. 둘째 문장이다! 셋째?')
    assert len(s) == 3


def test_banned_word_fails():
    out = _run(cp.check_banned, '냉각 방식이 벤더별로 갈라진다.')
    assert ('FAIL', 'P1') in out


def test_no_banned_word_passes():
    out = _run(cp.check_banned, '냉각 방식이 장비를 만드는 회사별로 갈라진다.')
    assert out == []


def test_glossary_term_without_gloss_fails():
    out = _run(cp.check_glossary, '공랭은 41kW에서 막힌다.', {'공랭': '공기로 식히는 방식'})
    assert ('FAIL', 'P2') in out


def test_glossary_term_with_paren_passes():
    out = _run(cp.check_glossary, '공랭(공기로 식히는 방식)은 41kW에서 막힌다.',
               {'공랭': '공기로 식히는 방식'})
    assert out == []


def test_glossary_term_with_plain_words_passes():
    # 괄호가 용어 뒤가 아니라 앞에 붙는 형태도 통과해야 한다 — 발열 한도(TDP)
    out = _run(cp.check_glossary, '칩 한 장의 발열 한도(TDP)가 올랐다.', {'TDP': '발열 한도'})
    assert out == []


def test_glossary_checks_first_occurrence_only():
    # 첫 등장에서 풀었으면 이후 등장은 그냥 써도 된다
    t = '공랭(공기로 식히는 방식)은 막힌다. 공랭 물량은 남아 있다.'
    out = _run(cp.check_glossary, t, {'공랭': '공기로 식히는 방식'})
    assert out == []
```

- [ ] **Step 3: 실패를 확인한다**

Run: `cd /c/Users/y/semianalysis/insights && PYTHONUTF8=1 py -m pytest test_prose.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'check_prose'`

- [ ] **Step 4: `check_prose.py`의 P1·P2를 쓴다**

`insights/check_prose.py` 신규:

```python
# 문체 게이트 — 인사이트 문장이 읽히는 한국어인지 검사한다. 윤문은 하지 않는다.
# 설계: docs/superpowers/specs/2026-07-30-스킬-분할-구조화-design.md ④
# 규칙이 이미 스펙 문체 절에 표로 있어 결정론적으로 검사할 수 있다 — 에이전트 호출 0회.
import os, io, re, json, glob, sys
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import check_atoms as ca

GLOSSARY = os.path.join(ca.ROOT, 'insights', 'views', 'glossary.json')

# 용어가 아니라 문장을 망가뜨리는 것들 — 어느 회사인지 모르면 문장이 성립하지 않고,
# 다른 분야 비유는 이 문서의 용어가 아니다
BANNED = ['벤더', '진영', '커스텀 실리콘', '헤지', '익스포저']

REF = re.compile(r'\(\s*A-\d{6}-\d{2}(?:\s*,\s*A-\d{6}-\d{2})*\s*\)')
FM = re.compile(r'^---\n.*?\n---\n', re.DOTALL)

findings = []


def add(level, where, rule, msg):
    findings.append((level, where, rule, msg))


def strip_refs(text):
    """원자 id 인용과 frontmatter를 떨어낸 서술만 남긴다 — 검사 대상은 사람이 쓴 문장이다."""
    text = FM.sub('', text or '')
    return REF.sub('', text)


def sentences(text):
    parts = re.split(r'(?<=[.!?])\s+|\n+', text or '')
    return [p.strip() for p in parts if p.strip()]


def load_glossary():
    if not os.path.exists(GLOSSARY):
        return {}
    return {k: v for k, v in json.load(io.open(GLOSSARY, encoding='utf-8')).items()
            if not k.startswith('_')}


def check_banned(text, where):
    for w in BANNED:
        if w in text:
            add('FAIL', where, 'P1', '금지어 "%s" — 어느 회사인지 밝히거나 뜻을 그대로 쓴다' % w)


def check_glossary(text, where, gloss):
    """사전 용어는 첫 등장 문장에서 풀어야 한다. 둘째 등장부터는 그냥 쓴다."""
    for term, plain in sorted(gloss.items(), key=lambda kv: -len(kv[0])):
        first = None
        for s in sentences(text):
            if term in s:
                first = s
                break
        if first is None:
            continue
        glossed = re.search(re.escape(term) + r'\s*\(', first) or (plain in first)
        if not glossed:
            add('FAIL', where, 'P2',
                '"%s" 첫 등장에 설명이 없다 — %s(%s) 형태로 풀거나 "%s"를 함께 쓴다'
                % (term, term, plain, plain))
```

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

Run: `cd /c/Users/y/semianalysis/insights && PYTHONUTF8=1 py -m pytest test_prose.py -q`
Expected: PASS (9 passed)

- [ ] **Step 6: 커밋**

```bash
cd /c/Users/y/semianalysis
git add insights/check_prose.py insights/test_prose.py insights/views/glossary.json
git commit -F - <<'EOF'
feat(insights): 문체 게이트 P1·P2 — 금지어·용어 사전

- P1 금지어(벤더·진영·커스텀 실리콘·헤지·익스포저) FAIL. 용어 문제가 아니라
  문장이 성립하지 않는 문제라 FAIL로 둔다
- P2 사전 용어는 첫 등장 문장에서 괄호로 풀거나 풀어쓴 말을 함께 써야 함.
  둘째 등장부터는 그냥 쓴다
- views/glossary.json으로 사전 관리 — 여기 없는 용어는 검사하지 않는다
- strip_refs로 원자 id 인용과 frontmatter를 떨어내고 사람이 쓴 문장만 검사
- 테스트 9개

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 2: 문체 게이트 — P3~P6 WARN 규칙 · CLI

문장에 따라 정당할 수 있는 것들이라 WARN이다. 그리고 실제 인사이트 5건에 돌려 본다.

**Files:**
- Modify: `insights/check_prose.py`
- Modify: `insights/test_prose.py`

**Interfaces:**
- Consumes: Task 1의 `strip_refs`, `sentences`, `load_glossary`, `check_banned`, `check_glossary`, `findings`, `add`.
- Produces:
  - `check_length(text, where) -> None` (P3), `check_translationese(text, where) -> None` (P4),
    `check_dup_claim(sections: dict, where) -> None` (P5), `check_order(names: list[str], where) -> None` (P6)
  - `shingles(s: str) -> set[str]` — 정규화 2-gram. P5가 쓴다.
  - `main() -> int` — `insights/synth/*.md` 전부 검사. FAIL이 있으면 1.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_prose.py`에 추가:

```python
def test_long_sentence_warns():
    out = _run(cp.check_length, '가' * 170 + '.')
    assert ('WARN', 'P3') in out


def test_two_em_dashes_warn():
    out = _run(cp.check_length, '앞이다 — 가운데다 — 뒤다.')
    assert ('WARN', 'P3') in out


def test_one_em_dash_short_sentence_passes():
    out = _run(cp.check_length, '앞이다 — 뒤다.')
    assert out == []


def test_translationese_warns():
    out = _run(cp.check_translationese, '냉각에 대한 논의가 되어진다.')
    assert ('WARN', 'P4') in out


def test_plain_korean_passes_translationese():
    out = _run(cp.check_translationese, '냉각을 어디서 하느냐가 갈린다.')
    assert out == []


def test_shingles_overlap_detects_duplicate():
    a = cp.shingles('확보 발표를 컴퓨트 증설 신호로 읽으면 안 된다')
    b = cp.shingles('확보 발표를 컴퓨트 증설 신호로 읽으면 안 된다')
    assert len(a & b) == len(a)


def test_dup_claim_warns():
    sec = {'주장': ['**확보 발표를 컴퓨트 증설 신호로 읽으면 안 된다.**'],
           '그래서 무엇이 달라지나': ['- 확보 발표를 컴퓨트 증설 신호로 읽으면 안 된다.']}
    cp.findings.clear()
    cp.check_dup_claim(sec, 'x.md')
    assert ('WARN', 'P5') in [(f[0], f[2]) for f in cp.findings]


def test_dup_claim_passes_when_different_angle():
    sec = {'주장': ['**확보 발표를 컴퓨트 증설 신호로 읽으면 안 된다.**'],
           '그래서 무엇이 달라지나': ['- 읽을 것은 웨이퍼·패키징·메모리 확보 계약이다.']}
    cp.findings.clear()
    cp.check_dup_claim(sec, 'x.md')
    assert cp.findings == []


def test_order_warns_when_evidence_before_payoff():
    cp.findings.clear()
    cp.check_order(['주장', '근거', '그래서 무엇이 달라지나'], 'x.md')
    assert ('WARN', 'P6') in [(f[0], f[2]) for f in cp.findings]


def test_order_passes_on_규정_순서():
    cp.findings.clear()
    cp.check_order(['주장', '그래서 무엇이 달라지나', '근거', '조건 충돌', '아직 모르는 것'], 'x.md')
    assert cp.findings == []
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd /c/Users/y/semianalysis/insights && PYTHONUTF8=1 py -m pytest test_prose.py -q`
Expected: FAIL — `AttributeError: module 'check_prose' has no attribute 'check_length'`

- [ ] **Step 3: P3~P6과 CLI를 넣는다**

`insights/check_prose.py` 끝에 추가:

```python
TRANSLATIONESE = ['에 대한', '되어진', '로 인해', '에 있어서', '라고 할 수 있다']
SECTION_ORDER = ['주장', '그래서 무엇이 달라지나', '되돌릴 수 없는 지점', '근거',
                 '조건 충돌', '아직 모르는 것', '검토 후 무관']
MAXLEN = 160


def check_length(text, where):
    """긴 문장과 em dash로 절을 여러 개 이은 문장 — 읽는 사람이 숨 쉴 곳이 없다."""
    for s in sentences(text):
        if len(s) > MAXLEN:
            add('WARN', where, 'P3', '문장이 %d자 — 끊는다: %s…' % (len(s), s[:40]))
        if s.count('—') >= 2:
            add('WARN', where, 'P3', 'em dash가 2개 이상 — 문장을 끊는다: %s…' % s[:40])


def check_translationese(text, where):
    for pat in TRANSLATIONESE:
        if pat in text:
            add('WARN', where, 'P4', '번역투 "%s"' % pat)


def shingles(s):
    """정규화 2-gram — 조사·부호 차이를 무시하고 문장이 겹치는지 본다."""
    s = re.sub(r'[^0-9A-Za-z가-힣]', '', s or '')
    return {s[i:i + 2] for i in range(len(s) - 1)}


def check_dup_claim(sec, where):
    """주장이 「그래서」 항목 하나를 그대로 옮겨 쓴 경우는 중복이다."""
    claim = ' '.join(sec.get('주장') or [])
    a = shingles(claim)
    if not a:
        return
    for line in sec.get('그래서 무엇이 달라지나') or []:
        b = shingles(re.sub(r'^-\s*', '', line))
        if not b:
            continue
        overlap = len(a & b) / float(min(len(a), len(b)))
        if overlap >= 0.6:
            add('WARN', where, 'P5',
                '주장과 「그래서」 항목이 %.0f%% 겹친다 — 항목을 다른 각도로 쓰거나 지운다: %s…'
                % (overlap * 100, line[:40]))


def check_order(names, where):
    idx = [SECTION_ORDER.index(n) for n in names if n in SECTION_ORDER]
    if idx != sorted(idx):
        add('WARN', where, 'P6', '절 순서가 규정과 다르다: %s' % ' → '.join(names))


def main():
    try:
        sys.stdout.reconfigure(encoding='utf-8')
    except Exception:
        pass
    gloss = load_glossary()
    files = sorted(glob.glob(os.path.join(ca.SYNTH, '*.md')))
    for p in files:
        where = os.path.basename(p)
        raw = io.open(p, encoding='utf-8').read()
        body = strip_refs(raw)
        meta, mdbody = ca.parse_synth(raw)
        sec = ca.sections(strip_refs(raw)) if meta else ca.sections(body)
        names = [n for n in sec.keys()]
        check_banned(body, where)
        check_glossary(body, where, gloss)
        check_length(body, where)
        check_translationese(body, where)
        check_dup_claim(sec, where)
        check_order(names, where)

    for level, where, rule, msg in findings:
        print('%s %s [%s] %s' % (level, where, rule, msg))
    fails = sum(1 for f in findings if f[0] == 'FAIL')
    warns = len(findings) - fails
    per = {}
    for f in findings:
        per[f[1]] = per.get(f[1], 0) + (0 if f[0] == 'FAIL' else 1)
    print('요약: 인사이트 %d건 / FAIL %d / WARN %d' % (len(files), fails, warns))
    heavy = [k for k, v in per.items() if v > 5]
    if heavy:
        print('WARN 5건 초과: %s — 이 파일은 humanize-korean 스킬을 부를 계기다'
              % ', '.join(heavy))
    return 1 if fails else 0


if __name__ == '__main__':
    sys.exit(main())
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd /c/Users/y/semianalysis/insights && PYTHONUTF8=1 py -m pytest test_prose.py -q`
Expected: PASS (19 passed)

- [ ] **Step 5: 실제 인사이트 5건에 돌린다**

Run: `cd /c/Users/y/semianalysis && py insights/check_prose.py`
Expected: 종료코드 0(FAIL 0). 금지어는 이미 제거됐고 용어도 풀려 있다.

**FAIL이 나오면**: 그 지점을 고치되 **용어를 지우지 말고 괄호로 푼다**(이 저장소의 확정 규칙이다). WARN은 목록만 보고에 적고 고치지 않는다 — 정당한 경우가 섞여 있다.

- [ ] **Step 6: 커밋**

```bash
cd /c/Users/y/semianalysis
git add insights/check_prose.py insights/test_prose.py insights/synth
git commit -F - <<'EOF'
feat(insights): 문체 게이트 P3~P6 · CLI

- P3 문장 160자 초과·em dash 2개 이상 WARN
- P4 번역투(에 대한·되어진·로 인해·에 있어서·라고 할 수 있다) WARN
- P5 주장과 「그래서」 항목의 2-gram 겹침 60% 이상이면 중복 WARN
- P6 절 순서 위반 WARN
- CLI: synth 전부 검사, WARN이 한 파일에 5건을 넘으면 humanize-korean을
  부를 계기라고 찍는다
- 테스트 19개, 인사이트 5건에서 FAIL 0

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 3: 구조 기록에 문서 유형 넣기

`structures.py`가 `kind_of_doc`·`thesis`·`legacy_atoms`를 검증하고, `[260313]`을 논증 문서로 표시한다.

**Files:**
- Modify: `insights/structures.py`
- Modify: `insights/views/structures.json`
- Create: `insights/test_structures.py`

**Interfaces:**
- Consumes: `structures.py`의 기존 `load()`, `validate(data, man_ids=None)`, `norm_label`, `jaccard`, `dedupe`.
- Produces: `validate(data, man_ids=None, atom_counts=None) -> list[str]` — 인자 하나 추가. `atom_counts`는 `{source_id: 원자 수}`. `main()`이 `ca.load_atoms()`로 만들어 넘긴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_structures.py` 신규:

```python
import structures as st


def _doc(sid='semi:a:b', **kw):
    d = {'source_id': sid, 'path': 'x.md', 'structures': []}
    d.update(kw)
    return d


def test_kind_of_doc_invalid_value():
    errs = st.validate({'docs': [_doc(kind_of_doc='essay')]})
    assert any('kind_of_doc' in e for e in errs)


def test_argument_requires_thesis():
    errs = st.validate({'docs': [_doc(kind_of_doc='argument')]})
    assert any('thesis' in e for e in errs)


def test_thesis_requires_line_and_text():
    errs = st.validate({'docs': [_doc(kind_of_doc='argument',
                                      thesis={'claim': '병목이 옮겨갔다'})]})
    assert any('line' in e for e in errs)


def test_argument_with_atoms_fails():
    # 논증 문서로 판정했으면 원자를 만들지 않는다 — thesis가 원자의 우회로가 되면 안 된다
    data = {'docs': [_doc(kind_of_doc='argument',
                          thesis={'line': 1, 'claim': 'x', 'line_text': 'y'})]}
    errs = st.validate(data, atom_counts={'semi:a:b': 13})
    assert any('원자' in e for e in errs)


def test_argument_with_atoms_passes_when_legacy():
    data = {'docs': [_doc(kind_of_doc='argument', legacy_atoms=True,
                          thesis={'line': 1, 'claim': 'x', 'line_text': 'y'})]}
    errs = st.validate(data, atom_counts={'semi:a:b': 13})
    assert errs == []


def test_quantitative_with_atoms_passes():
    data = {'docs': [_doc(kind_of_doc='quantitative')]}
    assert st.validate(data, atom_counts={'semi:a:b': 13}) == []


def test_missing_kind_of_doc_defaults_to_quantitative():
    assert st.validate({'docs': [_doc()]}, atom_counts={'semi:a:b': 5}) == []
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd /c/Users/y/semianalysis/insights && PYTHONUTF8=1 py -m pytest test_structures.py -q`
Expected: FAIL — `test_argument_requires_thesis` 등이 빈 오류 목록을 받아 실패한다

- [ ] **Step 3: `validate`를 확장한다**

`insights/structures.py`의 `validate` 함수를 아래로 교체한다:

```python
KINDS_OF_DOC = ('quantitative', 'argument')


def validate(data, man_ids=None, atom_counts=None):
    """구조 기록 자체의 무결성. 반환: (오류 목록)"""
    errs = []
    seen = set()
    atom_counts = atom_counts or {}
    for d in data.get('docs', []):
        sid = d.get('source_id')
        if not sid:
            errs.append('source_id 없음')
            continue
        if sid in seen:
            errs.append('%s: 같은 문서가 두 번' % sid)
        seen.add(sid)
        if man_ids is not None and sid not in man_ids:
            errs.append('%s: manifest에 없는 source_id' % sid)

        kod = d.get('kind_of_doc', 'quantitative')
        if kod not in KINDS_OF_DOC:
            errs.append('%s: kind_of_doc이 %s 중 하나여야 함 (%s)'
                        % (sid, '|'.join(KINDS_OF_DOC), kod))
        if kod == 'argument':
            th = d.get('thesis') or {}
            if not th:
                errs.append('%s: argument 문서는 thesis가 있어야 한다' % sid)
            else:
                if not isinstance(th.get('line'), int):
                    errs.append('%s: thesis에 line(원문 줄 번호)이 없다' % sid)
                if not (th.get('claim') or '').strip():
                    errs.append('%s: thesis에 claim이 없다' % sid)
                if not (th.get('line_text') or '').strip():
                    errs.append('%s: thesis에 line_text(그 줄 원문)가 없다' % sid)
            # thesis가 원자의 우회로가 되지 않게 — 소급분(legacy_atoms)만 예외
            if atom_counts.get(sid) and not d.get('legacy_atoms'):
                errs.append('%s: argument 문서인데 원자가 %d개 있다 — 둘 중 하나만 둔다'
                            % (sid, atom_counts[sid]))

        for stx in d.get('structures', []):
            where = '%s / %s' % (sid, stx.get('name', '?'))
            if stx.get('kind') not in KINDS:
                errs.append('%s: kind가 %s 중 하나여야 함 (%s)'
                            % (where, '|'.join(KINDS), stx.get('kind')))
            items = stx.get('steps') if stx.get('kind') == 'process' else stx.get('levels')
            if not items or len(items) < 2:
                errs.append('%s: 항목이 2개 미만 — 구조가 아니다' % where)
            if not isinstance(stx.get('line'), int):
                errs.append('%s: line(원문 줄 번호)이 없다' % where)
            if len({norm_label(x) for x in (items or [])}) != len(items or []):
                errs.append('%s: 항목 라벨 중복' % where)
    return errs
```

그리고 `main()`에서 원자 수를 만들어 넘긴다 — `errs = validate(data, man_ids)` 줄을 아래로 바꾼다:

```python
    counts = {}
    for a in ca.load_atoms():
        counts[a['_source_id']] = counts.get(a['_source_id'], 0) + 1
    errs = validate(data, man_ids, counts)
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd /c/Users/y/semianalysis/insights && PYTHONUTF8=1 py -m pytest test_structures.py -q`
Expected: PASS (7 passed)

- [ ] **Step 5: `[260313]`을 논증 문서로 표시한다**

`insights/views/structures.json`에서 `AI-실리콘-대란`이 들어간 `source_id`의 문서 항목을 찾아 세 필드를 추가한다. `thesis`의 `line`·`line_text`는 원문에서 직접 확인해 넣는다:

```bash
cd /c/Users/y/semianalysis
PYTHONIOENCODING=utf-8 py -c "import io,json;[print(s['id'],'||',s['path']) for s in json.load(io.open('insights/manifest.json',encoding='utf-8'))['sources'] if '260313' in s.get('path','')]"
PYTHONIOENCODING=utf-8 grep -nE "병목|관문" "content/newsletter/ai_infra/compute/[260313] AI 실리콘 대란 - TSMC N3부터 메모리까지.md" | head -8
```

그 줄 번호와 줄 원문을 그대로 넣는다(공백만 제거):

```json
    {
      "source_id": "<manifest의 id>",
      "path": "<manifest의 path>",
      "kind_of_doc": "argument",
      "legacy_atoms": true,
      "thesis": {
        "line": <확인한 줄 번호>,
        "claim": "병목이 전력에서 가속기 실리콘으로 넘어갔다 — 관문이 데이터센터 건설에서 칩 생산으로 옮겨간 구조적 전환이다",
        "line_text": "<그 줄 원문 그대로>"
      },
      "structures": []
    }
```

- [ ] **Step 6: 검증**

Run: `cd /c/Users/y/semianalysis && PYTHONIOENCODING=utf-8 py insights/structures.py`
Expected: `오류` 절이 나오지 않고, 요약에 문서 8편 / 구조 38개가 그대로. `legacy_atoms`가 붙었으므로 원자 13개가 있어도 통과해야 한다.

- [ ] **Step 7: 커밋**

```bash
cd /c/Users/y/semianalysis
git add insights/structures.py insights/test_structures.py insights/views/structures.json
git commit -F - <<'EOF'
feat(insights): 구조 기록에 문서 유형 — 논증 문서는 원자 대신 thesis

원자 111개 중 [260313] 실리콘 대란만 무수치 54%이고 구조 추출도 0개였다.
하나의 논증을 13조각으로 부수면 논증이 사라진다.

- validate에 kind_of_doc(quantitative|argument) 검증 추가. argument면
  thesis(line·claim·line_text) 필수 — 원자와 같은 원문 환원 기준
- argument 문서에 원자가 있으면 오류. thesis가 원자의 우회로가 되는 것을
  막는다. legacy_atoms: true인 소급분([260313])만 예외
- structures.json에 [260313]을 argument·legacy_atoms·thesis로 표시.
  원자는 소급 삭제하지 않는다 — 원문이 안 바뀌면 원자도 안 바뀐다
- 테스트 7개

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 4: insight-atomizer 에이전트 — 문서 유형 판별 + 구조 추출

추출 에이전트가 문서를 이미 읽으므로 원자·구조·유형 판정을 한 번에 낸다.

**Files:**
- Modify: `.claude/agents/insight-atomizer.md`

**Interfaces:**
- Consumes: Task 3의 `structures.py` 검증 규칙(`kind_of_doc`·`thesis`·`legacy_atoms`), `insights/views/structures.json` 형식.
- Produces: 에이전트가 `insights/atoms/<YYMMDD>-<슬러그>.json`과 `insights/views/structures.json`의 문서 항목을 함께 낸다.

- [ ] **Step 1: 정의에 「문서 유형 판별」 절을 넣는다**

`.claude/agents/insight-atomizer.md`의 `## 산출물` 절 **바로 앞**에 추가:

```markdown
## 먼저 판정: 이 문서가 원자화 대상인가

문서를 훑고 유형을 정한다. 판정을 보고에 반드시 적는다.

- **정량 문서(`quantitative`)** — 수치와 측정 조건이 본문에 반복해서 나온다. 원자화한다
- **논증 문서(`argument`)** — 하나의 주장을 여러 절로 밀고 나가며 수치는 예시로만 나온다. **원자를 만들지 않는다.** 대신 구조와 핵심 주장 한 줄만 기록한다

**기계 지표**: 원자를 뽑다가 `value`가 없는 비율이 **40%를 넘으면** 거기서 멈추고 논증 문서로 판정해 보고한다. 억지로 수치를 붙이지 않는다.

실측 사례: `[260313] AI 실리콘 대란`은 무수치 54%였고 구조도 0개였다. "관문이 전력에서 실리콘으로 넘어갔다"는 하나의 논증이라 13조각으로 부수면 논증이 사라진다.

논증 문서로 판정하면 원자 파일을 만들지 말고 `insights/views/structures.json`에만 기록한다:

```json
{
  "source_id": "<manifest의 id>",
  "path": "<manifest의 path>",
  "kind_of_doc": "argument",
  "thesis": { "line": 412, "claim": "한 문장", "line_text": "<그 줄 원문>" },
  "structures": []
}
```
```

- [ ] **Step 2: 정의에 「구조 추출」 절을 넣는다**

같은 파일에서 `## 완료 조건` 절 **바로 앞**에 추가:

```markdown
## 구조 추출 — 문서가 자기 본문에 갖고 있는 계층·순서

원자와 함께 낸다. 문서를 두 번 읽지 않는다.

기록처는 `insights/views/structures.json`의 `docs` 배열에 이 문서 항목 하나를 추가하는 것이다(기존 항목은 건드리지 않는다).

```json
{
  "source_id": "<manifest의 id>",
  "path": "<manifest의 path>",
  "kind_of_doc": "quantitative",
  "structures": [
    { "kind": "hierarchy", "name": "모듈러 3층 구조", "line": 90,
      "levels": ["부지", "셸", "시스템"], "note": "프리팹과 모듈러를 가르는 기준" },
    { "kind": "process", "name": "모듈화 사이클 5단계", "line": 361,
      "steps": ["설계", "문서화", "조립", "운송", "현장 커미셔닝"], "note": "공장에서 현장까지" }
  ]
}
```

- `kind`는 `hierarchy`(순서 없는 분류·층) 또는 `process`(시간·공정 순서). **헷갈리면 hierarchy**
- 항목이 2개 미만이면 구조가 아니다 — 기록하지 않는다
- `line`은 그 구조가 제시된 줄 번호(절 제목 줄이면 그 줄)
- **문서가 실제로 열거한 것만.** 그럴듯한 구조를 만들어 넣지 않는다. 열거가 없고 서술만 있으면 `"structures": []`
- 라벨은 문서 표현을 그대로 짧게. 의역·영문 번역 금지
- 찾는 방법: `grep -nE "^#{1,3} " <파일>`로 절 제목을 훑어 "N단계·3층·분류체계·사다리·사이클·N가지·N모델" 같은 표현이 있는 절만 부분 읽기
```

- [ ] **Step 3: 완료 조건과 보고를 갱신한다**

같은 파일의 `## 완료 조건` 절 코드 블록 뒤에 한 줄, `## 보고` 절은 통째로 교체한다:

완료 조건에 추가:

```markdown
구조를 기록했으면 이것도 오류 0건이어야 한다:

```bash
PYTHONIOENCODING=utf-8 py C:\Users\y\semianalysis\insights\structures.py
```
```

`## 보고` 교체:

```markdown
## 보고

- **문서 유형 판정**(`quantitative`/`argument`)과 그 근거 — 무수치 비율, 수치가 예시로만 쓰이는 대목
- 원자 수, 노드별 분포, `조건 명시 없음` 비율
- 기록한 구조 개수(계층/프로세스 구분)와 이름, 구조가 0개면 그 이유
- 판독 불가로 생략한 대목
- `check_atoms.py`·`structures.py` 요약 줄
```

- [ ] **Step 4: frontmatter 키 구성이 그대로인지 확인한다**

Run:
```bash
cd /c/Users/y/semianalysis && PYTHONIOENCODING=utf-8 py -c "import io,re;[print(f, sorted(re.findall(r'^(\w+):', re.match(r'^---\n(.*?)\n---\n', io.open(f,encoding='utf-8').read(), re.S).group(1), re.M))) for f in ('.claude/agents/semianalysis-transformer.md','.claude/agents/insight-atomizer.md')]"
```
Expected: 두 줄 모두 `['description', 'model', 'name', 'tools']`

- [ ] **Step 5: 커밋**

```bash
cd /c/Users/y/semianalysis
git add .claude/agents/insight-atomizer.md
git commit -F - <<'EOF'
feat(agents): insight-atomizer에 문서 유형 판별 + 구조 추출

- 먼저 판정: 정량 문서는 원자화, 논증 문서는 원자를 만들지 않고 thesis
  한 줄만. 무수치 40% 넘으면 거기서 멈추고 논증 문서로 보고
- 구조 추출을 원자와 한 번에 — 문서를 두 번 읽지 않는다. 계층/프로세스
  구분, 항목 2개 미만은 기록하지 않음, 문서가 열거한 것만
- 완료 조건에 structures.py 오류 0건 추가, 보고에 유형 판정 근거 추가

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 5: 스킬 둘 — insight-atomize 축소, insight-review 신설

**Files:**
- Modify: `.claude/skills/insight-atomize/SKILL.md`
- Create: `.claude/skills/insight-review/SKILL.md`

**Interfaces:**
- Consumes: Task 1~2의 `check_prose.py`, Task 3의 `structures.py`, 기존 `crosscheck.py`, Task 4의 에이전트.
- Produces: `/insight-atomize`(문서 1편), `/insight-review`(여러 문서). 후자는 `insights/views/structure_groups.json`을 쓴다.

- [ ] **Step 1: `insight-atomize`에서 대조 단계를 뗀다**

`.claude/skills/insight-atomize/SKILL.md`의 `## 5. 대조` 절 전체를 아래로 교체한다(6단계 제목은 `## 5. 커밋·보고`으로 바꾼다):

```markdown
## 5. 커밋·보고

원자 파일·`views/structures.json`·`views/process.json`·`views/actors.json`·`manifest.json`을 한 커밋으로. 메시지의 수치는 `check_atoms.py` 출력에서 그대로 복사한다(눈으로 세지 말 것).

```bash
git add insights/atoms/<파일> insights/views/structures.json insights/views/process.json insights/views/actors.json insights/manifest.json
git commit -F - <<'EOF'
feat(insights): <문서 제목> 원자화 — 원자 N개

- 문서 유형: quantitative
- 노드별: ...
- 프로세스 단계 배정 N개 / 미배정 N개
- 구조 N개(계층 N·프로세스 N)
- 검사기 FAIL 0

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: <세션 URL>
EOF
git push origin main
```

보고에 넣을 것: 문서 유형 판정과 근거, 원자 수·노드 분포·미배정 수, `조건 명시 없음` 비율, 기록한 구조, 그리고 **다음 단계 안내** — 아래 조건 중 하나면 `insight-review`를 돌려야 한다고 적는다.

```bash
PYTHONIOENCODING=utf-8 py insights/check_atoms.py 2>&1 | grep -c "C11"
```

- 직전 리뷰 이후 원자화된 문서가 3편 이상
- 위 명령의 STALE(C11) WARN이 1건 이상
- 인사이트를 새로 쓰거나 고치기 직전

**대조(STALE·충돌 후보·뭉침)와 좌표 승격 판단은 이 스킬의 일이 아니다.** `insight-review`가 한다.
```

- [ ] **Step 2: `insight-atomize`의 2단계에 구조 추출을 명시한다**

같은 파일 `## 2. 원자 추출 — 에이전트 1회` 절의 제목과 첫 문단을 교체한다:

```markdown
## 2. 원자·구조 추출 — 에이전트 1회

`insight-atomizer` 에이전트를 **한 번** 호출한다. 프롬프트는 짧게: 소스 id, 경로, hash, 발행일, 그리고 이 문서에서 특히 놓치지 말 주제(있으면). 스키마·규칙·문서 유형 판별 기준은 에이전트 정의에 이미 있으므로 다시 적지 않는다.

에이전트는 세 가지를 낸다 — 문서 유형 판정, 원자 파일(정량 문서일 때), `views/structures.json`의 문서 항목. **논증 문서로 판정하면 원자 파일이 없다.** 그 경우 3단계(단계 배정)를 건너뛴다.

문서 원문이 메인 대화에 들어오지 않게 하는 것이 이 분리의 목적이다. 직접 원문을 읽지 않는다.
```

- [ ] **Step 3: `insight-atomize`의 4단계에 문체 게이트를 넣는다**

같은 파일 `## 4. 검사` 절 끝에 추가:

```markdown
인사이트를 손대지 않았어도 게이트를 한 번 돌려 회귀가 없는지 본다:

```bash
PYTHONIOENCODING=utf-8 py insights/check_prose.py
```

FAIL 0이어야 한다. 여기서 FAIL이 나면 이번 회차와 무관한 기존 인사이트 문제이므로 보고에 적고 넘어간다.
```

- [ ] **Step 4: `insight-review` 스킬을 만든다**

`.claude/skills/insight-review/SKILL.md` 신규:

```markdown
---
name: insight-review
description: 문서가 쌓인 뒤 인사이트 체계를 점검한다 — 검사기·문체 게이트, 대조(STALE·충돌 후보·문서 내부 충돌·뭉침), 구조 의미 중복제거, 좌표 승격 후보 보고까지. "인사이트 리뷰해" / "쌓인 문서 점검해" / 인사이트를 새로 쓰거나 고치기 직전에 쓴다. 원자를 만들지 않고 인사이트 본문도 고치지 않는다.
---

# 인사이트 리뷰

선행 설계: `docs/superpowers/specs/2026-07-30-원자-뷰-인사이트-design.md`(체계), `2026-07-30-스킬-분할-구조화-design.md`(이 절차).

저장소 루트 `C:\Users\y\semianalysis`. 콘솔이 cp949라 파이썬 실행에는 `PYTHONIOENCODING=utf-8`을 붙인다.

## 언제 도나 (셋 중 하나)

- 직전 리뷰 이후 원자화된 문서가 **3편 이상**
- `check_atoms.py`의 **STALE(C11) WARN이 1건 이상**
- **인사이트를 새로 쓰거나 고치기 직전** — 문서 수와 무관하게 돈다. 충돌 후보를 모르고 쓰면 C9가 나중에 FAIL을 낸다

## 1. 검사

```bash
PYTHONIOENCODING=utf-8 py insights/check_atoms.py
PYTHONIOENCODING=utf-8 py insights/check_prose.py
```

둘 다 FAIL 0이어야 한다. `check_atoms.py`가 FAIL이면 여기서 멈추고 원자화 쪽 문제로 보고한다(줄 번호를 손으로 맞추지 말고 그 문서를 재추출한다). `check_prose.py`가 FAIL이면 **용어를 지우지 말고 괄호로 풀어** 고친다.

## 2. 대조

```bash
PYTHONIOENCODING=utf-8 py insights/crosscheck.py
```

네 가지가 나온다.

- **뭉침** — 원자 10개 이상인 칸. 한 문서가 60%를 넘으면 쪼개지 않는다(그 문서의 목차다). 비중이 흩어진 칸만 하위 단계 후보
- **STALE 인사이트** — 처리 4갈래: 뒷받침(`atoms:`에 id 추가) / 조건 다름(`## 조건 충돌` 갱신) / 뒤집음(`## 주장` 재작성, 이전 판단은 무너진 이유와 함께 보존) / 무관(`dismissed:` + `## 검토 후 무관` 절)
- **문서 내부 충돌** — 같은 문서 안에서 같은 단위·다른 조건인 쌍. 이 체계를 만든 5.4배 사고가 이 유형이다
- **충돌 후보** — 다른 문서와의 쌍. 한 인사이트에서 함께 인용하면 C9가 FAIL

## 3. 구조 의미 중복제거

```bash
PYTHONIOENCODING=utf-8 py insights/structures.py
```

기록 검증(오류 0건)과 1차 겹침(라벨 Jaccard)을 본다. **라벨 겹침은 거의 0으로 나온다** — 실측에서 구조 38개 중 0쌍이었고 문턱 0.15까지 낮춰도 0이었다. 리포트마다 자기 어휘로 틀을 만들기 때문이다. 그래서 의미 묶기는 직접 한다.

`insights/views/structures.json`을 **전수 읽는다**(라벨만 담아 짧다). 같은 것을 말하는 구조를 묶어 `insights/views/structure_groups.json`에 쓴다.

```json
{
  "note": "의미가 같다고 판정한 구조 묶음. members 2편 이상이면 좌표 승격 후보 — 실제 승격은 사람이 스펙 개정으로 한다",
  "groups": [
    {
      "name": "제작·조립 로드맵",
      "kind": "process",
      "members": [
        {"source_id": "...", "name": "모듈화 사이클 5단계"},
        {"source_id": "...", "name": "EMIB-T 범프 피치 로드맵"}
      ],
      "shared_order": ["설계", "제작", "검증", "현장 반영"],
      "note": "라벨은 다르지만 공장에서 현장까지의 순서라는 점이 같다",
      "promote": false,
      "promote_note": "대상 층이 달라(건물 vs 패키지) 공통 단계로 올리기엔 이르다"
    }
  ]
}
```

**규칙**
- **멱등**: 기존 묶음은 유지하고 새 구조만 배치한다. 묶음 이름을 바꾸는 것은 스펙 개정으로 본다
- `members`가 **2편 이상**이어야 승격 후보다. 단독 구조는 그 문서의 목차이므로 묶음을 만들지 않는다
- 순서가 없는 `hierarchy` 묶음은 단계로 승격하지 않는다. 분류는 서술로 쓴다
- `promote: true`는 **표시만** 한다. 실제 좌표 변경(하위 단계 추가 등)은 사람이 스펙 개정으로 한다

## 4. 보고

- `check_atoms.py`·`check_prose.py` 요약 줄. `check_prose.py`가 한 파일에 WARN 5건을 넘겼으면 그 파일은 `humanize-korean` 스킬을 부를 계기다
- STALE 인사이트 목록과 각각 4갈래 중 어느 쪽으로 보이는지
- 충돌 후보·문서 내부 충돌 중 눈에 걸리는 쌍
- 뭉침 중 승격 후보(독점 60% 미만)
- 구조 묶음 중 `members` 2편 이상인 것
- 원자 인용률 — 인사이트나 무관 처리에 한 번이라도 등장한 원자 비율. 과잉 추출 지표다

```bash
PYTHONIOENCODING=utf-8 py -c "import io,json,glob,re;cited=set();[cited.update(re.findall(r'A-\d{6}-\d{2}',io.open(p,encoding='utf-8').read())) for p in glob.glob('insights/synth/*.md')];ids=[a['id'] for f in glob.glob('insights/atoms/*.json') for a in json.load(io.open(f,encoding='utf-8'))['atoms']];print('인용 %d/%d (%.0f%%)'%(len(cited&set(ids)),len(ids),100*len(cited&set(ids))/len(ids)))"
```

## 5. 커밋

`structure_groups.json`만 커밋한다.

**고치지 않는 것**: 인사이트 본문, 원자 파일, `process.json`, 좌표 사전. 이 스킬은 판단 재료를 만들어 사람에게 넘긴다.
```

- [ ] **Step 5: 두 스킬이 등록되는지 확인한다**

Run:
```bash
cd /c/Users/y/semianalysis && PYTHONIOENCODING=utf-8 py -c "import io,re;[print(f, 'OK' if re.match(r'^---\n(.*?)\n---\n', io.open(f,encoding='utf-8').read(), re.S) else 'BAD') for f in ('.claude/skills/insight-atomize/SKILL.md','.claude/skills/insight-review/SKILL.md')]"
```
Expected: 두 줄 모두 `OK`

- [ ] **Step 6: 커밋**

```bash
cd /c/Users/y/semianalysis
git add -f .claude/skills/insight-atomize/SKILL.md .claude/skills/insight-review/SKILL.md
git commit -F - <<'EOF'
feat(skills): 주기로 분할 — insight-atomize 축소, insight-review 신설

한 스킬이 두 주기를 섞고 있었다. 추출·배정·검사는 문서 1편이 들어올 때,
대조와 좌표 승격 판단은 문서가 쌓인 뒤 하는 일이다.

- insight-atomize: 대조 단계 제거(5단계 → 커밋·보고). 2단계를 원자·구조
  동시 추출로, 4단계에 문체 게이트 추가. 보고에 insight-review 실행 조건
  세 개를 적게 함
- insight-review 신설: 검사(check_atoms·check_prose) → 대조(crosscheck)
  → 구조 의미 중복제거(structure_groups.json) → 보고. 원자를 만들지 않고
  인사이트 본문·process.json도 고치지 않는다
- 라벨 겹침이 거의 0으로 나오는 이유(리포트마다 자기 어휘)를 스킬에 적어
  다음 사람이 버그로 오해하지 않게 함

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 6: 완주 검증 — 구조 묶음 첫 생성 + 리뷰 절차 한 바퀴

**Files:**
- Create: `insights/views/structure_groups.json`
- Modify: `docs/superpowers/specs/2026-07-30-스킬-분할-구조화-design.md`

**Interfaces:**
- Consumes: Task 1~5 전부.
- Produces: 구조 38개에 대한 첫 묶음 파일. 스펙의 완주 실측 기록.

- [ ] **Step 1: `insight-review` 절차를 처음부터 끝까지 돈다**

`insight-review` 스킬을 호출한다. 1~4단계를 그대로 따르고, 3단계에서 `structures.json`의 구조 38개를 전수 읽어 `structure_groups.json`을 만든다.

- [ ] **Step 2: 묶음 결과를 확인한다**

Run: `cd /c/Users/y/semianalysis && PYTHONIOENCODING=utf-8 py -c "import io,json;d=json.load(io.open('insights/views/structure_groups.json',encoding='utf-8'));g=d['groups'];print('묶음 %d개'%len(g));[print(' ',x['kind'],x['name'],'members',len(x['members']),'promote',x['promote']) for x in g]"`

Expected: 묶음 1개 이상, 그중 `members` 2편 이상인 것이 1개 이상. **묶음이 0개면** 의미 묶기도 실패한 것이므로 멈추고 보고한다 — 그때는 구조 기록 자체를 다시 볼 문제다.

- [ ] **Step 3: 게이트·검사기 전부 통과하는지 확인한다**

Run:
```bash
cd /c/Users/y/semianalysis
PYTHONIOENCODING=utf-8 py insights/check_atoms.py | tail -3
PYTHONIOENCODING=utf-8 py insights/check_prose.py | tail -3
PYTHONIOENCODING=utf-8 py insights/structures.py | head -3
cd insights && PYTHONUTF8=1 py -m pytest -q
```
Expected: `check_atoms` FAIL 0, `check_prose` FAIL 0, `structures` 오류 0건, pytest 전부 통과

- [ ] **Step 4: 스펙에 완주 실측을 기록한다**

`docs/superpowers/specs/2026-07-30-스킬-분할-구조화-design.md`의 `## 검증` 절 끝에 `### 완주 실측 (2026-07-30)` 절을 추가한다. 넣을 것: 구조 38개가 묶음 몇 개로 접혔는지와 `members` 2편 이상인 묶음, 문체 게이트의 FAIL·WARN 수와 파일별 분포, 원자 인용률, `[260313]` 검증 통과 여부, 그리고 이번 회차에서 드러난 문제.

- [ ] **Step 5: 커밋**

```bash
cd /c/Users/y/semianalysis
git add insights/views/structure_groups.json docs/superpowers/specs/2026-07-30-스킬-분할-구조화-design.md
git commit -F - <<'EOF'
feat(insights): 구조 묶음 첫 생성 + 리뷰 절차 완주

- structure_groups.json: 구조 38개를 의미 기준으로 묶음. 라벨 Jaccard가
  0쌍이던 것이 의미 묶기로 몇 개까지 접히는지 실측
- insight-review 절차 한 바퀴 — 검사·대조·중복제거·보고
- 스펙에 완주 실측 기록(묶음 수·게이트 FAIL·WARN 분포·원자 인용률)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

## Self-Review

**1. 스펙 커버리지**

| 스펙 요구 | 태스크 |
|---|---|
| ① 스킬 분할 — atomize 축소 | Task 5 Step 1~3 |
| ① insight-review 신설 | Task 5 Step 4 |
| ① 경계 규칙(atomize는 가로지르지 않음, review는 원자 안 만듦) | Task 5 Step 1 마지막 줄, Step 4 마지막 줄 |
| ① review 실행 시점 3조건 | Task 5 Step 1(보고), Step 4(언제 도나) |
| ② `kind_of_doc` 판별 + 40% 문턱 | Task 3(검증), Task 4 Step 1(에이전트 판정) |
| ② `thesis`(line·claim·line_text) | Task 3 Step 3 검증, Task 3 Step 5 데이터 |
| ② `legacy_atoms` 예외 | Task 3 Step 1 테스트, Step 3 구현, Step 5 데이터 |
| ③ 의미 중복제거 → `structure_groups.json` | Task 5 Step 4(절차), Task 6 Step 1~2(실행) |
| ③ 멱등·members 2편·hierarchy 승격 금지·promote 표시만 | Task 5 Step 4의 규칙 절 |
| ③ Jaccard는 1차 필터로 남김 | 기존 `structures.py` 유지 — Task 3에서 `dedupe` 미변경 |
| ④ 문체 게이트 P1·P2 (FAIL) | Task 1 |
| ④ 문체 게이트 P3~P6 (WARN) + CLI | Task 2 |
| ④ `glossary.json` | Task 1 Step 1 |
| ④ WARN 5건 초과 시 humanize-korean 계기 | Task 2 Step 3의 `main()`, Task 5 Step 4 보고 절 |
| ④ 언제 도나(review 1단계·인사이트 수정 직후) | Task 5 Step 3(atomize 4단계), Step 4(review 1단계) |
| 검증 1: 묶음 생성 | Task 6 Step 2 |
| 검증 2: `[260313]` 통과 | Task 3 Step 6 |
| 검증 3: 논증 문서 판별 실측 | 다음 문서 원자화 회차로 이월 — Task 6 Step 4에 그 사실을 기록한다 |
| 검증 4: atomize 완주 | Task 6 Step 3(검사기·테스트)로 대체. 새 문서 완주는 검증 3과 함께 이월 |

검증 3·4는 **새 문서 1편이 들어와야** 실측되므로 이 계획에서 끝낼 수 없다. Task 6 Step 4에 "이월"로 명시한다.

**2. Placeholder 점검**

`<manifest의 id>`·`<확인한 줄 번호>`·`<문서 제목>`은 실행 시점에 채우는 값이며 바로 위 단계에 채우는 방법(명령)이 있다. "적절히 처리"류 표현 없음. 코드가 필요한 단계에는 전부 코드 블록이 있다.

**3. 타입·이름 일관성**

- `findings` 튜플 모양 `(level, where, rule, msg)` — Task 1에서 정의, Task 2의 `main()`과 테스트에서 같은 인덱스(`f[0]`=level, `f[1]`=where, `f[2]`=rule) 사용
- `strip_refs` / `sentences` / `load_glossary` / `check_banned(text, where)` / `check_glossary(text, where, gloss)` — Task 1 정의, Task 2 `main()`에서 같은 시그니처로 호출
- `check_length(text, where)` / `check_translationese(text, where)` / `check_dup_claim(sec, where)` / `check_order(names, where)` — Task 2에서 정의·호출 일치. 테스트 헬퍼 `_run`은 2인자 함수용이라 `check_dup_claim`·`check_order`는 직접 호출한다(테스트 코드가 그렇게 돼 있다)
- `validate(data, man_ids=None, atom_counts=None)` — Task 3에서 시그니처 확장, `main()` 호출부도 같은 단계에서 갱신
- `ca.parse_synth` / `ca.sections` / `ca.SYNTH` / `ca.load_atoms` — 모두 현재 `check_atoms.py`에 있는 이름
- `structure_groups.json`의 키 `groups`·`name`·`kind`·`members`·`shared_order`·`note`·`promote`·`promote_note` — Task 5 정의와 Task 6 Step 2 확인 스크립트가 같은 키 사용
