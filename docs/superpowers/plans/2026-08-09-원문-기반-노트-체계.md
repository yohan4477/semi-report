# 원문 기반 노트 체계 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 원자(521개)를 걷어내고, 문서 하나당 노트 한 장을 두는 3층 구조로 옮긴다.

**Architecture:** 원문 116편(278만 토큰)을 문서 노트 116장(15만 토큰)으로 8:1 압축한다. 노트는 문장을 쪼개지 않고 논지를 보존하며, 전부 한 콜에 들어가므로 교차 작업이 그 층에서 일어난다. 서술(인사이트·추적)은 노트나 원문을 읽고 쓰되 `(라벨 L줄)` 인용으로 원문에 되돌아간다. 인용은 `cites.json`에 줄 해시를 잠가 원문이 바뀌면 경고한다.

**Tech Stack:** Python 3.13 표준 라이브러리만. 테스트는 pytest 7.4.4. 외부 의존성 추가 금지.

## Global Constraints

- **경로 기준은 저장소 루트** `C:\Users\y\semianalysis`. 모든 스크립트는 `insights/` 안에 두고 `python insights/<name>.py`로 실행한다.
- **인코딩**: 모든 파일 입출력은 `io.open(..., encoding='utf-8')`. 실행 시 `PYTHONIOENCODING=utf-8`. 한글 경로가 많아 이걸 빠뜨리면 Windows에서 cp949 오류가 난다.
- **테스트 실행**: `insights/` 디렉터리에서 `py -m pytest <파일> -q`. 이 저장소에 pytest 설정 파일은 없고 테스트는 모듈을 직접 import 한다(예: `import check_prose as cp`).
- **의존성 없음**: pip 설치 금지. 표준 라이브러리(`io, os, re, json, glob, hashlib, sys, datetime`)만.
- **커밋 메시지**: 한국어. 무엇을 왜 바꿨는지 본문에 적는다. 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- **문체 규칙**(노트·서술 본문에 적용, `check_prose.py`가 검사): 한 문장 160자 이하, em dash는 한 문장에 하나까지, 번역투 금지(`에 대한`·`되어진`·`로 인해`·`에 있어서`·`라고 할 수 있다`), 전문용어는 지우지 말고 첫 등장에 괄호로 풀기.
- **삭제는 마지막 태스크에서만.** 그 전까지 원자 파일과 기존 검사기는 그대로 둔다. 마이그레이션 검증에 필요하다.

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `insights/paths.py` | 저장소 경로 상수 하나만. 지금은 `check_atoms.py`가 이 역할을 겸해서 그걸 지우면 다른 생성기가 같이 죽는다 |
| `insights/notes_lib.py` | frontmatter 파싱, 인용 정규식·해석, 줄 해시. 검사기·렌더러·마이그레이션이 공유 |
| `insights/check_notes.py` | N1~N7 검사기. `notes_lib`만 쓴다 |
| `insights/migrate_atom_refs.py` | 일회용. 인사이트 29건·thesis 3건의 `(A-xxx)`를 `(파일 L줄)`로 치환하고 `sources:` frontmatter를 넣는다 |
| `insights/gen_insightview.py` | ⚛️ 렌더러. `gen_atomview.py`를 대체하되 근거 지도(③)를 뺀다 |
| `insights/notes/<yymmdd>-<슬러그>.md` | 문서 노트. 사람(에이전트)이 쓴다 |
| `insights/cites.json` | 인용 잠금. 검사기가 갱신 |
| `docs/노트 쓰는 법.md` | 노트 작성 에이전트에게 그대로 주는 지시문 |

---

### Task 1: 경로 상수를 떼어낸다

`check_atoms.py`가 `ROOT`·`MAN` 같은 경로 상수를 들고 있고 다른 생성기 여섯 개가 그걸 import 한다. 이걸 안 떼면 마지막 삭제 태스크에서 멀쩡한 생성기가 같이 죽는다.

**Files:**
- Create: `insights/paths.py`
- Create: `insights/test_paths.py`
- Modify: `insights/gen_entity_board.py:14-16` (`import check_atoms as ca` → `import paths`)

**Interfaces:**
- Produces: `paths.ROOT` (str, 저장소 루트 절대경로), `paths.MAN` (str, `insights/manifest.json` 절대경로), `paths.NOTES` (str, `insights/notes` 절대경로), `paths.CITES` (str, `insights/cites.json` 절대경로)

- [x] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_paths.py`:

```python
import os
import paths


def test_root_is_repo_root():
    assert os.path.isdir(os.path.join(paths.ROOT, 'insights'))
    assert os.path.isdir(os.path.join(paths.ROOT, 'content'))


def test_manifest_path_exists():
    assert os.path.isfile(paths.MAN)


def test_notes_dir_under_insights():
    assert paths.NOTES == os.path.join(paths.ROOT, 'insights', 'notes')


def test_cites_path_under_insights():
    assert paths.CITES == os.path.join(paths.ROOT, 'insights', 'cites.json')
```

- [x] **Step 2: 실패를 확인한다**

Run: `cd insights && py -m pytest test_paths.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'paths'`

- [x] **Step 3: 최소 구현**

`insights/paths.py`:

```python
# 경로 상수만 둔다. 예전엔 check_atoms.py가 이 역할을 겸해서, 그걸 지우면
# 생성기 여섯 개가 같이 죽는 구조였다. 상수는 상수 파일에 둔다.
import os

HERE = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(HERE)
MAN = os.path.join(HERE, 'manifest.json')
NOTES = os.path.join(HERE, 'notes')
CITES = os.path.join(HERE, 'cites.json')
TRACKS = os.path.join(HERE, 'tracks')
SYNTH = os.path.join(HERE, 'synth')
THESES = os.path.join(HERE, 'theses')
WORLD = os.path.join(HERE, 'world_path.txt')
```

- [x] **Step 4: 통과를 확인한다**

Run: `cd insights && py -m pytest test_paths.py -q`
Expected: PASS (4 passed)

- [x] **Step 5: `gen_entity_board.py`를 옮긴다**

`insights/gen_entity_board.py`에서 아래 줄을 찾는다:

```python
import check_atoms as ca
import style

ROOT = ca.ROOT
```

이렇게 바꾼다:

```python
import paths
import style

ROOT = paths.ROOT
```

- [x] **Step 6: 추적판이 그대로 나오는지 확인한다**

Run: `cd .. && PYTHONIOENCODING=utf-8 python insights/gen_entity_board.py musk`
Expected: `OK: 일론 머스크 — 원문 5편 / 출처 표기 62 / 신호 6 -> ...`

Run: `git diff --stat "대시보드/추적 - 일론 머스크.html"`
Expected: 출력 없음 (결과물이 한 글자도 안 바뀌어야 한다)

- [x] **Step 7: 커밋**

```bash
git add insights/paths.py insights/test_paths.py insights/gen_entity_board.py
git commit -m "refactor(insights): 경로 상수를 paths.py로 분리

check_atoms.py가 ROOT·MAN을 들고 있어 그걸 지우면 생성기가 같이 죽는
구조였다. 상수만 떼어 두고 gen_entity_board를 먼저 옮긴다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 인용 라이브러리

`(라벨 L123)` 인용을 파싱·해석·검증하는 공용 코드. 지금 `gen_entity_board.py` 안에 박혀 있는 로직을 꺼내 검사기와 공유한다.

**Files:**
- Create: `insights/notes_lib.py`
- Create: `insights/test_notes_lib.py`

**Interfaces:**
- Consumes: `paths.ROOT`
- Produces:
  - `parse_front(text) -> (meta: dict, body: str)` — frontmatter를 dict로, 나머지를 본문으로
  - `sources_of(meta) -> list[dict]` — 각 항목은 `{'file': str, 'base': str, 'date': str, 'note': str}`. `base`는 확장자를 뗀 파일명
  - `CITE` — `re.Pattern`. 그룹 1은 라벨, 그룹 2는 `L`로 시작하는 줄 번호 문자열
  - `resolve(label, sources) -> dict | None` — 라벨로 source 하나를 찾는다
  - `cite_refs(body, sources) -> list[dict]` — `{'label','file','lines':[int],'ok':bool}`
  - `line_hash(abs_path, n) -> str | None` — 그 줄 내용의 sha1 앞 12자. 줄이 없으면 None

- [x] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_notes_lib.py`:

```python
import io
import os
import notes_lib as nl
import paths

FRONT = '''---
source: "input/clippings/Foo Bar.md"
date: 2026-08-07
sources:
  - {file: "input/clippings/Foo Bar.md", date: "2026-08-07", note: "가"}
  - {file: "content/newsletter/[251231] 온사이트 가스 딥다이브.md", date: "2025-12-31", note: "나"}
---

본문 첫 줄(Foo Bar L12).
두 번째 문단이다(온사이트 가스 딥다이브 L51, L60).
'''


def test_parse_front_splits_meta_and_body():
    meta, body = nl.parse_front(FRONT)
    assert meta['date'] == '2026-08-07'
    assert body.strip().startswith('본문 첫 줄')


def test_sources_of_reads_two_entries():
    meta, _ = nl.parse_front(FRONT)
    src = nl.sources_of(meta)
    assert len(src) == 2
    assert src[0]['base'] == 'Foo Bar'
    assert src[1]['file'].endswith('온사이트 가스 딥다이브.md')


def test_resolve_matches_by_prefix():
    meta, _ = nl.parse_front(FRONT)
    src = nl.sources_of(meta)
    assert nl.resolve('Foo Bar', src)['base'] == 'Foo Bar'


def test_resolve_matches_by_substring():
    # 사람은 「온사이트 가스 딥다이브」라고 줄여 쓰는데 파일명은 [251231]로 시작한다.
    # 앞머리 일치만 보면 놓친다 — 실제로 머스크 판에서 7건 놓쳤다
    meta, _ = nl.parse_front(FRONT)
    src = nl.sources_of(meta)
    assert nl.resolve('온사이트 가스 딥다이브', src) is not None


def test_resolve_returns_none_for_unknown():
    meta, _ = nl.parse_front(FRONT)
    assert nl.resolve('없는 문서', nl.sources_of(meta)) is None


def test_cite_refs_finds_both_and_multiline():
    meta, body = nl.parse_front(FRONT)
    refs = nl.cite_refs(body, nl.sources_of(meta))
    assert len(refs) == 2
    assert refs[0]['lines'] == [12]
    assert refs[1]['lines'] == [51, 60]
    assert all(r['ok'] for r in refs)


def test_cite_refs_marks_unresolved():
    meta, _ = nl.parse_front(FRONT)
    refs = nl.cite_refs('무엇(없는 문서 L3).', nl.sources_of(meta))
    assert refs[0]['ok'] is False


def test_line_hash_is_stable_and_none_past_eof():
    p = os.path.join(paths.ROOT, 'insights', 'world_path.txt')
    assert nl.line_hash(p, 1) == nl.line_hash(p, 1)
    assert len(nl.line_hash(p, 1)) == 12
    assert nl.line_hash(p, 10 ** 7) is None
```

- [x] **Step 2: 실패를 확인한다**

Run: `cd insights && py -m pytest test_notes_lib.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'notes_lib'`

- [x] **Step 3: 최소 구현**

`insights/notes_lib.py`:

```python
# 인용 한 가지만 다룬다 — (라벨 L123)을 파싱하고, 라벨을 원문 파일로 풀고,
# 그 줄의 해시를 낸다. 검사기·렌더러·마이그레이션이 이 파일만 공유한다.
import io, os, re, hashlib
import paths

FRONT_RE = re.compile(r'^---\n(.*?)\n---\n(.*)$', re.S)
SRC_RE = re.compile(r'^\s*-\s*\{(.+)\}\s*$', re.M)
KV_RE = re.compile(r'(\w+):\s*"([^"]*)"')
META_RE = re.compile(r'^(\w+):\s*(.+)$', re.M)

# 라벨은 사람이 줄여 쓴 이름, 뒤는 L12 또는 L51, L60 형태
CITE = re.compile(r'\(([^()]{2,80}?)\s*(L\d[\d,\sL–-]*)\)')


def parse_front(text):
    m = FRONT_RE.match(text)
    if not m:
        return {}, text
    head, body = m.group(1), m.group(2)
    meta = {k: v.strip() for k, v in META_RE.findall(head)}
    meta['_head'] = head
    return meta, body


def sources_of(meta):
    out = []
    head = meta.get('_head', '')
    for line in SRC_RE.findall(head):
        d = dict(KV_RE.findall(line))
        if d.get('file'):
            d['base'] = os.path.basename(d['file']).rsplit('.md', 1)[0]
            out.append(d)
    single = meta.get('source', '').strip().strip('"')
    if single and not out:
        out.append({'file': single,
                    'base': os.path.basename(single).rsplit('.md', 1)[0],
                    'date': meta.get('date', ''), 'note': ''})
    return out


def resolve(label, sources):
    """앞머리 일치를 먼저 보고, 없으면 부분 일치. 사람이 줄여 쓴 라벨은
    파일명 앞머리와 안 맞는 경우가 흔하다(예: [251231]로 시작하는 파일)."""
    key = label.strip().rstrip(',')[:18]
    if not key:
        return None
    for s in sources:
        if s['base'].startswith(key):
            return s
    for s in sources:
        if key in s['base']:
            return s
    return None


def cite_refs(body, sources):
    out = []
    for m in CITE.finditer(body):
        label, lines = m.group(1), m.group(2)
        hit = resolve(label, sources)
        out.append({'label': label.strip(),
                    'file': hit['file'] if hit else None,
                    'lines': [int(x) for x in re.findall(r'L(\d+)', lines)],
                    'ok': hit is not None})
    return out


def line_hash(abs_path, n):
    if not os.path.isfile(abs_path):
        return None
    with io.open(abs_path, encoding='utf-8', errors='replace') as f:
        for i, line in enumerate(f, 1):
            if i == n:
                return hashlib.sha1(line.strip().encode('utf-8')).hexdigest()[:12]
    return None


def abspath(rel):
    return os.path.join(paths.ROOT, rel.replace('/', os.sep))
```

- [x] **Step 4: 통과를 확인한다**

Run: `cd insights && py -m pytest test_notes_lib.py -q`
Expected: PASS (8 passed)

- [x] **Step 5: 커밋**

```bash
git add insights/notes_lib.py insights/test_notes_lib.py
git commit -m "feat(insights): 인용 라이브러리 — (라벨 L줄) 파싱·해석·해시

라벨 해석은 앞머리 일치 뒤 부분 일치로 본다. 사람이 「온사이트 가스
딥다이브」라고 줄여 쓰는데 파일명은 [251231]로 시작해서, 앞머리만 보면
놓친다(머스크 판에서 실제로 7건 놓쳤다).

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: 노트 검사기

**Files:**
- Create: `insights/check_notes.py`
- Create: `insights/test_check_notes.py`

**Interfaces:**
- Consumes: `notes_lib.parse_front`, `notes_lib.sources_of`, `notes_lib.cite_refs`, `notes_lib.line_hash`, `notes_lib.abspath`, `paths.NOTES`, `paths.CITES`, `paths.SYNTH`, `paths.THESES`, `paths.TRACKS`
- Produces:
  - `check_file(path, text, lock) -> list[tuple]` — 각 항목은 `(level, where, rule, msg)`, level은 `'FAIL'`/`'WARN'`
  - `main(write_lock=True) -> int` — FAIL 개수를 반환. 요약 한 줄을 출력

**검사 규칙**

| | 규칙 | 등급 |
|---|---|---|
| N1 | frontmatter의 `source`/`sources` 파일이 실재한다 | FAIL |
| N2 | 모든 인용 라벨이 그중 하나로 해석된다 | FAIL |
| N3 | 인용한 줄 번호가 파일 범위 안이다 | FAIL |
| N4 | 인용한 줄의 해시가 `cites.json`과 같다 | WARN |
| N5 | 노트가 3KB를 넘지 않는다 | WARN |
| N6 | `## 수치` 절의 각 항목에 인용이 있다 | FAIL |
| N7 | 숫자가 있는 문단에 인용이 없다 | WARN |

- [x] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_check_notes.py`:

```python
import check_notes as cn

GOOD = '''---
source: "insights/world_path.txt"
date: 2026-08-07
---

## 이 문서가 주장하는 것

무언가를 주장한다.

## 수치

- 2GW · 2026년 말 기준 · 업체 발표 (world_path L1)
'''

BAD_SRC = GOOD.replace('insights/world_path.txt', 'insights/없는파일.txt')
BAD_LABEL = GOOD.replace('(world_path L1)', '(엉뚱한이름 L1)')
BAD_LINE = GOOD.replace('(world_path L1)', '(world_path L9999999)')
NO_CITE = GOOD.replace(' (world_path L1)', '')


def _rules(text, path='insights/notes/x.md'):
    return {f[2] for f in cn.check_file(path, text, {})}


def test_good_note_has_no_findings():
    assert _rules(GOOD) == set()


def test_missing_source_file_fails():
    assert 'N1' in _rules(BAD_SRC)


def test_unresolved_label_fails():
    assert 'N2' in _rules(BAD_LABEL)


def test_line_past_eof_fails():
    assert 'N3' in _rules(BAD_LINE)


def test_hash_mismatch_warns():
    lock = {'insights/world_path.txt#L1': {'sha1': 'deadbeefdead'}}
    rules = {f[2] for f in cn.check_file('insights/notes/x.md', GOOD, lock)}
    assert 'N4' in rules


def test_oversize_note_warns():
    big = GOOD + ('가' * 3200)
    assert 'N5' in _rules(big)


def test_numbers_row_without_cite_fails():
    assert 'N6' in _rules(NO_CITE)


def test_levels_are_correct():
    lv = {f[2]: f[0] for f in cn.check_file('insights/notes/x.md', BAD_SRC, {})}
    assert lv['N1'] == 'FAIL'
```

- [x] **Step 2: 실패를 확인한다**

Run: `cd insights && py -m pytest test_check_notes.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'check_notes'`

- [x] **Step 3: 최소 구현**

`insights/check_notes.py`:

```python
# 노트와 서술의 인용을 검사한다. 원자 검사기(C1~C21)를 대신하되 실제로
# 인용한 줄만 본다 — 모든 문장을 미리 분해해 두지 않아서 훨씬 싸다.
import io, os, re, sys, json, glob
import paths
import notes_lib as nl

MAXKB = 3.0
NUM = re.compile(r'\d')
ROW = re.compile(r'^\s*-\s+(.+)$', re.M)


def check_file(path, text, lock):
    out = []
    meta, body = nl.parse_front(text)
    src = nl.sources_of(meta)
    if not src:
        out.append(('FAIL', path, 'N1', 'frontmatter에 source/sources가 없다'))
        return out
    for s in src:
        if not os.path.isfile(nl.abspath(s['file'])):
            out.append(('FAIL', path, 'N1', '원문이 없다: %s' % s['file']))

    for r in nl.cite_refs(body, src):
        if not r['ok']:
            out.append(('FAIL', path, 'N2', '라벨을 못 푼다: %s' % r['label']))
            continue
        ap = nl.abspath(r['file'])
        for n in r['lines']:
            h = nl.line_hash(ap, n)
            if h is None:
                out.append(('FAIL', path, 'N3',
                            '%s에 %d번 줄이 없다' % (r['file'], n)))
                continue
            key = '%s#L%d' % (r['file'], n)
            old = (lock.get(key) or {}).get('sha1')
            if old and old != h:
                out.append(('WARN', path, 'N4',
                            '원문이 바뀌었다 — 다시 볼 것: %s' % key))
            lock[key] = {'sha1': h}

    if len(text.encode('utf-8')) > MAXKB * 1024:
        out.append(('WARN', path, 'N5',
                    '노트가 %.1fKB — %.0fKB를 넘으면 원문을 다시 읽는 편이 낫다'
                    % (len(text.encode('utf-8')) / 1024.0, MAXKB)))

    m = re.search(r'^## 수치\s*\n(.*?)(?=\n## |\Z)', body, re.S | re.M)
    if m:
        for row in ROW.findall(m.group(1)):
            if not nl.CITE.search(row):
                out.append(('FAIL', path, 'N6', '수치에 인용이 없다: %s' % row[:40]))

    for para in re.split(r'\n\s*\n', body):
        p = para.strip()
        if p.startswith('#') or p.startswith('-') or len(p) < 40:
            continue
        if NUM.search(p) and not nl.CITE.search(p):
            out.append(('WARN', path, 'N7', '숫자가 있는데 인용이 없다: %s' % p[:40]))
    return out


def main(write_lock=True):
    lock = {}
    if os.path.isfile(paths.CITES):
        lock = json.load(io.open(paths.CITES, encoding='utf-8'))
    findings, n = [], 0
    # synth·theses는 Task 5(참조 치환)가 끝난 뒤에 넣는다. 지금 넣으면
    # sources: frontmatter가 없어 전부 N1 FAIL이 난다
    for d in (paths.NOTES, paths.TRACKS):
        for p in sorted(glob.glob(os.path.join(d, '*.md'))):
            n += 1
            rel = os.path.relpath(p, paths.ROOT).replace(os.sep, '/')
            findings += check_file(rel, io.open(p, encoding='utf-8').read(), lock)
    for lv, where, rule, msg in findings:
        print('%s %s [%s] %s' % (lv, os.path.basename(where), rule, msg))
    fails = sum(1 for f in findings if f[0] == 'FAIL')
    warns = len(findings) - fails
    print('요약: 문서 %d편 / FAIL %d / WARN %d' % (n, fails, warns))
    if write_lock:
        json.dump(lock, io.open(paths.CITES, 'w', encoding='utf-8'),
                  ensure_ascii=False, indent=1, sort_keys=True)
    return fails


if __name__ == '__main__':
    sys.exit(1 if main() else 0)
```

- [x] **Step 4: 통과를 확인한다**

Run: `cd insights && py -m pytest test_check_notes.py -q`
Expected: PASS (8 passed)

- [x] **Step 5: 실물에 돌려 본다**

Run: `cd .. && PYTHONIOENCODING=utf-8 python insights/check_notes.py`
Expected: `insights/tracks/musk.md`만 검사 대상이고 FAIL 0. `insights/cites.json`이 생긴다.

FAIL이 나오면 `musk.md`의 인용 라벨이 안 풀린 것이다. 라벨을 고치지 말고 `notes_lib.resolve`가 왜 못 푸는지부터 본다.

- [x] **Step 6: 커밋**

```bash
git add insights/check_notes.py insights/test_check_notes.py insights/cites.json
git commit -m "feat(insights): 노트 검사기 N1~N7 + 인용 잠금

인용한 줄의 해시를 cites.json에 잠근다. 원문이 갱신되면 N4가 경고하고
그 줄을 인용한 서술을 다시 본다. 원자 검사기의 C16·C17을 대신하되
실제로 인용한 줄만 검사해서 훨씬 싸다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 노트 쓰는 법 + 시범 3장

노트를 쓰는 것은 코드가 아니라 지시문이다. 지시문을 파일로 고정하고 3장을 만들어 검사기를 통과시킨다.

**Files:**
- Create: `docs/노트 쓰는 법.md`
- Create: `insights/notes/260807-스페이스X-10GW-베팅.md`
- Create: `insights/notes/251231-온사이트-가스.md`
- Create: `insights/notes/260702-메타-컴퓨트.md`

**Interfaces:**
- Consumes: `check_notes.main`
- Produces: `insights/notes/*.md` — Task 7의 대량 생성이 이 3장을 본보기로 삼는다

- [x] **Step 1: 지시문을 쓴다**

`docs/노트 쓰는 법.md`:

````markdown
# 노트 쓰는 법

문서 한 편을 읽고 노트 한 장을 만든다. **요약이 아니라 그 문서의 논지를 보존한 압축**이다. 목표 8:1(25KB → 3KB).

## 파일 위치

`insights/notes/<yymmdd>-<슬러그>.md` — 날짜는 문서 발행일, 슬러그는 한글 두세 낱말.

## 형식

```markdown
---
source: "input/clippings/....md"
title: 원문 제목
date: 2026-08-07
corpus: semi
lang: en
actors: [스페이스X, 마이크로소프트]
topics: [전력, 데이터센터]
---

## 이 문서가 주장하는 것

3~5문장. 요약이 아니라 주장이다. 「무엇을 다룬다」가 아니라 「무엇이라고 말한다」.

## 수치

- 값 · 조건 · 귀속 (라벨 L123)

5~15개. **조건 없는 수치는 적지 않는다.** 조건을 떼면 뒤집히는 사실이 실제로 있다.

## 저자가 추정이라고 밝힌 것

- … (라벨 L456)

## 이 문서가 반박하거나 뒤집는 것

다른 문서를 명시적으로 반박할 때만 쓴다. 없으면 절 자체를 뺀다.
```

## 인용

`(라벨 L123)` — 라벨은 파일명을 알아볼 만큼만 줄인 것. 줄 번호는 네가 실제로 읽은 그 줄.

## 문체

- 쉬운 한국말. 회사는 실명.
- 전문용어는 지우지 말고 첫 등장에 괄호로 푼다 — 「리트로핏(기존 건물 개조)」.
- 한 문장 160자 이하. em dash는 한 문장에 하나까지.
- 번역투 금지: `에 대한`, `되어진`, `로 인해`, `에 있어서`, `라고 할 수 있다`.

## 하지 말 것

- 상업적 해석(티커·수혜·피해)을 넣지 마라. 노트는 문서가 말한 것만 담는다.
- 문서에 없는 것을 채우지 마라. 모르면 안 적는다.
- 3KB를 넘기지 마라. 넘으면 논지가 아니라 내용을 옮기고 있다는 신호다.

## 끝내고

```bash
PYTHONIOENCODING=utf-8 python insights/check_notes.py
```
FAIL 0이어야 한다.
````

- [x] **Step 2: 시범 노트 3장을 만든다**

각 문서마다 서브에이전트 한 콜. 프롬프트는 `docs/노트 쓰는 법.md` 전문 + 대상 파일 경로 하나.

대상:
1. `input/clippings/SpaceX 10GW in 2027 – Why It's Real, Will Drive $300B ARR for SpaceX, and Why Microsoft Will Be the Largest Offtaker.md` → `insights/notes/260807-스페이스X-10GW-베팅.md`
2. `content/newsletter/ai_infra/power/[251231] AI 랩들은 어떻게 전력난을 해결하는가 - 온사이트 가스 딥다이브.md` → `insights/notes/251231-온사이트-가스.md`
3. `input/clippings/Meta Compute Everyone Wants To Be A Neocloud.md` → `insights/notes/260702-메타-컴퓨트.md`

1번은 이미 원자 30개가 있다(`insights/atoms/260807-스페이스X-10GW-베팅.json`). **원자를 보지 말고 원문에서 직접 써라.** 다 쓴 뒤 원자와 비교해 무엇이 빠졌는지 본다 — 그게 이 체계 전환의 검증이다.

- [x] **Step 3: 검사기를 돌린다**

Run: `PYTHONIOENCODING=utf-8 python insights/check_notes.py`
Expected: `요약: 문서 4편 / FAIL 0 / WARN 0`

WARN N5(3KB 초과)가 나오면 노트를 줄인다. FAIL N2가 나오면 라벨이 안 풀린 것이다.

- [x] **Step 4: 문체 게이트를 돌린다**

`insights/check_prose.py`는 `insights/synth/*.md`만 본다. 노트도 보게 경로를 넓힌다.

`insights/check_prose.py`에서 대상 경로를 모으는 부분을 찾아 `insights/notes/*.md`를 추가한다.

Run: `PYTHONIOENCODING=utf-8 python insights/check_prose.py`
Expected: FAIL 0

- [x] **Step 5: 원자와 대조한다**

1번 노트와 `insights/atoms/260807-스페이스X-10GW-베팅.json`(원자 30개)을 나란히 놓고 확인한다.

- 노트에 있는데 원자에 없는 것이 몇 개인가
- 원자에 있는데 노트가 빠뜨린 것이 몇 개인가

결과를 커밋 메시지에 적는다. 노트가 원자보다 덜 담으면 노트 형식을 고쳐야 한다는 신호다.

- [x] **Step 6: 커밋**

```bash
git add "docs/노트 쓰는 법.md" insights/notes insights/check_prose.py insights/cites.json
git commit -m "feat(insights): 노트 쓰는 법 + 시범 3장

원자를 보지 않고 원문에서 직접 썼다. 스페이스X 편을 원자 30개와 대조한
결과는 본문에 적는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: 기존 서술의 참조를 인용으로 치환

인사이트 29건과 thesis 3건의 `(A-260807b-01)`을 `(파일 L줄)`로 바꾸고 `sources:` frontmatter를 넣는다. **사람이 쓴 산문은 한 글자도 안 건드린다.**

**Files:**
- Create: `insights/migrate_atom_refs.py`
- Create: `insights/test_migrate_atom_refs.py`
- Modify: `insights/synth/*.md` (29건), `insights/theses/*.md` (3건) — 스크립트가 고친다

**Interfaces:**
- Consumes: `paths.ROOT`, `paths.SYNTH`, `paths.THESES`
- Produces:
  - `atom_index() -> dict[str, tuple[str, int]]` — 원자 id → (원문 경로, 줄 번호)
  - `convert(text, index) -> tuple[str, list[str]]` — 치환된 본문과, 쓰인 원문 경로 목록
  - `main(dry_run: bool) -> int` — 고친 파일 수

- [x] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_migrate_atom_refs.py`:

```python
import migrate_atom_refs as m

INDEX = {
    'A-260807b-01': ('content/newsletter/ai_infra/business/[260807] 스페이스X.md', 41),
    'A-260807b-15': ('content/newsletter/ai_infra/business/[260807] 스페이스X.md', 137),
    'A-250214-01': ('content/newsletter/ai_infra/cooling/[250214] 냉각.md', 12),
}


def test_single_ref_becomes_line_cite():
    out, used = m.convert('목표다(A-260807b-01).', INDEX)
    assert out == '목표다([260807] 스페이스X L41).'
    assert used == ['content/newsletter/ai_infra/business/[260807] 스페이스X.md']


def test_multiple_refs_in_one_paren():
    out, _ = m.convert('둘이다(A-260807b-01, A-260807b-15).', INDEX)
    assert out == '둘이다([260807] 스페이스X L41, L137).'


def test_refs_from_two_docs_split_into_two_cites():
    out, used = m.convert('섞였다(A-260807b-01, A-250214-01).', INDEX)
    assert '[260807] 스페이스X L41' in out
    assert '[250214] 냉각 L12' in out
    assert len(used) == 2


def test_unknown_ref_is_left_alone():
    out, used = m.convert('모른다(A-999999-99).', INDEX)
    assert out == '모른다(A-999999-99).'
    assert used == []


def test_prose_is_untouched():
    src = '전기를 더 사도 채울 칩이 없으면 소용이 없다(A-260807b-01).'
    out, _ = m.convert(src, INDEX)
    assert out.startswith('전기를 더 사도 채울 칩이 없으면 소용이 없다(')
```

- [x] **Step 2: 실패를 확인한다**

Run: `cd insights && py -m pytest test_migrate_atom_refs.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'migrate_atom_refs'`

- [x] **Step 3: 최소 구현**

`insights/migrate_atom_refs.py`:

```python
# 일회용. 인사이트·thesis의 (A-xxx) 참조를 (파일 L줄) 인용으로 바꾸고
# sources: frontmatter를 채운다. 사람이 쓴 산문은 건드리지 않는다.
import io, os, re, sys, json, glob
import paths

REF = re.compile(r'\((A-[0-9A-Za-z-]+(?:\s*,\s*A-[0-9A-Za-z-]+)*)\)')


def atom_index():
    idx = {}
    for p in glob.glob(os.path.join(paths.ROOT, 'insights', 'atoms', '*.json')):
        d = json.load(io.open(p, encoding='utf-8'))
        doc = d['path'].replace('\\', '/')
        for a in d['atoms']:
            idx[a['id']] = (doc, a.get('line') or 1)
    return idx


def _label(doc):
    return os.path.basename(doc).rsplit('.md', 1)[0]


def convert(text, index):
    used = []

    def one(m):
        ids = [x.strip() for x in m.group(1).split(',')]
        groups, order = {}, []
        for i in ids:
            if i not in index:
                return m.group(0)          # 하나라도 모르면 통째로 둔다
            doc, line = index[i]
            if doc not in groups:
                groups[doc] = []
                order.append(doc)
            groups[doc].append(line)
        for doc in order:
            if doc not in used:
                used.append(doc)
        return '(' + ', '.join(
            '%s %s' % (_label(doc), ', '.join('L%d' % n for n in groups[doc]))
            for doc in order) + ')'

    return REF.sub(one, text), used


def _inject_sources(head, docs):
    if 'sources:' in head or not docs:
        return head
    rows = '\n'.join('  - {file: "%s", note: ""}' % d for d in docs)
    return head.rstrip() + '\nsources:\n' + rows


def main(dry_run=False):
    idx = atom_index()
    n = 0
    for d in (paths.SYNTH, paths.THESES):
        for p in sorted(glob.glob(os.path.join(d, '*.md'))):
            t = io.open(p, encoding='utf-8').read()
            m = re.match(r'^---\n(.*?)\n---\n(.*)$', t, re.S)
            if not m:
                continue
            head, body = m.group(1), m.group(2)
            new_body, used = convert(body, idx)
            if new_body == body:
                continue
            out = '---\n%s\n---\n%s' % (_inject_sources(head, used), new_body)
            if not dry_run:
                io.open(p, 'w', encoding='utf-8').write(out)
            n += 1
            print('%s%s — 원문 %d편' % ('[dry] ' if dry_run else '', os.path.basename(p), len(used)))
    print('고친 파일 %d개' % n)
    return n


if __name__ == '__main__':
    main('--dry-run' in sys.argv)
```

- [x] **Step 4: 통과를 확인한다**

Run: `cd insights && py -m pytest test_migrate_atom_refs.py -q`
Expected: PASS (5 passed)

- [x] **Step 5: 먼저 dry-run으로 본다**

Run: `cd .. && PYTHONIOENCODING=utf-8 python insights/migrate_atom_refs.py --dry-run`
Expected: 32개 안팎의 파일이 나온다(인사이트 29 + thesis 3). 각 줄에 원문 편수가 붙는다.

「원문 0편」으로 나오는 파일이 있으면 그 파일의 참조가 안 풀린 것이다. 원자 id 오타이거나 삭제된 원자다. 진행 전에 확인한다.

- [x] **Step 6: 실제로 고치고, 검사 범위를 넓히고, 검사한다**

먼저 치환한다:

```bash
PYTHONIOENCODING=utf-8 python insights/migrate_atom_refs.py
```

이제 `synth`·`theses`에 `sources:`가 생겼으니 검사기가 그것들도 보게 한다.
`insights/check_notes.py`의 `main()`에서 아래 줄을 찾는다:

```python
    for d in (paths.NOTES, paths.TRACKS):
```

이렇게 바꾼다:

```python
    for d in (paths.NOTES, paths.TRACKS, paths.SYNTH, paths.THESES):
```

그리고 돌린다:

```bash
PYTHONIOENCODING=utf-8 python insights/check_notes.py
PYTHONIOENCODING=utf-8 python insights/check_prose.py
```
Expected: 둘 다 FAIL 0. 검사 대상이 4편에서 36편 안팎으로 는다.

N4 WARN(원문이 바뀌었다)이 무더기로 나오면 `cites.json`이 아직 그 줄들을 모르는 것이 아니라 실제로 줄 번호가 어긋난 것이다. 원자의 `line`이 오래돼 원문과 안 맞는 경우다. 그 파일들을 목록으로 뽑아 보고한다.

- [x] **Step 7: 산문이 안 바뀐 걸 눈으로 확인한다**

Run: `git diff insights/synth | grep "^-" | grep -v "^---" | grep -v "A-[0-9]" | head -20`
Expected: 출력이 없거나 frontmatter 줄만 나온다. 산문 문장이 지워진 줄로 나오면 스크립트가 잘못된 것이다.

- [x] **Step 8: 커밋**

```bash
git add insights/migrate_atom_refs.py insights/test_migrate_atom_refs.py insights/synth insights/theses insights/cites.json
git commit -m "refactor(insights): 인사이트·thesis 참조를 원자 id에서 원문 줄 인용으로

(A-260807b-01) → ([260807] 스페이스X L41). sources: frontmatter도 같이
채운다. 사람이 쓴 산문은 한 글자도 안 건드렸다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: ⚛️ 렌더러를 인용 기반으로

`gen_atomview.py`(1,600줄 규모, 근거 지도·구조 뷰·검증 대장을 다 그린다)를 대체한다. 새 렌더러는 인사이트 본문 + 인용 링크만 그린다.

**Files:**
- Create: `insights/gen_insightview.py`
- Modify: `scripts/gen_hub.py` — 🏭 제약과 회사 카드를 뺀다
- Delete: `insights/gen_atomview.py` (Task 9에서)

**Interfaces:**
- Consumes: `notes_lib.parse_front`, `notes_lib.sources_of`, `notes_lib.CITE`, `paths.SYNTH`, `paths.THESES`, `style.BASE`
- Produces: `대시보드/인사이트와 근거.html`

- [x] **Step 1: 인용 링크 함수를 가져온다**

`insights/gen_entity_board.py`의 `link_cites`와 `md_body`가 이미 이 일을 한다. 두 함수를 `notes_lib.py`로 옮기고 양쪽이 쓰게 한다.

`insights/notes_lib.py` 끝에 추가:

```python
BLOB = 'https://github.com/yohan4477/semi-report/blob/main/'


def esc(s):
    return (str(s or '').replace('&', '&amp;').replace('<', '&lt;').replace('>', '&gt;'))


def link_cites(text, sources):
    """(라벨 L123) → 원문 그 줄로 가는 각주. 서술과 근거를 한 클릭 거리에 둔다."""
    import urllib.parse

    def one(m):
        hit = resolve(m.group(1), sources)
        nums = re.findall(r'L(\d+)', m.group(2))
        if not hit or not nums:
            return m.group(0)
        url = BLOB + urllib.parse.quote(hit['file'].replace('\\', '/')) + '#L' + nums[0]
        return ('<a class="cite" href="%s" target="_blank" rel="noopener" title="%s">%s</a>'
                % (url, esc(hit['base']), esc(m.group(2).replace(' ', ''))))
    return CITE.sub(one, text)


def md_body(body, sources, h='h2', cls='tsec'):
    """작은 마크다운만 — ##, 문단, **굵게**. 그 이상은 이 판에 필요 없다."""
    out = []
    for block in re.split(r'\n\s*\n', body.strip()):
        b = block.strip()
        if not b:
            continue
        m = re.match(r'^##\s+(.+)$', b)
        if m:
            out.append('<%s class="%s">%s</%s>' % (h, cls, esc(m.group(1)), h))
            continue
        if b.startswith('- '):
            items = ''.join('<li>%s</li>' % link_cites(
                re.sub(r'\*\*(.+?)\*\*', r'<b>\1</b>', esc(x[2:])), sources)
                for x in b.split('\n') if x.startswith('- '))
            out.append('<ul>%s</ul>' % items)
            continue
        p = re.sub(r'\*\*(.+?)\*\*', r'<b>\1</b>', esc(b).replace('\n', ' '))
        out.append('<p>%s</p>' % link_cites(p, sources))
    return ''.join(out)
```

`insights/gen_entity_board.py`에서 자체 `link_cites`·`md_body`·`esc`를 지우고 `nl.link_cites`·`nl.md_body`·`nl.esc`를 쓰게 바꾼다.

- [x] **Step 2: 추적판이 그대로 나오는지 확인한다**

Run: `PYTHONIOENCODING=utf-8 python insights/gen_entity_board.py musk`
Expected: `출처 표기 62`

Run: `git diff --stat "대시보드/추적 - 일론 머스크.html"`
Expected: 출력 없음

- [x] **Step 3: 새 렌더러를 쓴다**

`insights/gen_insightview.py`:

```python
# ⚛️ 인사이트와 근거 — 판단을 읽고, 문장 옆 줄번호를 누르면 원문 그 줄로 간다.
# 옛 gen_atomview.py에 있던 근거 지도(스택 10칸·프로세스 7단계)와 구조 뷰,
# 검증 대장은 뺐다. 사용자가 실제로 읽는 것은 판단 본문 하나였다.
import io, os, sys, glob
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
import paths, style
import notes_lib as nl

OUT = os.path.join(paths.ROOT, '대시보드', '인사이트와 근거.html')


def cards():
    out, n = [], 0
    for d, kind in ((paths.THESES, '종합 판단'), (paths.SYNTH, '인사이트')):
        for p in sorted(glob.glob(os.path.join(d, '*.md')), reverse=True):
            meta, body = nl.parse_front(io.open(p, encoding='utf-8').read())
            src = nl.sources_of(meta)
            head = meta.get('headline') or os.path.basename(p)[:-3]
            sub = meta.get('subhead', '')
            out.append('<details class="ins"><summary><span class="cid">%s</span>'
                       '<span class="asof">as_of %s</span><h2>%s</h2>'
                       '<p class="sub">%s</p></summary><div class="body">%s</div></details>'
                       % (nl.esc(kind), nl.esc(meta.get('as_of', '')),
                          nl.esc(head), nl.esc(sub), nl.md_body(body, src, 'h4', 'bsec')))
            n += 1
    return ''.join(out), n


def build():
    body, n = cards()
    html = (TMPL.replace('__CSS__', style.BASE + CSS)
                .replace('__CARDS__', body)
                .replace('__N__', str(n)))
    io.open(OUT, 'w', encoding='utf-8').write(html)
    print('OK: 인사이트 %d건 -> %s' % (n, OUT))


CSS = r'''
  .ins{background:var(--card);border:1px solid var(--line);border-left:3px solid var(--accent);
       border-radius:var(--r);padding:var(--pad);margin-top:12px;box-shadow:var(--shadow)}
  .ins>summary{list-style:none;cursor:pointer;position:relative;padding-right:26px}
  .ins>summary::-webkit-details-marker{display:none}
  .ins>summary::after{content:"⌄";position:absolute;right:2px;top:-2px;font-size:22px;
                      color:var(--faint);transition:transform .3s cubic-bezier(.32,.72,0,1)}
  .ins[open]>summary::after{transform:rotate(180deg)}
  .cid{font-size:var(--t-lbl);font-weight:800;letter-spacing:.1em;color:var(--accent)}
  .asof{float:right;font-size:var(--t-meta);color:var(--faint);font-variant-numeric:tabular-nums}
  .ins h2{font-size:var(--t-h2);font-weight:850;letter-spacing:-.02em;line-height:1.36;margin:8px 0 2px}
  .ins .sub{font-size:var(--t-body);color:var(--faint);margin:3px 0 0}
  .bsec{font-size:var(--t-meta);font-weight:800;color:var(--accent2);margin:14px 0 5px;
        text-transform:uppercase;letter-spacing:.04em}
  .body p,.body li{font-size:var(--t-body);color:var(--sub);line-height:1.65}
  .body b{color:var(--ink)}
  .cite{font-size:.72em;font-weight:800;color:var(--accent);text-decoration:none;
        vertical-align:.28em;margin-left:2px;padding:0 3px;border-radius:4px;background:var(--sunk)}
'''

TMPL = '''<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>인사이트와 그 근거</title>
<style>__CSS__</style>
<div class="wrap">
<header>
  <p class="eyebrow">Insights &amp; Evidence</p>
  <h1>인사이트와 그 근거</h1>
  <p class="lede">판단을 먼저 읽고, 문장 옆의 파란 줄번호를 누르면 그 문장의 근거가 된
  원문 줄로 갑니다.</p>
  <div class="meta"><span>판단 __N__건</span>
    <a class="maplink" href="Yomianalysis.html">전체 입구 →</a></div>
</header>
__CARDS__
<footer>근거는 원문 줄 인용입니다. 종목 추천이 아니며 가격·밸류에이션·타이밍은
이 체계에 없습니다.</footer>
</div>
'''

if __name__ == '__main__':
    build()
```

- [x] **Step 4: 돌려 보고 눈으로 확인한다**

Run: `PYTHONIOENCODING=utf-8 python insights/gen_insightview.py`
Expected: `OK: 인사이트 32건 -> ...`

확인할 것: 각 카드에 파란 줄번호가 보이는가, 누르면 GitHub 원문으로 가는가, 링크 안 걸린 `(… L12)` 텍스트가 남아 있지 않은가.

```bash
python -c "import io,re;h=io.open('대시보드/인사이트와 근거.html',encoding='utf-8').read();print('각주',h.count('class=\"cite\"'),'| 안걸림',len(re.findall(r'\([^()]{2,60}? L\d[^)]*\)',h)))"
```
Expected: 각주가 100개 이상, 안 걸린 것 0

- [x] **Step 5: 허브에서 🏭 카드를 뺀다**

`scripts/gen_hub.py`의 `TOPICS`에서 `('🏭', '제약과 회사', …)` 항목을 지운다.

Run: `PYTHONIOENCODING=utf-8 python scripts/gen_hub.py`
Expected: `OK: 11장 -> ...`

- [x] **Step 6: 커밋**

```bash
git add insights/gen_insightview.py insights/notes_lib.py insights/gen_entity_board.py scripts/gen_hub.py "대시보드/인사이트와 근거.html" "대시보드/Yomianalysis.html"
git commit -m "feat(insights): ⚛️를 인용 기반 렌더러로 교체

근거 원자 펼침이 문장 옆 각주가 됐다. 근거 지도·구조 뷰·검증 대장은 뺐다 —
실제로 읽는 것은 판단 본문 하나였다. link_cites와 md_body를 notes_lib으로
올려 추적판과 공유한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: 1단계 대량 노트 — 변환본 47편

**Files:**
- Create: `insights/notes/*.md` (44장. Task 4에서 3장은 이미 만들었다)

**Interfaces:**
- Consumes: `docs/노트 쓰는 법.md`, `check_notes.main`
- Produces: `insights/notes/` 47장 — Task 8의 교차 작업이 이걸 통째로 읽는다

- [x] **Step 1: 대상 목록을 만든다**

```bash
PYTHONIOENCODING=utf-8 python -c "
import glob,os,io,sys
sys.stdout.reconfigure(encoding='utf-8')
done={os.path.basename(p)[:6] for p in glob.glob('insights/notes/*.md')}
todo=[p for p in sorted(glob.glob('content/newsletter/**/*.md',recursive=True))
      if os.path.basename(p)[1:7] not in done]
io.open('scratchpad/notes_todo.txt','w',encoding='utf-8').write('\n'.join(todo))
print(len(todo),'편')
"
```
Expected: 44편 안팎

- [ ] **Step 2: 다섯 편씩 묶어 돌린다**

서브에이전트 한 콜에 문서 5편. 프롬프트는 `docs/노트 쓰는 법.md` 전문 + 그 5편의 경로 + 「각 문서마다 노트 한 장씩, 총 5장을 써라」.

한 번에 전부 돌리지 않는 이유: 5편마다 검사기를 돌려 형식이 어긋나면 바로 잡는다. 44편을 다 쓰고 나서 형식이 틀린 걸 발견하면 전부 다시 써야 한다.

**원자가 이미 있는 문서(24편)는 물려받는다.** 대상 문서에 대응하는 `insights/atoms/<같은 날짜 슬러그>.json`이 있으면, 프롬프트에 그 JSON도 같이 넣고 이렇게 지시한다 — 「`## 수치` 절은 이 원자들의 `value`·`condition`·`line`을 옮겨 쓰고, 원문에서 새로 찾은 것만 더해라. `## 이 문서가 주장하는 것`은 원자에 없으니 원문을 읽고 써라.」 원자화된 24편이 나머지보다 싼 이유가 이것이다.

각 묶음이 끝날 때마다:

```bash
PYTHONIOENCODING=utf-8 python insights/check_notes.py
PYTHONIOENCODING=utf-8 python insights/check_prose.py
```
Expected: FAIL 0

- [ ] **Step 3: 묶음마다 커밋한다**

```bash
git add insights/notes insights/cites.json
git commit -m "feat(insights): 문서 노트 5장 (<범위>) — 1단계 n/44"
```

- [ ] **Step 4: 크기를 잰다**

44편이 끝나면 노트 층이 실제로 한 콜에 들어가는지 확인한다. 스펙의 「15만 토큰」 추정을 검증하는 단계다.

```bash
PYTHONIOENCODING=utf-8 python -c "
import glob,os,sys
sys.stdout.reconfigure(encoding='utf-8')
b=sum(os.path.getsize(p) for p in glob.glob('insights/notes/*.md'))
n=len(glob.glob('insights/notes/*.md'))
print('%d장 %.0fKB ≈ %.0fk 토큰' % (n, b/1024, b/1024*0.42))
"
```
Expected: 47장, 150KB 안팎, 6만 토큰 안팎

추정보다 크면 노트가 부풀었다는 뜻이다. N5 WARN이 몇 건인지 보고 3KB 상한을 지켰는지 확인한다.

---

### Task 8: 교차 인사이트 첫 회차

노트 층이 실제로 값을 하는지 확인하는 단계. 원자로는 6주에 두 건 나왔던 것이 한 번에 몇 건 나오는지 센다.

**Files:**
- Create: `insights/synth/cross-2026-08-<n>.md` (교차에서 나온 것 중 쓸 만한 것만)
- Create: `scratchpad/cross_0809.md` (원본 출력)

**Interfaces:**
- Consumes: `insights/notes/*.md` 47장
- Produces: 새 인사이트 md — Task 6의 렌더러가 그대로 그린다

- [ ] **Step 1: 노트를 통째로 넣고 묻는다**

서브에이전트 한 콜. 입력은 `insights/notes/*.md` 47장 전부. 묻는 것:

1. **같은 단위를 다르게 세는 곳** — 두 문서가 같은 단위(GW·%·달러)를 쓰는데 재는 대상이 다른 사례. 각각 어느 노트의 어느 수치인지 적을 것
2. **서로 어긋나는 판단** — A 문서가 말한 것을 B 문서가 뒤집는 사례
3. **아무도 안 다룬 구멍** — 여러 문서가 전제로 깔지만 어느 문서도 근거를 대지 않은 것
4. **시간에 따라 바뀐 판단** — 같은 주제를 다룬 문서들이 시점에 따라 다른 말을 하는 것

산출물은 `scratchpad/cross_0809.md`. 각 항목에 노트 파일명과 인용을 붙일 것.

- [ ] **Step 2: 결과를 센다**

몇 건이 나왔나. 그중 이미 알던 것과 새로운 것을 가른다.

원자 체계는 6주에 두 건을 잡았다(「60%」 단위 충돌, T-01 반증). 이번 한 콜에서 그보다 적게 나오면 노트 층이 원자보다 못하다는 뜻이고, 스펙의 전제가 틀린 것이다. **그 경우 Task 9(삭제)를 진행하지 말고 보고한다.**

- [ ] **Step 3: 쓸 만한 것을 인사이트로 쓴다**

교차에서 나온 것 중 판단할 가치가 있는 것만 골라 `insights/synth/`에 쓴다. 형식은 기존 인사이트와 같다(주장 / 그래서 무엇이 달라지나 / 근거 / 조건 충돌 / 아직 모르는 것). 근거는 노트가 아니라 **원문 인용**을 단다 — 노트는 찾아가는 길이지 근거가 아니다.

- [ ] **Step 4: 검사하고 커밋**

```bash
PYTHONIOENCODING=utf-8 python insights/check_notes.py
PYTHONIOENCODING=utf-8 python insights/check_prose.py
PYTHONIOENCODING=utf-8 python insights/gen_insightview.py
git add insights/synth "대시보드/인사이트와 근거.html" insights/cites.json
git commit -m "feat(insights): 교차 첫 회차 — 노트 47장에서 <n>건

원자 체계는 6주에 2건을 잡았다. 이번 한 콜에서 <n>건.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 9: 원자 걷어내기

**Task 8에서 교차 결과가 두 건 미만이면 이 태스크를 하지 않는다.** 스펙의 전제가 틀린 것이므로 보고하고 멈춘다.

**Files:**
- Delete: `insights/atoms/` (24개 JSON), `insights/check_atoms.py`, `insights/test_insights.py`, `insights/crosscheck.py`, `insights/test_crosscheck.py`, `insights/structures.py`, `insights/test_structures.py`, `insights/gen_actormap.py`, `insights/gen_atomview.py`, `insights/views/actors.json`, `insights/views/actor_map.json`, `insights/views/process.json`, `insights/views/structures.json`, `insights/verify.json`, `대시보드/제약과 회사.html`
- Modify: `README.md`, `REPORT_RULES.md`, `docs/왜 이렇게 됐나 — 설계 변천사.md`

- [ ] **Step 1: 남은 의존을 찾는다**

```bash
grep -rln "check_atoms\|load_atoms\|crosscheck\|structures\|actor_map\|gen_atomview" \
  insights/*.py scripts/*.py .claude/skills/ 2>/dev/null
```
Expected: 아무것도 안 나와야 한다. 나오면 그 파일을 먼저 고친다.

`insights/coverage.py`와 `insights/gen_map.py`가 걸릴 수 있다. `gen_map.py`는 `coverage`를 import 하고 `coverage`는 클러스터를 읽는다 — 원자와 무관하면 그대로 둔다.

- [ ] **Step 2: 지운다**

```bash
git rm -r insights/atoms
git rm insights/check_atoms.py insights/test_insights.py insights/crosscheck.py \
       insights/test_crosscheck.py insights/structures.py insights/test_structures.py \
       insights/gen_actormap.py insights/gen_atomview.py insights/verify.json \
       insights/views/actors.json insights/views/actor_map.json \
       insights/views/process.json insights/views/structures.json \
       "대시보드/제약과 회사.html"
```

- [ ] **Step 3: 전부 돌려 본다**

```bash
PYTHONIOENCODING=utf-8 python insights/check_notes.py
PYTHONIOENCODING=utf-8 python insights/check_prose.py
PYTHONIOENCODING=utf-8 python insights/gen_insightview.py
PYTHONIOENCODING=utf-8 python insights/gen_entity_board.py musk
PYTHONIOENCODING=utf-8 python insights/gen_map.py
PYTHONIOENCODING=utf-8 python scripts/gen_hub.py
PYTHONIOENCODING=utf-8 python scripts/gen_bmirror.py 10
cd insights && py -m pytest -q
```
Expected: 전부 성공, 테스트 전부 통과

- [ ] **Step 4: 문서를 고친다**

`README.md`의 「원자·뷰 기반 인사이트」 절을 「원문 기반 노트」로 다시 쓴다. 담을 것: 3층 구조, 노트 형식, 인용과 `cites.json`, 검사기 N1~N7과 P1~P7, 재생성 명령. 지운 것(원자·좌표 축·제약과 회사·검증 대장)은 「2026-08-09에 걷어냈다」고 한 줄로 적고 설계 변천사를 링크한다.

`REPORT_RULES.md`에서 원자를 전제한 문장을 찾아 고친다.

`docs/왜 이렇게 됐나 — 설계 변천사.md`의 「지금 살아 있는 것」 표에서 **폐기 예정**을 **폐기됨(2026-08-09)**으로 바꾸고, 교차 첫 회차 결과를 「네 번째 시도」 항목에 한 문단으로 덧붙인다.

- [ ] **Step 5: 메모리를 고친다**

`C:\Users\y\.claude\projects\C--Users-y-semianalysis\memory\`에서 원자를 전제한 것을 정리한다.

- `reference_atom_insight_artifact.md` — 「검사기 FAIL 0」을 `check_notes`·`check_prose` 기준으로 고친다
- `reference_actor_map_artifact.md` — 삭제(🏭이 없어졌다)
- `project_verify_ledger.md` — 삭제(검증 대장이 없어졌다)
- `reference_insight_architecture.md` — 3층 구조로 다시 쓴다
- `MEMORY.md` 색인에서 지운 항목의 줄을 뺀다

- [ ] **Step 6: 커밋**

```bash
git add -A
git commit -m "refactor(insights): 원자 체계 걷어냄 — 문서 노트로 완전 이행

원자 521개, check_atoms C1~C21, crosscheck, structures, 🏭 제약과 회사,
검증 대장 55건, 좌표 축 4종을 지웠다. git 이력에는 남는다.

남은 것: 인사이트·thesis(참조는 원문 줄 인용으로 치환됨), 문서 노트 47장,
check_notes N1~N7, check_prose P1~P7, 대시보드 11장.

이유와 실측은 docs/왜 이렇게 됐나 — 설계 변천사.md 참조.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## 나중에 (이 계획 밖)

- **2단계 노트** — 미변환 클리핑 45편. xAI 콜로서스 2처럼 체계 밖에 있던 것들
- **3단계 노트** — 제3자 코퍼스 46편. `corpus:` 필드로 가르되 서술에서 섞지 않는 규칙을 먼저 정해야 한다
- **교차 ② 신규 대조 자동화** — 새 노트가 들어올 때 기존 노트 전부와 대조하는 것. 1단계가 끝나 노트 층 크기를 실측한 뒤 설계한다
- **🏭 제약과 회사 되살리기** — 노트의 `actors:` 태그로 다시 만들 수 있다. 안 본다고 했으니 지금은 안 한다
