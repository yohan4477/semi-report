# 새 문서 원자화 스킬 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 새 문서 1편이 들어왔을 때 원자 추출 → 좌표 배정 → 검사 → 기존 산출물 대조 보고까지를 한 번의 스킬 호출로 재현 가능하게 만든다.

**Architecture:** 절차를 지휘하는 스킬(`.claude/skills/insight-atomize/SKILL.md`)과 추출만 하는 전담 에이전트(`.claude/agents/insight-atomizer.md`)를 분리한다. 스킬은 manifest 갱신·단계 배정·검사·대조·커밋을 순서대로 돌리고, 문서 원문(2~4만 자)은 에이전트 안에만 머문다. 대조는 새 스크립트 `insights/crosscheck.py`가 하고, 코퍼스 혼합 금지 검사(C18)는 기존 `insights/check_atoms.py`에 붙인다.

**Tech Stack:** Python 3.13(`py` 런처), pytest 9.1.1, 표준 라이브러리만. 마크다운 스킬·에이전트 정의.

## Global Constraints

- 저장소 루트는 `C:\Users\y\semianalysis`. 스크립트 안의 경로 상수는 기존 파일과 같은 형식(`ROOT = r"C:\Users\y\semianalysis"`)을 쓴다.
- 테스트는 `insights/` 디렉터리에서 실행한다: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`. `insights/test_insights.py`와 같은 스타일 — 평범한 함수, 클래스 없음, 모듈 직접 import.
- 콘솔이 cp949라 한글 출력이 깨진다. 검증할 때 `PYTHONIOENCODING=utf-8`을 붙이거나 파일로 써서 읽는다.
- 외부 라이브러리 추가 금지. `konlpy`·`pandas` 등 불필요.
- 단위 판정은 `check_atoms.py`의 `UNIT` 정규식을 **import해서** 재사용한다. 복사하면 C9와 리포트가 어긋난다.
- 커밋 메시지는 한국어 본문 + `feat:`/`fix:`/`docs:`/`chore:` 접두어. 끝에 다음 두 줄을 붙인다:
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq`
- 스택 8노드: `전자·공정`, `칩`, `메모리`, `열`, `랙`, `데이터센터`, `전력망`, `연료·지정학`.
- 원자 id 형식 `A-<YYMMDD>-<2자리 순번>`. 원자 파일명 `insights/atoms/<YYMMDD>-<슬러그>.json`.

---

## File Structure

| 파일 | 책임 |
|---|---|
| `insights/crosscheck.py` (신규) | 새 원자 대 기존 원자·인사이트 대조. STALE 목록과 충돌 후보 쌍만 계산·출력. 파일을 수정하지 않는다 |
| `insights/test_crosscheck.py` (신규) | `crosscheck.py` 단위 테스트. 픽스처는 함수 안에서 dict로 만든다 — 실제 원자 파일에 의존하면 원자가 늘 때 테스트가 깨진다 |
| `insights/check_atoms.py` (수정) | C18(코퍼스 혼합 금지) 추가. 나머지 규칙 불변 |
| `.claude/agents/insight-atomizer.md` (신규) | 추출 전담 에이전트 정의. 스키마·규칙·자체 검사 루프 |
| `.claude/skills/insight-atomize/SKILL.md` (신규) | 절차 6단계 지휘. 에이전트 호출·배정·검사·대조·커밋 |
| `docs/superpowers/specs/2026-07-30-원자-뷰-인사이트-design.md` (수정) | C18 행 추가 |

Task 1(C18) → Task 2(crosscheck 계산) → Task 3(crosscheck CLI) → Task 4(에이전트 정의) → Task 5(스킬 정의) → Task 6(실문서 완주 검증) 순서. 1~3은 코드라 테스트가 있고, 4~5는 정의 파일이라 Task 6의 완주가 그 검증이다.

---

### Task 1: C18 — 코퍼스 혼합 금지

한 인사이트가 인용하는 원자는 모두 같은 코퍼스여야 한다. `source_id` 접두어(`semi:` / `und:`)가 코퍼스다. 제3자 해설이 SemiAnalysis 판단의 근거로 섞이면 어느 주장이 1차 리포트에서 나왔는지 추적이 끊긴다.

**Files:**
- Modify: `insights/check_atoms.py` (`check_synth` 함수 안, C7 검사 바로 뒤)
- Modify: `docs/superpowers/specs/2026-07-30-원자-뷰-인사이트-design.md` (검사기 표에 C18 행)
- Test: `insights/test_crosscheck.py` (신규 — 이 태스크에서 파일을 만들고 C18 테스트를 먼저 넣는다)

**Interfaces:**
- Consumes: `check_atoms.load_atoms()`가 원자에 붙이는 `_source_id` 필드.
- Produces: `check_atoms.corpus_of(source_id) -> str` — 접두어를 돌려주는 한 줄 함수. Task 2의 `crosscheck.py`가 같은 함수를 쓴다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`insights/test_crosscheck.py` 를 새로 만든다:

```python
import check_atoms as ca


def test_corpus_of_semi():
    assert ca.corpus_of('semi:compute:베라-루빈') == 'semi'


def test_corpus_of_und():
    assert ca.corpus_of('und:권효재:이란-전쟁') == 'und'


def test_corpus_of_no_colon():
    # 접두어가 없으면 통째로 코퍼스로 본다 — 조용히 semi로 넘어가면 C18이 무력해진다
    assert ca.corpus_of('semi') == 'semi'
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`
Expected: FAIL — `AttributeError: module 'check_atoms' has no attribute 'corpus_of'`

- [ ] **Step 3: `corpus_of`를 넣는다**

`insights/check_atoms.py`의 `norm` 함수 바로 아래에 추가:

```python
def corpus_of(source_id):
    """source_id 접두어가 코퍼스다 — semi(SemiAnalysis) / und(언더스탠딩·제3자)."""
    return (source_id or '').split(':')[0]
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`
Expected: PASS (3 passed)

- [ ] **Step 5: C18 검사를 넣는다**

`insights/check_atoms.py`의 `check_synth` 안에서, 아래 기존 코드를 찾는다:

```python
        if len({a['_source_id'] for a in cited_atoms}) < 2:
            add('FAIL', where, 'C7', '출처 문서 %d편 (2편 이상 필요)' % len({a['_source_id'] for a in cited_atoms}))
```

바로 뒤에 추가한다:

```python
        # C18 — 제3자(und) 근거가 SemiAnalysis(semi) 판단에 섞이면 근거 무게가 뒤섞이고
        # 어느 주장이 1차 리포트에서 나왔는지 추적이 끊긴다. 인사이트 단위로 코퍼스를 가른다
        corpora = {corpus_of(a['_source_id']) for a in cited_atoms}
        if len(corpora) > 1:
            add('FAIL', where, 'C18', '한 인사이트에 코퍼스가 섞였다: %s' % ', '.join(sorted(corpora)))
```

- [ ] **Step 6: 기존 인사이트 5건이 여전히 통과하는지 확인한다**

Run: `PYTHONIOENCODING=utf-8 py insights/check_atoms.py` (저장소 루트에서)
Expected: `FAIL 0 / WARN 0`. 현재 원자는 전부 `semi:`라 C18이 걸리지 않아야 한다.

- [ ] **Step 7: 음성 테스트 — 섞으면 실제로 C18 FAIL이 나는지 확인한다**

`und:` 원자가 아직 없으므로 임시 인사이트 파일과 가짜 원자로 `check_synth`를 직접 돌린다. **사전 조건만 단정하는 테스트는 쓰지 않는다** — `findings`에 C18이 들어왔는지를 본다. `insights/test_crosscheck.py`에 추가:

```python
import io as _io


def _atom_min(aid, sid, stack='랙', time='2026-01-01'):
    return {'id': aid, '_source_id': sid, '_file': sid.split(':')[-1] + '.json',
            '_path': 'x.md', '_source_hash': 'h',
            'value': None, 'condition': 'c', 'line': 1, 'line_text': 't',
            'view': {'stack': stack, 'actor': [], 'time': time}}


def test_c18_mixed_corpora_fails(tmp_path, monkeypatch):
    p = tmp_path / 'mixed.md'
    _io.open(str(p), 'w', encoding='utf-8').write(
        '---\nview: stack\nnodes: [랙]\natoms: [A-1, A-2, A-3]\nas_of: 2026-01-03\n---\n\n'
        '## 주장\n**x**\n## 근거\n- x\n## 조건 충돌\n- 없음\n'
        '## 그래서 무엇이 달라지나\n- x\n## 아직 모르는 것\n- x\n')
    monkeypatch.setattr(ca, 'SYNTH', str(tmp_path))
    ca.findings.clear()
    atoms = [_atom_min('A-1', 'semi:compute:x'),
             _atom_min('A-2', 'und:권효재:y'),
             _atom_min('A-3', 'semi:compute:z')]
    ca.check_synth(atoms, None)
    assert 'C18' in [f[2] for f in ca.findings]


def test_c18_single_corpus_passes(tmp_path, monkeypatch):
    p = tmp_path / 'single.md'
    _io.open(str(p), 'w', encoding='utf-8').write(
        '---\nview: stack\nnodes: [랙]\natoms: [A-1, A-2, A-3]\nas_of: 2026-01-03\n---\n\n'
        '## 주장\n**x**\n## 근거\n- x\n## 조건 충돌\n- 없음\n'
        '## 그래서 무엇이 달라지나\n- x\n## 아직 모르는 것\n- x\n')
    monkeypatch.setattr(ca, 'SYNTH', str(tmp_path))
    ca.findings.clear()
    atoms = [_atom_min('A-1', 'semi:compute:x'),
             _atom_min('A-2', 'semi:memory:y'),
             _atom_min('A-3', 'semi:compute:z')]
    ca.check_synth(atoms, None)
    assert 'C18' not in [f[2] for f in ca.findings]
```

`_atom_min`은 Task 2·3의 테스트도 쓰므로 파일 상단 근처에 둔다.

Run: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`
Expected: PASS (5 passed)

- [ ] **Step 8: 스펙에 C18 행을 추가한다**

`docs/superpowers/specs/2026-07-30-원자-뷰-인사이트-design.md`의 검사기 표에서 `| C17 |` 행을 찾아 바로 뒤에 추가:

```markdown
| C18 | 한 인사이트가 인용하는 원자의 코퍼스(`source_id` 접두어)가 하나 | FAIL |
```

- [ ] **Step 9: 커밋**

```bash
git add insights/check_atoms.py insights/test_crosscheck.py docs/superpowers/specs/2026-07-30-원자-뷰-인사이트-design.md
git commit -F - <<'EOF'
feat(insights): C18 — 인사이트 단위 코퍼스 혼합 금지

제3자(und) 근거가 SemiAnalysis(semi) 판단에 섞이면 근거 무게가 뒤섞이고
어느 주장이 1차 리포트에서 나왔는지 추적이 끊긴다.

- corpus_of(source_id) 신설 — crosscheck.py도 같은 함수를 쓴다
- 기존 인사이트 5건 FAIL 0 유지

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 2: crosscheck 계산 — STALE과 충돌 후보

새 원자가 들어왔을 때 기존 인사이트 중 갱신 대상(STALE)과, 같은 단위인데 조건이 다른 기존 원자 쌍(충돌 후보)을 계산한다. 이 태스크는 **계산 함수만** 만들고 CLI는 Task 3에서 붙인다.

**Files:**
- Create: `insights/crosscheck.py`
- Modify: `insights/test_crosscheck.py`

**Interfaces:**
- Consumes: `check_atoms.load_atoms()`, `check_atoms.parse_synth()`, `check_atoms.UNIT`, `check_atoms.corpus_of()` (Task 1).
- Produces:
  - `units(value: str) -> set[str]` — 값 문자열에서 단위 토큰 집합.
  - `find_clashes(new_atoms: list[dict], old_atoms: list[dict]) -> list[dict]` — 각 항목은 `{'unit': str, 'new': dict, 'old': dict}`. 같은 스택 노드이고 단위가 겹치고 `condition`이 다른 쌍만.
  - `load_assign() -> dict` — `insights/views/process.json`의 `assign`을 한 번 읽어 돌려준다. 파일이 없으면 `{}`.
  - `find_stale(new_atoms, all_atoms, assign, synth_dir=None) -> list[dict]` — 각 항목은 `{'file': str, 'as_of': str, 'scope': str, 'uncited': [id, ...]}`. **`assign`은 인자로 받는다** — 인사이트 파일마다 `ca.check_process`를 다시 부르면 사전을 중복 파싱하고 `ca.findings`에 부산물을 남긴다.
  - Task 3의 `main()`이 이 네 함수만 쓴다.

- [ ] **Step 1: `units`와 `find_clashes` 실패 테스트를 쓴다**

`insights/test_crosscheck.py`에 추가:

```python
import crosscheck as cc


def _atom(aid, stack, value, cond, time='2026-01-01', sid='semi:compute:x'):
    return {'id': aid, '_source_id': sid, '_file': 'f.json',
            'value': value, 'condition': cond,
            'view': {'stack': stack, 'actor': [], 'time': time}}


def test_units_extracts_kw_and_mw():
    assert cc.units('공랭 랙 한계 41kW, GB200 120kW') == {'kW'}
    assert cc.units('CDU 2MW급 → 3~6MW급') == {'MW'}


def test_units_none_when_value_missing():
    assert cc.units(None) == set()


def test_clash_same_unit_different_condition():
    new = [_atom('A-260725-01', '랙', '랙당 132kW', 'Max-P 구성')]
    old = [_atom('A-250214-07', '랙', '공랭 랙 한계 41kW', '2025-02 H100 설계')]
    out = cc.find_clashes(new, old)
    assert len(out) == 1
    assert out[0]['unit'] == 'kW'
    assert out[0]['new']['id'] == 'A-260725-01'
    assert out[0]['old']['id'] == 'A-250214-07'


def test_no_clash_when_condition_same():
    new = [_atom('A-260725-01', '랙', '랙당 132kW', '같은 조건')]
    old = [_atom('A-250214-07', '랙', '공랭 41kW', '같은 조건')]
    assert cc.find_clashes(new, old) == []


def test_no_clash_across_different_nodes():
    # 노드가 다르면 쌍을 보지 않는다 — kW·%가 흔해서 안 좁히면 수십 쌍이 쏟아진다
    new = [_atom('A-260725-01', '열', '유량 4LPM에서 4kW', 'TSMC 시험차량')]
    old = [_atom('A-250214-07', '랙', '공랭 41kW', '2025-02 H100 설계')]
    assert cc.find_clashes(new, old) == []
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'crosscheck'`

- [ ] **Step 3: `crosscheck.py`의 계산부를 쓴다**

`insights/crosscheck.py` 신규:

```python
# 새 원자 대 기존 원자·인사이트 대조 — 설계: docs/superpowers/specs/2026-07-30-원자화-스킬-design.md
# 파일을 수정하지 않는다. 계산하고 출력만 한다.
import os, io, re, glob, sys
import check_atoms as ca

ROOT = ca.ROOT
SYNTH = ca.SYNTH


def units(value):
    """값 문자열에서 단위 토큰 집합. 판정은 check_atoms.UNIT을 그대로 쓴다 —
    복사하면 C9와 이 리포트가 어긋난다."""
    return set(ca.UNIT.findall(value or ''))


def find_clashes(new_atoms, old_atoms):
    """같은 스택 노드 · 단위 겹침 · condition 다름인 쌍. C9는 인사이트가 그 둘을
    함께 인용할 때만 FAIL을 내므로, 인사이트를 쓰기 전에 목록을 준다."""
    out = []
    for n in new_atoms:
        for o in old_atoms:
            if n['view']['stack'] != o['view']['stack']:
                continue
            if (n.get('condition') or '') == (o.get('condition') or ''):
                continue
            for u in sorted(units(n.get('value')) & units(o.get('value'))):
                out.append({'unit': u, 'new': n, 'old': o})
    return out
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`
Expected: PASS (10 passed)

- [ ] **Step 5: `find_stale` 실패 테스트를 쓴다**

`insights/test_crosscheck.py`에 추가:

`_io`는 Task 1 Step 7에서 이미 import했으므로 다시 쓰지 않는다.

```python
def _write_synth(tmp_path, name, view, coord_line, atoms_line, as_of):
    p = tmp_path / name
    body = ('---\n'
            'view: %s\n'
            '%s\n'
            'atoms: %s\n'
            'as_of: %s\n'
            '---\n\n## 주장\n**x**\n' % (view, coord_line, atoms_line, as_of))
    _io.open(str(p), 'w', encoding='utf-8').write(body)
    return str(tmp_path)


def test_stale_when_new_atom_in_same_node_and_newer(tmp_path):
    d = _write_synth(tmp_path, 's1.md', 'stack', 'nodes: [랙]',
                     '[A-250214-07]', '2025-02-14')
    new = [_atom('A-260725-01', '랙', '132kW', 'Max-P', time='2026-07-25')]
    allx = new + [_atom('A-250214-07', '랙', '41kW', '2025-02', time='2025-02-14')]
    out = cc.find_stale(new, allx, {}, d)
    assert len(out) == 1
    assert out[0]['file'] == 's1.md'
    assert out[0]['uncited'] == ['A-260725-01']
    assert out[0]['scope'] == '노드 랙'


def test_stale_for_process_view_uses_injected_assign(tmp_path):
    d = _write_synth(tmp_path, 's4.md', 'process', 'stages: [냉각 방식 확정]',
                     '[A-250214-07]', '2025-02-14')
    new = [_atom('A-260725-01', '랙', '132kW', 'Max-P', time='2026-07-25')]
    allx = new + [_atom('A-250214-07', '랙', '41kW', '2025-02', time='2025-02-14')]
    assign = {'A-260725-01': '냉각 방식 확정'}
    out = cc.find_stale(new, allx, assign, d)
    assert len(out) == 1
    assert out[0]['scope'] == '단계 냉각 방식 확정'
    assert out[0]['uncited'] == ['A-260725-01']


def test_not_stale_when_already_cited(tmp_path):
    d = _write_synth(tmp_path, 's2.md', 'stack', 'nodes: [랙]',
                     '[A-250214-07, A-260725-01]', '2025-02-14')
    new = [_atom('A-260725-01', '랙', '132kW', 'Max-P', time='2026-07-25')]
    allx = new + [_atom('A-250214-07', '랙', '41kW', '2025-02', time='2025-02-14')]
    assert cc.find_stale(new, allx, {}, d) == []


def test_not_stale_when_new_atom_older_than_as_of(tmp_path):
    d = _write_synth(tmp_path, 's3.md', 'stack', 'nodes: [랙]',
                     '[A-250214-07]', '2026-12-31')
    new = [_atom('A-260725-01', '랙', '132kW', 'Max-P', time='2026-07-25')]
    allx = new + [_atom('A-250214-07', '랙', '41kW', '2025-02', time='2025-02-14')]
    assert cc.find_stale(new, allx, {}, d) == []
```

- [ ] **Step 6: 실패를 확인한다**

Run: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`
Expected: FAIL — `AttributeError: module 'crosscheck' has no attribute 'find_stale'`

- [ ] **Step 7: `find_stale`을 넣는다**

`insights/crosscheck.py` 끝에 추가:

```python
def load_assign():
    """프로세스 단계 배정을 한 번만 읽는다. ca.check_process는 사전 무결성 검사용이라
    findings에 부산물을 남기므로 리포트 경로에서는 부르지 않는다."""
    p = os.path.join(ROOT, 'insights', 'views', 'process.json')
    if not os.path.exists(p):
        return {}
    return json.load(io.open(p, encoding='utf-8')).get('assign') or {}


def find_stale(new_atoms, all_atoms, assign, synth_dir=None):
    """새 원자가 건드린 칸을 쓰는 인사이트 중 as_of가 그 원자보다 이른 것.
    C11은 모든 인사이트를 훑는 상시 위생 검사이고, 이쪽은 방금 들어온 원자로
    범위를 좁혀 무엇을 처리해야 하는지까지 붙여 준다."""
    synth_dir = synth_dir or SYNTH
    assign = assign or {}
    out = []
    for p in sorted(glob.glob(os.path.join(synth_dir, '*.md'))):
        meta, _ = ca.parse_synth(io.open(p, encoding='utf-8').read())
        if not meta:
            continue
        view = meta.get('view') or 'stack'
        cited = set(meta.get('atoms') or []) | set(meta.get('dismissed') or [])
        as_of = meta.get('as_of') or ''
        nodes = meta.get('nodes') or ([meta['node']] if meta.get('node') else [])
        stages = meta.get('stages') or []
        if view == 'process':
            in_scope = lambda a: assign.get(a['id']) in stages
            scope = '단계 ' + '·'.join(stages)
        else:
            in_scope = lambda a: a['view']['stack'] in nodes
            scope = '노드 ' + '·'.join(nodes)
        hit = [a['id'] for a in new_atoms
               if in_scope(a) and a['id'] not in cited and a['view']['time'] > as_of]
        if hit:
            out.append({'file': os.path.basename(p), 'as_of': as_of,
                        'scope': scope, 'uncited': sorted(hit)})
    return out
```

`json`을 쓰므로 파일 상단 import 줄에 추가한다: `import os, io, re, json, glob, sys`.

- [ ] **Step 8: 테스트가 통과하는지 확인한다**

Run: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`
Expected: PASS (14 passed)

- [ ] **Step 9: 커밋**

```bash
git add insights/crosscheck.py insights/test_crosscheck.py
git commit -F - <<'EOF'
feat(insights): crosscheck 계산부 — STALE·충돌 후보

- units()는 check_atoms.UNIT을 import해 재사용 — 복사하면 C9와 어긋난다
- find_clashes: 같은 노드·단위 겹침·조건 다름인 쌍만. 노드로 좁히지 않으면
  kW·%가 흔해 수십 쌍이 쏟아진다
- find_stale: 새 원자가 건드린 칸으로 범위를 좁힌 STALE 목록
- 테스트 13개

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 3: crosscheck CLI

스킬이 부를 수 있는 실행부. 인수는 원자 파일 경로 또는 소스 id. 생략하면 git에서 아직 커밋되지 않은 원자 파일, 그것도 없으면 mtime이 가장 최근인 파일.

**Files:**
- Modify: `insights/crosscheck.py`
- Modify: `insights/test_crosscheck.py`

**Interfaces:**
- Consumes: Task 2의 `find_clashes`, `find_stale`, `load_assign`, `ca.load_atoms()`.
- Produces: `pick_target(argv: list[str]) -> str` (원자 파일 basename), `main() -> int`. 스킬은 `py insights/crosscheck.py [대상]`만 호출한다.

- [ ] **Step 1: `pick_target` 실패 테스트를 쓴다**

`insights/test_crosscheck.py`에 추가:

```python
def test_pick_target_from_path():
    assert cc.pick_target(['insights/atoms/251128-TPUv7.json']) == '251128-TPUv7.json'


def test_pick_target_from_basename():
    assert cc.pick_target(['251128-TPUv7.json']) == '251128-TPUv7.json'


def test_pick_target_none_returns_none_when_no_files(tmp_path, monkeypatch):
    monkeypatch.setattr(cc.ca, 'ATOMS', str(tmp_path))
    assert cc.pick_target([]) is None
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`
Expected: FAIL — `AttributeError: module 'crosscheck' has no attribute 'pick_target'`

- [ ] **Step 3: CLI를 넣는다**

`insights/crosscheck.py` 끝에 추가:

```python
def pick_target(argv):
    """인수가 있으면 그 파일. 없으면 미커밋 원자 파일 → mtime 최신 순."""
    if argv:
        return os.path.basename(argv[0])
    try:
        import subprocess
        r = subprocess.run(['git', '-C', ROOT, 'status', '--porcelain',
                            'insights/atoms'], capture_output=True, text=True)
        for line in r.stdout.splitlines():
            name = line[3:].strip().strip('"')
            if name.endswith('.json'):
                return os.path.basename(name)
    except Exception:
        pass
    files = glob.glob(os.path.join(ca.ATOMS, '*.json'))
    if not files:
        return None
    return os.path.basename(max(files, key=os.path.getmtime))


def main():
    atoms = ca.load_atoms()
    target = pick_target(sys.argv[1:])
    if not target:
        print('대상 원자 파일을 찾지 못했다')
        return 1
    new = [a for a in atoms if a['_file'] == target]
    if not new:
        print('원자가 없다: %s' % target)
        return 1
    old = [a for a in atoms if a['_file'] != target]

    stale = find_stale(new, atoms, load_assign())
    clashes = find_clashes(new, old)

    print('대상: %s (원자 %d개)' % (target, len(new)))
    print('')
    print('STALE %d건' % len(stale))
    for s in stale:
        print('  %s  as_of %s  [%s]' % (s['file'], s['as_of'], s['scope']))
        print('    미인용 신규 원자: %s' % ', '.join(s['uncited']))
    if stale:
        print('  처리 4갈래: 뒷받침(atoms에 id 추가) / 조건 다름(조건 충돌 절) /')
        print('             뒤집음(주장 재작성, 이전 판단 보존) / 무관(dismissed + 검토 후 무관 절)')
    print('')
    print('충돌 후보 %d쌍  (같은 단위 · 다른 조건 — 함께 인용하면 C9가 FAIL)' % len(clashes))
    for c in clashes:
        print('  %-4s %s "%s" [%s]' % (c['unit'], c['new']['id'],
                                       (c['new'].get('value') or '')[:40],
                                       (c['new'].get('condition') or '')[:40]))
        print('       %s "%s" [%s]' % (c['old']['id'],
                                       (c['old'].get('value') or '')[:40],
                                       (c['old'].get('condition') or '')[:40]))
    return 0


if __name__ == '__main__':
    sys.exit(main())
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

Run: `cd insights; $env:PYTHONUTF8=1; py -m pytest test_crosscheck.py -q`
Expected: PASS (17 passed)

- [ ] **Step 5: 실제 원자로 돌려 본다**

Run: `PYTHONIOENCODING=utf-8 py insights/crosscheck.py 260723-베라-루빈-NVL72-vs-GB200.json` (저장소 루트에서)
Expected: 종료코드 0. `STALE 0건`(그 문서 원자는 이미 인용·무관 처리됨), 충돌 후보는 kW·배 단위로 몇 쌍 출력. 쌍이 30쌍을 넘으면 노드 좁힘이 동작하지 않는 것이므로 `find_clashes`를 다시 본다.

- [ ] **Step 6: 커밋**

```bash
git add insights/crosscheck.py insights/test_crosscheck.py
git commit -F - <<'EOF'
feat(insights): crosscheck CLI — 대상 자동 선택·리포트 출력

- pick_target: 인수 → 미커밋 원자 파일 → mtime 최신
- STALE 출력에 처리 4갈래 안내를 붙임
- check_process가 남긴 findings를 출력 전에 비워 리포트 오염 방지
- 테스트 16개

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 4: insight-atomizer 에이전트 정의

추출 프롬프트를 파일로 못박는다. 이 파이프라인의 품질 결정 변수이므로 호출마다 다시 쓰이면 원자의 일관성이 무너진다.

**Files:**
- Create: `.claude/agents/insight-atomizer.md`

**Interfaces:**
- Consumes: 없음(정의 파일).
- Produces: `subagent_type: insight-atomizer`로 호출 가능한 에이전트. 입력은 소스 id 또는 문서 경로, 산출은 `insights/atoms/<YYMMDD>-<슬러그>.json` 하나.

- [ ] **Step 1: 에이전트 정의를 쓴다**

`.claude/agents/insight-atomizer.md` 신규. frontmatter 키는 기존 `.claude/agents/semianalysis-transformer.md`와 같은 네 개 — `name`, `description`, `tools`, `model`. `model: sonnet`으로 둔다: 추출은 판단이 섞인 내용 합성이라 haiku로는 `condition` 서술이 얕아지고, opus는 문서당 비용이 과하다.

```markdown
---
name: insight-atomizer
description: 변환 완료된 문서 1편에서 "원자"(조건이 붙은 최소 사실 단위)를 추출해 insights/atoms/<YYMMDD>-<슬러그>.json 하나를 만든다. 새 문서가 원자·뷰 체계에 아직 들어오지 않았을 때 insight-atomize 스킬이 호출한다. 커밋하지 않고, 다른 파일도 건드리지 않는다.
tools: Read, Write, Edit, Bash, Grep, Glob
model: sonnet
---

# 원자 추출 에이전트

문서 1편에서 원자를 뽑아 JSON 파일 하나를 만든다. 저장소 루트는 `C:\Users\y\semianalysis`.

## 먼저 읽을 것 (이 순서)

1. `docs/superpowers/specs/2026-07-30-원자-뷰-인사이트-design.md` — "## 원자", "## 뷰", "## 검사기" 절
2. 기존 원자 파일 2개 (형식·서술 톤 예시): `insights/atoms/251204-AWS-Trainium3.json`, `insights/atoms/260226-베라-루빈-익스트림-코디자인.json`

## 대상 확인

호출자가 준 소스 id 또는 경로로 manifest에서 `id`·`path`·`hash`를 확인한다. 이 세 값을 그대로 쓴다.

```bash
PYTHONIOENCODING=utf-8 py -c "import io,json;[print(s['id'],'|',s['hash'],'|',s['path']) for s in json.load(io.open(r'C:\Users\y\semianalysis\insights\manifest.json',encoding='utf-8'))['sources'] if '<검색어>' in s.get('path','')]"
```

manifest에 없으면 여기서 멈추고 그 사실을 보고한다. `gen_manifest.py`를 직접 돌리지 않는다 — 스킬이 이미 돌렸어야 한다.

## 산출물

`insights/atoms/<YYMMDD>-<짧은슬러그>.json`

```json
{
  "source_id": "<manifest의 id>",
  "path": "<manifest의 path>",
  "source_hash": "<manifest의 hash>",
  "date": "<발행일 YYYY-MM-DD>",
  "atoms": [ ... ]
}
```

원자 하나의 필드 순서와 형태:

```json
{
  "id": "A-260226-01",
  "line": 137,
  "line_text": "<line행 원문 그대로(양끝 공백만 제거)>",
  "claim": "한 문장. 원문의 주장을 옮기되 요약하지 않는다",
  "value": "단위 붙은 수치. 없으면 null",
  "condition": "측정 조건·비교군·전제. 비어 있으면 안 된다",
  "attributed_to": "저자 분석 / 업체 발표 / 제3자 측정",
  "view": { "stack": "칩", "actor": ["엔비디아"], "time": "2026-02-26" }
}
```

## 절대 규칙 (검사기가 기계적으로 잡는다)

- **`line`은 1-based 줄 번호.** 그 줄 안에 `value`의 모든 숫자 토큰이 실제로 있어야 한다(콤마·공백 제거 후 비교). 여러 줄에 흩어진 수치를 한 원자에 섞으면 C2 FAIL — 원자를 쪼갠다
- **`line_text`는 그 줄 원문 그대로.** 공백 무시 비교로 C17이 대조한다. 요약하거나 다듬으면 FAIL
- **`source_hash`는 manifest 값 그대로.** C16이 대조한다
- **`condition` 비어 있으면 C3 FAIL.** 원문에 없으면 `"조건 명시 없음"`으로 적고, 그 비율이 30%를 넘으면 보고에 적는다
- **`view.stack`은 8노드 중 하나**: `전자·공정`, `칩`, `메모리`, `열`, `랙`, `데이터센터`, `전력망`, `연료·지정학`. 애매하면 **더 하류**를 고른다(열 vs 랙이면 열)
- **`view.actor`는 `insights/views/actors.json`의 정식 이름만.** 없는 이름이 필요하면 그 파일에 별칭과 함께 추가한다(C5 WARN 해소)
- **`view.time`은 문서 발행일**

## 서술 원칙

- 원자는 **"이 줄이 말하는 것"**이지 "이 절이 말하는 것"이 아니다
- 비교 수치를 뽑을 때는 **본문 서술을 우선**하고 요약부만 근거로 삼지 않는다. `[260723]` 문서는 요약(46행)에서 "GB200 대비 5.4배"라 쓰고 본문(256·258행)에서는 그 5.4배가 GB300 대비이며 GB200은 그 측정 구간에 도달조차 못 한다고 쓴다. 비교군과 측정 조건을 `condition`에 반드시 적는다
- 수치가 없는 구조적 주장도 원자가 된다(`value: null`)
- 목표 원자 수 10~30개

## 완료 조건

```bash
PYTHONIOENCODING=utf-8 py C:\Users\y\semianalysis\insights\check_atoms.py
```

를 직접 실행해 **새 파일에서 FAIL 0**이 될 때까지 스스로 고친다. 가장 흔한 것은 C2(줄에 없는 수치)와 C17(line_text 불일치)이다. 다른 파일에서 나는 기존 WARN은 건드리지 않는다.

## 금지

- 커밋·푸시
- `insights/views/process.json` 수정 — 단계 배정은 호출한 스킬이 한다
- 기존 원자 파일·인사이트 파일 수정
- 이모지로 난이도·중요도 표시

## 보고

원자 수, 노드별 분포, `조건 명시 없음` 비율, 판독 불가로 생략한 대목, 검사기 요약 줄.
```

- [ ] **Step 2: frontmatter가 파싱되는지 확인한다**

Run: `PYTHONIOENCODING=utf-8 py -c "import io,re;t=io.open(r'.claude/agents/insight-atomizer.md',encoding='utf-8').read();m=re.match(r'^---\n(.*?)\n---\n',t,re.S);print(m.group(1) if m else 'NO FRONTMATTER')"`
Expected: `name`·`description`·`tools` 세 줄이 출력된다.

- [ ] **Step 3: 기존 에이전트 정의와 키 구성이 같은지 대조한다**

Run: `PYTHONIOENCODING=utf-8 py -c "import io,re;[print(f, sorted(re.findall(r'^(\w+):', re.match(r'^---\n(.*?)\n---\n', io.open(f,encoding='utf-8').read(), re.S).group(1), re.M))) for f in ('.claude/agents/semianalysis-transformer.md','.claude/agents/insight-atomizer.md')]"`
Expected: 두 줄 모두 `['description', 'model', 'name', 'tools']`. 다르면 기존 파일 쪽에 맞춘다.

- [ ] **Step 4: 커밋**

```bash
git add .claude/agents/insight-atomizer.md
git commit -F - <<'EOF'
feat(agents): insight-atomizer — 원자 추출 전담 에이전트

추출 프롬프트가 이 파이프라인의 품질 결정 변수인데 호출마다 인라인으로
다시 쓰여 드리프트했다. 파일로 못박는다.

- 스키마·C2/C3/C16/C17 규칙·8노드·"이 줄이 말하는 것" 원칙
- 요약부 대신 본문 서술 우선 ([260723] 5.4배 사례를 정의에 박음)
- 자체 검사 루프로 FAIL 0까지, 커밋·process.json 수정은 금지

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 5: insight-atomize 스킬 정의

절차 6단계를 지휘한다. 추출은 하지 않는다.

**Files:**
- Create: `.claude/skills/insight-atomize/SKILL.md`

**Interfaces:**
- Consumes: Task 3의 `insights/crosscheck.py`, Task 4의 `insight-atomizer` 에이전트, 기존 `insights/gen_manifest.py`·`insights/check_atoms.py`.
- Produces: `/insight-atomize` 스킬. 산출은 원자 파일 1개 + `process.json` 갱신 + 커밋 1개 + 보고.

- [ ] **Step 1: 스킬 정의를 쓴다**

`.claude/skills/insight-atomize/SKILL.md` 신규:

```markdown
---
name: insight-atomize
description: 변환 완료된 새 문서를 원자·뷰 체계에 넣는다 — 원자 추출(전담 에이전트), 프로세스 단계 배정, 검사기 FAIL 0, 기존 인사이트와의 대조(STALE·충돌 후보) 보고까지. "새 문서 원자화해" / "이 문서 인사이트 체계에 넣어" / 변환을 막 끝낸 문서가 insights/atoms/에 아직 없을 때 쓴다. 인사이트 본문은 고치지 않는다.
---

# 새 문서 원자화

선행 설계: `docs/superpowers/specs/2026-07-30-원자-뷰-인사이트-design.md`(체계), `docs/superpowers/specs/2026-07-30-원자화-스킬-design.md`(이 절차).

저장소 루트 `C:\Users\y\semianalysis`. 콘솔이 cp949라 한글이 깨지므로 파이썬 실행에는 `PYTHONIOENCODING=utf-8`을 붙인다.

## 1. manifest 갱신

```bash
PYTHONIOENCODING=utf-8 py insights/gen_manifest.py
```

대상 문서의 `id`·`path`·`hash`를 확인한다. manifest에 없으면 여기서 멈추고 변환 파이프라인 쪽 문제로 보고한다.

이미 원자가 있는 문서면 중단한다 — 원문이 안 바뀌었으면 원자도 안 바뀐다:

```bash
PYTHONIOENCODING=utf-8 py -c "import io,json,glob;print([json.load(io.open(f,encoding='utf-8'))['source_id'] for f in glob.glob('insights/atoms/*.json')])"
```

## 2. 원자 추출 — 에이전트 1회

`insight-atomizer` 에이전트를 **한 번** 호출한다. 프롬프트는 짧게: 소스 id, 경로, hash, 발행일, 그리고 이 문서에서 특히 놓치지 말 주제(있으면). 스키마·규칙은 에이전트 정의에 이미 있으므로 다시 적지 않는다.

문서 원문이 메인 대화에 들어오지 않게 하는 것이 이 분리의 목적이다. 직접 원문을 읽지 않는다.

## 3. 프로세스 단계 배정

에이전트가 만든 원자 파일을 읽고, 각 원자의 `claim`·`view.stack`을 보고 `insights/views/process.json`의 `assign`에 항목을 추가한다.

단계 사전(순서 고정): `웨이퍼 배정` → `칩·랙 설계 확정` → `부지·전력 계약` → `냉각 방식 확정` → `건물 착공` → `랙 발주·인수` → `가동`

- 현재 축(컴퓨트 배치 순서)에 걸리지 않는 원자는 **배정하지 않고 남긴다.** 없는 연결을 억지로 만들면 프로세스 뷰의 값이 사라진다
- 축을 새로 추가하지 않는다. 특정 도메인의 미배정이 쌓이면 스펙 개정으로 논의한다
- 배정은 원자 파일에 넣지 않는다 — 원자는 원문 환원 대상, 배정은 해석이다

## 4. 검사

```bash
PYTHONIOENCODING=utf-8 py insights/check_atoms.py
```

FAIL 0이 될 때까지. 새 문서에서 흔한 것: C2(줄에 없는 수치), C17(line_text 불일치), C16(원문 hash 불일치 — 추출 후 원문이 바뀐 경우), C5(actors.json 미등록, WARN).

C16 FAIL이면 그 문서의 원자를 재추출한다(2로 돌아간다). 줄 번호를 손으로 맞추지 않는다.

## 5. 대조

```bash
PYTHONIOENCODING=utf-8 py insights/crosscheck.py
```

두 가지가 나온다.

- **STALE 인사이트**: 새 원자가 건드린 칸을 쓰는 인사이트. 처리는 4갈래 — 뒷받침(`atoms:`에 id 추가) / 조건 다름(`## 조건 충돌` 갱신) / 뒤집음(`## 주장` 재작성, 이전 판단은 무너진 이유와 함께 보존) / 무관(`dismissed:` + `## 검토 후 무관` 절)
- **충돌 후보 쌍**: 같은 단위인데 조건이 다른 기존 원자와의 쌍. 그 둘을 한 인사이트에서 함께 인용하면 C9가 FAIL을 낸다

**여기서 인사이트 본문을 고치지 않는다.** 목록을 사람에게 넘긴다.

## 6. 커밋·보고

원자 파일과 `process.json`을 한 커밋으로. 메시지에 원자 수·노드 분포·미배정 수를 적는다.

```bash
git add insights/atoms/<파일> insights/views/process.json
git commit -F - <<'EOF'
feat(insights): <문서 제목> 원자화 — 원자 N개

- 노드별: ...
- 프로세스 단계 배정 N개 / 미배정 N개
- 검사기 FAIL 0

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: <세션 URL>
EOF
git push origin main
```

보고에 넣을 것: 원자 수, 노드 분포, 미배정 수, `조건 명시 없음` 비율, STALE 인사이트 목록, 충돌 후보 쌍 수와 그중 눈에 걸리는 것.

## 하지 않는 것

- 인사이트 본문 자동 수정
- 새 인사이트 후보 제안
- 프로세스 축 추가
- 기존 원자 파일 소급 수정
- 문서 원문을 메인 대화로 읽어들이기
```

- [ ] **Step 2: 스킬이 목록에 뜨는지 확인한다**

Run: `ls .claude/skills/insight-atomize/SKILL.md && PYTHONIOENCODING=utf-8 py -c "import io,re;t=io.open(r'.claude/skills/insight-atomize/SKILL.md',encoding='utf-8').read();m=re.match(r'^---\n(.*?)\n---\n',t,re.S);print('OK' if m and 'name: insight-atomize' in m.group(1) else 'BAD')"`
Expected: `OK`

- [ ] **Step 3: 커밋**

```bash
git add .claude/skills/insight-atomize/SKILL.md
git commit -F - <<'EOF'
feat(skills): insight-atomize — 새 문서 원자화 절차

절차 6단계: manifest → 추출(insight-atomizer 1회) → 단계 배정 →
check_atoms FAIL 0 → crosscheck → 커밋·보고 후 멈춤.

- 문서 원문은 에이전트 안에만 머문다 (메인 대화 오염 방지)
- 인사이트 본문 자동 수정·새 후보 제안·축 추가는 하지 않는다
- 미배정 허용 — 없는 연결을 억지로 만들지 않는다

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

### Task 6: 실문서 완주 검증 — 800VDC Part 1

스킬이 실제로 재현 가능한지는 문서 한 편을 끝까지 통과시켜 확인한다. 전력망 노드가 원자 0이므로 이 문서가 첫 대상으로 적합하다(사슬 하류가 끊긴 것도 함께 메워진다).

**Files:**
- Create: `insights/atoms/260526-800VDC-Part1.json` (에이전트가 만든다)
- Modify: `insights/views/process.json`

**Interfaces:**
- Consumes: Task 5의 스킬, Task 4의 에이전트, Task 3의 CLI.
- Produces: 전력망 노드 원자 > 0. 이후 인사이트 작성의 근거.

- [ ] **Step 1: 대상 문서가 manifest에 있고 원자가 없음을 확인한다**

Run:
```bash
PYTHONIOENCODING=utf-8 py insights/gen_manifest.py
PYTHONIOENCODING=utf-8 py -c "import io,json;[print(s['id'],'|',s['hash']) for s in json.load(io.open('insights/manifest.json',encoding='utf-8'))['sources'] if '260526' in s.get('path','')]"
```
Expected: `semi:...800VDC...` 한 줄과 12자 hash. 없으면 문서 경로를 `content/newsletter/` 아래에서 찾아 manifest 생성 규칙을 확인한다.

- [ ] **Step 2: 스킬을 호출한다**

`insight-atomize` 스킬을 대상 문서로 호출한다. 스킬 절차 1~6을 그대로 따른다.

- [ ] **Step 3: 전력망 노드가 채워졌는지 확인한다**

Run: `PYTHONIOENCODING=utf-8 py insights/check_atoms.py`
Expected: `FAIL 0`, 노드별 출력에 `전력망 N` (N ≥ 1)이 나타난다. 800VDC 문서는 배전·전압 이야기라 `랙`과 `전력망`에 걸린다.

- [ ] **Step 4: crosscheck가 실제로 뭔가를 잡는지 확인한다**

Run: `PYTHONIOENCODING=utf-8 py insights/crosscheck.py`
Expected: 충돌 후보에 최소 1쌍. 800VDC 문서의 전압·전력 수치는 기존 `A-250214-09`(500kW 랙 기준 48V 버스바)와 같은 단위(V·kW)를 쓰면서 조건이 다르므로 잡혀야 한다. 0쌍이면 `find_clashes`의 노드 좁힘이 과하게 걸린 것이므로 노드 배정을 다시 본다.

- [ ] **Step 5: 두 번째 문서로 드리프트를 본다**

같은 스킬로 `[251231] AI 랩들은 어떻게 전력난을 해결하는가 - 온사이트 가스 딥다이브`를 처리한다.

Run: `PYTHONIOENCODING=utf-8 py insights/check_atoms.py`
Expected: `FAIL 0`. 두 문서의 원자 형식·`condition` 서술 밀도가 비슷한지 눈으로 대조한다. 한쪽만 `조건 명시 없음`이 많으면 에이전트 정의의 해당 절을 보강한다.

- [ ] **Step 6: 스펙에 완주 결과를 기록한다**

`docs/superpowers/specs/2026-07-30-원자화-스킬-design.md`의 `## 검증` 절 끝에 실측 결과를 추가한다 — 문서별 원자 수, 노드 분포 변화, 미배정 수, 충돌 후보 쌍 수, 드리프트 관찰. 선행 스펙의 "파일럿 결과" 절과 같은 형식.

- [ ] **Step 7: 커밋**

```bash
git add insights/atoms insights/views/process.json docs/superpowers/specs/2026-07-30-원자화-스킬-design.md
git commit -F - <<'EOF'
feat(insights): 전력 문서 2편 원자화 — 전력망 노드 0 해소

insight-atomize 스킬 첫 완주. 800VDC Part 1(26-05-26)과 온사이트 가스
딥다이브(25-12-31).

- 전력망 노드가 처음 채워져 사슬 하류(데이터센터 → 전력망) 연결됨
- crosscheck가 잡은 충돌 후보 N쌍 — 전압·전력 단위가 기존 원자와 겹침
- 스펙에 완주 실측 기록

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013QAjUGMBYgDMVzxfQ6a9mq
EOF
git push origin main
```

---

## Self-Review

**1. 스펙 커버리지**

| 스펙 요구 | 태스크 |
|---|---|
| `.claude/skills/insight-atomize/SKILL.md` | Task 5 |
| `.claude/agents/insight-atomizer.md` | Task 4 |
| `insights/crosscheck.py` | Task 2(계산) + Task 3(CLI) |
| C18 코퍼스 혼합 금지 | Task 1 |
| 절차 6단계 | Task 5의 SKILL.md 본문 |
| 미배정 허용 | Task 5 Step 1의 3절, Task 6 Step 3 |
| 검증: 800VDC 완주 | Task 6 Step 1~4 |
| 검증: crosscheck 음성 테스트 | Task 2 Step 1(조건 같으면 안 잡힘·노드 다르면 안 잡힘) |
| 검증: C18 음성 테스트 | Task 1 Step 7 |
| 검증: 두 번째 문서 드리프트 | Task 6 Step 5 |
| 실패 모드: 충돌 후보 과다 | Task 2 Step 3(노드 좁힘) + Task 3 Step 5(30쌍 기준) |
| 실패 모드: 스킬이 인사이트를 고치려 함 | Task 5 SKILL.md "하지 않는 것" |

빠진 것 없음.

**2. Placeholder 점검**

`N`·`<문서 제목>`·`<파일>`은 커밋 메시지 템플릿 안의 실행 시점 값이며, 무엇을 채우는지 바로 위 단계에 명시돼 있다. "적절히 처리"류 표현 없음. 코드가 필요한 단계에는 전부 코드 블록이 있다.

**3. 타입·이름 일관성**

- `corpus_of(source_id)` — Task 1에서 정의, Task 2 Interfaces에서 소비. 이름 일치
- `units(value)` / `find_clashes(new_atoms, old_atoms)` / `find_stale(new_atoms, all_atoms, assign, synth_dir=None)` / `load_assign()` / `pick_target(argv)` / `main()` — Task 2·3에서 정의·사용 이름 일치
- `find_clashes` 반환 항목 키 `unit`·`new`·`old` — Task 3 출력부에서 같은 키 사용
- `find_stale` 반환 항목 키 `file`·`as_of`·`scope`·`uncited` — Task 2 테스트와 Task 3 출력부에서 같은 키 사용
- `ca.ATOMS`·`ca.SYNTH`·`ca.UNIT`·`ca.findings`·`ca.load_atoms`·`ca.parse_synth`·`ca.check_process` — 모두 현재 `check_atoms.py`에 있는 이름
