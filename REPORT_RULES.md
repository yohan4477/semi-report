# SemiAnalysis 카테고리 통합 리포트 규칙

카테고리 하나(예: `ai-infra/power`)에 걸친 여러 `source/newsletter/<카테고리>/` 문서를 하나로 종합하는 통합 리포트 작성 규칙입니다. 개별 문서 변환 규칙은 TRANSFORMATION_RULES.md, 카테고리 정의는 CATEGORIES.md를 참고하세요. 이 문서는 그 위에서 "여러 문서를 어떻게 하나로 합치는가"만 다룹니다.

## 목표 독자와 목적 (중요)

**독자**: 개별 문서 변환과 동일 — 전력·인프라 관련 투자·조달·전략 결정을 내려야 하는 경영진·임원·투자자 (TRANSFORMATION_RULES.md의 "목표 독자" 참고)

**목적**: **"SemiAnalysis 예측이 맞았는지 틀렸는지 채점"이 아님.** 통합 리포트는 애널리스트가 과거 예측 성적을 평가하는 문서가 아니라, 의사결정자가 "이 주제가 지금 어느 방향으로 얼마나 빠르게 움직이고 있고, 그래서 현재 전망을 얼마나 확신하고 베팅해도 되는지"를 빠르게 파악하도록 돕는 문서입니다.
- ❌ 틀린 프레이밍: "2024년 문서는 X라고 했는데 틀렸고, 2026년 문서가 Y로 정정했다" (성적표 채점)
- ✅ 맞는 프레이밍: "X 방향으로 흐름이 이어지고 있으며, 최근 문서가 이를 더 구체적인 수치로 재확인/가속화했다 → 이 방향에 대한 확신도가 올라감" (의사결정 참고)
- 이 구분은 아래 "핵심 원칙"과 "타임라인 다이어그램 규칙"의 노드 텍스트·화살표 라벨 작성 시 그대로 적용

## 트리거

두 가지 방식으로 생성:

1. **온디맨드**: "전력 카테고리 리포트 만들어줘" 같은 명시적 요청 — 아직 리포트가 없는 카테고리도 이 방식으로 처음 생성
2. **신규 문서 처리에 자동 연동 (v1.2)**: 새 SemiAnalysis 문서를 처리해달라는 요청을 받아 `source/newsletter/<카테고리>/`에 추가할 때, 그 문서의 카테고리 중 **이미 통합 리포트가 존재하는 카테고리**가 있으면, 문서 처리와 같은 요청 안에서 해당 통합 리포트도 갱신 — 새 문서를 반영해 시계열 흐름 섹션을 업데이트(새 데이터포인트 추가, 최신 노드 갱신)하고 문서별 요약에 새 항목 추가
   - 아직 통합 리포트가 없는 카테고리면 자동 생성하지 않음 — 그건 온디맨드 요청으로만 (트리거 1)
   - 정기 자동 생성(주간/월간)은 여전히 하지 않음 — 문서 유입이 비정기적이라 고정 주기가 실제 흐름과 안 맞음

## 대상 문서 판단

요청 시점에 CATEGORIES.md 기준으로 해당 카테고리 태그가 붙은 `source/newsletter/<카테고리>/` 문서를 전부 스캔. 문서 하나가 여러 카테고리에 걸쳐 있으면, 관련된 모든 카테고리 리포트에 중복 포함.

## 핵심 원칙: 최신 문서 가중치 + 시계열 흐름

같은 주제에 대해 여러 문서가 서로 다른 시점의 수치·전망을 담고 있을 때:

1. **최신 문서가 "현재 유효한 판단"**: 문서 간 수치·전망이 다르면, 발행일이 가장 최근인 문서의 내용을 지금 의사결정의 기준점으로 표시. 오래된 문서는 틀린 게 아니라 "그 시점 기준 판단"이자 지금 흐름을 이해하는 배경으로 취급
2. **흐름 자체가 확신도 신호**: 단순히 최신 값만 보여주지 않고, 시간에 따라 방향이 어떻게 움직였는지 타임라인으로 표시 — **같은 방향이 반복·가속되고 있다면 확신도를 높여도 되는 신호**이고, 방향이 뒤집혔다면 아직 불확실하다는 신호. "무엇이 맞았고 틀렸나"가 아니라 "이 방향에 얼마나 베팅해도 되는가"를 보여주는 것이 목적
3. **적용 대상**: 여러 문서에서 반복 등장하는 주제·수치에만 적용 (예: "미국 데이터센터 전력 수요 전망"은 5개 문서 중 3개에서 다른 시점 수치로 등장 → 타임라인 대상). 한 문서에만 나온 내용은 그 문서 요약으로만 취급

## 확신도 등급

시계열 주제마다 소제목 아래에 `**확신도: 높음/중간/낮음** — 근거 한 줄` 형식으로 표기한다. 판정 기준:

- **높음**: 2개 이상 문서가 같은 방향을 재확인했고, 최신 데이터포인트가 약 1년 이내
- **중간**: 단일 문서 기반이지만 정량 근거가 구체적인 경우, 또는 복수 문서지만 최신 데이터포인트가 오래된 경우
- **낮음**: 방향이 문서 간에 뒤집혔거나 근거가 정성적 — 방향 뒤집힘은 숨기지 말고 타임라인에 그대로 표시 (그 자체가 "아직 베팅하기 이르다"는 의사결정 신호)

## 다음 확인 포인트 규칙

원문 문서들에 실제로 언급된 예정 이벤트·지표만 쓴다 (예: 용량 경매 결과 발표, 규격 확정, 수주 공개, 제품 출하 시점). 리포트 작성자가 임의로 창작한 이벤트 금지. 각 항목에 영향받는 시계열 주제 번호(§1.N)와 방향(확신도 강화/약화 조건)을 표기.

## 문서 1개 카테고리 처리

온디맨드 요청 시 대상 문서가 1개뿐이어도 리포트 생성은 허용하되, "📌 현재 종합 판단"과 시계열 섹션은 넣지 않고 "문서별 요약 + 시계열 대기 안내" 형태로만 작성 (현행 냉각 리포트 방식을 공식화). 종합 판단·확신도는 문서 2개부터 의미가 있음.

## 구조

```markdown
---
categories: [ai-infra/power]
---

# [카테고리명] 통합 리포트

> **생성일**: YYYY-MM-DD (최초 생성 시점, 파일명에 고정된 날짜와 동일 — 이후 갱신되어도 안 바뀜)
> **최종 갱신일**: YYYY-MM-DD (트리거 2로 새 문서가 추가될 때마다 갱신)
> **대상 문서**: N개 (목록 + 발행일)

## 📌 현재 종합 판단

(3\~5개 불릿 + 결론 한 줄 — 이 카테고리가 지금 어느 방향으로 움직이고 있고, 어디에 얼마나 확신을 갖고 베팅해도 되는지. 경영진이 이 섹션만 읽어도 되도록 작성. 각 불릿에 관련 시계열 주제의 확신도를 함께 표기)

## 📑 목차

## 1. 시계열 흐름: 반복 등장 주제

(주제별로 하나씩, 발행일순 mermaid 순차 체인 다이어그램 — 최신 노드를 색상으로 강조. 각 주제 소제목 바로 아래에 확신도 한 줄 표기)

## 2. 다음 확인 포인트

(어떤 예정 이벤트·지표가 나오면 현재 판단이 강화/약화되는지 목록 — 각 항목이 어느 시계열 주제(§)에 영향을 주는지 표기)

## 3. 문서별 요약

(각 문서 1문단 요약 + 원본 링크)
```

## 타임라인 다이어그램 규칙

- TRANSFORMATION_RULES.md의 다이어그램 규칙(TD 방향, 형제 노드 최대 3개, 신호등 이모지 금지, 물결표 이스케이프)을 그대로 따름
- 시계열은 본질적으로 순차 관계이므로 `flowchart TD`의 순차 체인(A→B→C)으로 표현 — 각 노드는 "발행일 + 그 시점 전망"
- **최신 노드는 색상으로 강조** (예: `fill:#f0fdf4,stroke:#16a34a,stroke-width:2px`), 과거 노드는 중립색(`fill:#eff6ff,stroke:#3b82f6`)으로 구분 — "[현재 판단]" 같은 라벨은 괜찮지만 "[예측 적중]"·"[전망 실패]" 식 채점성 표현은 지양
- 노드 텍스트에 "왜 바뀌었는지"가 원문에 있으면 화살표 라벨로 표기 (예: `A -->|"ERCOT Batch Zero 제도 신설"| B`)
- 방향이 반복·가속되는 흐름은 화살표 라벨에 확신도로 표현 (예: `A -->|"6개월 뒤 같은 방향으로 재확인·가속"| B`)

## 출력 위치 및 파일명

`통합 리포트/카테고리별 통합 리포트/[YYMMDD 생성일] [카테고리명] 통합 리포트.md` — **파일명의 날짜는 최초 생성일로 고정**. 트리거 2로 나중에 내용이 갱신돼도 파일명은 그대로 두고 같은 파일을 수정 (새 파일을 또 만들지 않음). 갱신 이력은 git 커밋과 "최종 갱신일" 필드로 추적.

## 기업별 익스포저 리포트 (기업 축)

카테고리 축 통합 리포트를 보완하는 온디맨드 리포트 유형. 한 기업이 여러 문서·여러 흐름(전력 확보, 전력 배전, 냉각 등)에 걸쳐 어떤 포지션인지를 기업 단위로 종합한다.

**분리 기준 (중요)**: 파일은 **산업 클러스터 단위**로 분리한다 (예: "AI 인프라 전력·냉각", "반도체·메모리", "AI 모델·서비스"). **문서 카테고리 단위로는 분리하지 않는다** — 기업이 카테고리를 가로지르는 것(예: Vertiv가 냉각 문서에서는 CDU, 전력 문서에서는 전력 랙)이 이 리포트의 존재 이유이므로, 카테고리로 나누면 핵심 관점이 사라진다. 산업 클러스터는 기업 단위로 깔끔하게 갈라지므로 분리 기준으로 적합하다. 새 산업 클러스터의 기업들이 코퍼스에 들어오면 기존 리포트에 붙이지 말고 새 익스포저 리포트를 생성한다.

**형식**: 카테고리 리포트의 원칙을 그대로 준용 — 채점 금지 프레이밍, 확신도 등급(높음/중간/낮음), 원문 근거만 사용(코퍼스 밖 지식 금지), 모든 판단에 출처 표기(문서명+섹션). 구조: 📌 현재 종합 판단 → 익스포저 맵(기업 × 등장 문서 수 × 관련 흐름 × 방향 매트릭스 — 순수 분류표이므로 표 허용) → 주요 기업 상세(2개 이상 문서 등장 또는 단일 문서라도 비중 큰 기업, 기업당 방향/확신도 + 근거 + 지켜볼 포인트) → 단일 언급 기업(한 줄씩).

**방향 표기**: 수혜 / 리스크 / 혼재 / 중립(참고 언급). 문서 간 판단 축이 상충하는 기업(예: 단기 수주잔고 vs 장기 성장축)은 억지로 단일 방향으로 정리하지 말고 상충 자체를 표시하고 확신도 낮음으로 분류 — 사람 검토가 필요한 항목임을 명시.

**파일명**: `통합 리포트/기업별 익스포저 리포트/[YYMMDD 생성일] 기업별 익스포저 리포트 - [산업 클러스터].md`

**갱신**: 온디맨드로만 갱신 (신규 문서 처리 시 자동 갱신하는 트리거 2는 카테고리 리포트에만 적용 — 익스포저 리포트까지 자동 연동하면 문서 처리 작업이 과중해짐)

## 대시보드 (Artifact)

통합 리포트를 경영진 공유용 웹페이지로 렌더링한 뷰. **리포트(md)가 단일 소스이고 대시보드는 파생물** — 내용 판단(확신도, 방향, 확인 포인트)은 항상 리포트에서 먼저 갱신하고, 대시보드는 그것을 옮겨 그린다.

- **소스 보관**: HTML 소스는 `대시보드/` 폴더에 커밋해 보관 (예: `대시보드/전력 인프라 대시보드.html`). 세션 스크래치패드에만 두면 세션 종료 시 소스가 사라져 다음 세션에서 처음부터 다시 만들어야 함
- **발행**: Artifact 도구로 발행. **기존 대시보드를 갱신할 때는 반드시 같은 URL로 재발행** — 새 세션에서는 Artifact 도구의 `url` 파라미터에 기존 주소를 전달해야 함 (안 하면 새 URL이 생겨 공유 링크가 끊김). 발행된 URL은 메모리(reference)에 기록해 세션 간 유지. 파비콘(⚡)은 재발행 시에도 동일하게 유지
- **갱신 트리거**: 온디맨드 — 원천 리포트가 갱신되면(새 문서 반영, 확신도 변경) 대시보드도 같이 갱신하는 것을 권장하되 자동은 아님
- **정적 스냅샷 주의**: 대시보드는 발행 시점의 스냅샷 — 헤더의 "최종 갱신" 날짜로 기준 시점을 표기
- **디자인 원칙**: 리포 mermaid 색 체계 승계(파랑=과거 시점, 초록=현재 판단/수혜, 빨강=리스크, 호박=혼재/경고), 라이트·다크 테마 모두 지원, 모바일 1열 우선(개별 문서의 TD 다이어그램 원칙과 같은 이유), 채점성 표현 금지 등 리포트 원칙 준용

## 버전 히스토리

- v1.0 (2026-07-04): 초기 규칙 수립 — 온디맨드 트리거, 최신 문서 가중치 + 시계열 흐름을 핵심 원칙으로 채택
- v1.1 (2026-07-04): 목표 독자·목적 섹션 명시, "예측 채점" 프레이밍 금지
  - 배경: 초기 버전은 독자·목적을 개별 문서 규칙에서 암묵적으로 가져왔을 뿐 통합 리포트에 맞게 재검토하지 않았음. 사용자 피드백으로 "SemiAnalysis가 맞았나 틀렸나"를 채점하는 애널리스트용 문서가 아니라, 방향·확신도를 파악해 의사결정에 참고하는 경영진용 문서임을 명확히 함
  - 타임라인 다이어그램의 라벨·톤도 채점형("예측 적중") 대신 방향·확신도형("재확인·가속")으로 조정
- v1.2 (2026-07-05): 신규 문서 처리에 통합 리포트 갱신을 자동 연동
  - 배경: 사용자가 "새 문서 처리 요청 시 해당 카테고리에 통합 리포트가 있으면 자동으로 갱신하자"고 요청
  - 이미 리포트가 있는 카테고리에 새 문서가 추가되면, 그 리포트도 같은 요청 안에서 갱신 (파일명은 그대로, 내용만 갱신 — "최종 갱신일" 필드 추가)
  - 아직 리포트가 없는 카테고리는 자동 생성 대상이 아님 — 온디맨드 요청으로만 최초 생성
- v2.0 (2026-07-05): 경영진 요약·확신도 등급·다음 확인 포인트 섹션 신설, 문서 1개 카테고리 처리 공식화
  - 배경: 기존 리포트가 시계열 다이어그램+문서 요약만으로 구성되어, 정작 목적("어디에 얼마나 베팅해도 되는가")에 대한 종합 답이 없었음. 확신도를 명시적 등급으로, 판단을 바꿀 트리거를 "다음 확인 포인트"로 구조화
  - 문서 2개부터는 종합 판단·확신도·다음 확인 포인트 포함, 문서 1개 카테고리는 문서별 요약만으로 작성 (냉각 리포트 방식 공식화)
- v2.1 (2026-07-05): 기업별 익스포저 리포트(기업 축) 유형 추가
  - 배경: 첫 익스포저 리포트(AI 인프라 전력·냉각) 작성 후, 향후 반도체·메모리 등 다른 산업 기업이 코퍼스에 들어올 때의 분리 기준을 사용자와 논의 — 문서 카테고리 분리는 기업의 크로스 카테고리 관점을 해치므로 기각, 산업 클러스터 분리를 채택
  - 갱신은 온디맨드만 (트리거 2 자동 연동 대상 아님)
- v2.3 (2026-07-11): 리포트 디렉토리 2단 구조로 재편 (사용자 지시) — 카테고리 축은 `통합 리포트/카테고리별 통합 리포트/`, 기업 축은 `통합 리포트/기업별 익스포저 리포트/`에 보관. 기존 파일 전부 git mv로 이동, 파일명 규칙은 불변
- v2.2 (2026-07-05): 대시보드(Artifact) 섹션 추가
  - 배경: 첫 대시보드(전력 인프라)를 발행하면서, HTML 소스가 세션 스크래치패드에만 있어 세션 종료 시 사라지고 URL 재발행 방법이 규칙화되어 있지 않다는 공백 확인 — 소스를 `대시보드/`에 커밋 보관, 같은 URL 재발행 원칙, 리포트=단일 소스·대시보드=파생물 관계를 명시
