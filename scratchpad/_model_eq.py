# -*- coding: utf-8 -*-
"""보고서 「모델 총정리」 층의 수식 블록.

원문이 공표한 식을 그대로 낸다. 옮겨 적으면서 기호를 바꾸지 않는다 —
기호를 우리 말로 갈면 원문과 대조할 수 없다. 대신 기호마다 뜻을 붙인다.

각 식에는 우리 코드 어디가 그 식을 계산하는지 적는다. 글과 코드가 갈리면
그 줄이 먼저 눈에 띈다.
"""

# (묶음 제목, 출처, [(왼쪽, 오른쪽, 각주)], [(기호, 뜻)], 코드 자리)
EQ = {
    # ── 클러스터 TCO. 원문 L92·L96·L98 ────────────────────────────────
    'TCO': (
        '월 총비용은 여덟 항의 합이다',
        '클러스터영문 L92·L96·L98',
        [('TCO<sub>$/클러스터·월</sub>',
          'GPU + 저장 + 네트워크 + 컨트롤플레인 + 지원 + 굿풋 + 설치 + 디버깅', ''),
         ('GPU<sub>$/월</sub>', '$<sub>GPU·시간</sub> × #GPU × 720<sub>시간/월</sub>',
          '월을 720시간으로 센다'),
         ('저장<sub>$/월</sub>', '$<sub>GiB·월</sub> × #GiB', ''),
         ('컨트롤플레인<sub>$/월</sub>',
          '$<sub>VM·시간</sub> × #VM × 720<sub>시간/월</sub>', ''),
         ('지원<sub>$/월</sub>', '%할증 × (GPU + 저장 + 네트워크 + 컨트롤플레인)',
          '굿풋·설치·디버깅에는 안 붙는다'),
         ('굿풋<sub>$/월</sub>',
          'G<sub>chkpt-cold</sub> | G<sub>chkpt-hot</sub> | G<sub>tolerant</sub>',
          '복원 방식에 따라 셋 중 하나. 아래에서 편다'),
         ('설치<sub>$/월</sub>', '엔지니어비 × t<sub>설치</sub> ÷ t<sub>계약</sub>',
          '3년 계약이면 36으로 나눈다'),
         ('디버깅<sub>$/월</sub>',
          '엔지니어비 × t<sub>디버깅</sub> + %클러스터시간 × GPU<sub>$/월</sub>', '')],
        [],
        'insights/models/gpu_cluster_tco.py · TcoInputs'),

    # ── 굿풋 셋. 원문 L131·L143 을 기호까지 그대로 ──────────────────────
    'GOOD': (
        '고장이 원가로 바뀌는 식은 셋이다',
        '클러스터영문 L131·L143',
        [('G<sub>chkpt-cold</sub>',
          '[(t<sub>id</sub> + t<sub>chkpt</sub>/2) + t<sub>init</sub> + t<sub>repair</sub>] '
          '× j<sub>size</sub> × #failures × $<sub>GPU-hr</sub>',
          '작업이 수리를 기다린다. 원문이 최악이라고 부른 경우다'),
         ('G<sub>chkpt-hot</sub>',
          '{[(t<sub>id</sub> + t<sub>chkpt</sub>/2) + t<sub>init</sub>] × j<sub>size</sub> '
          '+ t<sub>repair</sub> × b<sub>radius</sub>} × #failures × $<sub>GPU-hr</sub>',
          '수리 시간에는 폭발 반경만 곱한다'),
         ('G<sub>tolerant</sub>',
          '[(t<sub>id</sub> + t<sub>failover</sub>) × j<sub>size</sub> '
          '+ t<sub>repair</sub> × b<sub>radius</sub>] × #failures × $<sub>GPU-hr</sub>',
          '체크포인트 손실이 없다. 작업이 계속 돈다'),
         ('클러스터 MTBF', 'GPU MTBF ÷ 클러스터 장수',
          '원문 L110. 클수록 고장 사이가 짧아진다'),
         ('#failures', '720<sub>시간/월</sub> ÷ 클러스터 MTBF', '')],
        [('t<sub>id</sub>', '고장을 알아차리기까지 걸리는 시간'),
         ('t<sub>chkpt</sub>', '체크포인트 주기. 평균 손실은 그 절반이다'),
         ('t<sub>init</sub>', '작업을 다시 띄우는 시간'),
         ('t<sub>repair</sub>', '노드를 고치거나 바꾸는 시간(MTTR)'),
         ('t<sub>failover</sub>', '뜨거운 예비 노드로 넘어가는 시간'),
         ('b<sub>radius</sub>', '폭발 반경. 한 번 고장에 함께 내려앉는 최소 단위'),
         ('j<sub>size</sub>', '평균 작업 크기'),
         ('$<sub>GPU-hr</sub>', 'GPU 한 장을 한 시간 쓰는 값')],
        'insights/models/gpu_cluster_tco.py · goodput_lost_gpu_hours'),

    # ── 추론 원가. 원문 L442·L446 과 표 그림 049·050 에서 읽어 세운 것 ──
    'INF': (
        '서버 값이 시간당 단가가 되는 식',
        'AMD영문 L442·L446 + 그림 049·050',
        [('선불<sub>서버당</sub>', '서버 값 + 서비스·망·저장·소프트웨어', ''),
         ('월 자본비', '선불 × r ÷ [1 − (1 + r)<sup>−n</sup>]',
          'r = WACC ÷ 12, n = 4년 × 12. 단순히 48로 나누는 것이 아니다'),
         ('전기<sub>kW·월</sub>',
          '$<sub>kWh</sub> × 730<sub>시간/월</sub> × 가동률 × PUE',
          '이 글은 월을 730시간으로 센다. 앞 글의 클러스터 원가 글은 720시간이다'),
         ('호스팅<sub>서버·월</sub>', '(전기<sub>kW·월</sub> + 코로케이션<sub>kW·월</sub>)'
          ' × 서버 전력<sub>kW</sub>', ''),
         ('운영비<sub>서버·월</sub>', '호스팅 + 상면 인건비 + 회선', ''),
         ('$<sub>GPU-시간</sub>', '(월 자본비 + 월 운영비) ÷ (8 × 730)',
          '서버 한 대에 GPU 여덟 장'),
         ('$<sub>백만 토큰</sub>',
          '$<sub>GPU-시간</sub> ÷ (초당 토큰 × 3,600) × 1,000,000',
          '원문은 이 값을 곡선으로만 냈다. 이 글은 여기까지 안 갔다')],
        [('WACC', '가중평균자본비용. 빌린 돈과 자기 돈을 섞어 조달할 때 붙는 연 할인율'),
         ('PUE', '전력사용효율. IT 장비 1와트를 쓰려고 실제로 끌어오는 전력의 배수'),
         ('가동률', '장비가 실제로 돌아간 시간의 몫. 전기는 이만큼만 쓴다')],
        'insights/models/inference_tco.py · levelized_monthly · Opex'),

    # ── 토러스 배선. 원문은 표만 냈고 식은 안 냈다 — 표에서 우리가 세웠다 ──
    'TORUS': (
        '칩이 놓인 자리가 배선을 정한다',
        'TPUv7영문 L278 + 그림 034 에서 세움',
        [('연결 수<sub>칩당</sub>', '6',
          '3차원 토러스라 ±x·±y·±z 여섯 이웃과 잇는다'),
         ('광<sub>칩당</sub>', '격자 끝에 걸린 축의 수',
          '되돌아가는 연결은 물리적으로 가장 멀어 광으로 간다. 꼭짓점 3, 모서리 2, '
          '면 1, 안쪽 0'),
         ('기판<sub>칩당</sub>', '2', '같은 보드 위 이웃. 자리와 무관하다'),
         ('구리<sub>칩당</sub>', '6 − 2 − 광<sub>칩당</sub>', '나머지가 구리다'),
         ('케이블<sub>랙</sub>', '∑(칩 × 구리<sub>칩당</sub>) ÷ 2',
          '두 칩을 잇는 한 물건이라 끝 수의 절반이 물건 수다'),
         ('트랜시버<sub>랙</sub>', '∑(칩 × 광<sub>칩당</sub>)',
          '연결 양끝에 하나씩 붙는 부품이라 안 나눈다. 여기가 갈리는 자리다')],
        [('격자 끝에 걸린 축', '좌표가 0 이거나 최댓값인 축. 그 축으로 되돌아가는 연결이 생긴다'),
         ('자리', '끝에 걸린 축이 셋이면 꼭짓점, 둘이면 모서리, 하나면 면, 없으면 안쪽')],
        'insights/models/torus_attach.py · classify · rack'),

    # ── 지연. 원문이 낸 식은 첫 줄 하나뿐이고 나머지는 거기서 따라 나온다 ──
    'LAT': (
        '지연 하나에서 넷이 따라 나온다',
        'AMD영문 L98 + 리틀의 법칙',
        [('E2E 지연',
          't<sub>첫 토큰</sub> + 출력 토큰 수 × t<sub>토큰 사이</sub>',
          '원문이 공표한 식이다. 나머지 넷은 이 식과 산수에서 나온다'),
         ('t<sub>토큰 사이</sub>', '(E2E 지연 − t<sub>첫 토큰</sub>) ÷ 출력 토큰 수',
          '지연 목표를 정하면 거꾸로 풀린다'),
         ('대화 속도', '1 ÷ t<sub>토큰 사이</sub>',
          '사용자가 초당 몇 토큰을 받나. 초당 6.7 과 35 는 다른 경험이다'),
         ('동시 요청<sub>GPU당</sub>',
          '(토큰/초/GPU ÷ 출력 토큰 수) × E2E 지연',
          '리틀의 법칙 — 시스템 안에 든 요청 수는 도착률 곱하기 머무는 시간이다'),
         ('$<sub>백만 토큰</sub>',
          '$<sub>GPU-시간</sub> ÷ 3,600 ÷ (토큰/초/GPU) × 1,000,000',
          '시간당 원가는 추론 원가 모델에서 온다')],
        [('t<sub>첫 토큰</sub>', '요청을 보내고 첫 글자를 받기까지. 입력을 다 읽는 시간이다'),
         ('t<sub>토큰 사이</sub>', '첫 글자 뒤로 글자와 글자 사이 간격'),
         ('E2E 지연', '요청을 보내고 마지막 글자를 받기까지의 전부'),
         ('리틀의 법칙', '줄 서 있는 사람 수는 들어오는 속도에 머무는 시간을 곱한 값이다')],
        'insights/models/latency_model.py'),

    # ── PJM. 원문은 식을 안 냈고 서술에서 우리가 세웠다 ──────────────────
    'PJM': (
        '가정 하나가 요금이 되기까지',
        'PJM영문 L254·L276·L280 에서 세움',
        [('경매 비용', '낙찰 용량 × 낙찰 가격 × 365일',
          '가격은 메가와트·일당이라 한 해치로 펴려면 365를 곱한다'),
         ('요구 용량', '신뢰도 기준 LOLE 0.1일/년을 맞추는 최소 용량',
          '십 년에 하루만 부족하도록. 이 한 줄이 사야 할 양을 정한다'),
         ('낙찰 가격', '수요곡선과 공급곡선이 만나는 자리',
          '공급곡선이 오른쪽 끝에서 수직이라 수요가 조금만 밀려도 절벽에서 내려온다'),
         ('절감액', '실제 비용 − 반사실 비용', ''),
         ('가격 몫', '(실제 가격 − 반사실 가격) × 실제 용량 × 365',
          '할인이 산 용량 전부에 걸린다'),
         ('용량 몫', '(실제 용량 − 반사실 용량) × 반사실 가격 × 365',
          '줄어든 만큼에만 걸린다. 2025/26 은 이 몫이 사실상 0 이다'),
         ('GW당 지렛대', '내려간 가격 ÷ 민 폭',
          '상수가 아니다 — 절벽의 어느 높이에 걸렸느냐로 달라진다')],
        [('LOLE', '기대 정전 일수. 하루에 한 시간이 모자라도 그 하루를 한 번으로 센다'),
         ('요구 용량', 'PJM 이 사야 한다고 정한 양. 신뢰도 기준에서 나온다'),
         ('반사실', '겨울철 발전 성능을 제대로 반영했다면 어땠을지. 원문이 자기 모형으로 낸 값')],
        'insights/models/pjm_capacity.py'),

    # ── 루프라인. 원문이 공표한 식은 산술 강도 셋이고 능선은 우리가 세웠다 ──
    'ROOF': (
        '성능은 둘 중 낮은 쪽에 걸린다',
        '세레브라스영문 L215·L217 + 능선은 우리가 세움',
        [('실현 연산량', 'min(최대 연산량, 대역폭 × 산술 강도)',
          '연산에 막히거나 메모리에 막히거나 둘 중 하나다'),
         ('연산량', '2 × M × N × K', '곱하고 더하니 2를 곱한다'),
         ('바이트', '(M×K + K×N + M×N) × 원소당 바이트',
          '입력 둘과 출력 하나가 전부 메모리를 거친다고 본다'),
         ('산술 강도', '연산량 ÷ 바이트', '바이트 하나가 낳는 연산 수'),
         ('정사각이면', '(2/3) × (한 변 ÷ 원소당 바이트)',
          'M=N=K=n 이면 위 식이 이렇게 줄어든다. 크기에 비례한다'),
         ('능선', '최대 연산량 ÷ 대역폭',
          '두 선이 만나는 자리. 왼쪽은 메모리에, 오른쪽은 연산에 막힌다'),
         ('가장자리 밀도', '대역폭 ÷ (4 × 한 변)',
          '데이터가 나가는 통로는 가장자리에 붙는다 — 면적이 아니라 둘레에 매인다')],
        [('산술 강도', '바이트 하나를 나른 대가로 몇 번 계산하나. 높을수록 연산에 매인다'),
         ('능선', '칩의 성격을 한 숫자로 줄인 것. 낮으면 메모리가 넉넉한 칩이다'),
         ('조밀과 희소', '희소는 0 을 건너뛴 셈으로 세는 값이라 조밀보다 크게 나온다')],
        'insights/models/roofline.py'),

    # ── 우리가 세운 식 둘. 원문에 없다 ──────────────────────────────────
    'OURS': (
        '원문에 없고 이 글이 세운 식 둘',
        '우리 계산',
        [('상대 처리량', '손익분기 임대료 ÷ 기준 SKU 임대료',
          '토큰당 원가가 같아지려면 시간당 값의 비율이 처리량 비율과 같아야 한다'),
         ('소유 문턱', '$<sub>GPU-시간</sub> ÷ 기준 SKU $<sub>GPU-시간</sub>',
          '빌리는 값이 아니라 자기 TCO 로 견줄 때 넘어야 할 처리량 비율')],
        [],
        'insights/models/inference_tco.py · implied_throughput_ratio'),
}


def eq_html(key):
    """수식 한 묶음. 왼쪽에 이름, 오른쪽에 식, 아래에 기호 뜻."""
    title, src, rows, terms, code = EQ[key]
    h = ['<div class="eqbox">',
         '<div class="eqhead"><b>%s</b><span class="eqsrc">%s</span></div>' % (title, src)]
    for lhs, rhs, note in rows:
        h.append('<div class="eqrow"><div class="eqlhs">%s</div>'
                 '<div class="eqrhs"><span class="eqop">=</span> %s%s</div></div>'
                 % (lhs, rhs,
                    '<div class="eqnote">%s</div>' % note if note else ''))
    if terms:
        h.append('<div class="eqterms">')
        for sym, mean in terms:
            h.append('<div class="eqterm"><span class="eqsym">%s</span>%s</div>'
                     % (sym, mean))
        h.append('</div>')
    h.append('<div class="eqcode">계산하는 자리 — <code>%s</code></div>' % code)
    h.append('</div>')
    return ''.join(h)


def all_text():
    """수식에 든 숫자를 글자로. 사실표가 받아 check_report 의 대조 재료로 쓴다."""
    out = []
    for key, (title, src, rows, terms, code) in EQ.items():
        out.append('### 수식 %s — %s (%s)' % (key, title, src))
        for lhs, rhs, note in rows:
            plain = (lhs + ' = ' + rhs + (' — ' + note if note else ''))
            for a, b in (('<sub>', '_'), ('</sub>', ''), ('<sup>', '^'), ('</sup>', '')):
                plain = plain.replace(a, b)
            out.append(plain)
        out.append('')
    return '\n'.join(out)
