# 대기 중 대장 메모 — [260401] Nvidia Blackwell 해부 - 텐서 코어, PTX 명령어, SASS, 플로어스위프, 수율

이 파일은 서브에이전트가 변환 작업 중 추출한 예측·핵심 숫자를 메인 세션이 나중에 `대장/예측 대장.md`, `대장/숫자 대장.md`, `CATEGORIES.md`에 통합하기 위한 대기 노트입니다. 공유 파일(CATEGORIES.md, 대장 파일) 자체는 수정하지 않았습니다.

- **원문**: Dissecting Nvidia Blackwell - Tensor Cores, PTX Instructions, SASS, Floorsweep, Yield (SemiAnalysis, Kimbo Chen)
- **발행일**: 2026-04-01
- **변환 문서 경로**: `content/newsletter/ai_infra/compute/[260401] Nvidia Blackwell 해부 - 텐서 코어, PTX 명령어, SASS, 플로어스위프, 수율.md`
- **카테고리**: `ai-infra/compute` (기존 카테고리, CATEGORIES.md에 이미 존재 — 카운트 +1 필요)

## CATEGORIES.md 갱신 필요사항

- 문서 수 표의 `ai-infra/compute` 행 카운트 +1 (통합 시점 CATEGORIES.md의 현재 값을 확인 후 +1 반영 — 이 작업 시작 시점 값은 8이었으나 병렬 작업 중이므로 통합 시점에 재확인 필요)
- `통합 리포트/`에 `ai-infra/compute`용 리포트가 있는지 확인 필요(이 작업 시작 시점에는 없었음 — `[260704] 냉각 통합 리포트.md`, `[260704] 전력 통합 리포트.md`, `[260706] 메모리 통합 리포트.md`, 기업별 익스포저 리포트 2종만 존재, compute 전용 리포트 없음). 없으면 REPORT_RULES.md 트리거 2에 따라 신규 생성하지 않음(신규 생성은 온디맨드 전용)

## 예측 대장 추가분

이 원문은 Blackwell(SM100) GPU의 마이크로아키텍처를 명령어 단위로 실측하는 기술 벤치마킹 리포트로, 시점이 명시된 예측(예: "2027년 X 출시")은 발견되지 않았습니다. "Future Work" 절에서 후속 계획(추가 PTX 명령어 벤치마킹, TPU Pallas·Trainium NKI·AMD CDNA4 어셈블리 벤치마킹 확장)을 언급하지만 구체적 기한은 명시되지 않아 예측 대장 형식(시점 박힌 예측)에 맞지 않습니다. 예측 대장에 추가할 날짜 박힌 항목 없음 — 이 문서 직전에 변환한 "[250623] NVIDIA 텐서 코어 진화" 문서와 동일한 패턴입니다.

## 숫자 대장 추가분 (형식: 지표 | 값 | 맥락 | 시점)

| 지표 | 값 | 맥락 | 시점 |
|---|---|---|---|
| LDGSTS(비동기 복사) 처리량 포화점 | 약 6.6TB/s (32KiB in-flight 기준) | FlashInfer MHA 커널 설정 재현 실측 | 2026-04 실측(B200) |
| LDGSTS 지연시간 | 기본 약 600ns → 8KiB in-flight 초과 시 거의 2배 | MIO(메모리 입출력) 스로틀로 인한 워프 정지 증가 | 2026-04 실측(B200) |
| MLA 커널 실측 LDGSTS 처리량 | 약 2.2TB/s (2워프·12스테이지 조건) | 포화 처리량(6.6TB/s) 대비 낮음, 소프트맥스 워프 레지스터 압박 추정 | 2026-04 실측(B200) |
| 다이-투-다이(die-to-die) 지연 페널티 | 약 300사이클(평균) | B200 듀얼다이 패키지, SM-SM 거리 행렬 실측 | 2026-04 실측(B200) |
| 다이별 SM 배치 추정치 | 다이 A 39개(10+10+10+9), 다이 B 35개(9+9+9+5\~8) | GPC별 SM 개수, 플로어스위핑 흔적 기반 추정(확정치 아님) | 2026-04 실측(B200) |
| 1SM MMA 처리량(이론치 대비) | M=64 최대 50%, M=128 약 100% | UMMA(5세대 MMA) 실측, 연산 경로 활용률 차이 | 2026-04 실측(B200) |
| 2SM MMA 처리량(이론치 대비) | M128N64 90%, 그 외 N 약 100%, M=256 전 구간 약 100% | 5세대 MMA cta_group::2 모드 실측 | 2026-04 실측(B200) |
| 2SM MMA 약한 스케일링 배수 | 일반 조건 정확히 2배, SS 레이아웃 소형 N에서는 2배 초과 | 2SM이 SMEM 대역폭 병목을 B행렬 분담으로 완화 | 2026-04 실측(B200) |
| In-flight MMA 1\~10개 조건 SoL 도달률 | 최대 N 90%, 최소 N 약 70%, 1SM이 2SM보다 약 5%p 높음 | 실전 커널의 낮은 동시 실행 개수 반영 실측 | 2026-04 실측(B200) |
| 실전 조건(동시 실행 4개) SoL 상한 | 78\~80% | 커널 설계 시 이론치 대신 쓸 현실적 목표치 | 2026-04 실측(B200) |
| SMEM(공유메모리) 대역폭 | 128바이트/사이클 (SM 로컬 기준) | DSMEM 처리량이 이 값보다 뚜렷이 낮음을 비교하는 기준선 | 아키텍처 스펙 |
| SMEM 병목 전환 예시(FP16, M128N64K16) | SMEM 소요 48사이클 vs 연산 소요 32사이클 → N=128부터 연산 병목 전환 | SS 레이아웃이 작은 N에서 느린 이유를 설명하는 계산 예시 | 원문 계산 예시 |
| 비동기 복사 vs TMA 처리량 역전점 | 약 32바이트 in-flight (그 미만은 비동기 복사 우위, 이상은 TMA 우위, 128KiB까지 확장) | FlashInfer MHA·MLA 커널 설정 기준 비교 | 2026-04 실측(B200) |
| 비동기 복사 vs TMA 지연시간 역전점 | 약 12KiB in-flight (그 미만은 비동기 복사 우위, 이상은 TMA 지연 급증) | 동일 비교 실측 | 2026-04 실측(B200) |

## 판단이 필요한 항목 (사람 검토 권장)

- 다이별 SM 배치 추정치([10,10,10,9] / [9,9,9,5+3])는 원문 저자 스스로도 "추정(guess)"이라 명시한 값입니다 — 숫자 대장에 통합 시 "확정 스펙 아님, 실측 기반 추정"이라는 출처 표기를 유지해주세요.
- 이 문서는 앞서 변환한 "[250623] NVIDIA 텐서 코어 진화" 문서의 후속편(같은 저자 계열, SemiAnalysis 마이크로벤치마킹 시리즈 1편)입니다 — 숫자 대장 통합 시 두 문서를 "칩 스펙·마이크로아키텍처 실측" 카테고리로 함께 묶어 정리하는 것을 권장합니다.
- 이 문서 역시 다른 인프라 문서(전력·냉각·랙 설계)가 다루는 "$/kW", "kW/랙" 같은 교차 도메인 비교 숫자가 없고, 대신 GPU 명령어 단위(TB/s, 사이클, SoL %)의 실측치입니다 — 통합 시 별도 단위 체계임을 유의해주세요.
- "실전 조건 SoL 상한 78\~80%"는 이 리포트가 제시하는 가장 실무적으로 유용한 숫자로 판단됩니다(커널 개발자가 이론 스펙 대신 참고할 현실적 목표치) — 향후 다른 벤치마킹 문서와 비교할 기준값으로 활용 가치가 있어 보입니다.
