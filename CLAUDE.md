# CLAUDE.md

## 이 저장소

반도체·AI 인프라(SemiAnalysis)와 제3자 해설(언더스탠딩·미주사·부동산)을 한국어로 옮기고, 문서를 가로질러 나온 판단을 대시보드로 내보낸다. 코드가 아니라 **글**이 산출물이다.

```
① 원문   content/newsletter/**  content/understanding/**  content/epoch/**  input/clippings/**
② 노트   insights/notes/*.md              화면에 안 나온다. 교차 작업용 중간물
③ 포스트 insights/synth/cross-*.md        교차 인사이트
          insights/briefs/*-지금-상태.md   현황 브리핑
④ 화면   대시보드/*.html                   생성물. 손으로 고치지 않는다
```

설계 SSOT는 `docs/superpowers/specs/2026-08-09-원문-기반-노트-체계-design.md`. 원자(atom) 체계는 폐기됐다 — `check_atoms.py`·`crosscheck.py`·`insights/atoms/`를 부르는 지시가 있으면 그게 낡은 것이다.

## 반드시 지킬 것

**③ 포스트 본문을 쓰거나 고치기 전에 `insight-review` 스킬을 연다.** 예외 없다. 한 장만 손대는 경우도 마찬가지다 — 그 한 장이 다른 카드와 겹치는지, 주장이 자기 사례를 덮는지는 스킬의 절차로만 걸린다.

② 노트를 새로 만들 때는 `insight-note`, 제3자 요약을 대시보드 카드로 올릴 때는 `insight-upload`.

**원문에서 이름을 찾을 때는 grep 이 아니라 `entity-search`.** 개체 이름 하나로 917편을 가로질러 줄 주소를 준다 — 별칭과 영문 표기를 사전이 알고, 답에 영수증(색인 판·본 줄·잘린 개수)이 붙는다. 이름을 모르고 개념만 아는 물음은 못 찾는다.
**Epoch AI 글(epoch.ai/gradient-updates)은 `epoch-gradient`.** 링크 하나만 와도 요약본·도해·카드·검사·푸시까지 그 스킬의 순서대로 간다 — 원문 그림은 PNG로 싣지 않고 전부 한국어 인라인 SVG로 다시 그린다.
**카드 여러 장을 한 물음으로 꿴 글(`대시보드/통합 보고서.html`)을 쓰거나 고칠 때는 `insight-report`.** 새 층은 카드가 아니라 **각도에서 세운다** — `structure` A 로 가르고, 대상으로 묶어 여러 편에 걸친 주체만 남기고, 그 주체가 편마다 어느 자리에 서는지를 절로 만든다. 2026-08-28 자금조달 층(`sec-fund`)이 그 첫 실물이다.

**절이 여섯을 넘는 글을 쓰기 전에는 `doc-structure` 스킬을 연다.** 앞머리 셋(물음·바탕·축)·목차·비교표·한계 절이 없으면 문서가 아니라 목록이 된다. 비교표에는 「언제 것 · 성격」 열을 반드시 둔다 — 표의 나란함이 공표치와 추정치를 같은 무게로 보이게 만든다.

**카드에 도해(그림)를 붙이거나 고치기 전에는 `insight-figure` 스킬을 연다.** 도해는 「없는 값을 그렸다」로 무너진다 — 막대 높이도 아이콘 개수도 수치로 읽히는데 원문에 그 수가 없는 일이 잦다. 배치는 `scratchpad/check_fig.py`가 검사한다.

**구조화는 `structure` 스킬 하나다.** 원문을 뽑는 일과 물음을 쪼개는 일이 레인 둘로 들어 있고, 어느 쪽인지는 입력이 정한다 — 원문(경로·링크)이 먼저면 A, 물음이 먼저면 B, 둘 다면 A 를 먼저 하고 그 산출을 B 의 재료로 쓴다.

```
A 원문 각도    원문 1편을 나중에 여러 글과 합칠 수 있는 꼴로 가른다. 산출은 insights/angles/
               등뼈 넷(대상·때·출처·성격)만 고정하고 각도는 글이 주는 대로 연다 — 표 하나에
               다 담으면 값 없는 것(이사회 개편·경영진 발언·개념)이 통째로 떨어진다
B 케이스 구조   「왜 이렇게 됐나」·「어디부터 봐야 하나」를 물었을 때. MBB 케이스 인터뷰식으로
               물음을 벼리고 컷 후보를 세우고 임팩트×불확실성으로 팔 데를 고른다 — 자료가 많은
               가지부터 파면 이미 판 데로 또 가고 맹점이 꼴찌로 간다. 카드를 분류하는 일이 아니다
```

**각도는 아직 노트를 대신하지 못한다.** 2026-08-28 실험(각도만으로 교차 카드 한 장)에서 재 본 결과다 — 고르는 데는 각도가 빠른데(구글 다섯 편·서른여섯 항목이 한 화면, 쌍 후보 고르기 5분 미만) 세우는 데는 줄 번호와 저자 논지가 모자라 원문을 여덟 번 열었다. 각도에 줄 번호 칸·「저자 논지」 절·도해 표시가 다 들어오기 전까지 교차 카드는 노트와 함께 쓴다. 진행은 `check_angles` 의 A7·A8 이 파일마다 센다.

절차 본문은 레인 파일(`references/원문-각도.md`·`references/케이스-구조.md`)에만 있다. **둘을 같이 열지 않는다** — 각도 이름과 컷 표가 한자리에 있으면 둘 다 메뉴가 된다.

건강 인사이트(🩺)는 규칙이 다르다 — 카드 단위가 영상이 아니라 주제이고 본문에 해부도가 들어간다. 카드를 쓰거나 그림을 고치기 전에 `health-insight` 스킬을 연다.

## 검사기

```bash
PYTHONIOENCODING=utf-8 python insights/check_notes.py   # 노트·인용 무결성
PYTHONIOENCODING=utf-8 python insights/check_prose.py   # 문체·용어·절 순서 (대시보드 HTML 포함)
PYTHONIOENCODING=utf-8 python insights/check_read.py    # 읽히는가
PYTHONIOENCODING=utf-8 python insights/check_cite.py    # 인용한 줄에 그 숫자가 있나
PYTHONIOENCODING=utf-8 python insights/check_fresh.py   # 아직 지금 이야기인가
PYTHONIOENCODING=utf-8 python insights/check_report.py  # 보고서 숫자가 원문에 있나
PYTHONIOENCODING=utf-8 python insights/check_index.py   # 색인 주소가 맞나, 색인이 낡지 않았나
PYTHONIOENCODING=utf-8 python scripts/check_deps.py    # 추적된 코드가 부르는 파일이 추적되나
PYTHONIOENCODING=utf-8 python insights/check_val.py     # 숫자 파이프라인 — 조정 표·기간 정합·박아 둔 상수
PYTHONIOENCODING=utf-8 python insights/check_debate.py   # 쟁점 — 화자 말과 진행자 말이 섞였나
PYTHONIOENCODING=utf-8 python insights/check_angles.py  # 각도 — 대상이 사전 정본인가, 성격이 여덟에 드나
PYTHONIOENCODING=utf-8 python insights/check_figval.py # 도해에 든 값이 원문에 있나 (확인 필요만 센다)
PYTHONIOENCODING=utf-8 python insights/check_struct.py # 구조 — 앞머리·목차·물음 절·성격 열이 서 있나
```

FAIL 0이어야 푸시한다. **일부만 돌리지 않는다 — 여기 있는 것을 전부 돌린다.** 2026-08-15에 앞의 셋만 돌리고 푸시해서 `check_fresh` FAIL 3건과 `check_cite` 확인필요 6건이 그대로 나갔다.

`check_struct`가 구조 규칙을 기계가 보는 자리다. 2026-08-29 에 카드 한 장을 놓고 규칙을
다시 세웠다 — 그날 사람이 잡아낸 결함 일곱을 검사기가 하나도 못 잡았기 때문이다. 각각을
규칙으로 옮기고, 결함을 일부러 넣어 무는지 확인했다.

```
S9   차례로 갈랐다면 마디가 차례를 이루나      같은 마디가 두 번 서면 걸린다.
                                        무엇이 먼저인지는 정해진 목록이 아니라 그 글이 안다
S10  절 제목 하나가 물음 하나인가           「무엇으로 쟀고 무엇이 안 재졌나」는 절이 둘이다
S11  절이 셋을 넘는 마디에 축이 있나         축 없이 늘어놓으면 층위가 다른 절이 나란히 선다
S12  나열을 열었으면 번호가 그 수만큼 붙었나   「셋 남는다」로 열고 ① 만 달면 걸린다
F1   카드 본문(report)에 든 도해도 검사하나   report 블록을 안 걷어 겹친 글자가 그대로 나갔다
F2   판 위에 줄임말을 썼나                 「칩당 초당 600Gb」는 본문을 읽어야 뜻이 통한다
F3   한 문장을 세 줄로 잘랐나              한 줄씩은 아무 뜻이 없다. 두 줄 라벨은 정상이다
```

규칙을 세울 때는 **결함을 넣어 무는지 먼저 본다.** S12 는 처음에 「번호가 하나라도 있으면
통과」였고, ① 만 남기고 ②③ 을 뗀 문단을 그대로 지나쳤다. 센 수와 번호 수를 맞춰 보게
고치고 나서야 물었다. S3 도 목차 상자를 자기 본문으로 세고 있어서 본문에 한 번도 안 나오는
마디를 통과시켰다 — 「물총새」를 넣어 보고 알았다.

그전까지 구조는 스킬(`doc-structure`)과 이 문서의 문장에만 있었다 — 스킬은 열어야 걸리고 문장은 어겨도 아무도 안 봤다. 실제로 Semi Doped 카드 스물일곱 장이 목차도 축도 없이 검사기 열셋을 전부 통과했다. 지금 게이트로 세운 장은 `STRICT` 에 적은 것뿐이고 나머지 장은 같은 것을 보되 WARN 으로만 센다 — AI Engineer 68편은 이 규칙보다 먼저 쓰였고 소급은 그 장의 규칙 문서를 고치는 일과 함께 한다.

앞의 여섯은 산문만 본다. 밸류에이션 결함 다섯(최근 12개월 기저 오지정·재무상태표 날짜 혼합·박아 둔 할인율·부호 뒤집힘·주식보상 가산)이 그 사이로 새어 나갔고 검사기가 잡은 것은 없었다. `check_val`이 그 자리를 막는다 — 판단은 `insights/valuation/adjust.py` 표에 줄로 세우고, 검사기는 그 표를 읽을 뿐 판단을 더하지 않는다.

콘솔이 cp949라 파이썬 실행에 `PYTHONIOENCODING=utf-8`을 붙인다.

## 글 규칙

- **글은 기본이 구조다. 나열이 아니다.** 산문으로 흐르는 글(보고서 층·원문 하나짜리 카드 본문·정리 문서)에 아래를 댄다 — 자세한 것은 `doc-structure`.
  **절 이름이 정해진 갈래가 먼저다** — 교차 인사이트는 `check_prose` 의 `SECTION_ORDER` 일곱 절(주장 → … → 검토 후 무관)을 쓰고, 대시보드 카드는 `card_lib` 스키마(points·clash·figs)를 쓴다. 거기서는 절 이름을 물음으로 바꾸지 않는다. 그 갈래에도 그대로 걸리는 것은 앞머리·대비 표·값의 「언제 것 · 성격」·도해 규칙이다.

```
앞머리   물음 하나 · 바탕(재료 몇 편·기간) · 축(묶음 둘셋). 축을 안 밝히면 독자가 묶어야 한다
각도     앞머리 다음, 목차 앞. **이 편이 어떤 각도로 뽑혔는지를 목차와 따로 밝힌다** —
         각도 파일(`insights/angles/`)의 angles 를 그대로 걸고, 그중 카드가 절로 세운
         것에 점을 찍는다. 목차는 「이 카드가 무엇을 말하나」이고 각도는 「원문이 어떤
         마디로 뽑혔나」다. 안 세운 각도를 감추면 카드가 원문 전부인 것처럼 읽힌다.
         **각도 아래 물음, 물음 아래 축까지 간다** — 물음만 MECE 하게 쪼개고 그 아래를
         사실 자루로 두면 한 물음 안에서 무엇이 겹치는지가 안 보인다. `(축, 값)` 으로
         적는다: 「무엇을 바꿨나 — 수 체계 선형→로그 · 연산 곱셈→덧셈 · 회로 곱셈기→덧셈기」.
         축이 하나뿐이면 물음을 너무 잘게 썬 것이니 형제 물음의 축으로 올린다.
         **갈 수 있으면 식으로 적는다** — `a = 가 + 나 + 다`, `매출 = 단가 × 수량`.
         숫자를 쓰라는 말이 아니라 무엇을 더하고 곱해 그 값이 나왔는지를 남기라는 말이다.
         식을 세우면 항이 그대로 축이 된다: 「48V 가 왜 막히나 — 600kW = 48V × 12,500A」.
         시간 흐름·계보·프레이밍처럼 곱하고 더할 것이 없는 물음에는 억지로 세우지 않는다.
         **요소를 가르는 꼴은 글의 꼴 일곱을 그대로 쓴다** — 쪼개는 방법을 새로 만들지
         않는다. **깊이도 미리 정하지 않는다** — 잎에 아직 여럿이 들어 있으면 한 층 더
         파고, 더 쪼개도 뜻이 안 늘면 거기가 잎이다.
         `check_struct` S10·S11·S12·S13
목차     **마디는 둘 중 하나다.** 다루는 것의 부분(대상의 마디)이거나, 글 전체를 한
         축으로 가른 차례다. **차례 이름을 미리 정해 두지 않는다** — 글마다 다르다.
         차례로 갈랐으면 목차에 무엇으로 갈랐는지를 적는다(`('toc', (축, 갈래목록))`).
         적으면 맨 위에 사슬이 걸리고, 마디 이름이 본문에 없어도 된다. **대상의 마디로 묶어 줄 단위로 세운다** — 왼쪽에 마디 이름,
         오른쪽에 그 마디를 다루는 절을 ①②③으로 건다. 마디는 **원문이 다루는 것 안에서
         찾는다**(수 체계·실리콘 면적·랙 안 패브릭·양산 일정처럼). 한 문단에 가운뎃점으로
         이어 붙이면 절이 일곱만 넘어도 벽이 된다. **화면에서는 들여쓴다**
절       제목이 물음을 답한다. 「여러 가지」는 서랍이고 「A와 B」는 두 절이다
대비     견주는 것이 둘 이상이면 산문이 아니라 표. 열은 모두에게 같은 물음
값       「언제 것 · 성격」을 붙인다 — 공표치·추정치·저자 제안이 표에서 같은 무게로 읽힌다
도해     말로만 가는 절에 붙인다. 원문에 없는 값은 안 그린다
끝       마지막 절은 한계 — 검증 안 한 것, 절을 세운 방식이 누구 것인지
```

- **구조는 둘이다. 목차에 둘 다 세운다.** 하나는 **대상의 마디** — 다루는 것이 실제로 어떤 마디로 되어 있는가(수 체계·실리콘 면적·쿼터랙). 다른 하나는 **글의 꼴** — 그 마디를 어떻게 썼는가(대비·인과 사슬·시간 흐름). 마디만 적으면 글이 어떻게 굴러가는지 안 보이고, 꼴만 적으면 무엇을 다루는 글인지가 안 보인다. 마디 이름은 원문 안에서 찾는다 — 본문에 실제로 나오는 말이어야 한다.

```
목차          마디            꼴          절
              수 체계         문제와 처방  ① 무엇을 바꿨나  ② 어려운 쪽은 왜 덧셈인가
              실리콘 면적     인과 사슬    ③ 비워 낸 자리에 무엇을 넣었나
              스케일업 패브릭  대비        ④ 패브릭은 왜 새로 만들지 않았나
              쿼터랙          대비        ⑤ 랙 하나에 무엇이 들어가나
              양산 일정       시간 흐름    ⑥ 언제 나오나  ⑦ 소프트웨어는 어떻게 넘나
              한계                        ⑧ 이 편이 밝히지 않은 것

글의 꼴 일곱   시간 흐름 · 대비 · 층위 · 인과 사슬 · 부분 나눔 · 조건 갈림 · 문제와 처방
```

왼쪽 칸만 읽어도 **추론 랙 한 대가 수 체계 → 실리콘 면적 → 패브릭 → 랙 → 양산으로 이어진 물건**이라는 것이 보이고, 옆 칸을 같이 읽으면 어느 마디를 견주고 어느 마디를 사슬로 풀었는지가 보인다. 마디를 못 찾겠으면 그 글은 아직 대상을 이해 못 한 것이다. `check_struct` S3 이 마디를(글의 꼴을 적으면 걸린다), S3b 가 꼴을 본다.
- **여러 구성 요소를 늘어놓을 때는 ①②③을 쓴다.** 「첫째·둘째」나 「1. 2.」로 쓰지 않는다 — 본문 안에서 번호가 문장과 섞여 어디까지가 한 항목인지 흐려진다. 목차·절 목록·나열이 전부 같은 기호를 쓴다.
- **용어 설명은 그 단어 옆에 붙인다.** 별도 「용어」 절로 몰지 않는다. 읽다가 막힌 자리에서 눈을 딴 데로 옮기게 만들면 설명이 있어도 안 읽힌다. 첫 등장에 괄호로 푼다 — 예: HBC(고대역폭 컴퓨트 — 연산 다이 위에 메모리를 쌓는 방식).

- **용어는 남기고 첫 등장에 괄호로 푼다.** 쉬운 말로 치환하지 않는다. 지웠다가 되돌린 이력이 있다
- **대시보드 산문도 같은 규칙이다.** `check_prose`가 `대시보드/*.html`까지 본다. 금지어 목록은 2026-08-17에 걷어냈다. 대신 밀도를 본다 — 대시 개수(P8), 「A가 아니라 B」 대구(P9), 볼드 밀도(P10), 한 문장 속 절 개수(P11)
- **뜻이 안 닿는 자리도 본다.** 무엇을 먹는지 안 밝힌 조건절(P12), 숫자 없이 정도만 말하는 주장(P13). "GLP-1은 먹으면 나오는 호르몬이다"를 그대로 내보낸 뒤 붙인 규칙이다. 전부 WARN이니 숫자를 줄이는 건 사람 몫이다
- **「돈을 댄다」로 뭉개지 않는다.** 빌려주는 것인지, 내는 것인지, 부도 뒤에만 떠안겠다는 약속인지가 전부 그 한 동사에 묻힌다. 빌려준다·낸다·건다·마련한다로 쓴다. `check_prose` P18
- **은유를 주어 자리에 두지 않는다.** 「돈이 도는 자리가 …로 옮겨 간다」는 매끄럽게 읽히는데 무엇이 움직이는지가 없다. 매출인지 주문인지 이익인지, 누구 것인지를 이름으로 댄다. 은유를 다른 은유로 바꾸는 것은 수정이 아니다 — 2026-08-18에 「값을 받는 자리」를 「돈이 도는 자리」로 바꿨다가 그대로 나갔다. `check_prose` P14가 FAIL로 막는다
- **일반론 금지.** 숫자·명명된 주체·비직관적인 것으로 쓴다
- **회계·금융은 돈이 움직이는 순서로.** 조문 번호와 용어를 늘어놓지 않는다. 언제 장부에 잡히고 언제 정산되는지, 어긋나면 어느 숫자가 움직이는지를 문장으로 쓴다 — `korean-readability` 「1-B」
- 문체·이해도 규칙은 `korean-readability` 스킬
- **「번역투 고쳐」에 재작성으로 답하지 않는다.** 먼저 표지를 재고(스킬 §6-1), 걸린 표지가 든 문장만 고친다. 문단 통째 재작성은 빈 수사를 없던 주장으로 바꾼다 — 논리구조가 망가지는 원인이 이것이다

## 작업 관례

- 의미 단위마다 바로 커밋·푸시한다. 몰아서 하지 않는다
- 단계마다 승인을 묻지 않는다. 판단해서 진행하고 의미 단위 끝에 한 번 보고한다
- **퀄리티가 유지되는 일은 하위 모델에 위임한다.** 토큰을 아끼는 것보다 메인 컨텍스트를 원문 덩어리로 오염시키지 않는 쪽이 더 큰 이유다. 기계적 편집(이름 일괄 치환·형식 맞추기·파일 하나 고치기)은 haiku, 원문을 읽어 요약·변환하는 일은 sonnet, 판단이 산출물인 일(인사이트 본문·검사 규칙·설계·리뷰)은 메인 세션이 직접 쓴다. 위임문에는 지킬 규칙(금지어 변형 금지 등)을 명시하고, 받은 결과는 검사기로 확인한다
- 대시보드 HTML은 생성물이다. 고칠 것은 `insights/` 아래 원본과 `insights/gen_*.py`
- **대시보드 첫 화면은 어느 장이든 섹션 타일이다.** 그 앞에 관문 버튼을 두지 않는다 — 성격이 다른 글도 타일 하나로 넣는다. 타일은 롤업(주간 리포트·드라이버 지도)보다 위에 선다. 조립은 `scratchpad/dash_common.py`의 `render()`만 거치고, 규약은 같은 파일 `check_ui()`가 검사한다
- **원문 분석도 보고서 작성도 하는 일은 쪼개기 하나다.** 쪼개는 법(수식 — 구성요소의 합이나 곱 · 프로세스 — 밸류체인이나 공정 · 부분·층위·대비·조건 갈림), MECE 점검, 깊이(제한 없다. 레벨 1 까지만 하는 것은 분류지 분석이 아니다. 층마다 다른 법을 써도 된다), 한 대상을 여러 뷰로 쪼갠 뒤 베스트 뷰를 고르는 기준(임팩트 · 쪼갬의 뜻 · 자료)이 `docs/쪼개기 — 원문 분석과 보고서 작성.md` 에 있다. **한 노드는 한 방법으로만 쪼갠다** — 한 노드의 자식에 방법이 둘 보이면 아직 안 쪼갠 것이다. 단위는 레벨이 아니라 노드라 형제 가지끼리는 방법이 달라도 된다. **쪼개는 법에 정해진 목록은 없다** — 목록에서 고르기 시작하면 모든 글이 같은 모양으로 굽는다
- **AI Engineer 대시보드도 규칙 문서가 따로 있다.** 카드가 「핵심·숫자·인용」이 아니라 번호글이고, 소제목이 축 노릇을 한다 — 번호를 늘어놓기만 하면 발표가 목록이 된다. 앞머리(물음·남는 것·때 조건)·견주는 표의 「언제 것 · 성격」·한계 문단은 `docs/AI Engineer 대시보드 — 만드는 규칙.md` 에 있다.
- **회계사 대시보드는 규칙 문서가 따로 있다.** 화면 세 겹(회사 타일 → 버튼 둘 → 지도·카드), 지도 네 겹(회사 → 연도 → 방법 → 값), 표 규칙(연도 가로축·구간은 선으로만·메모는 표 밖), 값 규칙(원문에 있는 것만, 표가 이미지면 떠서 읽는다)은 `docs/회계사 대시보드 — 만드는 규칙.md`에 있다. 그 장을 고치기 전에 읽는다
- **카드를 다른 회사 이름 아래 두지 않는다.** 삼성전자·SK하이닉스 공동 원문을 「SK하이닉스」 섹션에 넣으면 카드 제목과 안이 어긋난다. 원문이 여러 주체를 다루면 라벨을 넓히거나(「반도체 3사」) 주제 라벨로 바꾼다. `dash_common.check_labels()`가 생성 때 막는다 — 섹션을 먼저 세우고 카드를 거기 맞추는 순서가 사고의 원인이다
